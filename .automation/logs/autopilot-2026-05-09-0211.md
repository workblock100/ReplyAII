# Autopilot fire — 2026-05-09-0211

## Time used

~41 min — 16 actions plus this log itself (3 versions). Pin-
saturation point for theme tokens reached after action 5 (channel
+ semantic + accent opacity layers all literal-pinned to byte/
percent precision); fire diversified through actions 9–16 into a
BACKLOG diagnostic + TWO candidate-fix wip branches (testing both
the `.contentShape(Capsule)` and `.frame(height:)` hypotheses for
the gallery toolbar's "Open inbox →" button) + two passes of
dead-doc-reference cleanup (AGENTS.md + BACKLOG.md).

## State on entry

- Wip queue depth: **0** (empty before this fire)
- BACKLOG: 14 open, 0 in_progress, 1 false-positive `status: blocked`
  grep hit (no real blocker); all open tasks have proper `claimed_by`
  state (no orphan claims to reset)
- Build cache: WARM (~17–18s `swift test --skip` form)
- Last main commit: `7aebbc5 autopilot: 2026-05-09-0011 log v6`
- Test baseline: **1897 passing** under three-skip workaround

## Actions this fire

1. **P5 theme pin — per-channel dot color literals (6 colors):** the
   existing `testChannelColorsAreDistinct` proves no two channel
   colors collide and `ChannelTests.testDotColorMatchesThemeChannelToken`
   proves the `Channel.dotColor` switch wires each case to the
   matching token, but neither catches a literal edit that shifts a
   channel's hue without breaking distinctness or wiring (e.g.
   someone "tones down" the Slack purple from `#C57FE0` to
   `#9F70C0`, both still distinct from every other channel and
   still wired through the switch). Added
   `testChannelColorLiteralsArePinned` to `ThemeTokensTests.swift`
   asserting each channel token's byte-exact `#RRGGBBAA` projection
   (iMessage `#34C759FF`, WhatsApp `#25D366FF`, Slack `#C57FE0FF`,
   Teams `#6264A7FF`, SMS `#5AC8FAFF`, Telegram `#29B6F6FF`). Slack
   called out in the doc-comment as the priority non-iMessage
   channel post-pivot. +1 test (1897 → 1898). → main `2012f42`.

2. **P8 doc bump:** `agents.md: bump test count header 1897 → 1898`
   → main `f72d5de`.

3. **P5 theme pin — semantic status color literals (warn/err/ok):**
   the existing `testSemanticColorsAreDistinct` catches accidental
   collisions but not within-token drift — a designer who "softened"
   `err` from warm coral `#FF8B7A` to neon `#FF1010` would still
   pass distinctness while shifting the entire alarm vocabulary of
   the app. Added `testSemanticColorLiteralsArePinned` asserting
   `warn` = `#FFB347FF`, `err` = `#FF8B7AFF`, `ok` = `#34C759FF`.
   Doc-comment notes that `ok` and `channelIMessage` currently
   share `#34C759` because both render iOS system green; a future
   split keeps the pin local to each token. +1 test (1898 → 1899).
   → main `84e6f27`.

4. **P8 doc bump:** `agents.md: bump test count header 1898 → 1899`
   → main `0985a5f`.

5. **P5 theme pin — accent stack opacity literals (4 layers):**
   each `Theme.Color.accent*` opacity layer drives a specific
   surface in the design system: `accentSoft` (8%) row-highlight
   tint, `accentSofter` (5%) static card wash, `accentRule` (18%)
   divider tint, `accentGlow` (35%) focused-element halo. The
   existing `testAccentSoftLessOpaqueThanAccent` catches a
   regression that collapses two layers, but a designer "lifting"
   `accentSoft` from 8% to 10% (a 25% intensity shift on every
   accent-tinted card surface) would still pass distinctness.
   Added `testAccentStackOpacityLiteralsArePinned` asserting each
   layer's projection (`<n>% #D7FF3AFF`). +1 test (1899 → 1900,
   tidy round milestone). → main `b841343`.

6. **P8 doc bump:** `agents.md: bump test count header 1899 → 1900`
   (notes the 1900 milestone) → main `50a4f7c`.

7. **P5 Slack channel — empty-workspaceName preview-passthrough
   pin:** `SlackOAuthFlow` deliberately defaults `workspaceName` to
   `""` when Slack's `oauth.v2.access` response omits or empties
   the `team.name` field — the OAuth handshake must succeed even on
   a malformed-team-blob server response (covered by
   `SlackOAuthFlowTests.testSlackOAuthMissingTeamNameDefaultsToEmpty`
   and 5 sibling `*DefaultsToEmpty` cases). The downstream
   consequence is that `SlackChannel.parseThreads` produces rows
   whose `preview == ""` because Slack's per-row preview field is
   populated from the workspace name. `SetChannelsView` already
   routes `creds.workspaceName.isEmpty` to a fallback string for
   the Settings → Channels detail row, but the inbox sidebar
   surfaces the empty preview verbatim. Added
   `testRecentThreadsEmptyWorkspaceNamePropagatesToEmptyPreview`
   in `SlackChannelTests.swift` to pin the current shape so a
   future "improvement" (injecting `"Slack workspace"` as a default
   preview) lands as a deliberate change with a matching update to
   the OAuth-empty siblings, rather than as silent drift. +1 test
   (1900 → 1901). → main `c8d2024`.

8. **P8 doc bump:** `agents.md: bump test count header 1900 → 1901`
   → main `081c932`.

9. **P3 BACKLOG diagnostic — REP-AUDIT-260505 issue #1:**
   code-only walkthrough of the failing "Open inbox →" gallery
   toolbar button. Source: `Sources/ReplyAI/Screens/AppPrototypeView.swift:71–85`
   — `Button { openWindow(id: ReplyAIWindowSummoner.inboxWindowID) }
   label: { Text("Open inbox →").padding(...).background(Capsule().fill(...)) }
   .buttonStyle(.plain).keyboardShortcut("o", modifiers: [.command, .shift])`.
   Comparison points: (a) MenuBarContent's "Open inbox" button
   calls the SAME `openWindow(...)` and works (with `.frame(maxWidth:
   .infinity).frame(height: 30)` instead of padding-only); (b)
   ObDoneView's PrimaryButton calls the same and works; (c) the
   gallery's sibling `navButton` helper uses
   `.overlay(Capsule().stroke(...))` (no fill) and is reported
   working. Conclusion: the action closure is correct (`⌘⇧O`
   invokes the same closure successfully and opens the window), so
   the click event isn't reaching the button. Highest-likelihood
   pattern: a Button with `.buttonStyle(.plain)` whose label uses
   `.background(Shape().fill(...))` (no `.contentShape(...)`) can
   restrict hit-testing to the rendered Text glyphs only, leaving
   the surrounding capsule fill non-tappable; the keyboard shortcut
   bypasses hit-testing so it succeeds. Diagnostic added to BACKLOG
   entry as `diagnostic_2026-05-09-0211 (autopilot, no source
   change)`. → main `2cbf1b7`.

10. **P4 candidate fix wip — `.contentShape(Capsule)` for
    "Open inbox →":** branch
    `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test`
    (commit `1e7e364`) ships the candidate fix from the diagnostic
    above. Adds `.contentShape(Capsule(style: .continuous))` to the
    button label after `.background(...)` so the entire visible
    capsule area becomes hit-testable. Verified with `swift build`
    — compile clean, no new warnings beyond the pre-existing actor-
    context note in `SearchIndex.createSchema`. Pushed to wip
    rather than main because UI-sensitive (any visual or interaction
    change to the gallery toolbar warrants human visual review +
    smoke-launch verification). NB: confidence in this candidate
    softened during action 9's deeper code walk — MenuBarContent
    and PrimaryButton both use the same `.background(Capsule().fill())`
    pattern and work, so the bug may instead be specific to the
    padding-derived sizing (no `.frame(height:)`). The wip branch
    is one candidate hypothesis to test, not a confident fix; the
    BACKLOG diagnostic and candidate_fix entries together give the
    human two paths to try.

11. **P3 BACKLOG link — wip branch into REP-AUDIT-260505 entry:**
    added `candidate_fix_2026-05-09-0211 (autopilot, wip)` line to
    REP-AUDIT-260505 referencing branch
    `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test`
    (commit `1e7e364`), the verification status, and what flips
    when the fix lands on main (criterion 1 done, claim flips from
    `human` to autopilot). → main `04410e2`.

12. **P8 doc fix — dead "gotcha #243" reference in AGENTS.md test
    count line:** the Gotchas section uses bullet points, not
    numbers, so `#243` resolved to nothing on a `Cmd-F`. Replaced
    with a content-substring pointer ("the `swift test` full-suite
    hangs intermittently on Contacts XPC gotcha below") so a reader
    hunting for the linked context lands on the actual gotcha at
    line 263. → main `265381c`.

13. **P8 doc fix — same dead "gotcha #243" reference in BACKLOG.md
    (×2):** post-AGENTS.md fix, grep surfaced two more occurrences
    of the same phantom anchor inside REP-ALERT-260506-2010's
    workaround/scope description (both pointing at the same
    Contacts-XPC hang gotcha). Replaced both with the same
    content-substring pointer using `sed -i.bak` for one, an
    `Edit` tool call for the other (sed-pattern slightly differed
    between the two phrasings). Note: historical fire logs in
    `.automation/logs/` also use the `gotcha #243` shorthand but
    those are immutable artifacts of past fires — left untouched.
    NB: action 13 landed AFTER the consolidated log was committed
    as `d40c808` (so this addendum amends the original log post-
    hoc rather than the log having captured action 13 inline).
    → main `4cd33a1`.

14. **P3 BACKLOG verify + downgrade — REP-AUDIT-260505 wip-branch
    confidence:** end-of-fire follow-up on the wip candidate fix.
    Re-checked out
    `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test`,
    re-ran `swift test --skip ContactsResolverTests --skip
    InboxViewModelIsSyncingTests --skip InboxViewModelTests`,
    confirmed 1901 tests passing (same shape as main — the
    `.contentShape` addition doesn't break anything). Updated the
    BACKLOG entry with the verification result AND explicitly
    downgraded confidence in the candidate fix per the deeper
    code review during action 9: MenuBarContent's "Open inbox"
    button + ObDoneView's PrimaryButton both use the same
    `.background(Capsule().fill())` pattern and reportedly work,
    so the bug may be specific to padding-derived sizing rather
    than hit-test region. → main `3c74c37`.

15. **P4 alt candidate fix wip — `.frame(height: 26)` for
    "Open inbox →":** branch
    `wip/autopilot-2026-05-09-0211-open-inbox-button-frame-height`
    (commit `40904e2`) ships the SECOND hypothesis from the
    action-9 review as a separate wip — replaces
    `.padding(.vertical, 5)` with `.frame(height: 26)` on the
    button label. Sibling buttons that work (MenuBarContent's
    "Open inbox" + FDABanner's "Open System Settings") both use
    `.frame(height: 30)` to bind explicit dimensions; the broken
    gallery button is the only one using padding-derived sizing.
    Build verified clean. Two candidates now live side-by-side on
    wip — human can smoke-test each in turn and identify the
    winner without having to choose ahead of time.

16. **P3 BACKLOG link — second wip branch into REP-AUDIT-260505
    entry:** added the `.frame(height:)` candidate as a second
    bullet in the `candidate_fix_2026-05-09-0211 (autopilot, wip)`
    line, restructured the line as "TWO candidate-fix branches
    now exist for this issue, each testing a different hypothesis"
    so the human picking up the work doesn't have to discover the
    second branch via grep. → main `37390e1`.

## Files touched / REPs closed

- `Tests/ReplyAITests/ThemeTokensTests.swift` — actions 1, 3, 5
  (+3 pin tests, +13 assertions across 6 channel + 3 semantic + 4
  accent-opacity tokens). Total ThemeTokensTests: 17 → 20.
- `Tests/ReplyAITests/SlackChannelTests.swift` — action 7 (+1 pin
  test for empty-workspaceName passthrough). Total: 57 → 58.
- `AGENTS.md` — actions 2, 4, 6, 8 (test-count bumps), 12 (dead
  reference fix).
- `BACKLOG.md` — actions 9, 11 (REP-AUDIT-260505 diagnostic +
  wip-branch link).
- `Sources/ReplyAI/Screens/AppPrototypeView.swift` — action 10
  (candidate fix on wip branch only; main unchanged).

No REPs flipped to done — REP-AUDIT-260505 stays open with two new
investigative artifacts (diagnostic + wip candidate fix) for the
human to verify.

## Pipeline state at exit

- **Open BACKLOG:** 14 (unchanged from entry — no flips this fire,
  REP-AUDIT-260505 still has criterion 1 pending the wip branches'
  smoke-test outcomes)
- **Wip queue depth:** 2 (both candidate-fix branches for
  REP-AUDIT-260505 — `*-open-inbox-button-hit-test` and
  `*-open-inbox-button-frame-height`; no other wip branches)
- **Build cache:** WARM (validated by `swift test --filter
  ThemeTokensTests` running in ~0.005s repeatedly, full suite
  in ~18s on both main and both wip branches)
- **Test baseline:** 1901 passing (was 1897 on entry, +4 from
  actions 1/3/5/7 pin additions; both wip branches also pass 1901)
- **Last 3 commits on main:** `37390e1` (BACKLOG link to second
  wip), `3c74c37` (REP-AUDIT-260505 wip-1 verify + downgrade),
  `4cd33a1` (BACKLOG dead-ref ×2 fix)
- **Last main commit before this fire:** `7aebbc5`

## Next-fire hints

- **Wip queue contains 2 branches** awaiting human visual review,
  both for REP-AUDIT-260505 issue #1 (the gallery toolbar's
  "Open inbox →" click bug):
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test`
    — `.contentShape(Capsule)` hypothesis (action 10).
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-frame-height`
    — `.frame(height: 26)` hypothesis (action 15).
  Future autopilot fires picking these up should NOT auto-merge
  either: the smoke-launch + UI verification gate requires actually
  clicking the button to confirm which (if either) opens the
  inbox window, which the autopilot's headless smoke-launch
  (process-aliveness + AX-window introspection) cannot exercise.
  Once the human picks the winner, the losing branch should be
  archived (push to `archive/<name>`, delete `wip/<name>`).

- **Theme-pin saturation reached** for the visible color tokens.
  The remaining unpinned tokens (`bg0`, `bg2`, `bg3`, `fg`,
  `fgDim`, `fgMute`, `fgFaint`, `line`, `lineStrong`, `lineFaint`)
  are pinned only at the structural-distinctness level, not byte-
  exact. Future pin work in this lane has diminishing value —
  the structural tests already catch any regression that visibly
  reorders the typographic or surface hierarchy, and byte-pinning
  every shade adds maintenance cost (every intentional color tweak
  becomes a multi-test edit). Recommend pivoting future fires
  toward (a) test-coverage gaps in the channel-stub adapters
  (WhatsApp/Teams/Telegram are post-pivot first-class but their
  test files are smaller than Slack's), (b) the `Tone.cycled()`
  reverse-direction question if `⌘⇧/` ever ships, or (c) deeper
  audits of `present-but-empty-string` patterns in the IMessage
  channel's chat.db row decode path.
