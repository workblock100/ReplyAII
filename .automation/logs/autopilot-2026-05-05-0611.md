# autopilot 2026-05-05-0611

**Time used**: ~36 min — 7 actions

## Actions this fire

1. **P1 wip-rebase**: stale `wip/autopilot-2026-05-05-041911-slacktokenstore-edge-coverage` carried 3 valid SlackTokenStore edge tests but had stale AGENTS.md content from before fire 2026-05-05-0411 (would have reverted hang gotcha + keychain note). Cherry-picked just the test additions onto fresh branch `wip/autopilot-2026-05-05-061100-slacktokenstore-edges-rebased` (commit `2166793`). Targeted `swift test --filter SlackTokenStoreTests` 8/8 in 0.17s. Full suite hung at 180s. Pushed to wip per rule. Deleted stale branch from origin.
2. **P5 test-coverage-gap (DraftEngine.modelLoadStatus)**: existing `testLoadProgressTransitionsState` covers the happy path end-to-end but not (a) latest-chunk-wins for the visible status, (b) clear on first text chunk vs on done, (c) ModelLoadStatus value-equality. Added 3 cases + 2 mock services (`PausingProgressService`, `ProgressThenHoldingTextService`). Branch `wip/autopilot-2026-05-05-061800-draftengine-loadprogress-edges` (commit `3074cff`). Targeted DraftEngineTests 49/49 in 0.95s. Full suite hung at 240s. Pushed to wip.
3. **P8 audit (AGENTS.md hang gotcha)**: confirmed the suite hang reproduces deterministically at the same boundary (right after `InboxViewModelBulkFilterTests`, before `InboxViewModelChatGUIDDeduplicationTests` emits its first test case) across both 180s and 240s runs in this fire. Updated the gotcha on AGENTS.md:243 with this 2026-05-05-0611 observation so future fires don't burn cycles re-running with longer timeouts. Committed straight to main (`3797da8`) — pure doc, no code touched.
4. **P5 test-coverage-gap (Theme.Motion)**: `ThemeTokensTests` already pinned Color/Radius/Space tokens but Motion was untested. A designer edit to `fast`/`std`/`tone` durations would silently re-time animations across every screen with no test failure. Added 4 cases pinning durations (0.12 / 0.18 / 0.14) + the fast<tone<std ordering contract via `String(describing:)` on the BezierAnimation. Branch `wip/autopilot-2026-05-05-063100-theme-motion-pins` (commit `b70ad24`). Targeted ThemeTokensTests 14/14 in 0.015s.
5. **P8 audit follow-up (hang shifts when narrowing skip)**: tested whether `--skip InboxViewModelChatGUIDDeduplicationTests` (in addition to `--skip ContactsResolverTests`) would unblock the full suite — it did NOT, hang just shifted to after the next InboxViewModel-based class (`DraftStoreSeedTests`). Conclusion: hang fires at *whichever* InboxViewModel test class runs first after the Contacts XPC handshake exhausts; narrowing the skip list doesn't help. Augmented the AGENTS.md gotcha with this finding (commit `5d3d4e7` on main).
6. **P5 test-coverage-gap (IMessagePreview pass-through)**: 28 existing cases cover all the fallback paths (non-text, attachment, URL collapse) but none pin that the verbatim plain-text pass-through preserves leading/trailing whitespace, embedded newlines, and embedded tabs — which it does because `displayString` returns the original `body` (untrimmed) on the pass-through path. Added 3 cases. Branch `wip/autopilot-2026-05-05-064100-imessagepreview-passthrough` (commit `bc6e90e`). Targeted IMessagePreviewTests 31/31 in 0.005s.
7. **P5 test-coverage-gap (Folder value-equality)**: `FolderKindTests` covers the persistence contract on `Folder.Kind` raw values but nothing covers the wrapping `Folder` struct itself — its Identifiable.id-from-Kind, the synthesized Hashable equality over Kind+label+count, and the SidebarView diff path that depends on both. Added 3 cases in a new `FolderValueTests` class. Branch `wip/autopilot-2026-05-05-064600-folder-value-pins` (commit `ed6a4cb`). Targeted FolderValueTests 3/3 in 0.002s.

Mid-fire: ran `./scripts/build.sh debug` + smoke-launch with `pref.model.useMLX=false` — `SMOKE_LAUNCH_OK PID=7885 alive at 30s`. Confirms main is healthy at startup under the documented MLX workaround; REP-ALERT-260504-1650 still requires the structural REP-501→REP-505 fix to clear, but main is not introducing fresh runtime regressions.

## Files touched

- `AGENTS.md` (2 line edits, line 243 — full-suite hang gotcha augmented with 2026-05-05-0611 deterministic-spot observation, then re-augmented with the "skip-list narrowing doesn't help" finding from action 5) → main
- `Tests/ReplyAITests/KeychainHelperTests.swift` (+40 lines, 3 SlackTokenStore cases) → wip
- `Tests/ReplyAITests/DraftEngineTests.swift` (+127 lines, 3 cases + 2 mock services) → wip
- `Tests/ReplyAITests/ThemeTokensTests.swift` (+47 lines, 4 cases) → wip
- `Tests/ReplyAITests/IMessagePreviewTests.swift` (+37 lines, 3 cases) → wip
- `Tests/ReplyAITests/FolderToneTests.swift` (+40 lines, 3 cases in new FolderValueTests class) → wip

REPs closed: 0 — work was test/doc-coverage rather than backlog draining (no open REP touched).

## Pipeline state at exit

- Open BACKLOG: 21 (unchanged — no shipping to main this fire beyond the two AGENTS doc edits)
- In-progress: 0
- Blocked: 1 (REP-285, superseded by REP-500/501-505 chain)
- wip queue depth: **5** (all autopilot, all this fire):
  - `wip/autopilot-2026-05-05-061100-slacktokenstore-edges-rebased` (3 cases)
  - `wip/autopilot-2026-05-05-061800-draftengine-loadprogress-edges` (3 cases + 2 mocks)
  - `wip/autopilot-2026-05-05-063100-theme-motion-pins` (4 cases)
  - `wip/autopilot-2026-05-05-064100-imessagepreview-passthrough` (3 cases)
  - `wip/autopilot-2026-05-05-064600-folder-value-pins` (3 cases)
  All carve-out via the targeted-filter workaround; future fire can merge once the Contacts XPC hang clears OR a human can run `tccutil reset Contacts` and merge in batch.
- Build cache: WARM (built fresh in this fire for smoke-launch)
- Last 3 commits on main: `124a2d4` (this fire's log v2), `5d3d4e7` (AGENTS gotcha re-aug, this fire), `48db166` (this fire's log v1)
- Test count on main: **1249** (unchanged — all 16 new test cases are on wip)
- Smoke-launch state: OK with `pref.model.useMLX=false`

## Next-fire hints

- Five wip branches are ready for human or future-fire merge: see list above. Each commit message names the targeted-filter command that verified it (8/8, 49/49, 14/14, 31/31, 3/3 respectively).
- Hang gotcha now has the most actionable framing it can: NOT a specific test class, but the first InboxViewModel-using test class to run after the Contacts XPC handshake exhausts. Skip-list narrowing just shifts the boundary — don't chase it. Future investigator should look at why the Contacts framework is loading at all when no test method ever uses it (ContactsResolverTests has the only `import Contacts`; loading that bundle once is what triggers the eager XPC retry loop).
- REP-ALERT-260504-1650 (MLX startup exit) still requires REP-501→REP-505 chain. Smoke-launch verified `useMLX=false` workaround keeps main alive but the structural fix is still needed for users with persisted `useMLX=1`.

## Late-fire diagnostic note (post-action-7, pre-exit)

Tried `tccutil reset Contacts` to clear the XPC hang. The command failed with `tccutil: Failed to reset Contacts` (probably needs sudo or running from a TCC-privileged context). However, the *next* `swift test --skip ContactsResolverTests` invocation got noticeably further than the first three runs in this fire — it began emitting test cases from `InboxViewModelChatGUIDDeduplicationTests` (2 cases passed) before hanging mid-suite at `testIncomingNotificationWithNilGUIDCreatesNewThread`. So the hang isn't strictly deterministic at one boundary; it migrates a few test cases around depending on Contacts framework state, and even a *failed* `tccutil reset` invocation may partially poke the system. The structural conclusion in the gotcha (lazy framework load triggers the hang) still holds, but the next-fire investigator should NOT trust "the hang is at line N of class X" as a stable target — only "it's somewhere in the InboxViewModel suite cluster."
