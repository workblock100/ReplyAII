# Worker run — 2026-04-22-040356

## Outcome: shipped

## Tasks closed
- REP-050 (P1, effort M): Extract `Locked<T>` generic wrapper to consolidate NSLock pattern

## File changes
| File | Change |
|------|--------|
| `Sources/ReplyAI/Utilities/Locked.swift` | +37 lines (new) |
| `Sources/ReplyAI/Services/Stats.swift` | -18/+12 (replaced lock+synced with Locked<Snapshot>) |
| `Sources/ReplyAI/Channels/ContactsResolver.swift` | -20/+13 (replaced lock+cache+_access with Locked<ResolverState>) |
| `Tests/ReplyAITests/LockedTests.swift` | +72 lines (new) |
| `BACKLOG.md` | status: in_progress → done |

## Tests
- Before: 189 passing
- After: 197 passing (+8 new LockedTests)

## Notes
- `MLXDraftService` also uses `@unchecked Sendable + NSLock` but its lock/unlock pattern is interleaved with async callbacks (unlock before `Task.sleep`, re-lock after), making `Locked<T>` unsafe to apply there. Left as-is and noted in the source.
- `ChatDBWatcher` is marked `@unchecked Sendable` but does not use `NSLock` — no change needed.
- Both `ContactsResolver` and `Stats` had their `synced { }` private helper replaced entirely; the `Locked` box uses a class-backed struct so callers can store it as `let`.
