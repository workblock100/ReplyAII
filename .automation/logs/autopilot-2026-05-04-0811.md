# Autopilot 2026-05-04 08:11

**Time used**: ~36 min — 10 actions, 11 commits to main
**Pipeline state at exit**: HEALTHY — all 989 tests pass (973 + 16 added on main this fire), wip queue empty, build cache warm

## Actions this fire

1. **P1 wip merge**: cherry-picked `wip/autopilot-polish-2026-05-04-063800-oauth-error-localized` (commit ed8e778, OAuthError adopts LocalizedError + 4 tests) onto main as `5e3aa11`. The branch was stale vs main (last fire added many tests after the wip was made); cherry-picking the 2-file delta avoided pulling in test deletions. Pushed, deleted upstream wip branch.
2. **P5 tests**: added stub-channel `messages()` symmetry (SMS/Teams/Telegram/WhatsApp, 8 cases) → `2d10bbf`. The recentThreads auth gate was tested everywhere but the messages auth gate had no coverage — a refactor that removed it from messages would have shipped silently.
3. **P5 tests**: SlackHTTPClient user-actionable error copy (3 cases, 429/non-HTTP/5xx) → `9662dfd`. Existing tests confirmed the error type but never pinned the load-bearing copy tokens ("rate-limit", "connection", "try again").
4. **P8 audit**: AGENTS.md test-count sync 958 → 973 → `d3553c5`.
5. **P5 tests**: SlackChannel limit clamping + DM/system-message edges (5 cases) → `8f009f8`. Pins the 1..200 clamp on both `recentThreads` and `messages`, the user_display_name fallback chain, and the user==nil → .me parser quirk. Adds GetRecordingHTTP test double.
6. **P5 tests**: StubLLMService stream confidence + text round-trip (2 cases) → `002720f`. Pins that `.confidence` chunk equals `Fixtures.seedConfidence(threadID:tone:)` and that concatenated text chunks equal `Fixtures.seedDraft` verbatim.
7. **P5 tests**: ShortcutsExportHandler case-insensitive parsing + thread defaults (4 cases) → `db3eb66`. Channel raw value lowercased, "from" field case-insensitive, thread.time falls back to last message, avatar uses prefix(1).
8. **P5 tests**: ChatDBWatcher restart accumulation + stop() alias (2 cases) → `266f6e2`. Pins that repeated cancels each increment restartCount (so backoff actually grows) and that `stop()` is a true alias for `stopWatching()`.
9. **P5 tests**: GlobalHotkey contract (1 case) → `71d0f18`. New file `GlobalHotkeyContractTests.swift` — pins the `co.replyai.summon.inbox` notification name that AppPrototypeView observes, since this IPC has no compiler-enforced seam.
10. **P5 tests**: voiceExampleMessages boundary cases (4 cases) → `1a62453`. Getter returns [] when unset, exactly 20 entries pass through, exactly 500-char entry not truncated (threshold is `>` not `>=`), empty array writes clear the list.

## Files touched / REPs closed
- `Sources/ReplyAI/Channels/LocalhostOAuthListener.swift` (via wip merge)
- `Tests/ReplyAITests/LocalhostOAuthListenerTests.swift` (via wip merge)
- `Tests/ReplyAITests/SMSChannelTests.swift`
- `Tests/ReplyAITests/TeamsChannelTests.swift`
- `Tests/ReplyAITests/TelegramChannelTests.swift`
- `Tests/ReplyAITests/WhatsAppChannelTests.swift`
- `Tests/ReplyAITests/SlackHTTPClientTests.swift`
- `Tests/ReplyAITests/SlackChannelTests.swift`
- `Tests/ReplyAITests/LLMServiceTests.swift`
- `Tests/ReplyAITests/ShortcutsExportHandlerTests.swift`
- `Tests/ReplyAITests/ChatDBWatcherTests.swift`
- `Tests/ReplyAITests/GlobalHotkeyContractTests.swift` (new)
- `Tests/ReplyAITests/PreferencesTests.swift`
- `AGENTS.md` (test-count sync)

No REPs in BACKLOG closed — all this fire's work landed under P1 (wip drain) or P5 (test coverage). The remaining 21 open REPs are either claimed by `human` or are L-effort (REP-501→REP-505 MLX extraction chain) that the BACKLOG itself flags as too risky for autopilot fires.

## Pipeline state at exit
- BACKLOG open: 21 (unchanged); blocked: 1 (REP-285, deprioritized by REP-500 chain)
- wip queue: 0 — drained completely this fire
- Build cache: warm (`./scripts/build.sh debug` clean as of action 1)
- Last 3 commits on main: `1a62453`, `71d0f18`, `266f6e2`
- Test count: 989 (was 962 at fire start; +4 from wip merge already on main, +23 from this fire's P5 actions)

## Next-fire hints
None — every action this fire was a clean self-contained commit. Future fires should look at:
- Stub-channel `send()` paths (Slack only has send tests; Teams/SMS/Telegram/WhatsApp don't expose send yet but if they grow it, mirror the pattern from `tests: stub-channel messages()` here)
- NotificationCoordinator coverage is solid (23 tests) but `handleIncomingNotification` thread-dedup edges may have more pinnable behavior
- The L-effort REP-501→REP-505 MLX extraction chain still needs a human-paired multi-fire session per BACKLOG's own note

## Addendum — action 12

Hit the 50-min budget rule with one extra P5 push:

12. **P5 tests**: IMessageChannel.formatRelative zero-sentinel + non-empty contract (2 cases) → `74f49a8`. Pins `appleDate=0 → ""` (no-message sentinel for empty threads) and the inverse (any non-zero produces non-empty label). Exact h:mm/MMM-d output is locale-dependent and not pinned; the contract under test is structural.

Final test count: 991+2 = 993 tests. AGENTS.md was synced to 991 in commit `694649e` BEFORE this addendum landed; if the next fire surveys, expect actual to be 993.

## Addendum 2 — action 13

Past the 50-min budget; one final P5 push:

13. **P5 tests**: PromptBuilder.systemPrompt per-tone semantic markers (3 cases) → `d06ba71`. Pins one load-bearing token per tone (warm→"warm", direct→"direct", playful→"playful"). Existing guards covered non-empty + distinct-across-tones but a refactor that swapped two tone tails would have slipped past both.

Final test count after this addendum: 996. AGENTS.md last synced to 993 in commit `2bc3c00` BEFORE this; next fire should expect 996.
