# Autopilot fire — 2026-05-08 12:11 EDT

## Time used

~28 min through action 8; targeting ~50 min total. Continuing pin work after log save.

## Actions this fire

1. **P5 tests:** 6 new MLXDraftService pins on user-visible banner shapes — byte-form separator (space + U+00B7 MIDDLE DOT + space), fraction-form separator parity, `Int()` truncation policy on percentage rendering (0.999 → 99%), no-clamp policy above 100% (1.5 → 150%), and U+2026 ellipsis byte on `preparingMessage` and `warmingMessage`. Pins user-visible banner copy that drift would change without changing source obviously. → main `141b0b5`.
2. **P5 tests:** 2 new LLMService tokenizer pins — Swift `Character` clusters CRLF as ONE grapheme, so `tokenize("hi\r\nthere")` produces ONE token (surprising-but-safe, drift toward `text.unicodeScalars` would split mid-cluster); ASCII-space-only delimiter contract (NBSP, em-space, ideographic space NOT delimiters). → main `fbf1b9e`.
3. **P5 tests:** 3 new SlackOAuthFlow token-exchange body shape pins — `redirect_uri` is sent VERBATIM not percent-encoded (asymmetry vs the other 3 escape()-routed fields; drift to uniform escape() triggers `redirect_uri_mismatch`); body field ORDER is `code, client_id, client_secret, redirect_uri`; `&` separator with exactly 3 instances. → main `0c9584a`.
4. **P5 tests:** 3 new SlackChannel parse-default pins — `parseThreads` emits constant inbox defaults (`pinned=false, time="", contextCount=0, contextSummary=nil, hasAttachment=false`) regardless of Slack response shape; `chatGUID=c.id` survives display-name divergence (dotted channel names, comma group DMs); `parseMessages` emits `rowID=0, isRead=true` (chat.db-only fields). Drift toward "let's surface c.is_starred" or "hash ts into rowID" would silently couple Slack to chat.db rule-engine assumptions. → main `f22275a`.
5. **P5 tests:** 3 new PromptBuilder prompt-shape pins — LF-only line endings (no `\r` byte anywhere, drift toward CRLF would silently degrade MLX tokenization); 3 `\n\n` paragraph-break separators between sections (no triple-newlines); derived system-prompt cap = 1800 = historyCharBudget (2000) − minHistoryReserve (200). → main `7e588d8`.
6. **P5 tests:** 3 new ShortcutsExportHandler import-path pins — `chatGUID == payload.id` verbatim (route key for IMessageSender.send); channel field is INDEPENDENT of id format (Shortcut authors own the channel; iMessage-shaped id with channel="slack" must produce .slack thread); empty-string channel fallback through defaultChannel. → main `de8382e`.
7. **P8 audit:** AGENTS.md test-count header bump 1846 → 1866 (this fire's first six commits added 20 cases). → main `0a0bc28`.
8. **P5 tests:** 2 new UNNotificationContentParser title-fallback pins — whitespace-only title (`title: " "`) is treated as a VALID sender handle (drift toward `.trimmingCharacters(.whitespacesAndNewlines).isEmpty` would silently flip resolution to nil); exactly-empty title with absent sender keys returns nil. Pins both legs of the empty-vs-whitespace distinction so the title-fallback contract isolates from the broader resolution-order shape. → main `5776e31`.

## Files touched

- `Tests/ReplyAITests/MLXDraftServiceTests.swift` (+6 cases)
- `Tests/ReplyAITests/LLMServiceTests.swift` (+2 cases)
- `Tests/ReplyAITests/SlackOAuthFlowTests.swift` (+3 cases)
- `Tests/ReplyAITests/SlackChannelTests.swift` (+3 cases)
- `Tests/ReplyAITests/PromptBuilderTests.swift` (+3 cases)
- `Tests/ReplyAITests/ShortcutsExportHandlerTests.swift` (+3 cases)
- `Tests/ReplyAITests/UNNotificationContentParserTests.swift` (+2 cases)
- `AGENTS.md` (test-count header 1846 → 1866)

## Pipeline state at log-save (will close out below 50-min mark)

- Open tasks: 14 (unchanged this fire — all P5 work; P3 reconciles drained in 2026-05-08-1010 fire)
- Wip queue depth: 0
- Build cache: WARM
- Last 3 commits on main: 5776e31 (UNNotificationContentParser title pins), 0a0bc28 (AGENTS.md 1846→1866), de8382e (ShortcutsExport pins)
- Total test count: 1866 (1830 passing under three-skip; +20 since fire start)

## Next-fire hints

- Tested-density survey of pure-logic files (LOC / direct test count): InboxViewModel 1287/168, IMessageChannel 670/95, SearchIndex 444/108, Stats 433/73, AppleScriptMessageReader 408/57, IMessageSender 377/84, ContactsResolver 337/30, SmartRule 319/0 (covered indirectly via RulesTests' 196), SlackOAuthFlow 319/39, SlackChannel 301/56, LocalhostOAuthListener 279/31, NotificationCoordinator 252/43, RulesStore 249/0 (covered indirectly), KeychainHelper 233/42, Preferences 226/90. Lower-density files most worth pinning further next fire: ContactsResolver (~10 LOC/test), SearchIndex (4.1 LOC/test) at the lower end is already saturated. ContactsResolver is the next obvious target.
- The chatGUID-empty-string contract divergence between `UNNotificationContentParser.parse` and `NotificationCoordinator.resolveChatGUID` is still pinned in tests on both sides; harmonization is a deliberate-decision task, not an inline fix. Unchanged from previous fire's hint.
