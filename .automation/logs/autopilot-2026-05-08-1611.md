# autopilot 2026-05-08-1611 fire log (final)

## Time used
~43 min — 17 P5 pin actions + 3 P8 (AGENTS test count bumps) + 1 P8 (interim log) = 21 commits

## Final test count
1906 tests (1870 passing under three-skip workaround), up from 1889 at fire start (+17 cases).

## Actions this fire

All P5 (test coverage gap pins on main) — wip queue empty at fire start, 14
open backlog items either L-effort/risky (REP-501→REP-505 MLX SPM split,
REP-XCUI-001), claimed by human, or UI-sensitive. Stayed in P5 lane the
whole fire. Build cache was warm at start; full test suite green
(1867/1903 passing under three-skip workaround) at exit.

1. **P5 pin** (`72f59c9`): `testOverallAcceptanceRateAllowsOverOneWhenAggregateSentExceedsGenerated`
   in `StatsTests.swift` — parallel to the per-tone path's existing
   `*OverOne*` pin. The aggregate `overallAcceptanceRate()` divides
   summed-byTone sent / summed-byTone generated and applies the same
   no-clamp policy: idempotency-miss bugs surface as raw ratios > 1.0
   for the audit log rather than clamped silently to 1.0. Drift toward
   "let's clamp the aggregate to [0,1] for cleaner stakeholder charts"
   would silently flip behavior on an idempotency-miss surface.

2. **P5 pin cluster** (`484a54d`): `testFlushNowOnNilFileURLIsSafeNoOp`
   + `testFlushNowOnNilFileURLCancelsAnyPendingWriteSafely` in
   `StatsTests.swift` — pin the file-less Stats path. The 21 existing
   `flushNow()` callsites all have a non-nil fileURL, so the no-persistence
   safety has had no test coverage. Pin: flushNow on nil-fileURL Stats
   is a safe no-op (no crash, no default-path fallback, no counter
   mutation), AND survives concurrent flushNow under contention (lock
   path race-free).

3. **P5 pin** (`63f661b`): `testChatGUIDForReturnsStoredGUIDVerbatimRegardlessOfChannelPrefixMismatch`
   in `IMessageSenderTests.swift` — pin both directions of cross-channel
   chatGUID storage: an SMS thread with an iMessage-shaped chatGUID
   round-trips verbatim through `IMessageSender.chatGUID(for:)` (no
   channel-prefix coercion), and vice versa. Drift toward "coerce the
   prefix to the channel's service to be safe" would silently route
   AppleScript sends through the wrong Messages.app service for every
   chat.db row whose service_name disagreed with our channel mapping.

4. **P8 audit** (`171ac3f`): AGENTS.md test count header bump
   1889 → 1893 (commits 1–3 added 4 cases).

5. **P5 pin** (`e1eba54`): `testSendChatPostMessageBodyShapeIsExactlyTwoKeys`
   in `SlackChannelTests.swift` — pin that the chat.postMessage POST
   body has EXACTLY two keys (channel, text). Existing
   `testSendSucceedsWhenAckOk` only asserts the two known keys are
   SET; would silently pass if a future refactor added a third
   field (parse, link_names, as_user, unfurl_links). Each of those
   Slack parameters has user-visible behavior. Sibling shape to the
   prior SlackOAuthFlow body-shape pin from a previous fire.

6. **P5 pin** (`e2e2c97`): `testWhitespaceOnlyChatGUIDPassesThroughVerbatimAndProducesDuplicate`
   in `NotificationCoordinatorTests.swift` — companion to the existing
   empty-string normalization fix. The normalize predicate is
   `.isEmpty == false`, which only catches zero-length. A whitespace-
   only chatGUID propagates verbatim into applyIncomingNotification,
   routes through the chatGUID-keyed exact-equality branch, fails to
   match any seeded thread, and creates a duplicate keyed by
   senderHandle. Pin the current duplicate-creation behavior so a
   future trim-before-normalize tightening (the correct fix) surfaces
   as a deliberate change. Mirrors the IMessageSender chatGUID-
   whitespace pin from a prior fire.

7. **P5 pin** (`325c515`): `testAvatarDivergesFromIMessageChannelAvatarInitialOnCaseAndPhone`
   in `ShortcutsExportHandlerTests.swift` — pin two intentional
   divergences vs `IMessageChannel.avatarInitial(for:)`: no uppercasing
   (preserves user-authored casing) and no phone-glyph fallback. Empty-
   displayName divergence is already pinned by
   `testAvatarFromEmptyDisplayNameIsEmpty`; this closes the remaining
   two cases.

8. **P8 audit** (`ac8e581`): Initial fire-log saved at ~24 min.

9. **P5 pin** (`a2ec661`): `testStatsSnapshotDecodesExplicitNullKeysAsDefaults`
   in `StatsTests.swift` — fills the explicit-JSON-null leg of the
   Snapshot decode contract (the existing missing-keys pin covers
   absent keys). Pin: every counter set to JSON `null` decodes to the
   all-zero default Snapshot. Drift toward `decode(_:forKey:)` (which
   throws on null) on any single site would silently break stats.json
   read for any third-party tool / older build that wrote nulls
   instead of omitting keys.

10. **P5 pin** (`f8ca0c6`): `testWhatsAppChannelEmptyTokenBypassesAuthGate`
    in `WhatsAppChannelTests.swift` — pin the surprising-but-safe
    contract that an empty-string token in Keychain passes the
    `keychain.get(key:) != nil` auth gate. KeychainHelper round-trips
    empty values as Some(""), so an empty token is "authorized" and
    the stub returns its empty list. Drift toward
    `?.isEmpty == false` is reasonable tightening once real API calls
    land but would silently flip every malformed-but-stored credential
    into authorizationDenied without surfacing as a deliberate change.

11. **P5 pin cluster** (`671630b`):
    `testResolveSenderHandleNonStringValuesFallThroughResolutionOrder`
    + `testResolveChatGUIDNonStringValuesProduceNil` in
    `NotificationCoordinatorTests.swift` — mirror the parser-side
    non-String pins for the inline willPresent path's static helpers.
    The parser pin doesn't transitively cover the helpers (they're
    distinct callsites that share userInfo keys + resolution order).
    Drift toward `String(describing:)` on either helper would surface
    a coerced numeric handle/chatGUID and break thread keying.

12. **P5 pin** (`972d7ae`): `testPrettyPhoneStripsLettersWhenDigitCountMatchesUSPhoneShape`
    in `AppleScriptMessageReaderTests.swift` — pin the surprising-but-
    safe behavior that letters interleaved with 10 digits get stripped
    silently and reformatted as US phone. `prettyPhone("555ab1234567c")`
    → `"(555) 123-4567"`. Lower-bound case (`"55512345abc"` → 8 digits
    → default branch → verbatim) pins the format-vs-passthrough
    boundary. Drift toward 'reject anything with letters' or 'try to
    format any digit-having input' surfaces here.

13. **P5 pin** (`428c77c`): `testRecordRuleFiredAcceptsWhitespaceOnlyActionVerbatim`
    in `StatsTests.swift` — companion to the existing empty-action
    verbatim pin. `recordRuleFired(action: " ")` tracks under literal
    `" "`, NOT folded to `""`. Drift toward `.trimmingCharacters()`
    would silently merge whitespace-only call-site typos into the
    empty-action bucket. Same drift class as IMessageSender chatGUID-
    whitespace and NotificationCoordinator chatGUID-whitespace pins.

14. **P8 audit** (`851ca1a`): AGENTS.md test count header bump
    1893 → 1902 (commits 5–13 added 9 more cases).

15. **P5 pin** (`bacae63`): `testFormatTimeZeroDoesNotEarlyReturnAndDivergesFromFormatRelative`
    in `IMessageChannelTests.swift` — pin the divergence between
    formatTime and formatRelative on `appleDate: 0`. formatRelative
    early-returns noTimestampSentinel (''); formatTime has no
    zero-guard and renders 2001-01-01 verbatim. Cross-pin via NotEqual
    so harmonize-on-either-side surfaces in code review.

16. **P8 audit** (`895bca9`): AGENTS.md test count bump 1902 → 1903
    (commit 15 added 1 case) + final fire log update.

17. **P5 pin** (`ba3c109`): `testSMSChannelEmptyTokenBypassesAuthGate`
    in `SMSChannelTests.swift` — mirror of the WhatsApp empty-token-
    bypass pin for the SMS-relay stub.

18. **P5 pin cluster** (`67f9fcd`):
    `testTeamsChannelEmptyTokenBypassesAuthGate` +
    `testTelegramChannelEmptyTokenBypassesAuthGate` — mirror of the
    same pin for the remaining two channel stubs. The four channel
    stubs (SMS, Teams, Telegram, WhatsApp) now share the empty-token-
    bypass pin uniformly, so a future tightening to `?.isEmpty == false`
    surfaces consistently across the cluster, not just on one stub.

19. **P8 audit** (`89fe41d`): final AGENTS.md test count bump
    1903 → 1906 (commits 17–18 added 3 cases).

## Files touched

- `Tests/ReplyAITests/StatsTests.swift` (+5 cases)
- `Tests/ReplyAITests/IMessageSenderTests.swift` (+1 case)
- `Tests/ReplyAITests/SlackChannelTests.swift` (+1 case)
- `Tests/ReplyAITests/NotificationCoordinatorTests.swift` (+3 cases)
- `Tests/ReplyAITests/ShortcutsExportHandlerTests.swift` (+1 case)
- `Tests/ReplyAITests/WhatsAppChannelTests.swift` (+1 case)
- `Tests/ReplyAITests/AppleScriptMessageReaderTests.swift` (+1 case)
- `Tests/ReplyAITests/IMessageChannelTests.swift` (+1 case)
- `Tests/ReplyAITests/SMSChannelTests.swift` (+1 case)
- `Tests/ReplyAITests/TeamsChannelTests.swift` (+1 case)
- `Tests/ReplyAITests/TelegramChannelTests.swift` (+1 case)
- `AGENTS.md` (test-count header bumps × 3)

Total: 17 new pin-test cases across 11 files. The four channel-stub
files (SMS, Teams, Telegram, WhatsApp) now share the empty-token-bypass
pin uniformly — drift toward `?.isEmpty == false` will surface across
the cluster, not just on one stub.

## Pipeline state at exit

- Open BACKLOG: 14 (unchanged)
- Blocked BACKLOG: 1 (REP-280 superseded — unchanged)
- Wip queue: 0 (drained going in, drained going out)
- Last commits all on main, all green under three-skip workaround
- `swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests`:
  1870 passing in ~17s warm (was 1853 at fire start; +17 cases)
- AGENTS.md test count header: 1906

## Next-fire hints

State is fully derivable from `git log`; no special continuation needed.
The L-effort REP-501→REP-505 MLX SPM split is still untouched and remains
human-paired-session territory per its own blocker note. P5 lane on
remaining open backlog items is exhausting common drift-surface targets;
future fires may want to widen to P4 (UX polish on wip branch) or P8
(audit drift between AGENTS.md sections and current state).
