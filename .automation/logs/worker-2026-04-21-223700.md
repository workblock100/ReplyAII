# Worker run 2026-04-21-223700

## Tasks
- REP-021 — IMessageChannel: configurable thread-list pagination

## Outcome
shipped

## What was already done (pre-existing)
- `IMessageChannel.recentThreads(limit:)` already accepted a `limit` parameter and used `LIMIT ?1` in SQL
- `InboxViewModel.syncFromIMessage()` already passed `limit: 50`
- `ChannelService` protocol already had `func recentThreads(limit: Int)` signature
- `MockChannel` in RulesTests already implemented `recentThreads(limit:)`

## What this commit adds
- `ChannelService` protocol extension: `recentThreads()` convenience overload defaulting to 50
- `IMessageChannelTests`: `testThreadListHonorsLimit` (60 rows → 50 returned)
- `IMessageChannelTests`: `testThreadListSortedByRecencyWithLimit` (60 rows → 50 most recent, newest-first)

## File changes
- `Sources/ReplyAI/Channels/ChannelService.swift`: +4 lines (extension overload)
- `Tests/ReplyAITests/IMessageChannelTests.swift`: +38 lines (2 new tests)
- `BACKLOG.md`: status → done

## Tests
- Before: 156
- After: 158
- All green

## Notes
Swift protocol requirements cannot have default parameter values. Used protocol extension
`func recentThreads()` calling `recentThreads(limit: 50)` as the conventional Swift approach.
