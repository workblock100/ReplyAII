# Autopilot fire — 2026-05-07-2011

- **Time used**: 43 min — 14 actions (all P5 hoist+pin to main)
- **Cadence**: average ~3 min/action; mostly single-file hoists, one cross-service refactor (6 files)
- **Wip queue at start**: empty — no P1 drain work, fire was 100% P2 ship-to-main

## Actions this fire

1. P5 hoist+pin: `URLSessionSlackClient.ErrorMessage.{unusableResponse,rateLimited,unexpected(status:)}` + `Header.{authorizationField,contentTypeField,contentTypeJSON,bearer(_:)}`; both verbs share one `handle(response:data:)` dispatcher → main `458cfbe`
2. P5 hoist+pin: `SlackSocketClient.Envelope.{typeKey,ping,hello,eventsCallback}` (Socket Mode `type` discriminator vocab) → main `4b85820`
3. P5 hoist+pin: `SlackOAuthFlow.FormField.{clientID,clientSecret,scope,redirectURI,code}` + `ResponseKey.{ok,accessToken,team,teamName}` (auth-leg + token-exchange-leg + response-parse alignment) → main `601e87c`
4. P5 hoist+pin: `LocalhostOAuthListener.codeQueryParameterName = "code"` (OAuth 2 §4.1.2 spec key) → main `1719d5e`
5. P5 hoist+pin: `NotificationCoordinator.InlineReplyAction.{title,buttonTitle,placeholder}` (system notification action chrome strings) → main `eb6a64f`
6. P5 hoist+pin: `SearchIndex.SQL.{beginTransaction,commitTransaction,rollbackTransaction,truncateAll,deleteByThreadID,insertRow}` (5 writers re-typed identical SQL inline; pin asserts truncateAll has NO WHERE, deleteByThreadID DOES, insertRow column order matches schema) → main `e87b71f`
7. P5 hoist+pin: `ShortcutsExportHandler.payloadQueryParameterName = "data"` (URL-scheme contract with user-authored Shortcut) → main `f9f0856`
8. P5 hoist+pin: `AppleScriptMessageReader.{rowDelimiter,missingValueSentinel,outgoingDirectionValue}` + 2 cross-pin tests verifying AppleScript heredoc emitter still contains the delimiter the Swift parser splits on → main `574afce`
9. P5 hoist+pin: `IMessageSender.{chatGUIDFieldSeparator,chatGUID1to1Marker,chatGUIDGroupMarker,chatGUID1to1Separator}` + synthesis-round-trips-validation pin (validators rewritten to gate on Character markers) → main `611ea58`
10. P5 hoist+pin: `DraftStore.fileExtension = "md"` (writer/enumerator alignment) + write-then-list round-trip test → main `ef3c004`
11. P5 hoist+pin: `PreferenceKey.wipeNamespacePrefix = "pref."` + cross-key pin verifying every declared `PreferenceKey` matches the prefix (catches new-key declarations that forget the prefix at code-review time) → main `cebf28f`
12. P5 hoist+pin (cross-service): `Preferences.appSupportDirectoryName = "ReplyAI"` routed across 6 sites in 5 services (Preferences last-threads cache, Stats, DraftStore, RulesStore, SearchIndex production DB) → main `8111cee`
13. P5 visibility hoist: `SlackTokenStore.storageKey` `private` → `internal` + route 5 inline-literal test references through the constant + new in-test pin → main `286a90e`
14. P5 hoist+pin: `ChannelError.{authorizationDeniedCopy,databaseCorruptedCopy}` (parameterless cases' user-visible toast strings); switch arms route through constants; pins both freeze literal AND verify the case routes through the constant → main `359cdd0`

## Files touched

- Sources: `Channels/SlackHTTPClient.swift`, `Channels/SlackSocketClient.swift`, `Channels/SlackOAuthFlow.swift`, `Channels/LocalhostOAuthListener.swift`, `Channels/ShortcutsExportHandler.swift`, `Channels/AppleScriptMessageReader.swift`, `Channels/IMessageSender.swift`, `Channels/KeychainHelper.swift`, `Channels/ChannelService.swift`, `Services/NotificationCoordinator.swift`, `Services/Preferences.swift`, `Services/Stats.swift`, `Services/DraftStore.swift`, `Rules/RulesStore.swift`, `Search/SearchIndex.swift`
- Tests (peer suites updated): `SlackHTTPClientTests`, `SlackSocketClientTests`, `SlackOAuthFlowTests`, `SlackChannelTests`, `LocalhostOAuthListenerTests`, `ShortcutsExportHandlerTests`, `AppleScriptMessageReaderTests`, `IMessageSenderTests` (in `IMessageSenderAppleScriptTemplateTests` class), `DraftStoreTests`, `PreferencesTests` (in `PreferencesLastThreadsCacheURLTests` class), `KeychainHelperTests`, `NotificationCoordinatorTests`, `SearchIndexTests`, `ChannelErrorTests`

## Pipeline state at exit

- main: 1623 total `func test` declarations (was 1598 at fire start; +25 across 14 commits)
- wip queue: empty (entered empty, exited empty — no P1 drain work this fire)
- BACKLOG: 18 open (no change), 1 blocked, 0 in_progress
- build cache: warm (`swift build` 2.46s after last commit)
- last 3 commits on main: `359cdd0` channel-error copy, `286a90e` slack-token-store visibility, `8111cee` app-support directory cross-service hoist

## Next-fire hints

- All work this fire is structural (no behavior change). Nothing left half-done.
- The cross-service `Preferences.appSupportDirectoryName` hoist (action 12) touched 5 services — if a future fire adds a 6th service that builds an app-support path, route it through `Preferences.appSupportDirectoryName` rather than re-typing `"ReplyAI"`. The wipe-namespace cross-key pin (action 11) will catch the parallel slip on the UserDefaults side.
- Two existing fires (2026-05-07-1611, 2026-05-07-1810) and this one (2026-05-07-2011) have collectively pushed ~50 hoist+pin commits to main with zero behavior change. The supply of high-value hoist targets is shrinking — most remaining magic literals are either (a) inside SwiftUI view bodies (UI-sensitive, branch-only), (b) in deprioritized chat.db code (pivot says don't invest), or (c) already hoisted. Future fires should consider rotating to other P-lanes (P4 polish on wip branches, P5 test coverage for InboxViewModel branches not currently tested) rather than continuing the hoist sweep.
- `swift test` (full) still hangs in this autopilot environment for unclear reasons (background invocation completes but produces no output for >60s). Filtered runs (`swift test --filter <ClassName>`) are reliable and fast. All commits this fire were verified with filtered tests + `swift build`. A future fire may want to investigate why the unfiltered `swift test` invocation under the autopilot harness behaves differently than under a manual shell.
- BACKLOG-listed open items requiring autopilot follow-up: REP-501→REP-505 MLX extraction chain (still L-effort, recommend human-paired session); REP-XCUI-001 XCUITest target bootstrap (still open, deferred); REP-ALERT-260504-1650 MLX startup-crash structural fix (workaround still in place via `defaults write co.replyai.mac pref.model.useMLX -bool false`).
