# Worker run 2026-04-22-043231

## Task
REP-023 — InboxViewModel: rule re-evaluation when RulesStore changes

## Outcome
shipped

## File changes
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` — +38 lines: `reEvaluateRulesForAllThreads()` + `startObservingRules()` methods; `startObservingRules()` wired into `init()`
- `Tests/ReplyAITests/InboxViewModelTests.swift` — +70 lines: `InboxViewModelRuleObservationTests` class with `testRuleAdditionTriggersReEvaluation` and `testRuleChangeUpdatesPinnedThreads`

## Tests
- Before: 165
- After: 167
- Result: 0 failures

## Build
`./scripts/build.sh debug` — success, codesign verified

## Notes
Used `withObservationTracking` single-shot pattern on `rules.rules` with re-arm in `onChange`; the `Task { @MainActor [weak self] }` dispatch is necessary because `onChange` can fire off-actor. Tests use two `Task.yield()` calls to allow the dispatch to land — same pattern as existing async tests in this suite.

Only pin and setDefaultTone are applied on re-evaluation (focus-time actions); archive/markDone/silentlyIgnore remain incoming-message-only as per the existing `applyRules(for:)` contract.
