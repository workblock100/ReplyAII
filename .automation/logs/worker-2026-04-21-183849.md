# worker-2026-04-21-183849

## Task

- REP-013 — Preferences factory-reset + defaults round-trip tests

## Outcome

shipped to main. Solo-shipped: last S at P2.

## Files changed

- `Sources/ReplyAI/Services/Preferences.swift` (+8 / -4) — `registerReplyAIDefaults(in:)` + `wipeReplyAIDefaults(in:)` now accept an injectable `UserDefaults` (defaults to `.standard`)
- `Tests/ReplyAITests/PreferencesTests.swift` (new, +90) — 5 cases

## Tests

- before: 140 passing
- after: 145 passing (5 new: register seeds expected values, default-tone enum alignment, wipe removes pref keys, wipe preserves non-pref keys, wipe idempotent)
- `./scripts/build.sh debug` — success

## Notes

- Dropped the original scope's `testDefaultValueMatchesEnum` as a separate case and folded it into `testDefaultToneMatchesEnumConstant` — same coverage, one less ceremony test.
- First pass tried to verify wipe with `defaults.object(forKey:)` on `PreferenceKey.*` values. That fails under cross-test contamination: `UserDefaults.register(defaults:)` writes to a process-global registration domain that sibling tests seed, so even a fresh suite sees registered defaults. Switched to `persistentDomain(forName:)` reads against locally-scoped `pref.wipe-test.*` keys so the assertions test the actual persistent-store scrub.
- `UserDefaults.registerReplyAIDefaults()` and `wipeReplyAIDefaults()` are still callable with no arguments — production code calls weren't touched.
