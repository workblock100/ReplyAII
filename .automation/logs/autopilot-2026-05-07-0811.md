# Autopilot fire 2026-05-07 08:11 EDT

**Time used**: ~40 min — 12 actions (10 substantive + 2 docs)

## Actions this fire

1. **P5 wip**: pinned 8 AppleScriptMessageReader parser edge cases (`recentChats` trailing pipes / blank-name fallback / Group-chat label / empty + whitespace executor output; `messagesForChat` body-with-`||` lossy / no-separator default-to-`.them` / empty executor) → wip/autopilot-tests-2026-05-07-081717-applescript-edges (`eb4a1af`).
2. **P3 reconciliation**: REP-AUDIT-260505 — annotated issue #2 (the "● error · database is locked" sidebar pill) as DONE on main commit `b4fbae6` (2026-05-06 fix routes pivot-ignored chat.db errors to `syncStatus = .idle`). Issue #1 (prototype-gallery "Open inbox →" click handler) remains open. Pushed to main `8f12f61`.
3. **P1 merge**: smoke-launched + cherry-picked the AppleScript pins to main as `1eba0e5` (fast-forward failed due to the BACKLOG commit between branch base and current main; cherry-pick used instead). Smoke layers all green: process alive at 10s, 2 windows ([Inbox 1180x752][ReplyAI 1360x852]), no crash signatures. Wip branch deleted on origin.
4. **P5 wip**: pinned 3 LocalhostOAuthListener edges (HTTP-200 response body literal pin; method-only request line silently dropped; first-of-duplicate `?code=a&code=b` wins) → wip/autopilot-tests-2026-05-07-083208-oauth-listener-response (`b4071b9`).
5. **P1 ff-merge**: smoke-launched the OAuth listener wip + fast-forwarded to main as `b4071b9`. Wip branch deleted.
6. **P8 doc-sync**: AGENTS.md repo-layout test count `1448 → 1459`, total `~1474 → ~1485` (`f9d6d75`).
7. **P0 fix on main**: `testSuccessResponseBodyIsExactlyOK` (added in step 5) revealed as timing-flaky on subsequent full-suite runs — `conn.receive(...)` fires with empty data before the listener's `.idempotent` send flushes through the loopback. The two sibling pins (request line method-only, duplicate code param) are deterministic and stay; only the body-literal pin removed (`2fcca44`). Note: keeping a flaky test on main blocks every future autopilot P1 wip-merge gate (which runs the full suite).
8. **P5 + P1 merge**: pinned 5 RuleContext.from(thread:) edges (empty-name → senderKnown=true; whitespace-only → phonelike; "+Marketing" → phonelike short-circuit; channel passthrough for every Channel.allCases; default messageCount=0) → cherry-picked + smoke-verified onto main as `e07a3f5`. Wip branch deleted.
9. **P8 doc-sync**: AGENTS.md count `1459 → 1463`, total `~1485 → ~1489` (`63bcbc1`).
10. **P3 reconciliation**: REP-ALERT-260506-2010 workaround note refresh — 4th validation on this fire, pass count `1384 → 1463` (`0e86bb7`).
11. **P8 fire log v1**: wrote `.automation/logs/autopilot-2026-05-07-0811.md` covering actions 1-10 (`2ef32b3`).
12. **P8 gotcha doc**: added a new AGENTS.md gotcha capturing the loopback-HTTP-roundtrip test flakiness pattern (the listener's `defer { connection.cancel() }` races `.idempotent` send semantics; pattern to use instead is exposing the response template at package level for direct XCTAssertEqual). Future fires hunting the OAuth listener path will not repeat the same flaky-test trap (`c740821`).

## Files touched / REPs touched

- Tests/ReplyAITests/AppleScriptMessageReaderTests.swift (+105 LOC, +8 tests)
- Tests/ReplyAITests/LocalhostOAuthListenerTests.swift (+154 LOC then -64 LOC = +90 LOC, net +2 tests after flaky removal)
- Tests/ReplyAITests/RulesTests.swift (+62 LOC, +5 tests)
- AGENTS.md (test-count refresh, twice)
- BACKLOG.md (REP-AUDIT-260505 + REP-ALERT-260506-2010 refresh)

REPs touched: REP-AUDIT-260505 (issue #2 marked done in note), REP-ALERT-260506-2010 (workaround validation note refreshed).

## Pipeline state at exit

- BACKLOG: 18 open, 0 in_progress, 1 blocked (no change in counts; only annotations)
- Wip queue depth: **0** (drained fully — all three wips this fire merged or cherry-picked to main)
- Build cache: WARM (`./scripts/build.sh debug` ran twice this fire, last as part of action 8 smoke-launch)
- Last 3 commits on main: `0e86bb7`, `63bcbc1`, `e07a3f5`
- Net test-count delta: 1448 → 1463 (+15 pin additions, -1 flaky test removed, +1 = net +14 by my count; the actual main `swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` is 1463 with 0 failures in ~14s warm)

## Next-fire hints

- Wip queue is empty. Next fire's P1 lane has nothing to drain; P2 / P5 / P8 lanes are the entry points.
- The flaky `testSuccessResponseBodyIsExactlyOK` was removed but its source-comment-level intent (pinning the literal browser-visible HTTP/1.1 200 OK body) is still uncovered. A future re-add should not depend on `conn.receive` timing — exposing the response string at package level (e.g. an internal `static let okResponseTemplate` on `LocalhostOAuthListener`) would let a deterministic XCTAssertEqual replace the loopback-roundtrip test.
- REP-AUDIT-260505 still has issue #1 open (prototype-gallery "Open inbox →" button doesn't fire on click; `⌘⇧O` works). UI sensitive, requires an XCUITest or careful manual verification — may be a P4 polish candidate but kept on `claimed_by: human` so a future fire shouldn't grab it without explicit reassignment.
- The smoke-launch protocol's MLX workaround (`defaults write co.replyai.mac pref.model.useMLX -bool false`) was applied at the start of each smoke launch and remains set; do not undo. Structural fix lives on REP-501→REP-505.
