---
fire: autopilot-2026-05-07-0011
model: claude-opus-4-7 medium-effort
started: 2026-05-07 00:11:01 EDT
---

# Autopilot fire log — 2026-05-07 00:11

## Time used

~38 min — 11 actions across P5 (test coverage) + 1 P0/P5 hybrid (flaky-test fix).

## Pipeline state at start

- main HEAD: `01a2998` (autopilot 2026-05-06-2210 final log)
- wip queue: empty (drained by previous fire)
- BACKLOG: 18 open, 0 in_progress, 1 blocked
- Build cache: WARM
- Test count claim: 1372

## Actions this fire

1. **P0/P5 — flaky `testFormatRelativeYesterdayBucketReturnsYesterdayLiteral`.**
   Discovered the test was failing on this fire's full-suite run because at
   00:23 EDT, "25 hours ago" lands in 2-days-ago (Tue 23:23) instead of
   "Yesterday" (Wed). The `Date() - 25h` math has a 1-hour window between
   midnight and 01:00 local time where it fails. Replaced with
   `startOfToday - 12h` (yesterday-at-noon), guaranteed to fall inside
   `isDateInYesterday(_:)`'s window regardless of when the test runs. → main
   `a165d55`.

2. **P5 — bulk-action edge cases** (6 tests). `bulkMarkAllRead` and
   `bulkArchiveRead` each had only one happy-path test. Added: empty-list
   no-op, idempotent on already-read, archived-threads-also-touched
   (pinning the `threads.map(\.id)` no-archive-filter contract), pinned
   read threads aren't shielded, already-archived not double-counted. Also
   fixed `makeVM` to use a per-test `threadsCacheURL` so passing
   `threads: []` doesn't fall back to a stale disk cache. → main `6a4d1e7`.

3. **P5 — `InboxViewModel.loadMessages` direct contract** (4 tests). Zero
   prior direct coverage despite being the thread-pane backfill path.
   Added a `RecordingMessagesChannel` mock and pinned: success populates
   liveMessages with limit=40, early-return when already populated, error
   does NOT poison liveMessages (try? semantics), empty response DOES
   populate `[]` so subsequent calls early-return. → main `8618a34`.

4. **P5 — `InboxViewModel.applyRules(for:)` direct contract** (5 tests).
   Zero prior direct coverage; existing rule tests covered only the
   indirect path through `reEvaluateRulesForAllThreads`. Pinned: pin
   action, setDefaultTone (and the contract that distinguishes applyRules
   from reEvaluateRulesForAllThreads — the former updates regardless of
   selectedThreadID), setDefaultTone idempotent, archive/markDone/silently
   Ignore branch falls through, non-matching rule no-op. → main `def6716`.

5. **P5 — `advanceToNextThread` via confirmSend** (3 tests). The private
   advanceToNextThread had no observable-contract pin. Added: success
   advances selection, last-thread-wraps-to-first, failed-send does NOT
   advance. Pins the ⌘↵ "burn down the inbox" muscle memory. → main
   `be956a1`.

6. **P5 — `chatDBAvailable` per SyncStatus** (5 tests). One test per
   SyncStatus enum case (idle, syncing, live, denied, failed). Pins that
   only `.live` is "available", which is the gate that determines whether
   `applyIncomingNotification` (UNNotification fallback) runs. → main
   `225c237`.

7. **P5 — `applyIncomingNotification` gate + reorder + dedup heuristics**
   (4 tests). Existing 3 tests covered only chatGUID matching paths. Added:
   chatDBAvailable gate (live → no-op), updated thread moves to index 0
   (recency sort), nil-chatGUID + senderHandle == thread.name dedupes
   (AppleScript-fallback path), nil-chatGUID + chatGUID-suffix dedupes
   (notification carries phone, thread carries `iMessage;-;+15551234567`).
   → main `f3f70ab`.

8. **P5 — `confirmSend` default-arm: unsupported channel** (1 test). The
   `default:` arm of confirmSend's per-channel switch (whatsapp/teams/
   telegram throws `IMessageSender.SendError.unsupported`) had no test.
   Pinned: a sendToast is set + selectedThreadID does NOT advance. → main
   `b232b62`.

9. **P5 — Snooze edge cases** (3 tests). Added: re-snooze before original
   wake fires keeps the latest entry (pins `wakeIfStillSnoozed`'s
   `current == expectedWake` guard against stale Tasks blowing away new
   snoozes), snooze overwrites prior wake, unsnooze on non-snoozed is
   no-op. → main `de32685`.

10. **P5 — `filteredThreads` searchQuery filter** (6 tests). Zero prior
    coverage despite searchQuery being the entire input path for the
    inbox search field. Pinned: empty pass-through, whitespace-only
    trimmed → empty, name match case-insensitive, preview match
    case-insensitive, archive composes (archived hidden during search),
    channel filter composes (search narrows within active channel). →
    main `d92a38f`.

11. **P5 — Derived counts (folderLabel, needsYouCount, handledCount)**
    (4 tests). Zero direct coverage despite driving the inbox header label
    + "X need you / Y handled" badges. Pinned: folderLabel resolves via
    `folders` array, falls back to "Inbox" when activeFolder isn't
    represented, needsYouCount counts unread *threads* (not sum of unread
    counts) and respects archive, handledCount + needsYouCount ==
    filteredThreads.count invariant. → main `acacb78`.

12. **P5 — `ViewState.==` operator** (3 tests). The custom Equatable for
    ViewState had no direct coverage despite gating SwiftUI's @Observable
    re-render decisions. Pinned: .error(_) == .error(_) compares by
    errorDescription, distinct error descriptions are unequal, cross-case
    pairs (loading vs populated, populated vs demo, etc.) always return
    false via the `default:` branch. → main `625cce4`.

## Files touched

- `Tests/ReplyAITests/IMessageChannelTests.swift` — yesterday-bucket
  flake fix (action 1).
- `Tests/ReplyAITests/InboxViewModelTests.swift` — +38 new tests across
  actions 2–12, plus per-test `threadsCacheURL` isolation in the bulk
  filter test class.
- `AGENTS.md` — test count 1372 → 1416 across 11 increments.
- `.automation/logs/autopilot-2026-05-07-0011.md` (this file).

## Test totals

- **Test declarations on main: 1416** (was 1372 at fire start; +44 net,
  with 1 modify-only on the yesterday-bucket flake. New cases by suite:
  bulk filter +6, loadMessages +4, applyRules +5, send/advance +4,
  chatDBAvailable +5, applyIncomingNotification +4, snooze +3,
  searchQuery +6, derived counts +4, viewstate equality +3 = 44 new).
- **Full-suite gate (`swift test --skip ContactsResolverTests --skip
  InboxViewModelIsSyncingTests`): 1378 passed, 0 failed**, completes in
  ~17s after build.

## REPs added/closed

- **No new REPs added.**
- **No REPs closed** — this fire was pure P5 test pinning + 1 flaky test
  fix; no scope-bound REPs were in flight.

## Pipeline state at exit

- **Open BACKLOG count:** 18 (unchanged — no scope work this fire).
- **In-progress:** 0.
- **Blocked:** 1 (unchanged).
- **wip queue depth:** 0.
- **Build cache:** WARM.
- **Last 5 commits on main:**
  - `625cce4` tests: pin ViewState.== — error/error, error mismatch, cross-case default
  - `acacb78` tests: pin folderLabel / needsYouCount / handledCount derived counts
  - `d92a38f` tests: pin filteredThreads searchQuery — name/preview/compose/trim
  - `de32685` tests: pin snooze edge cases — re-snooze, overwrite, no-op unsnooze
  - `b232b62` tests: pin confirmSend default-arm — unsupported channel surfaces toast

## Next-fire hints

- **`testFormatRelativeYesterdayBucketReturnsYesterdayLiteral` is now
  date-stable.** Future fires that fall in the 00:00–01:00 local-time
  window won't be tripped by this. The same flake class — date math
  using `Date() - Nh` where N is close to a 24h multiple — could exist
  elsewhere; if a future fire hits a similar boundary failure, prefer
  the `startOfToday + cal.date(byAdding:value:to:)` pattern over wall-
  clock offsets.
- **InboxViewModel coverage is now strong.** 8 of the 11 actions this
  fire targeted InboxViewModel methods/properties. Methods that still
  lack direct (non-integration) coverage: `rescheduleSnoozeWakes` (hard
  to test without exposing the private Task), `selectedThread` fallback
  on unknown ID (would assert against `threads[0]` which crashes on
  empty list — not a useful pin), private `markPinned`/`markUnreadZero`
  (covered via callers), `consumeNotificationReply` already at 2 tests,
  `processIncomingForRules` covered by RulesTests. Future P5 lanes
  should look at non-InboxViewModel surfaces.
- **Wip queue empty + no in-flight scope work.** P2 (ship next high-
  value task to main) is the natural next lane, but the open non-ui-
  sensitive non-claimed REPs are limited to the MLX P0 chain (REP-501
  → REP-505 — risky for autopilot, M-effort) and REP-XCUI-001
  (XCUITest target bootstrap — M-effort, complex). Most P2/P4 work is
  ui_sensitive → push to wip.
