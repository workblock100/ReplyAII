# worker-2026-04-22-013926

## Task
REP-025 — IMessageSender: AppleScript execution timeout

## Outcome
shipped

## File changes
- `Sources/ReplyAI/Channels/IMessageSender.swift` +30/-12
  - Added `SendError.timedOut` case
  - Added `nonisolated(unsafe) static var sendTimeout: TimeInterval = 10`
  - Added `nonisolated(unsafe) static var executeHook: ((String) throws -> Void)? = nil`
  - Refactored `sendRaw` to capture executor (real or hooked), run it on `DispatchQueue.global(qos: .userInitiated)`, and time out via `DispatchSemaphore`
- `Tests/ReplyAITests/IMessageSenderTests.swift` +40/-0
  - Added `testSendTimeoutReturnsError`: injects 0.1s timeout + sleeping executor; verifies `.timedOut` within 0.5s
  - Added `testNormalSendCompletesBeforeTimeout`: injects instant no-op executor; verifies no throw

## Tests
- Before: 171 (all green)
- After:  173 (all green)

## Notes
Used `SendError.timedOut` (enum-local) rather than `ChannelError.sendFailed` (backlog
description was loose). `ChannelError` is for channel-level read errors; send errors
live on `SendError`. The timeout and hook are injected via `nonisolated(unsafe) static`
vars — consistent with how the codebase handles other static mutable state in enums.
REP-059 (retry on -1708) can now inject its test timeout via `sendTimeout`.
