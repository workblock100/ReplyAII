# Worker log — 2026-04-23-064432

## Tasks
- REP-105 (P2, M): Stats — persist lifetime counters to disk across app launches
- REP-139 (P2, S): Stats — flushNow() for clean-shutdown counter persistence
- REP-159 (P2, S): IMessageChannel — MessageThread.hasAttachment from message-level SQL

## Outcome: shipped

## File changes
- `Sources/ReplyAI/Services/Stats.swift`: ~+30/-10 lines
  - `fileURL` property changed from `URL` to `URL?` (nil = no file I/O, for tests)
  - `Stats.shared` now explicitly passes `defaultFileURL()` (no behavior change for production)
  - `persist()` refactored to debounced 2 s `DispatchWorkItem` via a dedicated serial queue
  - Added `writeLock: NSLock` + `pendingWrite: DispatchWorkItem?` for race-free cancel/replace
  - Added `flushNow()`: cancels pending debounce, writes synchronously, no-op when URL nil
  - `defaultFileURL()` promoted from `private static` to `static` so shared can call it
- `Sources/ReplyAI/App/ReplyAIApp.swift`: +7 lines
  - `NSApplication.willTerminateNotification` observer in `init()` calls `Stats.shared.flushNow()`
- `Tests/ReplyAITests/StatsTests.swift`: ~+90 lines
  - Existing round-trip tests (`testStatsRoundTripThroughJSON`, `testPerChannelCountersRoundTrip`,
    `testPerToneCountersRoundTrip`, `testMatchedCountRoundTrip`) updated to call `flushNow()`
    before reading the second Stats instance (required now that persist() is debounced)
  - New class `StatsLifetimePersistenceTests` (REP-105): 3 tests
    - `testLifetimeCountersSeedFromDisk`
    - `testLifetimeCountersAccumulateAcrossInits`
    - `testNilURLSkipsPersistence`
  - New class `StatsFlushNowTests` (REP-139): 3 tests
    - `testFlushNowPersistsBeforeDebounce`
    - `testFlushNowIsIdempotent`
    - `testFlushNowWithNilURLIsNoop`
- `Tests/ReplyAITests/IMessageChannelTests.swift`: ~+90 lines
  - New class `IMessageChannelThreadHasAttachmentTests` (REP-159): 2 tests
    - `testThreadHasAttachmentTrueWhenMessageHasAttachment`
    - `testThreadHasAttachmentFalseWhenNoMessagesHaveAttachment`

## Tests
- Before: 463 (based on AGENTS.md)
- After: 475 (+12 new test cases)
- Result: 475 passed, 0 failed

## Notes
- Debounce design: rapid-fire increments (e.g. 4000 calls in concurrent stress test) now coalesce
  into one I/O call after a 2 s idle window. `flushNow()` is the escape hatch for shutdown.
- REP-159 tests verify thread-level hasAttachment via recentThreads() (not messages()), which
  derives the value from the most-recent message's cache_has_attachments column.
- No behavior change for production: Stats.shared still persists to the same default path.
