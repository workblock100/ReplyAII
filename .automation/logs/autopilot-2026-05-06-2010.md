# Autopilot fire 2026-05-06 20:10 EDT

**Time used:** ~50 min — 13 commits + 1 abandoned merge attempt

**Model:** claude-opus-4-7 medium-effort

## Actions this fire

1. **P7 doc-comments — Inbox SwiftUI views.** Added WHY-style doc-comments to the 8 view structs in `Sources/ReplyAI/Inbox/` that had no module-level doc: `ComposerView`, `TonePills`, `SidebarView`, `ContextCard`, `MessageBubble`, `ThreadDetailView`, `ThreadListView`, `ThreadRow`. Each new comment captures the non-obvious design constraint that motivated the component — e.g. why `ComposerView` is nested inside `ThreadDetailView` rather than a sibling, why the 28-pt sidebar top spacer is load-bearing under the hidden title bar, why the 2-pt selection accent bar is rendered always-present (clear when unselected) to avoid row-shift on selection animations. → main `ffdacd2`.

2. **P7 doc-comments — Settings panes.** Added WHY-style doc-comments to the 4 Settings views that had no module-level doc: `SetAccountView`, `SetPrivacyView`, `SetShortcutsView`, `SetVoiceView`. The two prototype-only views (Account, Voice) are now flagged as static placeholders pending the cloud-sync and voice-fine-tuning pipelines so the next reader doesn't waste time wiring them to non-existent backends. The Privacy and Shortcuts docs explain the load-bearing patterns (direct `@AppStorage` for instant toggle feedback; the hardcoded `rows` array as the canonical keyboard-shortcut reference that the menu bar and AGENTS.md must mirror). → main `4ea888d`.

3. **P1 wip-merge attempt — abandoned.** Tried to drain `wip/autopilot-tests-2026-05-06-183430-menubar-waiting` (a tests-only branch adding 10 `InboxViewModelMenuBarWaitingTests` cases — clean banned-pattern scan, +157 LOC, no UI sensitivity). `swift test --skip ContactsResolverTests` made real progress this time (589 test cases passed) but **hung on `InboxViewModelIsSyncingTests.testIsSyncingFalseAfterSuccess`** at `Tests/ReplyAITests/InboxViewModelTests.swift:1193`. xctest wrote no further output for 2+ min while the process stayed alive. Killed it. No merge. Branch returned to origin untouched.

4. **P7 doc-comments — InboxViewModel nested types.** Added WHY doc-comments to `SyncStatus` (single source of truth for sidebar pill + FDA banner + viewState — explains why the lifecycle lives on the view model rather than being derived) and `SendConfirmation` (snapshot at request-time so the user confirms exactly what they're sending even if active thread/tone changes between request and confirm). → main `253638a`.

5. **P7 doc-comments — DraftEngine + ContactsResolver nested types.** Added WHY doc-comments to `DraftEngine.ModelLoadStatus` (Sendable because MLX progress callbacks fire off the main actor and hop back through `@MainActor`) and `ContactsResolver.Access` (tracked separately from `CNAuthorizationStatus` for test override-ability without touching the real Contacts daemon). → main `9fd7eb7`.

6. **P0/P3 BACKLOG addition — REP-ALERT-260506-2010.** Codified the `swift test` hang diagnostic into a new BACKLOG P0 entry with reproducer, suspected root cause (leaked `BlockingMockChannel` continuation across test boundary causing actor deadlock), success criteria, and an interim workaround path (second `--skip` flag in SKILL.md) until the test is fixed. So the next fire (or a human) can act on the finding without re-deriving it. → main `b9098af`.

7. **P8 audit — AGENTS.md gotcha refresh.** Appended a 2026-05-06 data point to the existing "swift test full-suite hangs intermittently" gotcha. Today's fire got 589 cases through (deeper than 2026-05-05 runs), reinforcing that the boundary is non-deterministic — depends on AddressBook daemon retry timing per run. Cross-referenced the new BACKLOG REP. → main `1e3ddf9`.

8. **P7 doc-comments — IMessage nested types.** Added WHY doc-comments to `IMessageChannel.Pending` (cursor-staging shape decoupled from contact-name resolution to keep the SQLite worker thread off the Contacts daemon's XPC retry loop) and `IMessageSender.SendError` (each `errorDescription` string renders directly in the inbox toast, so adding a case is a UI change). → main `d3c5ec8`.

9. **autopilot log v2.** Updated this log mid-fire to reflect the 4 commits that landed after the original log was committed (action 4 timeline midpoint). → main `8309695`.

10. **P7 doc-comments — SettingsShell.Tab.** Added a WHY doc-comment explaining the case-list / label-switch / SettingsView-routing lockstep — adding a new pane is a single-file change with three required edits, designed so a missing pane fails to compile rather than rendering a dead nav row. → main `4c5de0d`.

11. **P3 — REP-ALERT-260506-2010 hypothesis refinement.** Code-inspected the suspected leak path (`testIsSyncingTrueWhileSyncFromIMessageIsRunning` at `Tests/ReplyAITests/InboxViewModelTests.swift:1170`). Found that test already has a `defer { channel.blocking = false; channel.resumeAll() }` safety guard at line 1183, weakening the original leaked-continuation hypothesis. Updated the BACKLOG entry to favor two more-likely candidates: (a) the broader "InboxViewModel-class-boundary hang" from AGENTS.md gotcha #243, (b) a pending `@MainActor` task from the previous test waiting behind the new vm's sync on the same actor lane. → main `0b9df91`.

12. **autopilot log v3.** Reflect actions 9–11. → main `d1cd3bc`.

13. **P7 doc-comments — SetChannelsView.Row + SetModelView.CacheStatus.** Two more nested types: `Row` documents how it's built from `Channel.allCases` + live keychain state and why `canConnect=false` gates the trailing button for `.comingSoon` (avoid no-op auth flows). `Row.Status` flags that its text strings render literally to the user (changing one is a UI change). `CacheStatus.unknown` documented as the pre-probe state so the UI doesn't flash "Not downloaded" before the disk read completes. → main `6ec1ae3`.

## Diagnostic — new blocker for `swift test` on autopilot

The `--skip ContactsResolverTests` workaround that previous fires assumed would unblock `swift test` is **insufficient**. There is a second hang point at `InboxViewModelIsSyncingTests.testIsSyncingFalseAfterSuccess` (or possibly an earlier test in that suite that leaks an unresumed `BlockingMockChannel` continuation onto the `@MainActor`, with the next test inheriting the deadlock — needs investigation). Without resolving this, autopilot fires cannot complete a `swift test` run, and the merge gate stays effectively locked.

**Recommended next-fire actions** (in priority order):
- **Open a new BACKLOG REP** for the `InboxViewModelIsSyncingTests` hang. Suggested: `REP-AUTOPILOT-TEST-HANG`, claimed_by: human, files_to_touch: `Tests/ReplyAITests/InboxViewModelTests.swift` (lines 1180-1217 area). Repro: `cd ~/.cache/replyai-autopilot && swift test --skip ContactsResolverTests --filter InboxViewModelIsSyncingTests`.
- **As a workaround** for autopilot fires, extend the test-run flag to `swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests` until the hang is fixed. Update SKILL.md's P1 step to use both `--skip` flags. (Risk: if more hangs surface, the skip list grows — eventually we'd be running too few tests for the merge gate to mean anything. Fix > skip.)
- **Suspect**: a prior test in the file (perhaps `testIsSyncingTrueWhileSyncFromIMessageIsRunning` at line ~1180) creates a `BlockingMockChannel` with `blocking = true`, awaits a sync task, and depends on `channel.resumeAll()` unblocking it — if any execution path skips `resumeAll()`, the continuation is leaked. The next test (`testIsSyncingFalseAfterSuccess`) creates a new channel but the leaked continuation may still be holding the actor.

## Files touched

- 8 Inbox SwiftUI view structs (`Composer/ComposerView`, `Composer/TonePills`, `Sidebar/SidebarView`, `Thread/ContextCard`, `Thread/MessageBubble`, `Thread/ThreadDetailView`, `ThreadList/ThreadListView`, `ThreadList/ThreadRow`)
- 4 Settings panes (`SetAccountView`, `SetPrivacyView`, `SetShortcutsView`, `SetVoiceView`)
- 6 nested types (`InboxViewModel.SyncStatus`, `InboxViewModel.SendConfirmation`, `DraftEngine.ModelLoadStatus`, `ContactsResolver.Access`, `IMessageChannel.Pending`, `IMessageSender.SendError`)
- `BACKLOG.md` — new REP-ALERT-260506-2010 entry
- `AGENTS.md` — gotcha #243 swift test hang gets 2026-05-06 data point
- `.automation/logs/autopilot-2026-05-06-2010.md` (this file)

~110 lines of WHY-comment added across 18 source files + 2 doc files. Zero runtime impact.

## REPs added/closed

- **Added** `REP-ALERT-260506-2010` (P0, status: open, claimed_by: null) — `swift test` hangs at `InboxViewModelIsSyncingTests.testIsSyncingFalseAfterSuccess`, blocking autopilot wip-merges. Includes reproducer + suspected root cause + interim workaround.
- **No closures.** All 8 commits are doc/diagnostic-only; no open BACKLOG REP tracks "doc gaps in views" or "swift test hang" prior to today's REP-ALERT.

## Pipeline state at exit

- **Open BACKLOG count:** 21 (+1 this fire — new REP-ALERT-260506-2010)
- **In-progress:** 0
- **Blocked:** 1
- **wip queue depth:** 4 (all from the prior 2026-05-06 18:10 autopilot fire — none rotted yet, all 2 hours old):
  - `wip/autopilot-polish-2026-05-06-181306-channel-count-fix` (1-file polish, mergeable iff `swift test` gate clears)
  - `wip/autopilot-polish-2026-05-06-181549-reducemotion` (REP-083 — ui_sensitive)
  - `wip/autopilot-polish-2026-05-06-183151-rep082-thread-selection-bar` (REP-082 — ui_sensitive)
  - `wip/autopilot-tests-2026-05-06-183430-menubar-waiting` (this fire's failed-merge target)
- **Build cache:** WARM (`.build` mtime <6h)
- **Last 5 commits on main:**
  - `6ec1ae3` docs: SetChannelsView.Row + SetModelView.CacheStatus
  - `d1cd3bc` autopilot: 2026-05-06-2010 log v3
  - `0b9df91` backlog: REP-ALERT-260506-2010 hypothesis refinement
  - `4c5de0d` docs: SettingsShell.Tab WHY comment
  - `8309695` autopilot: 2026-05-06-2010 log v2
- **Test count drift:** none. Actual `func test*` declarations in `Tests/ReplyAITests/`: **1334**, AGENTS.md claims **1334**.

## Time accounting

Roughly 14 min went to the two `swift test` attempts (10 min hung on the first try with `tail -25` truncating output; 4 min hung on the second try after I saw 589 tests pass and felt confident). That spend produced the diagnostic finding now codified as REP-ALERT-260506-2010 — the highest-leverage thing this fire surfaced for future fires. The remaining ~36 min went to 8 commits across 4 distinct lanes (P7 file-level docs × 2, P7 nested-type docs × 3, P0 BACKLOG addition, P8 AGENTS.md audit refresh).

## Next-fire hints

- **The `swift test` hang at `InboxViewModelIsSyncingTests.testIsSyncingFalseAfterSuccess` is now a known structural blocker for autopilot wip-merges**, codified as REP-ALERT-260506-2010 in BACKLOG. Don't repeat the diagnosis; either fix the test, add a second `--skip` flag to SKILL.md, or stay in lanes that don't require `swift test` (P3, P4 polish-to-wip, P7 doc, P8 audit). Note the broader pattern from AGENTS.md gotcha #243 — narrowing the skip list just moves the hang boundary, so adding `--skip InboxViewModelIsSyncingTests` may not actually help.
- The 4 wip branches at the head of the queue are NOT rotted yet (all 2h old as of 20:42 EDT) but will start aging out around 2026-05-11. The non-ui-sensitive ones (`channel-count-fix`, `menubar-waiting`) are mergeable iff the test gate clears.
- **Doc-comment lane is now thoroughly drained.** Inbox/ SwiftUI views ✓, Screens/Settings/ ✓, Onboarding/ already had docs, key nested types (InboxViewModel, DraftEngine, ContactsResolver, IMessageChannel, IMessageSender) ✓. Remaining doc-comment gaps are inside private helper computed properties or per-case Codable enums — much lower-leverage than what this fire shipped. Future fires should not lead with P7 unless a specific new type lands.
- **Highest-leverage open work without a `swift test` dependency**: REP-501→REP-505 (MLX SPM target split — would unblock fast `swift test` on fresh clones, but L-effort and risky for autopilot fires per existing BACKLOG note); REP-AUDIT-260505 audit findings (UI fixes — ui_sensitive, push to wip); a fix attempt on REP-ALERT-260506-2010 itself (S-effort).
