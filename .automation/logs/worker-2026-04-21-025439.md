# Worker run: worker-2026-04-21-025439

**Outcome:** shipped  
**Tasks:** REP-022, REP-024  
**Bundle rationale:** Both S-effort P1 tasks; SUBSTANTIVENESS GATE requires bundling when multiple S tasks exist at same priority.

## File changes

| File | Change |
|------|--------|
| `Sources/ReplyAI/Services/Stats.swift` | +15 lines: `static let shared`, `ruleLoadSkips` counter in Snapshot, `recordRuleLoadSkips()` method |
| `Sources/ReplyAI/Rules/RulesStore.swift` | +30 lines: init takes `stats: Stats`, `loadOrSeed` returns `([SmartRule], skips: Int)`, element-by-element JSON decode via JSONSerialization |
| `Sources/ReplyAI/Inbox/InboxViewModel.swift` | +8 lines: `isSyncing` property, guard+defer in `syncFromIMessage()`, `contacts` made injectable |
| `Tests/ReplyAITests/InboxViewModelTests.swift` | +90 lines: new file, 3 tests covering the sync guard |
| `Tests/ReplyAITests/RulesTests.swift` | +70 lines: 4 new tests for malformed-rule skipping |
| `BACKLOG.md` | status fields updated |

## Test results

- Before: 145 tests, all green  
- After: 162 tests, all green (EXIT_CODE:0)  
- New tests: `testConcurrentSyncCallsDoNotOverlap`, `testSyncGuardResetsAfterError`, `testSyncGuardResetsAfterSuccess`, `testMalformedRuleIsSkipped`, `testPartiallyCorruptRulesFileLoadsValidRules`, `testFullyCorruptJSONFallsBackToSeeds`, `testEmptyArrayLoadsZeroRules`

## Notes

- `ContactsResolver` had to be made injectable in `InboxViewModel.init` (added `contacts: ContactsResolver? = nil`) to avoid `contacts.ensureAccess()` blocking on the system Contacts permission dialog during tests. Production default is `ContactsResolver()` — no behavior change.
- `Stats.shared` singleton added as the default for `RulesStore.init(stats:)`; tests pass their own `Stats(fileURL:)` instances for isolation.
- NSLock warnings in async context (InboxViewModelTests) are in Swift 5 compat mode — warnings not errors, consistent with existing project patterns.
