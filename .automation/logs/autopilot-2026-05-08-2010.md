# Autopilot fire — 2026-05-08-2010

## Time used

~17 min — 5 actions (pace stayed in cache window for all turns).

## State on entry

- Wip queue depth: **0** (empty before this fire)
- BACKLOG: 14 open, 0 in_progress, 1 blocked
- Build cache: WARM
- Last 5 commits on main were all autopilot pin work (1916→1919 test count)

## Actions this fire

1. **P5 test coverage:** `channel-stub-tests: pin newIncomingMessages empty-shim default per stub` → main `f7bea18`
   - Added `Tests/ReplyAITests/ChannelStubNewIncomingMessagesDefaultTests.swift` (+9 cases)
   - Pins the deliberate asymmetry on the 4 stubs (SMS, Teams, Telegram, WhatsApp): `recentThreads`/`messages` are auth-gated and throw `authorizationDenied` without a token, but `newIncomingMessages` inherits the protocol-extension empty-shim and returns `[]` without throwing — so the rule engine's incoming-message replay loop can polymorph across all configured channels without an unauthorized stub aborting the cycle.
   - Each stub gets a with-token + without-token case, plus a single cross-stub asymmetry contract case.
   - Bumps the AGENTS.md test count claim 1919 → 1928 (1883 → 1892 under the three-skip workaround).

2. **P3 BACKLOG reconcile:** `agents.md: priority-queue resolved REP-082 + REP-083 (shipped a004936)` → main `a2d9b90`
   - Both polish items were marked "Awaiting merge" in AGENTS.md but landed on main in the consolidation commit `a004936` on 2026-05-07. Wip branches gone from origin. Verified by checking source: `ThreadListView.swift:13` declares `@Namespace selectionNamespace`, `ThreadRow.swift:86–89` uses `matchedGeometryEffect(id: "thread-selection-bar", in: ns)` gated on `!reduceMotion && isSelected`; 5 SwiftUI files declare `@Environment(\.accessibilityReduceMotion)`.
   - Strikethrough both bullets and pointed at the shipping commit + actual code paths.

3. **P3 BACKLOG cleanup:** `backlog: drop 8 stale 'pending human review/merge' blocker lines on done tasks` → main `3d6a522`
   - 8 REPs (209, 224, 244, 246, 248, 249, 258, 269) carried stale "pending human review/merge tracked in REP-282/284/286/287/288" blocker lines from the worker-push-to-wip era. Every one of those tracker REPs is itself `status: done` now, and every blocked task has a real `done_on:` commit on main. Reading "status: done" alongside "blocker: pending review" misleads both human readers and any planner that re-reads the BACKLOG cold.
   - Stripped only the blocker lines; left status / done_on / claimed_by intact.

4. **P3 BACKLOG cleanup:** `backlog: drop 30 more stale blocker lines on done tasks` → main `110d554`
   - Same shape, broader sweep. Patterns matched (case-insensitive, on done tasks only): `wip/`, `MLX fresh-clone`, `MLX cold-build`, `MLX full build`, `human should run`, `human review`, `code complete`, `Implementation complete`, `Pivot P0 implementation complete`, `Superseded by`, `cleared — merged`, `TWO complete implementations`. `blocker: null` left untouched.
   - REPs affected: 228, 236, 255, 266, 278, 280, 237, 238, 256, 257, 260, 261, 264, 267, 272, 274, 129, 135, 177, 179, 183, 187, 205, 206, 208, 229, 231, 220, 243, 247.
   - **Post-cleanup invariant:** zero done tasks in BACKLOG carry a non-null blocker line.

5. **P8 audit:** `backlog: header now describes single-agent autopilot mode, not legacy pipeline` → main `079cb31`
   - The BACKLOG header still claimed "maintained by the planner agent. The hourly worker picks the highest-priority open, non-ui-sensitive task". That hasn't been true since the 2026-05 transition to the `replyai-autopilot` scheduled task. Mirrored the same "Operating mode (2026-05): single-agent autopilot" callout that already lives at the top of `.automation/README.md` — the two docs no longer disagree about which pipeline is live.

## Files touched

- `Tests/ReplyAITests/ChannelStubNewIncomingMessagesDefaultTests.swift` (new, +148 lines)
- `AGENTS.md` (test count header 1919→1928; REP-082 + REP-083 priority-queue bullets)
- `BACKLOG.md` (38 stale blocker lines removed; header 1-line update)

## REPs closed

None directly — the BACKLOG cleanups only stripped misleading metadata from already-done REPs; they were already `status: done` going in. Reconciliation, not closure.

## Pipeline state at exit

- Wip queue depth: **0**
- BACKLOG: 14 open (REP-ALERT-260504-1650, REP-501→504 chain, REP-505 human, REP-XCUI-001, REP-AUDIT-260505 human, REP-043/044/045 ui-sensitive, REP-062 human, REP-259 ui-sensitive), 0 in_progress, 1 blocked. Zero `status: done` task carries a non-null blocker line anymore.
- Build cache: WARM
- Test count on main: **1928** (full enumerate via `grep -h "^[[:space:]]*func test" Tests/ReplyAITests/*.swift | wc -l`); under the three-skip workaround (`--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests`), 1892 pass / 2 skipped / 0 failures in ~17.4s warm.
- Last 5 commits on main (newest first): `079cb31`, `110d554`, `3d6a522`, `a2d9b90`, `f7bea18`.

## Next-fire hints

- **The autopilot's available-action surface keeps shrinking as the codebase saturates with pin tests.** Most public Swift surfaces (Channel cases, SearchIndex SQL, Stats keys, Preferences keys, AppleScriptMessageReader constants, IMessageChannel formatting helpers, MLXDraftService progress-message constants, IMessagePreview URL-collapse pins, etc.) are now pinned with both literal-equality AND cross-call-site routing tests. Future P5 work should look for *new* surfaces (e.g. anything in `Sources/ReplyAI/` whose mtime is recent and whose peer test file is small) rather than scanning the whole tree.
- **No "done task with stale blocker" cleanup remains** — confirmed via Python sweep at exit. If a future fire finds one, it means a worker pushed a wip-style blocker line on a task that subsequently shipped without re-cleaning.
- **The legacy 7-agent prompts at `.automation/*.prompt` still exist on disk** but the README + AGENTS.md + BACKLOG header all now agree they're reference-only. Don't re-activate them without a token-budget reason.
- **REP-501→505 (MLX SPM split) remains the highest-leverage open task** — it would close the P0 runtime alert REP-ALERT-260504-1650 and unblock the autopilot's `swift test` gate (currently masked by the three-skip workaround). It's L-effort and risky; recommend a human-paired session.
