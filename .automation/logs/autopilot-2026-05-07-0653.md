# autopilot fire — 2026-05-07-0610

**Time used**: 41 min so far — 6 P5 actions + 1 doc bump (continuing per the 50-min mandate)
**Model**: claude-opus-4-7 medium-effort

## Actions this fire

1. **P5 tests**: pinned `Stats.writeWeeklyLog` empty-rules emits literal
   `- rulesFiredByAction: {}` placeholder, plus multi-entry sort-by-key
   + `, ` join contract. Empty placeholder keeps weekly archives column-
   aligned; sort pin protects week-over-week diffs from dictionary
   hash-order churn. +2 tests → main `4bded07`
2. **P5 tests**: pinned `KeychainHelper` default service literal
   (`co.replyai.app`) and the `ReplyAI-` `kSecAttrAccount` prefix
   contract via a direct `SecItemCopyMatching` lookup. Renaming either
   silently orphans every existing user's tokens with no compile error.
   +2 tests → main `5c7b672`
3. **P5 tests**: pinned `ShortcutsExportHandler` two cascade paths —
   missing-preview-and-missing-messages produces empty preview + empty
   time (cascade through `??` chain) rather than throwing; unrecognized
   `from` values ("system"/""/arbitrary) all map to `.them`. Pin so a
   future schema change adding a "system" sender doesn't silently land
   in `.them`. +2 tests → main `848e146`
4. **P5 tests**: pinned `IMessagePreview` SHOUTY/Mixed-case `WWW.`
   prefix lowercase + strip behavior, plus the `🔗 ` link-prefix
   one-ASCII-space spacing literal. A future refactor that dropped
   `host.lowercased()` would silently regress to the full host. +2
   tests → main `7059b26`
5. **P5 tests**: pinned `UNNotificationContentParser` non-String
   `userInfo` safety — NSNumber/NSNull values for `CKSenderID`,
   `sender`, `CKChatIdentifier`, `CKChatGUID` cascade safely through
   the resolution order rather than crash or `String(describing:)`
   leak a numeric value into thread matching. userInfo arrives as
   `[AnyHashable: Any]` so the `as? String` casts must fail-soft.
   +2 tests → main `882ae21`
6. **P5 tests**: pinned `IMessageSender.validateChatGUID` extra-
   separator rejection (4-part GUID with trailing `;<chunk>` fails
   the `parts.count == 3` check) and case-sensitive `iMessage` prefix
   (lowercase `imessage` and uppercase `IMESSAGE` both fail).
   `split(...maxSplits:3)` can return up to 4 parts; case sensitivity
   matches what chat.db emits verbatim. +2 tests → main `bcb89fe`
7. **P8 doc bump**: AGENTS.md test-count line updated from 1434 → 1446
   to reflect the +12 tests added this fire (commit pending in the
   fire-log push below).

## Files touched / tests added

- `Tests/ReplyAITests/StatsTests.swift` — +2 weekly-log pin tests
- `Tests/ReplyAITests/KeychainHelperTests.swift` — +2 service/prefix pin tests
- `Tests/ReplyAITests/ShortcutsExportHandlerTests.swift` — +2 cascade pin tests
- `Tests/ReplyAITests/IMessagePreviewTests.swift` — +2 www/spacing pin tests
- `Tests/ReplyAITests/UNNotificationContentParserTests.swift` — +2 non-String safety pin tests
- `Tests/ReplyAITests/IMessageSenderTests.swift` — +2 validateChatGUID pin tests
- `AGENTS.md` — test-count bump 1434 → 1446

Total: +12 tests, 0 source changes. Every pin validated via
targeted `swift test --filter ...` (each ran in <0.05s).

## Pipeline state at exit

- BACKLOG.md: 18 open + 0 in_progress + 1 blocked (unchanged — no
  REP picked up; this fire is pure P5 pin work)
- wip queue depth: 0 (none added or merged this fire)
- build cache: WARM
- last 6 commits on main:
  - `bcb89fe` tests: pin validateChatGUID extra-separator + case-sensitive prefix
  - `882ae21` tests: pin UNNotificationContentParser non-String userInfo safety
  - `7059b26` tests: pin IMessagePreview www-strip case-insensitivity + link-prefix spacing
  - `848e146` tests: pin ShortcutsExportHandler missing-preview cascade + unknown-from default
  - `5c7b672` tests: pin KeychainHelper default service + ReplyAI- account prefix
  - `4bded07` tests: pin writeWeeklyLog empty + sorted rulesFiredByAction emission

## Next-fire hints

- Stale full-suite swift-test (PIDs 97075/97844) was killed early in the
  fire after running 26 min without progress; if `swift test` (no
  `--filter`) hangs again, kill and use the validated three-skip form
  (`--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests
  --skip InboxViewModelTests`) per AGENTS.md gotcha #243.
- ContactsResolverTests' `XCTSkipIf` setup is in place; the `CoreData`
  XPC noise from system AddressBook still spams to stderr but the
  test suite skips correctly. No structural action this fire.
- REP-501→REP-505 SPM split chain remains untouched (L-effort,
  human-paired session recommended per its blocker note).
- REP-ALERT-260504-1650 (MLX exit-on-launch) workaround
  (`pref.model.useMLX = false` in defaults) still active; structural
  fix lives behind REP-501.
