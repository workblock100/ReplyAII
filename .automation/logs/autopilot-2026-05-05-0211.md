# Autopilot fire — 2026-05-05 02:11 EDT

**Time used:** 44 min — 6 actions

## Actions

1. **P1 wip merge:** `wip/autopilot-2026-05-04-1700-rep047-sidebar-time-tick`
   → main `5dc08e6` (rep-047: SidebarView sync-chip auto-ticks every 10s
   via `TimelineView`). Branch had to be rebased onto main first
   (diverged 2 commits since the wip push 9h prior). Validation chain:
   banned-pattern scan clean → `swift test` 1131/1131 pass on the
   pre-rebase branch (no environmental ContactsResolverTests failures
   this fire) → `./scripts/build.sh debug` clean → smoke-launch alive
   at 30s with `pref.model.useMLX=false` (the established workaround
   for REP-ALERT-260504-1650). Original wip branch + the temporary
   `-rebased` push deleted from origin after ff merge.

2. **P3 reconcile:** REP-047 flipped from `in_progress` → `status:
   done, done_on: main commit 5dc08e6` in BACKLOG.md → main `68ee62e`.
   The wip-branch commit only set `in_progress`; this fire closes the
   loop now that the implementation has landed on main.

3. **P5+P6 channels:** pinned production keychain service+key
   identifiers for the four non-iMessage channel stubs (SMS, Teams,
   Telegram, WhatsApp). Refactored each channel to expose `static let
   keychainService` and `static let keychainTokenKey` instead of
   inline string literals; `init` defaults and method bodies now read
   through the constants. New `ChannelStubKeychainContractTests.swift`
   (10 cases) pins every value and asserts cross-channel uniqueness of
   both the service identifiers and the token keys. The motivating
   bug-class: a "consistency" rename of `TelegramChannel`'s reverse-DNS
   `co.replyai.telegram` service to `ReplyAI-Telegram` (matching the
   other three) would silently orphan every authorized user's bot
   token at next launch — keychain identity follows the literal, not
   the type. Pushed to
   `wip/autopilot-2026-05-05-025046-channel-stubs-keychain-contract`
   (commit 1: `75d3a0a`). Filter run of the affected suites
   (SMSChannelTests + TeamsChannelTests + TelegramChannelTests +
   WhatsAppChannelTests + ChannelStubKeychainContractTests) = 30/30
   pass; full `swift build` clean. Did not run the full 1131-case
   `swift test` (compile pipeline kept hanging mid-fire, killing
   would have wasted the budget) — reviewer should run the full suite
   before merging. Net new public surface: 8 `static let`s, 10 tests.

4. **P7 doc-comments:** added type-level `///` headers on `ChannelDot`
   and `Avatar` (Sources/ReplyAI/Components/). Both lacked them
   despite being top-level public Views referenced from sidebar +
   message rows. ChannelDot's `cutout` parameter contract (must match
   host surface or the border reads as a ring rather than a hole) is
   now documented at the type level. Pushed to the same wip branch
   (commit 2: `ff1e6bc`).

5. **P7 doc-fix:** clarified `KeychainHelper`'s top-level doc comment
   in `Sources/ReplyAI/Channels/KeychainHelper.swift`. Prior wording
   ("Keys are stored under the `ReplyAI-` prefix") was ambiguous about
   which keychain attribute carries the prefix; a reader could
   reasonably infer the SERVICE side is uniformly prefixed and then
   "fix" Telegram's reverse-DNS service. Updated doc walks both
   `kSecAttrService` (per-channel, may diverge — see each channel's
   `keychainService` constant) and `kSecAttrAccount` (uniformly
   `ReplyAI-` prefixed). Pushed as a 3rd commit (`c9022b6`) on the
   same wip branch.

6. **(implicit) P8 audit:** verified no stale orphan claims, no
   archive-eligible wip branches (only the one this fire merged), and
   the build cache is now warm post-this-fire's swift build.

## Files touched

- `Sources/ReplyAI/Channels/SMSChannel.swift` (wip)
- `Sources/ReplyAI/Channels/TeamsChannel.swift` (wip)
- `Sources/ReplyAI/Channels/TelegramChannel.swift` (wip)
- `Sources/ReplyAI/Channels/WhatsAppChannel.swift` (wip)
- `Sources/ReplyAI/Channels/KeychainHelper.swift` (wip — doc-only)
- `Sources/ReplyAI/Components/ChannelDot.swift` (wip)
- `Sources/ReplyAI/Components/Avatar.swift` (wip)
- `Tests/ReplyAITests/ChannelStubKeychainContractTests.swift` (new, wip)
- `Sources/ReplyAI/Inbox/Sidebar/SidebarView.swift` (main `5dc08e6`)
- `BACKLOG.md` (main `5dc08e6` + `68ee62e`)

## REPs closed (main)

- REP-047 — Sidebar relative-time chip auto-tick

## Pipeline state at exit

- BACKLOG open: 22 (unchanged this fire — wip work isn't reflected
  until reviewer merges)
- BACKLOG in_progress: 0 (down from 1 — REP-047 moved to done)
- BACKLOG blocked: 1
- wip queue depth: 1
  (`wip/autopilot-2026-05-05-025046-channel-stubs-keychain-contract`,
  fresh, 3 commits, +151 LOC, +10 tests)
- build cache: WARM (`./scripts/build.sh debug` ran successfully this
  fire)
- last 3 commits on main:
  - `68ee62e` docs: mark REP-047 done
  - `5dc08e6` rep-047: SidebarView sync-chip auto-ticks every 10s
  - `290d278` autopilot: 2026-05-05-0011 final log v3 — 25 actions

## Next-fire hints

- The Telegram-vs-others keychain service-name divergence is now
  documented in source (`/// **Note:** uses reverse-DNS form...`) and
  pinned by the new tests. If a future task proposes "normalize
  channel keychain service names," the tests will fail loudly — that
  is the desired behavior. Do not delete or relax those assertions.
- REP-ALERT-260504-1650 (MLX exit on launch) is still open and
  unfixed; the `pref.model.useMLX=false` workaround on this machine
  let the smoke-launch pass this fire. The structural fix remains the
  REP-501→REP-505 SPM target split, which is too risky for autopilot
  to attempt unattended (touches Package.swift + target directory
  structure).
- The new wip branch's full-suite `swift test` was not run (compile
  was hanging late in the fire and re-running would have blown the
  budget). All channel tests + new tests pass under filter. Risk of
  full-suite regression is low — the source change is purely
  literal-extraction with no behavior change. Reviewer / next fire's
  P1 lane can confirm.
