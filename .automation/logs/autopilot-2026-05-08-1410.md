# autopilot 2026-05-08-1410 fire log

## Time used
~40 min — 18 actions, all P5 pin-test additions

## Actions this fire

All P5 (test coverage gap pins on main) — no wip queue at fire start, 14 open
backlog tasks were either L-effort/risky (REP-501→REP-505 MLX SPM split,
REP-XCUI-001), claimed by human, or UI-sensitive. Stayed in P5 lane.

1. **P5 pin** (`798b24f`): `testEscapeCarriageReturnPassesThroughVerbatim`
   in `IMessageSenderTests.swift` — pin that `escapeForAppleScriptLiteral`
   doesn't rewrite `\r` (only `\\`, `"`, `\n` get rewritten). CRLF input
   pin: CR survives verbatim, `\n` becomes the AppleScript `\\n` escape.

2. **P5 doc bump** (`4d8f8c3`): AGENTS.md test count 1872 → 1873 for the CR
   pin landing.

3. **P5 pins** (`f36a036`): `testTokenizerAllSpacesProducesOneTokenPerSpace`
   + `testTokenizerAllNewlinesProducesOneTokenPerNewline` in
   `LLMServiceTests.swift` — pin that `StubLLMService.tokenize` of an
   all-delimiter input produces one single-character token per delimiter,
   not a single multi-char blob and not zero tokens. Filled the
   pure-whitespace gap left by the existing `consecutive-spaces`
   between-words test.

4. **P5 pins** (`dc7ccde`): `testAccentBrandLiteralIsPinnedChartreuseLime`
   + `testSurfaceBg1LiteralIsPinnedDarkInbox` in `ThemeTokensTests.swift`
   — pin SwiftUI's hex projection of `Theme.Color.accent` (`#D7FF3AFF`
   chartreuse-lime, the brand identity) and `Theme.Color.bg1`
   (`#0A0B0DFF` inbox surface). Existing distinctness pins catch a
   single-stop edit but pass under a global hue/lift; the byte-pin
   closes the gap.

5. **P5 pins** (`3243e9a`): `testWaitingPreservesSourceOrderFromThreadsArray`
   + `testWaitingPreservesSourceOrderEvenWithSilentlyIgnored` in
   `InboxViewModelMenuBarWaitingTests.swift` — pin that
   `menuBarWaitingThreads` preserves the order of `threads` (the menu-bar
   popover renders top-to-bottom in this order). Existing
   `testMixedReadAndUnreadOnlyShowsUnread` projects through `Set(...)`
   which loses order — would still pass under a "sort by recency on the
   way out" refactor that silently changes UX.

6. **P5 doc bump** (`35f95fa`): AGENTS.md 1873 → 1879 for actions 3–5
   (six new tests).

7. **P5 pin** (`029cc9f`): `testIncomingNotificationCreatesNewThreadAtIndexZero`
   in `InboxViewModelTests.swift` — pin that newly-created threads from a
   notification land at index 0 (top of inbox), not appended at end. The
   existing `testIncomingNotificationWithNilGUIDCreatesNewThread` only
   checks the count.

8. **P5 pin** (`9cf9ead`): `testMessageAgeOlderThanIsStrictlyGreaterAtExactBoundary`
   in `RulesTests.swift` — pin both legs of the strict-greater (`>`)
   boundary in `.messageAgeOlderThan(hours: N)`. Existing tests cover 25h
   (matches) and 1h (doesn't) — clearly above and below — but leave the
   exact-N boundary unpinned.

9. **P5 pin** (`0db8a92`): `testSenderContainsSearchesBothNameAndHandleIndependently`
   in `RulesTests.swift` — pin that `.senderContains` searches BOTH
   `senderName` and `senderHandle` (OR'd), with each leg covered
   independently. Existing `testSimpleEvaluationMatches` uses a context
   where the pattern matches both fields and can't isolate which.

10. **P5 doc bump** (`ffe6838`): AGENTS.md 1879 → 1882 for actions 7–9.

11. **P5 pin** (`0f199cd`): `testDefaultToneSkipsNonToneActionsInPriorityOrder`
    in `RulesTests.swift` — pin that `defaultTone(for:in:)` walks past
    matching rules with non-`setDefaultTone` actions and returns the first
    matching tone-rule. Existing `testDefaultToneFromRules` only covers
    the leg where the first rule fails the predicate.

12. **P5 pins** (`30c86db`): `testVoiceExampleAtExactly500CharsPassesThroughUntruncated`
    + `testVoiceExampleAt501CharsTruncatesByExactlyOne` in
    `PreferencesTests.swift` — pin both legs of the strict-greater
    boundary in `setVoiceExampleMessages`'s per-entry length cap. Existing
    `testVoiceExampleTruncatedAtFiveHundredChars` only exercises 600
    chars (clearly over) — exact-500 boundary unpinned.

13. **P5 doc bump** (`7515802`): AGENTS.md 1882 → 1885 for actions 11–12.

14. **P5 fire log** (`65773b7`): wrote initial fire log capturing
    actions 1–13.

15. **P5 pin** (`38e8b14`): `testChatGUIDSynthesisPrefixIsSMSForSMSChannelWhenGUIDAbsent`
    in `IMessageSenderTests.swift` — pin the SMS branch of
    `chatGUID(for:)` synthesis. Existing
    `testChatGUIDSynthesisPrefixIsIMessageForNonSMSChannels` covers
    slack/teams/whatsapp/telegram fallback to `iMessage;-;` but skips
    the SMS leg, where production code intentionally routes through
    `SMS;-;`. Pinned both the nil-chatGUID and empty-string-chatGUID
    cases.

16. **P5 doc bump** (`25a8298`): AGENTS.md 1885 → 1886.

17. **P5 pin** (`c3d8f47`): `testMatchingProducesStableOrderAcrossThreeWayTie`
    in `RulesTests.swift` — pin the stable sort across three same-
    priority rules + one higher-priority winner. Existing
    `testPriorityTiebreakerPreservesInsertionOrder` covers two-way
    ties only; this fills the realistic 3+ tie case so a "sort by
    hash" refactor would surface deliberately.

18. **P5 doc bump** (`2fccb75`): AGENTS.md 1886 → 1887.

## Files touched

- `Tests/ReplyAITests/IMessageSenderTests.swift` (+1 case)
- `Tests/ReplyAITests/LLMServiceTests.swift` (+2 cases)
- `Tests/ReplyAITests/ThemeTokensTests.swift` (+2 cases)
- `Tests/ReplyAITests/InboxViewModelMenuBarWaitingTests.swift` (+2 cases)
- `Tests/ReplyAITests/InboxViewModelTests.swift` (+1 case)
- `Tests/ReplyAITests/RulesTests.swift` (+3 cases — age boundary,
  senderContains both-fields, defaultTone skip)
- `Tests/ReplyAITests/PreferencesTests.swift` (+2 cases)
- `AGENTS.md` (test count header bumps)

## Pipeline state at exit

- Open BACKLOG: 14 (no change — all open items remain L-effort, claimed,
  or UI-sensitive)
- wip queue depth: 0
- Build cache: WARM (existed at fire start)
- Last 3 commits on main: `7515802` (count bump), `30c86db` (voice cap),
  `0f199cd` (defaultTone skip)

## Test count delta

1872 → 1887 (+15 cases), 1836 → 1851 under the three-skip workaround.

## Next-fire hints

None — all work landed in main, no in-progress wip branches, no
half-finished implementations. The codebase is exhaustively pinned in
the lanes I can reach from autopilot; future P5 work likely needs to
target untouched files (Inbox/Composer subtrees, more SwiftUI surfaces
that grew accessibility identifiers, or post-pivot Slack/Telegram code
paths once the backlog tasks unblock).
