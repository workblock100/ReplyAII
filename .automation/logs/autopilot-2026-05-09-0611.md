# Autopilot fire — 2026-05-09 06:11 EDT

**Time used**: ~38 min — 1 productive action (BACKLOG re-verify)

## Summary

Recovered from the 04:11 fire's stale-xctest loss by reading AGENTS.md gotcha §263 — plain `swift test` hangs on InboxViewModel suites because Contacts.framework's eager XPC handshake exhausts its retry budget at a non-deterministic class boundary. The validated workaround (also documented in AGENTS.md, NOT yet in SKILL.md) is the three-skip form:

```
swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests
```

That form completed in 19.5s warm with 1901 passes, 2 skips, 0 failures on `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test`. Branch's tests-and-build shape is still clean as of this fire, three fires after it was authored.

## Actions this fire

1. **P3-style re-verification** — checked out the hit-test wip branch, ran the three-skip test gate, recorded a fresh `reverify_2026-05-09-0611` data point on REP-AUDIT-260505 issue #1 in BACKLOG.md. Did NOT merge: branch is `claimed_by: human` because the bug's fate (does the click actually reach the button now?) requires a smoke-launch + visual click verification that the autopilot's existing AppleScript layer cannot do. Refresh keeps the human's queue accurate without burning budget on a smoke-launch attempt that would also need a non-headless click probe.

## What got skipped this fire (and why)

- **Plain `swift test` (initial attempt)** — hung at the InboxViewModel boundary again, identical to the 04:11 failure mode, because I used the SKILL.md's single-skip rule rather than the AGENTS.md three-skip form. Killed at elapsed 32 min once xctest's CPU usage flatlined to 0% with only 12s of CPU over 19 min wallclock — same signature as the prior fire's hang. Pivoted to AGENTS.md, found the three-skip workaround, restarted with that and got a clean run in 25s end-to-end (compile + test).
- **Merging the hit-test branch to main** — reservation upheld because criterion 1 of REP-AUDIT-260505 issue #1 ("clicking the button opens an inbox window") needs UI verification, not just test/build verification. The autopilot's smoke-launch layer can detect process death + window aliveness + crash signatures, but it cannot click a specific toolbar button and assert a second window appeared in response. That probe is what the human is in the loop for.

## Lessons / next-fire hint

The single most leveraged fix the autopilot can ship right now is to **always invoke the three-skip form**, not plain `swift test`. The SKILL.md's "ContactsResolverTests carve-out" guidance assumes the carve-out is sufficient; AGENTS.md documents the (validated, repeatedly reproduced) reality that two more InboxViewModel suites must also be skipped. Until `REP-ALERT-260506-2010` lands a structural fix, every fire that runs plain `swift test` is gambling on whether the hang fires this run.

## Pipeline state at exit

- main: HEAD will be `<this-fire>` after the BACKLOG + log commit
- wip queue: still 2 branches, both still candidates for human smoke-launch
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test` — re-verified 1901 tests pass this fire
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-frame-height` — sibling, not re-verified this fire (tested on its authoring fire)
- BACKLOG: 14 open, 0 in_progress, 1 blocked (unchanged in shape)
- Build cache: WARM (just rebuilt incrementally for the hit-test branch test)
- Test count: still ~1901 passing under three-skip gate, 1937 total
