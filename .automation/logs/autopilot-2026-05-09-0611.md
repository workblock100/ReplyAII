# Autopilot fire — 2026-05-09 06:11 EDT

**Time used**: ~50 min — 6 productive actions (2 reverifys + 4 doc audit fixes across AGENTS.md / AUTOMATION.md)

## Summary

Recovered from the 04:11 fire's stale-xctest loss by reading AGENTS.md gotcha §263 — plain `swift test` hangs on InboxViewModel suites because Contacts.framework's eager XPC handshake exhausts its retry budget at a non-deterministic class boundary. The validated workaround (also documented in AGENTS.md, NOT yet in SKILL.md) is the three-skip form:

```
swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests
```

That form completed in 19.5s warm with 1901 passes, 2 skips, 0 failures on `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test`. Branch's tests-and-build shape is still clean as of this fire, three fires after it was authored.

## Actions this fire

1. **P3-style re-verification** (commit `e6d30b9`) — checked out the hit-test wip branch, ran the three-skip test gate, recorded a fresh `reverify_2026-05-09-0611` data point on REP-AUDIT-260505 issue #1 in BACKLOG.md. Did NOT merge: branch is `claimed_by: human` because the bug's fate (does the click actually reach the button now?) requires a smoke-launch + visual click verification that the autopilot's existing AppleScript layer cannot do. Refresh keeps the human's queue accurate without burning budget on a smoke-launch attempt that would also need a non-headless click probe.
2. **P8 audit: AGENTS.md test-count timestamp bump** (commit `ca7e4d5`) — the test-count summary line was dated `2026-05-09-0211`; my fresh run reproduced the same shape (1901 passes, 2 skipped, 0 failures) so I bumped the date to `2026-05-09-0611` and noted the warm-run was 19.5s (was previously logged as ~18s). No count drift — purely a freshness signal for the next collaborator.
3. **P8 audit: surface three-skip form in AGENTS.md dev-cmd quickstart** (commit `c11dfa8`) — the `swift test` line in the dev-cmd quickstart at the top of AGENTS.md was the bare command. That's the first thing a new collaborator (human or autopilot fire) reaches for, and it's the exact incantation that hangs. Added an inline pointer to the three-skip form so future fires don't burn 30+ min on the trap before discovering gotcha §263 way down at line 263. Cheap signpost; high leverage.
4. **P8 audit: signpost the stale priority queue in AGENTS.md** (commit `8777ac4`) — items 1–4 of the "Priority queue" section are all SHIPPED or DEPRIORITIZED post-pivot, while BACKLOG.md has 14 open items the autopilot actually picks from. A new collaborator reading AGENTS.md top-to-bottom lands on the queue and infers there's nothing to do. Added a 6-line note right under the queue header pointing to `BACKLOG.md` as the active list, with concrete pointers (REP-501→REP-505 chain, REP-XCUI-001, REP-AUDIT entries). Preserves the historical queue without misleading future readers.
5. **P3-style re-verification: frame-height sister branch** (commit `917ddcb`) — extended the reverify to the second wip candidate (`...-frame-height`) so both candidate fixes have a fresh same-fire data point. Three-skip warm run: `Executed 1901 tests, with 2 tests skipped and 0 failures (0 unexpected) in 19.375 (19.511) seconds`. Identical shape to the hit-test branch and to main. Both branches now confirmed clean within minutes of each other; the human's verdict on which (if either) actually fixes the click bug is the only remaining gate.
6. **P8 audit: AUTOMATION.md intro line was double-stale** (commit `bd2499c`) — the intro paragraph said "Three Claude agents run on cron," but the table directly below lists 7 roles, and the operating-mode block right after that says "single-agent autopilot." So the intro was wrong on two counts. Replaced with a single-paragraph accurate summary plus a parenthetical noting why the legacy multi-agent table is still kept as reference. Doesn't change any behavior, but the doc no longer self-contradicts within 5 lines of itself.

## What got skipped this fire (and why)

- **Plain `swift test` (initial attempt)** — hung at the InboxViewModel boundary again, identical to the 04:11 failure mode, because I used the SKILL.md's single-skip rule rather than the AGENTS.md three-skip form. Killed at elapsed 32 min once xctest's CPU usage flatlined to 0% with only 12s of CPU over 19 min wallclock — same signature as the prior fire's hang. Pivoted to AGENTS.md, found the three-skip workaround, restarted with that and got a clean run in 25s end-to-end (compile + test).
- **Merging the hit-test branch to main** — reservation upheld because criterion 1 of REP-AUDIT-260505 issue #1 ("clicking the button opens an inbox window") needs UI verification, not just test/build verification. The autopilot's smoke-launch layer can detect process death + window aliveness + crash signatures, but it cannot click a specific toolbar button and assert a second window appeared in response. That probe is what the human is in the loop for.

## Lessons / next-fire hint

The single most leveraged fix the autopilot can ship right now is to **always invoke the three-skip form**, not plain `swift test`. The SKILL.md's "ContactsResolverTests carve-out" guidance assumes the carve-out is sufficient; AGENTS.md documents the (validated, repeatedly reproduced) reality that two more InboxViewModel suites must also be skipped. Until `REP-ALERT-260506-2010` lands a structural fix, every fire that runs plain `swift test` is gambling on whether the hang fires this run.

## Pipeline state at exit

- main: HEAD `<this-fire-final>` (5 commits ahead of where the fire started — `5472bd8` → `e6d30b9` (BACKLOG reverify) → `ca7e4d5` (AGENTS test-count bump) → `c11dfa8` (AGENTS quickstart three-skip pointer) → `6275bae` (log v2) → `8777ac4` (AGENTS priority-queue signpost) → this commit)
- wip queue: still 2 branches, both still candidates for human smoke-launch
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test` — re-verified 1901 tests pass this fire
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-frame-height` — also re-verified this fire, 1901 pass / 0 fail / 19.4s
- BACKLOG: 14 open, 0 in_progress, 1 blocked (unchanged in shape)
- Build cache: WARM (just rebuilt incrementally for the hit-test branch test)
- Test count: still ~1901 passing under three-skip gate, 1937 total
