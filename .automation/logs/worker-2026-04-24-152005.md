# worker-2026-04-24-152005

## Task
REP-278 — InboxViewModel: persist last-known thread list to disk for cold-launch resilience

## Outcome
**wip** — code complete but MLX build cache absent; `swift test` would exceed 13-min budget.
Branch: `wip/2026-04-24-152005-thread-cache`

## File changes
| File | Delta |
|------|-------|
| `Sources/ReplyAI/Services/Preferences.swift` | +14 lines (`enum Preferences` with `lastThreadsCacheURL`) |
| `Sources/ReplyAI/Inbox/InboxViewModel.swift` | +57 lines (cache struct, save/load helpers, init param, write-on-sync) |
| `Tests/ReplyAITests/InboxViewModelTests.swift` | +119 lines (5 new tests in `InboxViewModelThreadCacheTests`) |

## Tests
- Before: could not verify (cold build)
- New tests added (5): `testSuccessfulSyncWritesCache`, `testColdInitFromCache`, `testSecondSyncUpdatesCacheFile`, `testFailedSyncLeavesThreadsUnchanged`, `testMissingCacheAtInitProducesEmptyThreads`
- All inject a temp `threadsCacheURL` for isolation; no file-system side-effects on existing tests

## Implementation notes
- `Preferences.lastThreadsCacheURL` → `~/Library/Application Support/ReplyAI/last-threads-cache.json`
- `CachedThread` Codable struct serializes: id, displayName, chatGUID, previewText, channel, isRead
- `InboxViewModel.init(threadsCacheURL:)` param (nil = production URL) enables test isolation
- Cache written only after `syncStatus = .live` (≥1 thread returned); failed syncs skip write
- Init loads cache only when `threads` param is empty; non-empty init is unchanged
- `avatar` on re-hydrated threads is `String(displayName.prefix(1))` — same convention as the rest of the codebase

## Substantiveness note
Only one P0 open task (`REP-278`, effort=S). No other P0 tasks available to bundle — shipping alone per gate rules.

## Next action for human
Run `swift test` on `wip/2026-04-24-152005-thread-cache`. If green, merge to main and mark REP-278 done.
