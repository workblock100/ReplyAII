# autopilot-2026-05-08-0010

## Summary

- Time used: ~43 min — 20 actions (this log + commits)
- Pipeline state at entry: 0 wip branches, 18 BACKLOG open, build cache WARM, last main `066419c` (2026-05-07-2210 fire log)
- Pipeline state at exit: 0 wip branches, 18 BACKLOG open, last main `c6e4628` (search-index in-memory db sentinel hoist)
- Fire signal: empty wip queue → P1 exhausted, all 20 actions in P4-style hoist+pin lane to main
- Pre-existing flake (NOT a regression): `IMessageChannelFormatTimeTests.testFormatRelativeUsesHoistedYesterdayLabel` fails when the fire runs in the early-AM window (`Date() - 25h` falls on the day before "yesterday" relative to wall clock). Verified the same failure on pre-fire main `066419c` — not introduced this fire. The earlier `a165d55 tests: fix day-boundary flake` attempted a fix but didn't fully cover the early-AM window.

## Actions

1. **P4 hoist** — `applescript-message-reader: hoist sidebar fallback copy + pin` (`1264c93`).
   Lifted `emptyPreviewPlaceholder = "—"` and `groupChatDisplayLabel = "Group chat"` into named constants on `AppleScriptMessageReader`. Two existing route-through tests now reference the constants; two new freeze tests pin the literal values. 55 AppleScriptMessageReaderTests passing.

2. **P4 hoist** — `stats: hoist recordRuleFired action vocabulary + pin` (`ca3bfa9`).
   Lifted the five rule-action discriminator strings into a typed `Stats.RuleAction` constants enum (`archive`, `silentlyIgnore`, `markDone`, `setDefaultTone`, `pin`). Seven InboxViewModel callsites updated. 3 new pin tests (freeze + pairwise-distinct + route-through). 35 StatsTests passing.

3. **P4 hoist** — `imessage-channel: hoist FDA-denial errmsg substrings + pin` (`d0d63d6`).
   Lifted the two lowercase substrings (`"authorization"`, `"unable to open"`) the chat.db open path matches against the SQLite errmsg payload to classify FDA-denied failures into a `FDADenialErrorMessageSubstring` enum. The `if` chain now lowercases the errmsg once and reuses the result. 2 new pin tests (freeze + lowercase invariant). 20 IMessageChannelTests passing.

4. **P4 hoist** — `chatdb-watcher: hoist default paths + queue label + cross-file pin` (`6fd5b69`).
   Hoisted the watcher's two default tilde-anchored paths (`chat.db`, `chat.db-wal`) and the dispatch queue label (`co.replyai.chatdb-watcher`) into named static constants. 5 new pin tests including the cross-file invariant pinning watcher↔reader chat.db path symmetry against `IMessageChannel.chatDBPath`. 22 ChatDBWatcherTests passing.

5. **P4 hoist** — `imessage-sender: hoist parameterized toast formats + pin` (`fe7cbe1`).
   Lifted `.messageTooLong` and `.invalidChatGUID` interpolated SendError toast formats into static helpers `messageTooLongToast(chars:)` and `invalidChatGUIDToast(guid:)`. The `errorDescription` switch routes through helpers. 4 new pin tests (format-roundtrip + embed-production-max + embed-validator-shape). 17 IMessageSendErrorCopyTests passing.

6. **P4 hoist** — `applescript-reader-error: hoist user-visible toast copy + pin` (`30d590c`).
   Lifted three AppleScript-error toast literals (`scriptCreationFailedDescription`, `executionErrorDescriptionPrefix`, `missingMessageFallback`) into static constants on `AppleScriptReaderError`. The `errorDescription` switch and `defaultExecutor` nil-coalesce now route through constants. 1 new pin test for the missing-message fallback. 55 AppleScriptMessageReaderTests passing.

7. **P4 hoist** — `keychain-error: hoist unknown-status fallback toast format + pin` (`8eb7bc4`).
   Lifted the parameterized unknown-OSStatus fallback toast into `unhandledErrorFallbackToast(status:)` static helper. The `default` switch arm routes through the helper. 2 new pin tests (format-roundtrip + integer-status-embed). 15 KeychainHelperTests passing.

8. **P4 hoist** — `contacts-resolver: hoist US country-code normalization + cross-site pin` (`286ec45`).
   Lifted the inline `digits.count == 11 && digits.hasPrefix("1")` rule into a typed `USPhoneNormalization` constants enum (`prefixedLength = 11`, `countryCode = "1"`). Three call sites — two in `ContactsResolver`, one in `AppleScriptMessageReader.prettyPhone` — now route through the constants. 2 new pin tests including a cross-call-site invariant. 14 ContactsResolverPhoneDigitsTests passing.

9. **P4 hoist** — `prompt-builder: hoist system-prompt base + per-tone suffixes + pin` (`e94ef77`).
   Lifted the system-prompt base instruction and three per-tone suffixes (warm / direct / playful) into a `PromptBuilder.SystemPrompt` constants enum. The `rawSystemPrompt(tone:)` switch now concatenates from the constants. 4 new pin tests (freeze + route-through + pairwise-distinct + space-prefixed). 60 PromptBuilderTests passing.

10. **P4 hoist** — `slack-oauth-flow: hoist token-exchange failure reasons + pin` (`9906ff4`).
    Lifted three inline failure-reason strings ("invalid endpoint URL", "empty response", "response missing ok=true or access_token") into a `TokenExchangeFailureReason` constants enum. Three call sites in `exchangeCodeForToken` now pass the constants. 2 new pin tests (freeze + pairwise-distinct + non-empty). 29 SlackOAuthFlowTests passing.

11. **P4 hoist** — `localhost-oauth-listener: hoist parse host + creation-failure format` (`7aa1b5a`).
    Lifted the loopback synthetic-parse-URL host (`http://localhost`) and the parameterized listener-creation failure-reason format into named constants/helpers. 3 new pin tests including URL-acceptance and integer-port-embed invariants. 31 LocalhostOAuthListenerTests passing.

12. **P4 hoist** — `rule-evaluator: hoist phonelike character set + behavioral pin` (`fc688ff`).
    Lifted the inline `"+0123456789 ()-"` literal used by `RuleContext.from(thread:)` for senderKnown classification into `RuleContext.phonelikeCharacterSet`. 2 new pin tests including a behavioral test routing a US-formatted phone name through the public API. 142 RulesTests-class passing.

13. **P4 hoist** — `messages-activation-observer: hoist dispatch-queue label + pin` (`85830ca`).
    Hoisted `co.replyai.messages-activation` dispatch-queue label into a static constant (sibling to the recent `ChatDBWatcher.dispatchQueueLabel` hoist). 1 new pin test (freeze + reverse-DNS prefix invariant). 14 MessagesAppActivationObserverTests passing.

14. **P4 hoist** — `preferences: hoist app-support root relative path + cross-file pin` (`fdb9660`).
    Lifted the hand-built `Library/Application Support` parent path duplicated at SearchIndex.productionDatabaseURL and RulesStore's URL fallback into `Preferences.appSupportRootRelativePath`. Both fallback writers now reference the constant. 1 new pin test (freeze + no-leading-slash + no-trailing-slash invariants). 59 PreferencesTests passing.

15. **P4 hoist** — `inbox-vm: hoist incoming-notification time label + pin` (`4f17209`).
    Lifted `"now"` time-chip literal used at TWO call sites in `applyIncomingNotification` (refresh-existing + create-new) into `InboxViewModel.incomingNotificationTimeLabel`. 1 new freeze pin.

16. **P4 hoist** — `inbox-vm: hoist sent-to toast format + pin` (`e760c56`).
    Lifted inline `"Sent to \(recipient)"` success-toast format into `sentToToast(recipient:)` static helper on InboxViewModel. 1 new pin test (format-roundtrip + recipient-verbatim invariant).

17. **P4 hoist** — `rule-validation-error: hoist parameterized toast formats + pin` (`094b59a`).
    Lifted `.invalidRegex` and `.tooManyRules` interpolated toast formats into static helpers (`invalidRegexToast(pattern:reason:)`, `tooManyRulesToast(limit:)`) on `RuleValidationError`. 2 new pin tests (format-roundtrip + embedded-substring invariants).

18. **P4 hoist** — `draft-engine: hoist primingKey format + cross-site pin` (`b93ec7e`).
    Lifted inline `"\(threadID):\(tone.rawValue)"` priming-task dictionary-key format used at FOUR call sites (`prime`, `evict`, `invalidate`, `dismiss`) into `primingKey(threadID:tone:)` static helper. 1 new pin test (format-roundtrip + pairwise-distinct). 51 DraftEngineTests passing.

19. **P4 hoist** — `global-hotkey: hoist NSLog prefix + cross-site pin` (`19eada5`).
    Lifted `[ReplyAI] GlobalHotkey: ` NSLog prefix used at three sites into `GlobalHotkey.logPrefix`. 1 new pin test (freeze + leading-bracket + trailing-colon-space invariants). 6 GlobalHotkeyContractTests passing.

20. **P4 hoist** — `search-index: hoist in-memory database sentinel + pin` (`c6e4628`).
    Lifted inline `":memory:"` SQLite-defined in-memory-DB sentinel filename used by `open(url:)` into `SearchIndex.inMemoryDatabasePath`. 1 new pin test (freeze + leading-colon + trailing-colon invariants).

## Files touched

- `Sources/ReplyAI/Channels/AppleScriptMessageReader.swift` (actions 1, 6, 8)
- `Sources/ReplyAI/Services/Stats.swift` (action 2)
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` (actions 2, 15, 16)
- `Sources/ReplyAI/Channels/IMessageChannel.swift` (action 3)
- `Sources/ReplyAI/Channels/ChatDBWatcher.swift` (action 4)
- `Sources/ReplyAI/Channels/IMessageSender.swift` (action 5)
- `Sources/ReplyAI/Channels/KeychainHelper.swift` (action 7)
- `Sources/ReplyAI/Channels/ContactsResolver.swift` (action 8)
- `Sources/ReplyAI/Services/PromptBuilder.swift` (action 9)
- `Sources/ReplyAI/Channels/SlackOAuthFlow.swift` (action 10)
- `Sources/ReplyAI/Channels/LocalhostOAuthListener.swift` (action 11)
- `Sources/ReplyAI/Rules/RuleEvaluator.swift` (action 12)
- `Sources/ReplyAI/Channels/MessagesAppActivationObserver.swift` (action 13)
- `Sources/ReplyAI/Services/Preferences.swift` (action 14)
- `Sources/ReplyAI/Search/SearchIndex.swift` (actions 14, 20)
- `Sources/ReplyAI/Rules/RulesStore.swift` (action 14)
- `Sources/ReplyAI/Rules/SmartRule.swift` (action 17)
- `Sources/ReplyAI/Services/DraftEngine.swift` (action 18)
- `Sources/ReplyAI/Services/GlobalHotkey.swift` (action 19)

## Pipeline state at exit

- BACKLOG: 18 open, 0 in_progress, 1 blocked
- wip queue: 0 branches
- Build cache: WARM (re-built each fire iteration; final `swift build` at 41 min completed in ~9s)
- Last 3 commits on main: `c6e4628` (search-index in-memory db sentinel), `19eada5` (global-hotkey log prefix), `b93ec7e` (draft-engine primingKey).

## Next-fire hints

- The 18 open BACKLOG items are still primarily L-effort tasks (REP-501→505 MLX extraction chain, REP-XCUI-001 XCUITest target bootstrap, REP-ALERT-260504-1650 MLX-on-launch crash) that are riskier than autopilot fires can absorb in a single 50-min window. P4 hoist+pin remains the productive lane.
- Pre-existing flaky test `IMessageChannelFormatTimeTests.testFormatRelativeUsesHoistedYesterdayLabel` reproduces on pre-fire main `066419c`; affects the early-AM window (`Date() - 25h` falls on the day before "yesterday"). The earlier `a165d55 tests: fix day-boundary flake` attempted a fix but didn't cover the post-midnight case. Worth a dedicated wip branch when an autopilot fire has the budget.
