# Worker run: 2026-04-22-122448

## Tasks
- REP-080 — SearchIndex: channel TEXT column in FTS5 for per-channel filtered search (S)
- REP-085 — SearchIndex: prefix-match query support for ⌘K partial-word search (S)
- REP-092 — SearchIndex: sanitize FTS5 special-character input (S)

## Outcome
**shipped** — committed to main

## Rationale for bundle
All three tasks target SearchIndex.swift + SearchIndexTests.swift. REP-085 explicitly notes
"implement alongside REP-092 in the same PR if possible." REP-080 adds the channel column
which the other two don't conflict with. Bundling 3×S satisfies the substantiveness gate.

## File changes
- `Sources/ReplyAI/Search/SearchIndex.swift` (+55 lines, −17 lines)
  - `createSchema`: added `channel UNINDEXED` column to FTS5 virtual table
  - `rebuild`: added `channel` to INSERT (6th bind param), channelsByID lookup dict
  - `upsert`: added `channel` to INSERT (6th bind param)
  - `search(_ query:)`: refactored to call new `search(query:channel:)` overload
  - `search(query:channel:)`: new method with optional per-channel WHERE filter
  - `ftsQuery`: strip hyphens alongside quotes; use `compactMap` to skip empty tokens;
    updated special set to `()*:` (removed `"` since stripping happens before the check)
- `Tests/ReplyAITests/SearchIndexTests.swift` (+95 lines)
  - Added 10 new test functions (REP-085: 3, REP-092: 4, REP-080: 3)

## Tests
- Before: 260 test functions (full suite exit 0)
- After:  270 test functions (full suite exit 0)
- SearchIndexTests: 22 → 32

## Build
`./scripts/build.sh debug` → ✓ ReplyAI.app valid on disk

## Notes
- The existing `ftsQuery` already had all-tokens prefix matching (more capable than
  the REP-085 spec which said "last token only"). Kept the stronger behavior since
  existing tests assert it and "existing tests remain green" is a hard requirement.
- REP-092 stripped `"` from special-set check (it's stripped before the check anyway)
  and added `-` stripping to prevent bare-hyphen FTS5 parse confusion.
- `testEmptyQueryReturnsEmpty` already existed; did not add a duplicate.
