# Autopilot fire 2026-05-07 12:11

**Time used**: ~50 min — 9 actions (all P5 test pins to main)

## Actions this fire

1. **P5 test pin** → main `913aa1a` — SlackChannel.messages limit=0 + negative clamp (lower-bound symmetry to existing `testRecentThreadsClampsZeroLimitToOne`; upper-bound was already pinned via `testMessagesClampsLimitAbove200ToSlackMaximum`)
2. **P5 test pin** → main `977c820` — SlackOAuthFlow.exchangeCode failure-msg literals (3 cases: missing access_token, empty access_token, ok:false). All three branches share one guard and one literal `"response missing ok=true or access_token"`; pin so a future split keeps copy aligned across the user-visible OAuthError.errorDescription.
3. **P5 test pin** → main `5c7728f` — ChatDBWatcher default `restartDelay = 5.0` + trailing-closure-only init shape. Production call site (`InboxViewModel.swift:~1041`) inherits every default; halving restartDelay would double the cancel-storm rate during iCloud sync.
4. **P5 test pin** → main `10d5588` — StubLLMService production timing defaults (`tokenDelay = 22-58ms`, `initialDelay = 180ms`). Tests routinely override both to zero, so a default-only regression would never surface; demo composer's perceived typing speed depends on these literals.
5. **P5 test pin** → main `a21792e` — IMessageSender AppleScript template structure. Capture via executeHook + pin the four substrings (`tell application "Messages"`, `end tell`, `chat id "<guid>"`, `send "<text>" to targetChat`). Empty-text variant pinned separately so a "guard against empty text" refactor can't drop the send call entirely.
6. **P5 test pin** → main `43cb0f7` — IMessageChannel chat.db open flags pinned exactly to `SQLITE_OPEN_READONLY | SQLITE_OPEN_NOMUTEX`. Negative pins on READWRITE + CREATE so a flag flip surfaces against both halves of the bitmask. Drift to READWRITE would let ReplyAI corrupt Apple's chat.db.
7. **P5 test pin** → main `ee7c62e` — SlackHTTPClient apiBase exact-literal absoluteString pin for both GET + POST verbs. Existing piecewise checks (scheme/host/path) would still pass against `slack.com/api/v2/...`; the full-string pin catches a "support a future API version" refactor that would land every request at the wrong path.
8. **P5 test pin** → main `214086e` — SlackChannel `unread_count` missing-key + explicit-null fallback to 0. Existing happy-path tests always include `unread_count: <int>`, so the `?? 0` branch was never exercised. Drift to `-1` sentinel or a non-optional decode would silently render phantom badges or crash on the now-required key.
9. **P5 test pin** → main `266f008` — SlackChannel.recentThreads negative-limit clamp (symmetric counterpart to action 1). Both endpoints share the `min(max(limit, 1), 200)` clamp; pin both halves so a future "split clamps per endpoint" refactor can't diverge them silently.

## Files touched / REPs closed

- Tests/ReplyAITests/SlackChannelTests.swift (+39, +47, +18)
- Tests/ReplyAITests/SlackOAuthFlowTests.swift (+99)
- Tests/ReplyAITests/ChatDBWatcherTests.swift (+41)
- Tests/ReplyAITests/LLMServiceTests.swift (+30)
- Tests/ReplyAITests/IMessageSenderTests.swift (+63)
- Tests/ReplyAITests/IMessageChannelTests.swift (+42)
- Tests/ReplyAITests/SlackHTTPClientTests.swift (+31)

No REPs closed — pure test-coverage hardening, all on main.

## Pipeline state at exit

- BACKLOG: 18 open, 0 in_progress, 1 blocked
- wip queue: empty
- Build cache: warm
- Last 3 commits on main: 266f008, 214086e, ee7c62e

## Next-fire hints

None — every action this fire was self-contained and merged cleanly. No partial work to resume.

The next fire should re-survey and pick again. Useful pin gaps spotted but not landed (all low-priority — covered indirectly):
- Stats `persist()` 2-second debounce literal (private constant; not directly testable without exposing)
- IMessageSender `sendTimeout = 10` default (mutable static var; default isn't preserved across test runs without ordering guarantees)
- IMessageChannel `openReadOnly` 0.1s SQLITE_BUSY retry sleep duration (would need to inject a clock hook)
- GlobalHotkey default keyCode/modifiers (`kVK_ANSI_R`, `cmdKey | shiftKey`) — function-default constants reachable only by calling register(), which fires Carbon and isn't headless-testable.
