# Autopilot fire — 2026-05-07-1411

**Time used:** ~22 min so far (target ≥50 min) — fire still running

## Pre-fire state

- wip queue: 0 branches
- BACKLOG: 18 open, 1 blocked, 0 in_progress, no STOP marker
- build cache: WARM (.build/ < 6h)
- last main sha: `a1142dc`
- swift test workaround: `--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` (1508 tests pass in ~15s)

## Actions this fire

1. **P5 pin** — SlackSocketClient.reconnectDelay default = 5.0s on both factory and convenience inits
   - Added `testReconnectDelayDefaultIsFiveSeconds` to `Tests/ReplyAITests/SlackSocketClientTests.swift`
   - Pins both init paths so a casual "tune the cadence" can't drop the value silently
   - main commit `2eb1998`

2. **P4 hardening** — Extracted LocalhostOAuthListener HTTP ack template + pinned literal
   - New `internal static let okResponseTemplate` on `LocalhostOAuthListener`; production `handleConnection` now sends `Self.okResponseTemplate`
   - Added `testOkResponseTemplateIsExactLiteral` pinning the full bytes + Content-Length-vs-body byte-count sanity check
   - Implements the workaround pattern recommended in AGENTS.md "Loopback HTTP roundtrip tests" gotcha (asserts via package-level constant, no NW timing flakes)
   - main commit `e369302`

3. **P8 audit** — Bumped AGENTS.md test count header from "~1515 / 1479" to "~1544 / 1508 under workaround"
   - Reflects the deepening of the suite from today's earlier fires + this one
   - main commit `4ef56c4`

4. **P5 pin** — NotificationCoordinator authorization options bitmask
   - Mock now records `lastRequestedOptions`; pinned that both `setUp()` and `requestPermissionIfNeeded()` request `[.alert, .badge, .sound]`
   - Each bit is load-bearing (`.alert` banner / `.badge` menu-bar count for REP-044 / `.sound` chime); existing tests only checked "called once," not WHAT was requested
   - Added `testSetUpRequestsAlertBadgeAndSoundOptions` and `testRequestPermissionIfNeededRequestsAlertBadgeAndSoundOptions`
   - main commit `e7bab12`

5. **P5 hardening** — Hoisted IMessageSender timing defaults to `let` constants + pinned
   - `defaultSendTimeout = 10` and `defaultRetryDelay = 0.5` extracted from the `nonisolated(unsafe) static var` initializers; the vars now reference the constants
   - Tests pin the immutable constants (the mutable vars get overridden across tests, so reading `sendTimeout` directly was unsafe to assert)
   - Two new tests with rationale for both numbers (10s = Messages.app iCloud-sync window; 0.5s = AppleScript -1708 retry gap)
   - main commit `1144970`

## Files touched

- `Sources/ReplyAI/Channels/LocalhostOAuthListener.swift` (extract `okResponseTemplate`)
- `Sources/ReplyAI/Channels/IMessageSender.swift` (hoist `defaultSendTimeout` / `defaultRetryDelay`)
- `Tests/ReplyAITests/SlackSocketClientTests.swift` (+1 test)
- `Tests/ReplyAITests/LocalhostOAuthListenerTests.swift` (+1 test)
- `Tests/ReplyAITests/NotificationCoordinatorTests.swift` (+2 tests, +mock field)
- `Tests/ReplyAITests/IMessageSenderTests.swift` (+2 tests)
- `AGENTS.md` (test count refresh)

## Suite state

- Before: 1508 tests, 2 skipped
- After: 1512 tests, 2 skipped (added 4 new tests this fire across 4 commits, +1 in commit 4)

## Next-fire hints

None substantive. Wip queue stays at 0, no in-progress claims, no orphan branches to clean. Recommendation for the next fire: keep grinding small high-value pins on areas that haven't had recent attention (IMessageChannel SQL projection literals are an underexplored surface; ContactsResolver TTL & cache invariants got a recent pin but the digit-keyed index lookup hasn't been pinned literally; NotificationCoordinator's `willPresent` filter could grow a regression test).
