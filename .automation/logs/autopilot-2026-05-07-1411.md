# Autopilot fire — 2026-05-07-1411

**Time used:** ~45 min — fire complete

## Pre-fire state

- wip queue: 0 branches
- BACKLOG: 18 open, 1 blocked, 0 in_progress, no STOP marker
- build cache: WARM (.build/ < 6h)
- last main sha: `a1142dc`
- swift test workaround: `--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` (1508 tests pass in ~15s)

## Actions this fire

1. **P5 pin** — SlackSocketClient.reconnectDelay default = 5.0s on both factory and convenience inits (`testReconnectDelayDefaultIsFiveSeconds`) — main `2eb1998`

2. **P4 hardening** — Extracted LocalhostOAuthListener HTTP ack template + pinned literal. New `internal static let okResponseTemplate`; production `handleConnection` now sends `Self.okResponseTemplate`. `testOkResponseTemplateIsExactLiteral` pins full bytes + Content-Length-vs-body byte-count sanity. Implements the workaround pattern in AGENTS.md "Loopback HTTP roundtrip tests" gotcha — main `e369302`

3. **P8 audit** — Bumped AGENTS.md test count header — main `4ef56c4`

4. **P5 pin** — NotificationCoordinator authorization options bitmask. Mock now records `lastRequestedOptions`; pinned that both `setUp()` and `requestPermissionIfNeeded()` request `[.alert, .badge, .sound]` — main `e7bab12`

5. **P5 hardening** — Hoisted IMessageSender timing defaults to `let` constants + pinned. `defaultSendTimeout = 10`, `defaultRetryDelay = 0.5` extracted from the `nonisolated(unsafe) static var` initializers — main `1144970`

6. **P5 hardening** — Hoisted `Stats.debounceWriteWindow = 2` + pinned. `persist()` now uses the constant — main `b8af572`

7. **P5 pin** — SlackOAuthFlow token-exchange URL exact-literal. Existing sibling only substring-checked `oauth.v2.access`; now pin full `https://slack.com/api/oauth.v2.access` — main `43d05fd`

8. **P5 pin** — SmartRule.validatePredicateRegexes recursion contract. Four sibling tests covering `.and`, `.or`, `.not`, and a four-level nested tree. Catches a refactor that flattened the validator's switch and let nested invalid regexes slip past — main `e5fac55`

9. **P5 hardening** — NotificationCoordinator.foregroundPresentationOptions hoisted + pinned. `[.banner, .sound]` extracted from inline `willPresent` callback — main `60ffcf5`

10. **P5 hardening** — LocalhostOAuthListener.defaultPort = 4242 / .defaultTimeout = 120 hoisted + pinned — main `ea52e5c`

11. **P5 followup** — Routed SlackOAuthFlow's `listenerFactory(4242, 120)` through the new constants for single source of truth — main `f28aa54`

12. **P8 audit** — End-of-fire AGENTS.md test count refresh (1544/1508 → 1557/1521) — main `f74a47f`

13. **P5 hardening** — SearchIndex.defaultSearchLimit = 50 hoisted + pinned. Both `search(_:limit:)` and `search(query:channel:limit:)` now reference the constant — main `2a0f2ad`

## Files touched

Source:
- `Sources/ReplyAI/Channels/LocalhostOAuthListener.swift` (template + port + timeout constants)
- `Sources/ReplyAI/Channels/IMessageSender.swift` (default timeout + retry constants)
- `Sources/ReplyAI/Channels/SlackOAuthFlow.swift` (use new listener constants)
- `Sources/ReplyAI/Search/SearchIndex.swift` (default search limit constant)
- `Sources/ReplyAI/Services/NotificationCoordinator.swift` (foreground options constant)
- `Sources/ReplyAI/Services/Stats.swift` (debounce window constant)

Tests (new pin coverage):
- `Tests/ReplyAITests/SlackSocketClientTests.swift` (+1)
- `Tests/ReplyAITests/LocalhostOAuthListenerTests.swift` (+3)
- `Tests/ReplyAITests/NotificationCoordinatorTests.swift` (+3, +mock field)
- `Tests/ReplyAITests/IMessageSenderTests.swift` (+2)
- `Tests/ReplyAITests/StatsTests.swift` (+1)
- `Tests/ReplyAITests/SlackOAuthFlowTests.swift` (+1)
- `Tests/ReplyAITests/RulesTests.swift` (+4)
- `Tests/ReplyAITests/SearchIndexTests.swift` (+1)

Docs:
- `AGENTS.md` (test count refresh, twice)

## Suite state

- Before: 1508 tests pass under workaround (~15s warm)
- After: 1521 tests pass under workaround (~15.2s warm)
- Total tests in tree: 1557 (was 1544)
- Net +13 passing tests across 13 commits

Two-skip form (`ContactsResolverTests` + `InboxViewModelIsSyncingTests`) still
hangs in this fire — three-skip remains the validated form (consistent with
AGENTS.md gotcha).

## Pipeline state at exit

- main: 13 commits ahead of pre-fire (`a1142dc` → `2a0f2ad`)
- wip queue: 0 (autopilot worked entirely on main)
- BACKLOG: status fields unchanged (no completions to flip)

## Next-fire hints

None substantive. Theme this fire and the prior 1011/1211 fires has been
small-but-high-rationale literal pins on shipped UX surfaces. Diminishing
returns ahead — most config literals across the channel layer, OAuth flow,
notification coordinator, stats persistence, and search are now pinned via
either direct literal pins or `let` constants. Future fires should look
toward harder targets: tests for the InboxViewModel surface (currently
gated by the Contacts daemon hang), or genuine new feature work on Slack
Socket Mode wiring or alt-iMessage source paths.
