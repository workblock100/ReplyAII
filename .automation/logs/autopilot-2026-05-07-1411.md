# Autopilot fire — 2026-05-07-1411

**Time used:** ~40 min and counting (target ≥50 min)

## Pre-fire state

- wip queue: 0 branches
- BACKLOG: 18 open, 1 blocked, 0 in_progress, no STOP marker
- build cache: WARM (.build/ < 6h)
- last main sha: `a1142dc`
- swift test workaround: `--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` (1508 tests pass in ~15s)

## Actions this fire

1. **P5 pin** — SlackSocketClient.reconnectDelay default = 5.0s on both factory and convenience inits
   - `testReconnectDelayDefaultIsFiveSeconds` in `Tests/ReplyAITests/SlackSocketClientTests.swift`
   - main `2eb1998`

2. **P4 hardening** — Extracted LocalhostOAuthListener HTTP ack template + pinned literal
   - New `internal static let okResponseTemplate`; production `handleConnection` now sends `Self.okResponseTemplate`
   - `testOkResponseTemplateIsExactLiteral` pins full bytes + Content-Length-vs-body byte-count sanity
   - Implements the workaround pattern in AGENTS.md "Loopback HTTP roundtrip tests" gotcha
   - main `e369302`

3. **P8 audit** — Bumped AGENTS.md test count header from "~1515 / 1479" → "~1544 / 1508"
   - main `4ef56c4`

4. **P5 pin** — NotificationCoordinator authorization options bitmask
   - Mock now records `lastRequestedOptions`; pinned that both `setUp()` and `requestPermissionIfNeeded()` request `[.alert, .badge, .sound]`
   - main `e7bab12`

5. **P5 hardening** — Hoisted IMessageSender timing defaults to `let` constants + pinned
   - `defaultSendTimeout = 10`, `defaultRetryDelay = 0.5` extracted from the `nonisolated(unsafe) static var` initializers
   - main `1144970`

6. **P5 hardening** — Hoisted Stats.debounceWriteWindow = 2 + pinned
   - The persist() flush was scheduled with `.now() + 2` inline; now references `Stats.debounceWriteWindow`
   - `testDebounceWriteWindowDefaultIsTwoSeconds`
   - main `b8af572`

7. **P5 pin** — SlackOAuthFlow token-exchange URL exact-literal
   - Existing sibling only substring-checked `oauth.v2.access`; pinned full `https://slack.com/api/oauth.v2.access`
   - main `43d05fd`

8. **P5 pin** — SmartRule.validatePredicateRegexes recursion contract
   - Four sibling tests covering `.and`, `.or`, `.not`, and a four-level nested tree
   - Pin recursive walker behavior so a refactor that flattened the validator can't slip nested invalid regexes past
   - main `e5fac55`

9. **P5 hardening** — NotificationCoordinator.foregroundPresentationOptions hoisted + pinned
   - `[.banner, .sound]` extracted from inline `willPresent` callback
   - `testForegroundPresentationOptionsBitmaskIsBannerAndSound` pins the bitmask
   - main `60ffcf5`

10. **P5 hardening** — LocalhostOAuthListener.defaultPort = 4242 / .defaultTimeout = 120 hoisted + pinned
    - Init defaults now reference the constants
    - `testDefaultPortIs4242` and `testDefaultTimeoutIs120Seconds`
    - main `ea52e5c`

11. **P5 followup** — Routed SlackOAuthFlow's `listenerFactory(4242, 120)` through the new constants
    - Single source of truth for the OAuth port/timeout numbers
    - main `f28aa54`

## Files touched

Source:
- `Sources/ReplyAI/Channels/LocalhostOAuthListener.swift` (new constants + template extraction)
- `Sources/ReplyAI/Channels/IMessageSender.swift` (default constants)
- `Sources/ReplyAI/Channels/SlackOAuthFlow.swift` (use the new listener constants)
- `Sources/ReplyAI/Services/NotificationCoordinator.swift` (foreground options constant)
- `Sources/ReplyAI/Services/Stats.swift` (debounce window constant)

Tests (new pin coverage):
- `Tests/ReplyAITests/SlackSocketClientTests.swift` (+1)
- `Tests/ReplyAITests/LocalhostOAuthListenerTests.swift` (+3)
- `Tests/ReplyAITests/NotificationCoordinatorTests.swift` (+3, +mock field)
- `Tests/ReplyAITests/IMessageSenderTests.swift` (+2)
- `Tests/ReplyAITests/StatsTests.swift` (+1)
- `Tests/ReplyAITests/SlackOAuthFlowTests.swift` (+1)
- `Tests/ReplyAITests/RulesTests.swift` (+4)

Docs:
- `AGENTS.md` (test count refresh)

## Suite state

- Before: 1508 tests, 2 skipped (~15s warm)
- After: 1521 tests, 2 skipped (~15s warm) — net +13 across 11 commits

Two-skip form (`ContactsResolverTests`+`InboxViewModelIsSyncingTests`) hangs in
this fire same as the boundary documented in AGENTS.md gotcha — three-skip
remains the validated form.

## Pipeline state at exit

- main: 11 commits ahead of pre-fire (`a1142dc` → `f28aa54`)
- wip queue: 0 (still empty — autopilot worked entirely on main)
- BACKLOG: status fields unchanged (no completions to flip)

## Next-fire hints

None substantive. Same theme as today's other fires: small, high-rationale
literal pins on areas that ship user-visible behavior. Next-fire candidates:
- `IMessagePreview.attachmentFallback`/`nonTextFallback` literal pin
  audit (suspect partly pinned) — sentinels are user-visible row text
- `KeychainHelper` default service `co.replyai.mac` literal (not yet checked)
- `MessagesAppActivationObserver` debounce default (private, would need
  hoist similar to today's pattern)
