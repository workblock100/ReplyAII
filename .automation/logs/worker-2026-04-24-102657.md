# Worker Log — worker-2026-04-24-102657

- **Date**: 2026-04-24
- **Tasks**: REP-239 (S), REP-265 (M)
- **Outcome**: success — all 527 tests green

## Tasks shipped

### REP-239 — MessagesAppActivationObserver
New file `Sources/ReplyAI/Channels/MessagesAppActivationObserver.swift`. Watches `NSWorkspace.shared.notificationCenter` for `didActivateApplicationNotification`. Fires `onMessagesActivated` callback when `com.apple.MobileSMS` becomes frontmost. 600ms debounce via `DispatchWorkItem` cancellation. Injectable `notificationCenter` and `bundleIDExtractor` for test isolation (real `NSRunningApplication` can't be constructed in XCTest). 3 new tests in `MessagesAppActivationObserverTests.swift`.

### REP-265 — InboxViewModel activation wiring
Modified `Sources/ReplyAI/Inbox/InboxViewModel.swift`. Added `@ObservationIgnored private var activationObserver: MessagesAppActivationObserver?` and `@ObservationIgnored private var lastActivationDate: Date?` (annotation required because `@Observable` macro expansion context doesn't include types added after the initial compilation batch starts). Added `activationObserver` to `init` signature, wires `onMessagesActivated` callback via `Task { @MainActor [weak self] in await self?.handleMessagesActivation() }`. `handleMessagesActivation()` enforces 5s debounce then calls `syncFromIMessage()`. 3 new tests in `InboxViewModelTests.swift` (`InboxViewModelMessagesActivationTests` class).

## File changes
- `Sources/ReplyAI/Channels/MessagesAppActivationObserver.swift` — new
- `Tests/ReplyAITests/MessagesAppActivationObserverTests.swift` — new
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` — modified (activationObserver property + handleMessagesActivation)
- `Tests/ReplyAITests/InboxViewModelTests.swift` — modified (InboxViewModelMessagesActivationTests appended)
- `BACKLOG.md` — REP-239 + REP-265 marked done
- `AGENTS.md` — test count 521→527, log entry added

## Test counts
- Before: 521
- After: 527 (+6)
- Result: 527 tests, 0 failures (2026-04-24 08:10:08)

## Notes
- `@ObservationIgnored` required on both new `InboxViewModel` private properties to prevent `@Observable` macro from generating `@ObservationTracked` wrappers that reference the new type before it's in scope.
- Injectable `bundleIDExtractor: (Notification) -> String?` pattern avoids `NSRunningApplication` in tests; test closure reads `userInfo["bundleID"]` as `String`.
- Push required `git pull --rebase origin main` due to planner commit between clone and push.
