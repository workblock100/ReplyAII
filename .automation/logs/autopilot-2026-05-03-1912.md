# autopilot 2026-05-03-1912

## Time used
61 min — 1 action

## Actions this fire

1. P2 refactor: REP-162 IMessageSender.validateChatGUID(_:for:) → wip/autopilot-2026-05-03-181512-rep162-validate-guid
   - Renamed iMessage-only `isValidChatGUID` to per-channel `validateChatGUID(_:for:)` (throws)
   - Added private `isValidIMessageGUID` + `isValidSMSGUID` helpers
   - sendRaw now takes `channel: Channel` parameter
   - Migrated 5 existing tests from Bool-returning to throws-based API
   - Added 2 new tests: `testSMSGUIDFormatRecognized`, `testWrongChannelGUIDThrows`
   - Result: 41/41 IMessageSenderTests pass; full swift test hangs on ContactsResolverTests XPC
   - Pushed to wip branch; human merge needed

## Files touched
- `Sources/ReplyAI/Channels/IMessageSender.swift`
- `Tests/ReplyAITests/IMessageSenderTests.swift`

## Pipeline state at exit
- Open BACKLOG tasks: 57
- Wip queue depth: 1
- Build cache: WARM (used throughout)
- Last 3 commits on main:
  ae373ea backlog: claim REP-162 (IMessageSender.validateChatGUID refactor) for autopilot-2026-05-03-181348
  6fed6d5 docs: WHY notes on Channel, Folder, Tone, Message types
  07f4b16 rep-240: AppleScriptMessageReader.messagesForChat — fetch a single chat's recent messages

## Blocking issue — ContactsResolverTests XPC hang
Full `swift test` hangs indefinitely in headless runner (ContactsResolverTests hits NSXPCConnection to AddressBook). This prevents the autopilot from confirming 100% pass on the broader suite. Only the changed suite (IMessageSenderTests, 41 cases) was verified. The P9 task (add XCTSkipIf for ContactsResolverTests) would fix this permanently — recommend prioritizing it.

## Next-fire hints
- REP-162 wip branch is merge-ready; human or next fire can merge if swift test confirms green
- ContactsResolverTests XPC hang is blocking every fire from running full suite — P9 fix is urgent
- REP-170 (contactGroupMatchesName predicate, M/no-UI) and REP-111 (snooze action, M/no-UI) are high-value next targets
