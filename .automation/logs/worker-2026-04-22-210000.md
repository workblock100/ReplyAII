# worker-2026-04-22-210000

## Tasks
- REP-127: DraftEngine: trim leading/trailing whitespace from accumulated LLM stream output
- REP-131: ChatDBWatcher: stop() idempotency test
- REP-132: DraftEngine: rapid regenerate() calls do not spawn parallel LLM streams
- REP-136: AGENTS.md: consolidate duplicate test-count lines

## Outcome
shipped

## File changes
- `Sources/ReplyAI/Services/DraftEngine.swift` (+1 line): trim accumulated text before .ready transition on .done chunk
- `Tests/ReplyAITests/DraftEngineTests.swift` (+82 lines): 5 new tests (REP-127 x3, REP-132 x2) + 2 private test doubles (FixedTextService, SlowFixedTextService)
- `Tests/ReplyAITests/ChatDBWatcherTests.swift` (+32 lines): 2 new tests (REP-131 x2)
- `AGENTS.md` (-3 lines): removed duplicate hard-coded test count from What's done; removed stale parenthetical count from Testing expectations
- `BACKLOG.md`: marked all 4 tasks done, moved to Done/archived

## Tests
- Before: 365
- After: 372 (7 new)

## Notes
- REP-131: `testCallbackNotFiredAfterStop` redesigned from initial version — original attempted to verify stop() prevents scheduleFire()-after-stop from firing, which is explicitly NOT guaranteed by the implementation (per comment in testStopCancelsPendingFire). Redesigned to test that stop() cancels a pending work item burst.
- REP-132: Guard already existed via `tasks[key]?.cancel()` in generate(). Tests confirm the invariant holds with a slow stub that overlaps two regenerate() calls.
- REP-136: Repo-layout header already reflected current count (365→372 after this commit). Removed standalone hard-coded count line from What's done section. Removed stale parenthetical count from Testing expectations (now relies on grep instruction already present).
