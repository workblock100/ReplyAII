# Polisher run — 2026-04-24-191718

## Files changed
- `Sources/ReplyAI/Inbox/Composer/TonePills.swift`

## Diff summary

```diff
+                .accessibilityLabel("\(tone.rawValue) tone")
```

Applied to the `Button` for each tone pill inside `ForEach(Tone.allCases)`.

## Strings before / after

| Before | After |
|---|---|
| (no label — VoiceOver read bare `tone.rawValue`, e.g. "casual") | "casual tone", "formal tone", etc. |

## Test delta

No existing tests for `TonePills` view code. No test changes needed.

## Verification state

`verification: cold-cache-skipped` — `.build/` absent on fresh clone; MLX cold compile exceeds 13-min budget (see REP-254). Merger will validate on next warm-build fire.
