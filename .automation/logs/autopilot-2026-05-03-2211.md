# autopilot-2026-05-03-2211

## Time used

~38 min — 9 actions

## Actions this fire

1. **P2 ship: REP-178 InboxViewModel pin persistence → main `b271194`.** Added `pinnedThreadIDs: Set<String>` persisted under `pref.inbox.pinnedThreadIDs`, plus `pinThread(_:)` / `unpinThread(_:)` API and `applyPinned(_:pinnedIDs:)` overlay called from `syncAllChannels`. Centralised the field copy in `InboxViewModel.copying(_:pinned:)` — the previous `markPinned` silently lost `chatGUID` and `hasAttachment` when rebuilding the MessageThread. 3 new tests, full suite 735 → 738.
2. **P2 wip: REP-111 InboxViewModel snooze + persisted resumption → `wip/autopilot-2026-05-03-2235-rep111-snooze`.** Implemented `snoozedUntil: [String: Date]` persisted under `pref.inbox.snoozedUntil`. `snooze(threadID:until:)` hides the thread via `filteredThreads` and schedules a Task that drops the entry on the wake date. Init re-arms wake Tasks for entries that survived a relaunch (past dates fire on next runloop tick). Stale-Task guard via expectedWake equality. 4 new tests pass in isolation. Pushed to wip rather than main: full-suite verification kept hanging on `testIsSyncingTrueWhileSyncing` — confirmed pre-existing flake by stashing my changes and reproducing the same hang on a clean main.
3. **P8 ship: AGENTS.md test count 722 → 738 → main `6a3f637`.** Doc-only.
4. **P5 wip: AppleScriptMessageReader test coverage → `wip/autopilot-2026-05-03-2240-tests-applescript-reader`.** New file with 18 cases on the parsing surface (prettyPhone US 11/10, email, chat-key passthrough, recentChats well-formed/missing-value/group-chat fallback/contact resolution, messagesForChat direction mapping/missing-value filtering/limit clamp/GUID escaping). All pass; test-only addition.
5. **P3 ship: 12 blocked REPs reconciled → main `5ec77d6`.** REP-272 + REP-229 had landed-commit pointers; the 10 others (REP-177/179/183/187/193/194/195/196/198/220) each have a `// MARK: - REP-NNN` test section in their named test file confirming the implementation shipped. Blocked count 14 → 3.
6. **P3 ship: REP-257 reconciled → main `cfc9c04`.** `SlackChannel.messages(forThreadID:)` is on main calling `conversations.history`; tests cover parsing/empty/timestamp/auth/network paths. Blocked count 3 → 2.
7. **P5 wip: ChannelError descriptions + ChannelService default overloads → `wip/autopilot-2026-05-03-2244-tests-channel-error-defaults`.** 10 new tests pinning the user-facing copy on every ChannelError case (with hand-written-recovery-hint assertions on `databaseCorrupted` and `authorizationDenied`) plus the protocol-extension defaults (`recentThreads()` → 50, `messages(forThreadID:)` → 20, `newIncomingMessages` default → []). Test-only addition.
8. **P5 wip: Folder.Kind + Tone enum contracts → `wip/autopilot-2026-05-03-2245-tests-folder-tone`.** 10 new tests pinning `allCases` order, raw-value strings, Codable round-trip, `Tone.cycled()` wrap-at-end, `id == rawValue`. Both enums are persistence contracts (Folder.Kind via `Preferences.lastSelectedFolder`; Tone via rules.json setDefaultTone + per-thread draft cache keys), so case renames are silent migrations.
9. **P5 wip: Channel raw values + ordering + identifier → `wip/autopilot-2026-05-03-2247-channel-rawvalues`.** 4 tests added to existing ChannelTests pinning all six rawValues, the allCases declaration order, `id == rawValue`, and `displayName == label`.

## Files touched

- `Sources/ReplyAI/Inbox/InboxViewModel.swift` — pin persistence (REP-178, on main) + snooze (REP-111, on wip)
- `Tests/ReplyAITests/InboxViewModelTests.swift` — 3 pin tests (main) + 4 snooze tests (wip)
- `Tests/ReplyAITests/AppleScriptMessageReaderTests.swift` — new file, 18 tests (wip)
- `Tests/ReplyAITests/ChannelTests.swift` — 10 ChannelError + ChannelService default tests (wip) + 4 Channel rawValue tests (separate wip)
- `Tests/ReplyAITests/FolderToneTests.swift` — new file, 10 tests (wip)
- `BACKLOG.md` — REP-178/111/229/257/272/177/179/183/187/193/194/195/196/198/220 status flips
- `AGENTS.md` — test count bump

## REPs closed (status: done on main)

REP-178, REP-272, REP-229, REP-257, REP-177, REP-179, REP-183, REP-187, REP-193, REP-194, REP-195, REP-196, REP-198, REP-220 — 14 in total. (REP-111 marked done on its wip branch; the merger will pick up the BACKLOG flip when the branch merges.)

## Pipeline state at exit

- BACKLOG: 20 open, 0 in_progress, 2 blocked, 264 done. Blocked dropped from 14 → 2 (only REP-501 MLX extraction step 1 and REP-280 the warm-build wip-drain meta-task remain blocked).
- Wip queue depth: 5 wip branches all created in this fire (REP-111 snooze, AppleScriptMessageReader tests, ChannelError + Defaults tests, Folder/Tone tests, Channel rawValues tests). Merger should pick them up on the next warm-build drain.
- Build cache state: WARM at exit.
- Last 5 commits on main:
  - cfc9c04 backlog: REP-257 → done — SlackChannel messages(forThreadID:) on main
  - 5ec77d6 backlog: reconcile 12 blocked reps → done — implementation lives on main
  - 6a3f637 agents: bump test count 722 → 738 to match main
  - b271194 rep-178: persist InboxViewModel pin state across relaunch
  - 199edce autopilot: 2026-05-03-2125 log — 5 actions, 19 REPs closed

## Next-fire hints

- **`testIsSyncingTrueWhileSyncing` pre-existing flake.** Reproduced on a stashed-clean `main` by simply running `swift test --filter "InboxViewModelIsSyncingTests/testIsSyncingTrueWhileSyncing"` — xctest sits at ~5s of CPU forever without printing the usual "Test Case … started" line. Other tests in the same class (`testIsSyncingFalseAfterError`, `testIsSyncingFalseAfterSuccess`) run instantly. The hang blocks every fire from getting a clean full-suite signal, which is why all 4 P5 wip branches I shipped this fire could only be verified in isolation. Worth a P0 investigation in the next fire — likely a `BlockingMockChannel` resumeAll() race, or a continuation that's never resumed when `recentThreadsCallCount == 0` after the 100-iteration yield loop times out.
- **Concurrent `swift-test` invocations corrupt the build cache.** Two `swift test` runs against the same `.build` directory deadlock each other on something — both processes end up burning ~5s of CPU and never finishing. Fire 2026-05-03-2211 hit this once when I had a leftover hung xctest from a previous filter command running in the background while a new full-suite run started. Future fires should `pkill -f "/Users/elijahosik/.cache/replyai-autopilot/.build"` before any `swift test` invocation as a defensive measure.
- **5 wip branches awaiting merger.** All five are test-only or non-ui code adding strict regression guards. Should drain cleanly on a warm-build merger run modulo the testIsSyncing flake.
