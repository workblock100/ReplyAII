# Worker run 2026-04-24-170301

## Task
REP-244 — InboxViewModel: `syncAllChannels()` merges results from all registered ChannelServices

## Outcome
**blocked** — code complete, pushed to wip/2026-04-24-170301-sync-all-channels; MLX cold-clone build time would exceed 13-min budget (no .build/ cache present); cannot run `swift test` to verify.

## File changes
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` — +44 LOC: `registeredChannels: [any ChannelService]` property; `registeredChannels` parameter added to `init`; `syncAllChannels() async -> [MessageThread]` method (concurrent withTaskGroup fetch, dedupe by threadID, pinned-first sort)
- `Tests/ReplyAITests/InboxViewModelTests.swift` — +74 LOC: `InboxViewModelSyncAllChannelsTests` class with 4 tests: merge, deduplicate, partial-failure isolation, empty-list guard

## Tests before/after
- Before: 541 (AGENTS.md estimate)
- After: 545 (4 new tests in `InboxViewModelSyncAllChannelsTests`)
- Status: **unverified** — requires human `swift test` run

## Notes
- `withTaskGroup(of: (Int, [MessageThread]).self)` preserves registration order for deduplication so first-channel wins on duplicate threadID, matching spec.
- `any ChannelService` existentials are `Sendable` (protocol declares `: Sendable`), so task-group capture is safe.
- Default `registeredChannels` is `[self.imessage]` (the already-configured iMessage channel with contacts resolver wired in), not a fresh `IMessageChannel()`, so existing production behavior is unchanged when `registeredChannels` is not injected.
- REP-280 (warm-drain merge task) was evaluated but skipped — requires fresh `.build/` which is absent.
