# autopilot 2026-05-06-0617

**Time used**: ~36 min — 19 actions

## Actions this fire

P1 wip-queue drain (6 cherry-picks — branches were based on stale main; FF-merge failed, cherry-picked clean):

1. P1 wip merge: `wip/autopilot-2026-05-05-061100-slacktokenstore-edges-rebased` → main `e6b5904` — 3 SlackTokenStore tests (overwrite / empty-token / unicode workspace name). Branch deleted.
2. P1 wip merge: `wip/autopilot-2026-05-05-061800-draftengine-loadprogress-edges` → main `30a1a9b` — DraftEngine loadProgress edges (+127 lines). Branch deleted.
3. P1 wip merge: `wip/autopilot-2026-05-05-063100-theme-motion-pins` → main `80d570a` — Theme.Motion duration pins (+47 lines). Branch deleted.
4. P1 wip merge: `wip/autopilot-2026-05-05-064100-imessagepreview-passthrough` → main `009ac28` — IMessagePreview whitespace/newline/tab pass-through (+37 lines). Branch deleted.
5. P1 wip merge: `wip/autopilot-2026-05-05-064600-folder-value-pins` → main `c729ff5` — Folder value-equality + id-from-Kind contract (+40 lines). Branch deleted.
6. P1 wip merge: `wip/autopilot-2026-05-05-065500-promptbuilder-truncate-edges` → main `a2f7f0f` — PromptBuilder.truncate over-budget edges (+35 lines). Branch deleted.

P5 test coverage additions to main:

7. P5: ChannelStubAuthGateEdgeTests.swift NEW — pins empty-token bypass behavior across SMS/WhatsApp/Teams/Telegram auth gates (9 tests, +140 lines). Main `b214114`.
8. P5: AppleScriptMessageReader edges — sort order, nameFor echo, empty-string echo (3 tests, +49 lines). Main `e9adc89`.
9. P5: Fixtures.sidebarChannels == Channel.allCases equality pin (+15 lines). Main `572a7ca`.
10. P5: DraftStore sanitization defense-in-depth — sanitized-filename, traversal-style, dot-prefix-skips (3 tests, +91 lines). Main `58296aa`.
11. P5: IMessageChannel.formatRelative — Yesterday + today bucket pins (2 tests, +34 lines). Main `e8a34d5`.
12. P5: StubLLMService.tokenize — tabs-inside-tokens + single-char path (2 tests, +26 lines). Main `26916c0`.
13. P5: SearchIndex.ftsQuery — all-sanitized-empty + hyphen-collapse-internal (2 tests, +28 lines). Main `abf762a`.
14. P5: InboxViewModel.editKey format pin (`<threadID>|<tone.rawValue>`, +43 lines, 3 tests). Main `798052c`.

P4 polish (UI-sensitive — pushed to wip):

15. P4 polish: AppInboxEmptyView "5 channels" → `\(Channel.allCases.count)` so the count auto-updates as channels are added. Branch `wip/autopilot-polish-2026-05-06-063434-channel-count-fix`.

P7/P8 doc-fixes (comment-only, pushed to main):

16. P7 docs: ScreenRouter comment 28 → 34 screens (matches enum). Main `13e8382`.
17. P8 audit: AGENTS.md repo-layout test count 1249 → 1279 → 1292 (drift catch-up across fires). Main `c1a5e6c` and final update at end.
18. P7 docs: IMessagePreview.singleURLHost comment dropped stale "mailto" from scheme list. Main `f67fa8f`.

## Notable observations

- **All 6 wip branches required cherry-pick** — they were based on commits older than current `main`, so `git merge --ff-only` rejected them. Cherry-picking the single test commit per branch worked cleanly because each branch carried exactly one tests-only diff. No banned patterns in any of the six.
- **ContactsResolverTests passed** — the `XCTSkipIf` guard at `setUpWithError` (already in main as of this fire) cleanly skipped the suite when Contacts permission isn't authorized. The 21-case carve-out documented in SKILL.md is no longer needed for filtered runs in this environment.
- **No P2 ship-to-main candidate.** The `claimed_by: null, ui_sensitive: false` open tasks are all blocked: REP-501→504 depend on a wip branch that was lost (REP-501 explicitly recommends a human-paired session), REP-XCUI-001 is L-effort (full XCUITest target setup), and REP-AUDIT-260505 is `ui_sensitive: true`. Dropping into P5/P7/P8 lanes was the right call.
- **Drift catches**: the `AppInboxEmptyView` "5 channels" copy was real — Channel.allCases.count is 6. The ScreenRouter `28 screens` comment was wrong — ScreenID.allCases is 34. The IMessagePreview `mailto` doc claimed support that never existed in code. The AGENTS.md repo-layout test count was 30+ behind reality.

## Pipeline state at exit

- **Test count on main**: 1292 (was 1249 at fire start, +43)
- **Open BACKLOG**: 23 (unchanged — no autopilot-actionable shippable tasks)
- **wip queue**: 1 (`wip/autopilot-polish-2026-05-06-063434-channel-count-fix` — UI-sensitive polish awaiting human review)
- **Build cache**: WARM
- **Last 3 commits on main**: `798052c` (editKey pin), `abf762a` (ftsQuery edges), `f67fa8f` (IMessagePreview doc)

## Next-fire hints

- The `wip/autopilot-polish-...-channel-count-fix` branch is the only outstanding wip. UI-sensitive (touches a SwiftUI view body), so a future fire shouldn't auto-merge it; needs human visual review. Auto-derive from `Channel.allCases.count` is the safe pattern — should probably be propagated to similar drift-prone copy in `ObDoneView` ("4 channels connected") if the human approves the AppInboxEmptyView pattern first.
- AGENTS.md repo-layout test count keeps drifting — fires should consider auto-updating it as part of the fire log step. Today's drift was 30 tests behind reality.
- The 6 wip branches that required cherry-pick (all test-only) suggests autopilot should rebase wip branches onto main before pushing them, so future fires can FF-merge instead of cherry-picking.
