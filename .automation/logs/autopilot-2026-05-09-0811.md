# Autopilot fire — 2026-05-09 08:11 EDT

**Time used**: ~50 min — 9 productive actions (6 test-only pins + 1 BACKLOG cross-ref audit + 2 AGENTS.md staleness fixes). Test count went 1937 → 1946; passes under the three-skip gate went 1901 → 1909.

## Summary

The wip queue (both `...-hit-test` and `...-frame-height` candidates from REP-AUDIT-260505) is still `claimed_by: human` and waiting on a smoke-launch verdict — same as the 06:11 fire saw. Rather than re-reverifying for the third fire in a row (low marginal value), this fire spent its budget on:

1. A code-only audit that **strengthens the case for the frame-height candidate** by grepping every Capsule-fill `Button.buttonStyle(.plain)` site in the codebase. Result: the buggy gallery toolbar at `AppPrototypeView.swift:73–84` is the **only** site that uses padding-derived height instead of `.frame(height:)` — every other working site (PrimaryButton, MenuBarContent's Open inbox, FDABanner's Open System Settings) anchors with `.frame(height:)` BEFORE `.background(Capsule(...).fill(...))`. The frame-height candidate brings the buggy site into structural parity with the empirical working pattern.
2. **Four pin-tests** filling concrete coverage gaps that pre-existing test docstrings flagged as "design contract" but never actually asserted: ContactsResolver's Some("") cache-collapse leg, Theme's line-stack opacity literals, Theme's foreground-stack hex literals, Theme.Motion's bezier control-point coefficients.

All commits to main, none to wip.

## Actions this fire

1. **P8 cross-ref audit on REP-AUDIT-260505** (commit `16aa469`) — appended a `crossref_audit_2026-05-09-0811` note documenting the codebase-wide grep findings. Recommends to the human: prefer merging `wip/autopilot-2026-05-09-0211-open-inbox-button-frame-height` first since it brings the buggy site into structural parity with all four working Capsule-filled buttons; the `...-hit-test` candidate adds an orthogonal `.contentShape` mechanism that doesn't match the cross-codebase shape. No source change — purely an evidence-strengthening note plus a recommendation, anchored to the existing `reverify_2026-05-09-0611` data point.
2. **P5 test-only pin: ContactsResolver Some("") cache-collapse** (commit `8ffc329`, +AGENTS.md count bump) — added `testCacheTreatsEmptyStringResolutionLikeNil` next to the existing `testCacheRemembersMissesAsEmpty`. Pins the "Some('') and nil reach the same cache state, return the raw handle on every fresh-window call, consume one store lookup" behavior at the resolver's `name(for:)` API surface. Covers exactly the empty-string surprising-but-safe pattern AGENTS.md gotcha §270 asks pins for. Test lives in the `--skip ContactsResolverTests` suite so it doesn't run in the autopilot gate, but compiles clean and runs in Xcode-with-Contacts-grant. Test count 1937 → 1938.
3. **P5 test-only pin: line-stack opacity literals** (commit `d08ecce`, +AGENTS.md count bump) — added `testLineStackOpacityLiteralsArePinned` next to the existing `testLineStackIsDistinct`. Pins the design-contract opacities of `Theme.Color.line` (6%), `lineStrong` (12%), `lineFaint` (4%) — the distinctness test only catches collapses, not uniform shifts that would still keep the three layers distinct yet visibly reweight every divider in the app. Mirrors `testAccentStackOpacityLiteralsArePinned` for the neutral-line stack. Format pinned is `"6% white"` (the description shape SwiftUI.Color.white.opacity produces, distinct from the `#XXXXXXFF` shape RGB-constructed colors produce — the format choice is itself documented in the test so a future re-route through `Color(red: 1, green: 1, blue: 1).opacity(...)` surfaces deliberately). Test count 1938 → 1939; passes 1901 → 1902.
4. **P5 test-only pin: foreground-stack hex literals** (commit `a67a4b2`, +AGENTS.md count bump) — added `testForegroundStackHexLiteralsArePinned` next to the existing `testForegroundStackIsDistinct`. Pins the hex literals of fg=#F2F2EE warm off-white primary, fgDim=#C9CBC6 secondary, fgMute=#8A8D86 tertiary metadata, fgFaint=#55584F disabled. The slight green undertone (G > R > B in every shade) is intentional and ties into the warm-paper accent ecosystem; uniform shifts that don't collapse stops would slip through distinctness alone. Same pattern as the line-stack pin. Test count 1939 → 1940; passes 1902 → 1903.
5. **P5 test-only pin: Theme.Motion bezier control points** (commit `055326a`, +AGENTS.md count bump) — added `testMotionCurveControlPointsArePinned`. The duration pins (`testMotionFastDurationPinned` etc.) only catch retiming; a swap from the design's `timingCurve(0.25, 0.1, 0.25, 1)` to SwiftUI's default `easeInOut` would still pass `duration: 0.12/0.14/0.18` while changing the feel of every animation in the app. The existing test-doc comment claimed the control points were pinned but no actual assertion existed — this fills the gap by substring-matching the SwiftUI CubicSolver's `cx: 0.75` coefficient (derived from x1=0.25 via the Bernstein-basis transform), which is stable under SwiftUI's printing format and unique to this specific curve. Same substring-match pattern as the duration pins for format-robustness. Test count 1940 → 1941; passes 1903 → 1904.
6. **P5 test-only pin: Theme.Font weight routing** (commit `9a73a23`, +AGENTS.md count bump) — added 3 new pins covering an entire previously-unpinned subsystem.

7. **P8 audit: AGENTS.md dev-cmd quickstart staleness fix** (commit `4c01f99`) — the test-count summary at line 142 was bumped to `1944 / 1907` over the 5 pin-additions in this fire, but the code-block comment in the dev-cmd quickstart at line 52 still said "completes in ~19s warm with 1901 passes". The quickstart is the FIRST thing a new collaborator (human or autopilot) copies and runs, and a stale comment there silently signals "the test count is fresh" when actually it's already 6 passes ahead. Bumped both the count (1901 → 1907) and the elapsed time (~19s → ~18.5s, observed warm-run average across the 5 verification cycles this fire). No code change.

9. **P8 audit: AGENTS.md dev-cmd quickstart count second bump** (commit `c04de8b`) — action 8 added 2 more passes so the line-52 comment that I'd just bumped in action 7 was stale again 4 commits later. Bumped to 1909 + 18s. The header at line 142 stays in lockstep with this code-block comment, but they need separate edits because they're different lines.

8. **P5 test-only pin: Theme.Font.serifItalic flow-through + family distinctness** (commit `1e89390`, +AGENTS.md count bump) — Theme.Font.serifItalic was the only Theme.Font helper with ZERO test coverage. It's the InstrumentSerif-Italic display face used on every onboarding hero ("you" pull-quote, "Teach ReplyAI how you text."), every empty-state hero ("Inbox zero."), and every error screen heading. (a) `testSerifItalicSizeArgumentFlowsThrough` pins that serifItalic(14) and serifItalic(38) are distinct fonts — catches a "let's default the size" refactor that would silently shrink every onboarding hero. (b) `testSerifItalicProducesDistinctFontFromSansAndMono` pins that serifItalic, sans, and mono at the same size are pairwise distinct — catches a regression that swapped InstrumentSerif-Italic for the default sans face (silent un-italicization). Same Equatable-on-Font pattern as the mono routing pin. Test count 1944 → 1946; passes 1907 → 1909. SwiftUI's Font type is opaque under `String(describing:)` so the family-name route I'd hoped for doesn't work, but Font is Equatable — so the routing STRUCTURE is pinnable via `==` / `!=` assertions on the produced fonts. (a) `testMonoWeightRoutingMapsBoldToMedium` pins that `.medium / .semibold / .bold / .heavy / .black` all collapse to JetBrainsMono-Medium (pairwise equal) while `.regular / .light` both map to JetBrainsMono-Regular, and the two buckets are pairwise distinct — catches a switch-arm collapse that would silently un-bold every monospaced affordance. (b) `testMonoDefaultWeightArgumentIsRegular` pins that `mono(_:)` without an explicit weight equals `mono(_:weight:.regular)` — catches a "default to medium for readability" edit that would silently thicken every no-weight caller (the dominant call shape in the codebase). (c) `testSansWeightModifierIsAppliedAndProducesDistinctFonts` pins that `sans(.regular) != sans(.bold)` — Inter Tight is a single variable-weight TTF so distinctness depends on the `.weight()` modifier on the resolved Font; dropping it would render every weight at the same axis position. Test count 1941 → 1944; passes 1904 → 1907.

## What got skipped this fire (and why)

- **Wip-queue drain** — both `wip/autopilot-2026-05-09-0211-open-inbox-button-{hit-test,frame-height}` are `claimed_by: human` and need a UI smoke-click that the autopilot's existing smoke-launch layer can't perform. Re-running the test gate against them for the third fire in a row would just re-stamp the same data point. The cross-ref audit (action 1) was the higher-leverage move toward unblocking this — gives the human structural evidence to choose between candidates, on top of the existing test/build green-light.
- **REP-501→REP-505 (MLX SPM extraction)** — explicitly flagged L-effort and risky for a single autopilot fire in BACKLOG. Untouched.
- **REP-XCUI-001 (XCUITest target bootstrap)** — M-effort and would require validating XCUITest's interaction with SwiftPM's `xctest` runner, which has known quirks. Not viable in 50 min; left for a human-paired session.
- **Pinning bg0/bg2/bg3 hex literals** — considered but rejected. The existing `testSurfaceBg1LiteralIsPinnedDarkInbox` doc-comment explicitly says "Sibling stops aren't pinned individually because the distinctness invariant + the bg1 pin suffice to catch any realistic regression." Respecting the documented design choice; adding the same pattern to fg/line stacks (where no such reservation was documented) is consistent with the already-shipped accent-stack pin pattern.
- **Pinning Theme.Font family-name literals** — explored, partially mitigated by action 6. SwiftUI's `String(describing:)` for a `Font` projects to an opaque `Font(provider: SwiftUI.FontBox<...>)` shape that doesn't expose the family name. The clean fix would be to hoist the name strings as static constants on `Theme.Font` and pin those, but Theme.swift edits are ui_sensitive per the SKILL.md rules and would need to land on a wip branch. Action 6 caught the next-best thing — pinned the routing STRUCTURE via Font equality (which IS observable). The family-name literal pin (e.g. asserting `Theme.Font.monoRegularFamily == "JetBrainsMono-Regular"`) still wants a wip-track source-edit fire.

## Lessons / next-fire hint

The autopilot has been doing pin-only fires productively for several runs in a row, but the marginal pin opportunity is genuinely thinning out. After this fire:
- Channel enum: fully pinned (label, iconName, dotColor, raw values, displayName parity, init-from-unknown).
- Theme: accent stack literals + opacities, channel colors, semantic colors, surface bg1, fg stack, line stack, motion durations + curve coefficients, radius, space — all pinned. Only bg0/bg2/bg3 individually + Font family names remain, and both have documented reasons not to pin (or require a Theme.swift source edit that should go to wip).
- Slack: HTTPClient + Channel + OAuthFlow + SocketClient + TokenStore — extensively pinned, including empty-workspace propagation and HTTP error literals.
- ChannelError + NotificationCoordinator + DraftStore + PromptBuilder + Stats — all heavily pinned (80+ tests on Stats alone).
- Empty-string class (gotcha §270): pinned at the rules engine, IMessageChannel chatGUID column, Slack workspace name, regex-empty pattern, and now ContactsResolver name(for:).

What's NOT yet test-pinned and would benefit from it:
- Inbox/Composer/Sidebar UI string literals (`"Loading on-device model"`, `"⌘K"`, `"R"`, `"Pro · mac"`, the SendConfirmSheet copy, etc.). These all need a hoist-to-static-let in their View files first → wip-branch territory, not main.
- Theme.Font family names — same constraint.
- Bg0/bg2/bg3 hex — explicitly opted out by the existing doc.

Suggestion for next fire: instead of hunting for one more pin, take a real pass at REP-XCUI-001 scaffolding on a wip branch. Even just adding `Tests/ReplyAIUITests/` with one stub test case + the Package.swift target wiring would be a meaningful step forward, and would get pushed to wip rather than main so any SwiftPM/XCUITest interaction issues are caught before they touch main. Alternative: a wip-branch fire that hoists the inbox/composer/sidebar UI literals to static constants and pins them — that's a lot of small high-confidence pins behind a single human review.

## Pipeline state at exit

- main: HEAD `4c01f99` (7 commits + 2 log commits ahead of fire start `d95108b`):
  - `16aa469` BACKLOG cross-ref audit on REP-AUDIT-260505
  - `8ffc329` ContactsResolver Some("") cache pin (+AGENTS.md count 1937→1938)
  - `d08ecce` line-stack opacity pin (+1938→1939)
  - `a67a4b2` foreground-stack hex pin (+1939→1940)
  - `055326a` Motion bezier-curve pin (+1940→1941)
  - `76616b0` initial fire log
  - `9a73a23` Theme.Font weight routing pins (+1941→1944)
  - `461459d` log v2 — capture action 6
  - `4c01f99` AGENTS.md dev-cmd quickstart count bump (1901→1907)
  - `f0a568e` log v3 — capture action 7
  - `1e89390` Theme.Font.serifItalic pins (+1944→1946)
- wip queue: still 2 branches (both `claimed_by: human`, neither older than 6h, neither archived):
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test` — last verified 06:11 fire
  - `wip/autopilot-2026-05-09-0211-open-inbox-button-frame-height` — last verified 06:11 fire; this fire's audit recommends preferring this one
- BACKLOG: 13 open, 0 in_progress (the `1 blocked` reading from earlier was a `status:` substring inside a `files_to_touch` line; no real blocked rows). No tasks flipped status this fire.
- Build cache: WARM (rebuilt incrementally for each pin verification — all `swift build --build-tests` runs were ≤ 9s after the first 25s)
- Test count: 1909 passing under three-skip gate, 1946 total (was 1901 / 1937 at fire start); the new ContactsResolver pin lives in the skipped suite so it adds to total but not to passes-under-gate, while the six ThemeTokensTests pins (line stack, foreground stack, motion curve, mono routing, mono default, sans distinctness, serifItalic flow-through, serifItalic family distinctness) all run in the gate.
- No regressions; full three-skip gate passes in 18.5s warm.
