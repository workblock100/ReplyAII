# Autopilot fire — 2026-05-03 21:25

## Time used
43 min — 5 actions

## Actions this fire

1. **P3 reconcile**: 12 stale wip-merge gate tasks → done
   `c05cf44` — REP-275, 276, 277, 279, 280, 281, 282, 283, 284, 286, 287, 288.
   All referenced wip branches pruned long ago; underlying impl REPs
   verified on main.

2. **P5 tests**: REP-221 — IMessageChannel `messages(forThreadID:)` AttributedBody
   fallback regression test → main `e974a1b`. recentThreads already had
   the guard; this pins the same fallback in the per-thread message stream
   so rich-only iMessages don't surface as "[deleted]".

3. **P2 ship**: REP-170 — SmartRule `contactGroupMatchesName(groupName:)`
   predicate + 6 tests → main `959a508`. Adds `RuleContext.contactGroupNames`
   field (defaults to []), `localizedCaseInsensitiveContains` evaluator,
   humanize string, JSON Codable round-trip. ContactsResolver
   CNContactStore.groups(matching:) wiring left for follow-up — predicate is
   functional today for any caller that populates contactGroupNames.

4. **P3 reconcile**: 4 more stale wip-merge gates → done `a2f26a1`.
   REP-017 (consolidation, moot since referenced branches gone), REP-200
   (REP-135/177/179 → b163ec1), REP-217 (REP-129 → 8bcfaed), REP-232
   (REP-190/210/212/215 → 51e8330).

5. **P2 ship**: REP-016 — senderKnown classification operator-precedence
   bug fix + 6 tests → main `75dc7ee`. Pre-fix expression
   `!hasPrefix("+") && !contains("@") || !allSatisfy {phoneChars}`
   parsed as `(notPlus && notAt) || !isPhonelike`, so emails and
   digit-only handles got classified as KNOWN — the opposite of intent.
   Fix is explicit `!isEmail && !isPhonelike`. Reconstructed from lost
   wip/quality-2026-04-21-193800-senderknown-fix branch.

## Files touched
- `Sources/ReplyAI/Rules/SmartRule.swift` (new predicate case + Codable)
- `Sources/ReplyAI/Rules/RuleEvaluator.swift` (new context field + 2 evaluator branches + senderKnown rewrite)
- `Sources/ReplyAI/Screens/Surfaces/SfcRulesView.swift` (humanize)
- `Tests/ReplyAITests/RulesTests.swift` (+12 cases: 6 contactGroup + 6 senderKnown)
- `Tests/ReplyAITests/IMessageChannelTests.swift` (+1 case: messages(forThreadID:) fallback)
- `BACKLOG.md` (16 status flips + 1 new claim)

## REPs closed
REP-016, 017, 170, 200, 217, 221, 232, 275, 276, 277, 279, 280, 281, 282,
283, 284, 286, 287, 288 — 19 total this fire.

## Pipeline state at exit
- Open BACKLOG: 18 (down from 40 at fire start; 22 closed: 19 new + 3
  pre-existing reconciles in the gate batches)
- Wip queue: 0 branches (no merges needed)
- Build cache: WARM
- Last 3 commits on main:
  - `75dc7ee` rep-016: senderKnown classification fix
  - `a2f26a1` backlog: reconcile 4 more stale wip-merge gates
  - `959a508` rep-170: contactGroupMatchesName predicate

## Test verification

- Targeted runs: RulesTests (82 cases) + IMessageChannelTests (18 cases)
  all pass green with new code.
- Full suite hung mid-run on InboxViewModelMLX-adjacent suite (after 245
  passing tests); behavior pre-dates this fire's changes — same
  environmental hang seen on a stale pid 15129 (xctest from a prior fire
  on `/Users/elijahosik/Code/ReplyAI/.build/`, never exited from 21:12).
  Killed both stale and current xctest after 4-min stall.
- Carve-outs applied: `--skip ContactsResolverTests` (known XPC),
  `--skip AccessibilityAPIReaderTests` (suspected XPC; needs further
  triage).

## Next-fire hints

- Investigate why `swift test` hangs after the InboxViewModelDraftStoreSeed
  suite. Either a specific test is making a blocking system call (XPC,
  ServiceManagement, NSWorkspace), or the fire just before me left a
  shared resource locked. Worth reproducing: run test suites individually
  in alphabetical order until one hangs without the prior fire's stale
  process.
- REP-178 cannot be implemented as scoped (test-only) — there is no
  `pinnedThreadIDs` Preferences key today. Either implement persistence
  AND tests in one wip branch, or close REP-178 as a scoping bug.
- REP-502/503/504 (MLX extraction continuation) need REP-501's wip branch
  as base, which doesn't exist. Either re-do REP-501 from scratch or
  flip the whole REP-500 chain to blocked-on-rework.
- REP-016 fix may surface a behavior change for any user who happened to
  rely on the buggy `senderUnknown` classification. Spot-checked: the
  predicate has no test fixtures relying on the old behavior, but a
  watchful eye on the next live run is warranted.
