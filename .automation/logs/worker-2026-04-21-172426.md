# Worker Run — 2026-04-21-172426

## Tasks
- REP-001: persist `lastSeenRowID` across app launches (P0, effort S)
- REP-002: SmartRule priority + conflict resolution (P0, effort M)

**Bundled per substantiveness gate**: REP-001 is effort=S with another open P0 task (REP-002, effort=M).

## Outcome
**shipped** — pushed to main

## File changes

| File | Lines +/- |
|------|-----------|
| `Sources/ReplyAI/Inbox/InboxViewModel.swift` | +22 / -2 |
| `Sources/ReplyAI/Rules/SmartRule.swift` | +37 / -11 |
| `Sources/ReplyAI/Rules/RuleEvaluator.swift` | +11 / -3 |
| `Tests/ReplyAITests/RulesTests.swift` | +100 / -0 |
| `AGENTS.md` | +1 / -1 |
| `BACKLOG.md` | status updates |

## Tests
- Before: 46 tests (per AGENTS.md; actual run showed 55 before changes)
- After: 60 tests, 0 failures
- New tests added:
  - `testLastSeenRowIDPersistsAcrossInstances` (REP-001)
  - `testHigherPrioritySetDefaultToneWins` (REP-002)
  - `testPriorityFieldMissingDefaultsToZero` (REP-002)
  - `testPriorityRoundTripsThroughJSON` (REP-002)
  - `testPriorityTiebreakerPreservesInsertionOrder` (REP-002)

## Build
`./scripts/build.sh debug` — passed, codesign verified.

## Notes
- REP-001: Used exact same pattern as `archivedThreadIDs` (didSet → UserDefaults write, init hydrates). JSON-encodes `[String: Int64]` natively. Key: `pref.inbox.lastSeenRowID`.
- REP-002: `SmartRule` gained explicit `Codable` conformance (separated from auto-synthesis) to use `decodeIfPresent` for `priority` so existing `rules.json` files without the field decode cleanly as priority 0. `RuleEvaluator.matching` uses `enumerated()` + explicit comparator for deterministic priority-DESC, insertion-order tiebreaker sort.
- AGENTS.md stubbed-item for "Global vs per-rule tone priority" updated to reflect resolution.
