# Autopilot fire — 2026-05-03 11:33 (interrupted)

**Time used**: ~14 min (interrupted by Elijah at usage-limit boundary, far short of the 50-min target)
**Actions this fire**: 2 — both pushed to wip (no main-branch changes)

1. **P9 ContactsResolverTests fix** → `wip/autopilot-2026-05-03-114558-fakecontactstore-normalize`
   - Diagnosed as a FakeContactStore implementation bug, NOT the
     environmental `NSXPCConnection` failure the autopilot prompt
     described. The fake was doing a literal `names[handle]` lookup,
     but the resolver passes the original handle (e.g. `+15551234567`)
     and the tests populate the fake with the 10-digit canonical form
     (`5551234567`) per their own `// normalized key` comments.
   - Fix: mirror `CNContactStoreBackedStoring`'s digit-keyed index in
     the fake — try the original key first, fall back to the
     normalised key. Email + group handles (`@`, `chat`) pass through.
   - Verified locally: 17 failures / 29 → 0 failures / 29 with no
     other test impact in the warm `~/.cache/replyai-autopilot/`
     workspace.
   - **This branch removes the carve-out the autopilot prompt
     describes** — once merged, future autopilot fires no longer need
     the "21 expected ContactsResolverTests failures" workaround when
     merging wip branches. The 17 vs 21 mismatch and the assertion-
     failure pattern (rather than CoreData XPC errors) suggests the
     prompt's carve-out documentation is stale.

2. **P2/REP-262 ShortcutsExportHandler** → `wip/autopilot-2026-05-03-114647-rep262-shortcuts-export`
   - Pivot-aligned alternative iMessage source: parses
     `replyai://import-messages?data=<percent-encoded-json>` into
     `[Export]` (thread + messages). No FDA required.
   - 8 XCTest cases (happy path, 4 failure modes, 3 edge cases).
   - Strategic split: parser only. Did NOT wire `onOpenURL` in
     `ReplyAIApp` (UI-sensitive Scene change — separate PR).
   - **NOT compile-verified in this fire** — was about to copy into
     the warm workspace for `swift test --filter` when Elijah hit
     usage limits and interrupted. Treat this branch as needing a
     human or next-fire compile + test verification before merge.

## Pipeline state at exit

- main HEAD: `cb2692f` (concurrent fire's REP-240 BACKLOG claim)
- wip queue depth: **2** (both this fire's, both unverified by full
  `swift test`)
- BACKLOG counts (pre-fire): 59 open / 3 in_progress / 15 blocked
- BACKLOG counts (post-fire): unchanged — neither task moved off `open`
  because both branches need verification before claiming
- Build cache: warm in `~/.cache/replyai-autopilot/`, cold in
  `~/.cache/replyai-autopilot-1133/`

## Concurrent-fire detection

A second autopilot instance was already operating on the shared
`~/.cache/replyai-autopilot/` workspace when this fire started. It
committed `cb2692f` (BACKLOG claim for REP-240) at 11:36 and was
mid-implementation of `AppleScriptMessageReader.messagesForChat` +
`IMessageChannelTests` additions when this fire detected the race.

To avoid clobbering its uncommitted state, this fire:
- Cloned a separate working dir at `~/.cache/replyai-autopilot-1133/`
- Did all branching, committing, and pushing from the isolated clone
- Avoided touching `Sources/` files the other fire was editing
- Left the shared `.cache/replyai-autopilot/` workspace alone except
  for the FakeContactStore patch verification + a Shortcuts handler
  test-copy attempt that was rejected by the user before it ran

## Next-fire hints

- `wip/autopilot-2026-05-03-114558-fakecontactstore-normalize` —
  needs `swift test --filter ContactsResolverTests` (already verified
  green by this fire in the warm workspace; fast-merge candidate).
- `wip/autopilot-2026-05-03-114647-rep262-shortcuts-export` — needs
  `swift test --filter ShortcutsExportHandlerTests` to confirm; one
  follow-up task (REP-262 still requires the `ReplyAIApp.swift`
  `onOpenURL` Scene wiring to fully close, but that's UI-sensitive
  and should be its own commit).
- The autopilot prompt's "21 expected ContactsResolverTests failures"
  carve-out should be REMOVED from the prompt once branch 1 above is
  merged. The actual baseline failure count was 17 (not 21), and the
  failures were assertion mismatches, not the documented CoreData XPC
  error.
- The shared-workspace race is a real failure mode worth fixing in
  the autopilot prompt. Two fires can corrupt each other's
  uncommitted state in `~/.cache/replyai-autopilot/`. Suggest each
  fire mint its own `~/.cache/replyai-autopilot-<fire-id>/` clone
  rather than re-using the shared dir.
