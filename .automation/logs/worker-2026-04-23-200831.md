# Worker Run — 2026-04-23-200831

## Tasks

- **REP-237** — SlackHTTPClient: injectable URL session wrapper for Slack API GET calls (P1, S)
- **REP-238** — KeychainHelper: `deleteAll(prefix:)` for factory reset (P1, S)

Bundled per substantiveness gate (both S-effort at P1; another S task at same priority existed).

## Outcome

**BLOCKED** — Implementation complete; `swift test` exceeded 13-min budget due to MLX cold-cache compilation (same structural issue as REP-254). Branch: `wip/2026-04-23-200831-slack-http-keychain-deleteall`.

Human should: `swift test` locally and merge if green.

## File Changes

| File | Change | +/- |
|------|--------|-----|
| `Sources/ReplyAI/Channels/SlackHTTPClient.swift` | new — `SlackHTTPClient` protocol + `URLSessionSlackClient` + `HTTPSessionProtocol` | +56 |
| `Sources/ReplyAI/Channels/ChannelService.swift` | add `networkError(String)` case to `ChannelError` | +4 |
| `Sources/ReplyAI/Channels/KeychainHelper.swift` | add `deleteAll(prefix:)` | +22 |
| `Tests/ReplyAITests/SlackHTTPClientTests.swift` | new — 5 tests with `MockHTTPSession` | +86 |
| `Tests/ReplyAITests/KeychainHelperTests.swift` | add 3 tests + `Security` import | +50 |

## Tests

- Before: 502
- Expected after: 510 (+8: 5 SlackHTTPClient + 3 KeychainHelper)

## Design Notes

`SlackHTTPClient` is the HTTP layer needed by `SlackChannel.recentThreads()` (REP-242). `HTTPSessionProtocol` seam lets tests inject `MockHTTPSession` without live network. `URLSession` satisfies the protocol via retroactive conformance — it already implements `data(for:)`.

`KeychainHelper.deleteAll(prefix:)` lists all items for the service with `kSecReturnAttributes`, filters by `hasPrefix`, deletes matching items individually. Factory reset calls with `"ReplyAI-"`.

`ChannelError.networkError(String)` added to `ChannelService.swift` — needed by `URLSessionSlackClient` to surface HTTP 4xx/5xx through the `ChannelError` type. All existing exhaustive switches have `default` branches; non-breaking addition.
