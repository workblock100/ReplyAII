# Autopilot fire 2026-05-07 10:11 EDT

**Time used**: ~28 min — 6 actions (5 test pins + 1 doc sync). All on main, no wip branches created or merged.

## Actions this fire

1. **P5 main**: pinned 6 ShortcutsExportHandler edge cases — `Some("")` preview wins over message fallback (gotcha #243 pattern); `Some("")` id passes through to chatGUID verbatim; whitespace-padded `from` doesn't trim (literal `lowercased() == "me"`); whitespace-padded channel value falls through `?? .imessage` rawValue miss; emoji first grapheme survives `prefix(1)`; first of duplicate `?data=` query items wins. Pushed directly to main as `effc2fd`.

2. **P5 main**: pinned 6 IMessagePreview URL component edges — explicit port stripped (`https://example.com:8080` → `🔗 example.com`); userinfo stripped (`user:pass@example.com` → `example.com`); uppercase `WWW.` stripped after lowercasing (composition order matters); subdomain preserved after `www.` strip (`www.api.example.com` → `api.example.com`, NOT `example.com`); short-host `www.x.com` (length 9) passes the `count > 4` guard; `displayString` round-trip with port shows bare host. Pushed to main as `d1b970e`.

3. **P5 main**: pinned 4 IMessageSender chat-GUID validator edges that aren't covered by existing trailing-extra-part / case-sensitive pins — trailing semicolon (`iMessage;-;`) fails `!parts[2].isEmpty`; empty middle (`iMessage;;…`) fails strict `+`/`-` clause; non-+/- middle (`X`, `~`, digit `0`) fails closed-set; whitespace-padded middle (`+ `, ` -`) fails literal `==` (no trim). Guards against well-meaning "be tolerant" refactors that would surface as opaque -1708 send errors instead of clear `invalidChatGUID`. Pushed to main as `ed88de5`.

4. **P8 doc-sync**: AGENTS.md repo-layout test count `1489 → 1515`, three-skip pass count `1463 → 1479`. Pushed as `c2f65b7`.

5. **P5 main**: pinned 3 RuleEvaluator messageAgeOlderThan edges — exactly-24h-old does NOT match `>24h` (strict `>`, not `>=`); `hours: 0` matches any positive-age past message (comparator `> 0`); future-dated `lastMessageDate` (clock skew) does NOT match (negative `timeIntervalSince` fails the `>` check). Pushed as `06d8f44`.

6. **P5 main**: pinned 2 SearchIndex LIMIT-clause boundaries — `limit: 0` returns zero rows (SQLite `LIMIT 0` semantic, NOT a fallback to default-50); `limit: 1` returns exactly one row even with 10 indexed matches. Guards the `⌘K` palette's "single best match" callsite against a future "treat 0 as default" shortcut. Pushed as `4dc1f6e`.

## Files touched

- `Tests/ReplyAITests/ShortcutsExportHandlerTests.swift` (+144 LOC, +6 tests)
- `Tests/ReplyAITests/IMessagePreviewTests.swift` (+67 LOC, +6 tests)
- `Tests/ReplyAITests/IMessageSenderTests.swift` (+55 LOC, +4 tests)
- `Tests/ReplyAITests/RulesTests.swift` (+59 LOC, +3 tests)
- `Tests/ReplyAITests/SearchIndexTests.swift` (+37 LOC, +2 tests)
- `AGENTS.md` (test-count refresh)

**Net**: +21 tests on main (1463 → 1484 under the standard three-skip workaround). Full suite passes in ~14.7s warm. No regressions; no wip branches; nothing merged from origin (queue was empty at fire start).

## REPs touched

None — purely defensive test pinning + doc sync. No BACKLOG status changes.

## Pipeline state at exit

- **wip queue**: 0 branches (empty at fire start, empty at exit — no new wip pushes this fire because every action was tests-only and went directly to main, matching the recent-fire pattern of treating pure regression-pin commits as P5-with-direct-main).
- **BACKLOG**: 18 open (unchanged), 1 blocked (REP-ALERT-260506-2010), 0 in-progress.
- **Test count**: 1515 funcs in `Tests/ReplyAITests/` (was 1485 before this fire); 1479 pass under `--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` (was 1463). 2 always-skipped in headless (`GlobalHotkeyContractTests` AppKit-touching cases gated behind `RUN_APPKIT_TOUCHING_TESTS=1`).
- **Build cache**: warm (used throughout fire — never cold-rebuilt).
- **Smoke launches**: none this fire — all changes were tests-only with no runtime impact, so the P0 smoke gate wasn't tripped.
- **Last 6 commits on main**:
  - `4dc1f6e` tests: pin SearchIndex limit=0 returns zero, limit=1 returns single row
  - `06d8f44` tests: pin RuleEvaluator messageAgeOlderThan boundary + zero/future cases
  - `c2f65b7` docs: AGENTS.md — bump test count to 1515 (1479 pass under three-skip)
  - `ed88de5` tests: pin IMessageSender chat-GUID validator middle/empty/separator edges
  - `d1b970e` tests: pin IMessagePreview URL component edges (port, userinfo, www+subdomain)
  - `effc2fd` tests: pin ShortcutsExportHandler edge cases

## Next-fire hints

- **P0 unchanged**: `useMLX = false` workaround (REP-ALERT-260504-1650) still required for smoke launches. Structural fix lives in REP-501→REP-505, blocked on a human-paired session.
- **REP-501→REP-505 chain** still flagged "risky for autopilot fires" per its blocker note; no autopilot-claimable code work in this fire. Continue the regression-pin lane until a wip queue forms or a human-paired session lands the MLX SPM split.
- **Next P5 candidates** that are getting harder to find — most public surfaces have 30+ pin tests already. Productive remaining lanes: deeper SQL-fixture coverage on IMessageChannel.recentThreads NULL-column COALESCE behavior; SlackOAuthFlow scope-string literal pins (currently asserts contains, not equals); SearchIndex `limit: -1` (SQLite "no limit" semantic — surprising and worth pinning explicitly).
- **AGENTS.md gotcha #243** still active. `swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` continues to be the validated three-flag form; this fire's runs reproduced 14-15s clean with this flag set. No new `--skip` needed.
