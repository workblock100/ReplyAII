# Worker Run — 2026-04-22-150000

## Tasks
- REP-074 (S) — ContactsResolver: per-handle cache TTL
- REP-095 (S) — IMessageChannel: per-thread message-history cap
- REP-096 (S) — InboxViewModel: send() success/failure state transition tests
- REP-102 (S) — SearchIndex: empty-query returns empty list

## Outcome
**shipped** — all four tasks closed, 10 new tests added, all 304 tests green.

## File Changes

| File | Change |
|------|--------|
| `Sources/ReplyAI/Channels/ContactsResolver.swift` | +30 lines — `CacheEntry` struct with `cachedAt`, `ttl: TimeInterval = 1800` init param, `isFresh()` helper; updated `name(for:)` and `resolveAll` to respect TTL |
| `Sources/ReplyAI/Channels/ChannelService.swift` | +5 lines — convenience overload `messages(forThreadID:)` defaulting to `limit: 20` |
| `Tests/ReplyAITests/ContactsResolverTests.swift` | +45 lines — 3 new tests: `testFreshCacheHitSkipsStore`, `testStaleEntryTriggersFetch`, `testZeroTTLAlwaysFetches` |
| `Tests/ReplyAITests/IMessageChannelTests.swift` | +85 lines — new `IMessageChannelMessageLimitTests` class with 3 tests: `testMessageLimitCapsResults`, `testMessageLimitDoesNotDropShortThreads`, `testMessageLimitPreservesMostRecent` |
| `Tests/ReplyAITests/InboxViewModelTests.swift` | +55 lines — new `InboxViewModelSendTests` class with 2 tests: `testSendSuccessClearsConfirmationAndShowsToast`, `testSendFailurePreservesEditAndShowsErrorToast` |
| `Tests/ReplyAITests/SearchIndexTests.swift` | +25 lines — 2 new tests: `testEmptyQueryReturnsEmptyList`, `testWhitespaceOnlyQueryReturnsEmptyList` |
| `AGENTS.md` | test count updated 294→304 |
| `BACKLOG.md` | REP-074/095/096/102 marked done |

## Tests Before / After
- Before: 294
- After: 304 (+10)

## Notes
- SUBSTANTIVENESS GATE: 4 S-effort tasks bundled into one commit as required.
- `SearchIndex.search()` already correctly returns `[]` for empty/whitespace queries via the `guard !trimmed.isEmpty` check — REP-102 was a pure test addition verifying this invariant.
- `messages(forThreadID:limit:)` already supported a `limit` parameter; added the default-20 convenience overload in `ChannelService` extension.
- ContactsResolver TTL uses `ttl=0` to mean "always re-query" (zero TTL = no cache), which makes tests simple without requiring clock injection.
- One fix required during development: test used `Tone.auto` which doesn't exist — corrected to `Tone.warm` (the default `activeTone`).
