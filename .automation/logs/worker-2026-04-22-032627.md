# Worker Run — 2026-04-22-032627

## Task
REP-028 — UNNotification: register inline reply action on launch

## Outcome
**shipped** — committed to main

## File changes
| File | Change |
|------|--------|
| `Sources/ReplyAI/Services/NotificationCoordinator.swift` | +102 lines (new) |
| `Tests/ReplyAITests/NotificationCoordinatorTests.swift` | +123 lines (new) |
| `Sources/ReplyAI/App/ReplyAIApp.swift` | +3 lines (coordinator @State + .environment wiring) |
| `Sources/ReplyAI/Inbox/InboxScreen.swift` | +2 lines (@Environment + setUp call) |
| `Sources/ReplyAI/Inbox/InboxViewModel.swift` | +4 lines (pendingNotificationReply field) |
| `BACKLOG.md` | status: done |

## Tests
- Before: 183 tests
- After: 189 tests (+6)
- All 189 passing, 0 failures

## Notes
- `.task {}` modifier is not available on SwiftUI `Scene` (only on `View`); wired `coordinator.setUp()` into InboxScreen's `initial-sync` task instead.
- `UNNotificationSettings` cannot be instantiated in tests; factored the protocol to expose `authorizationStatus() async -> UNAuthorizationStatus` instead of `getNotificationSettings(completionHandler:)`.
- `handleReply(actionIdentifier:userText:notificationID:)` extracted as an internal method so tests drive it directly without constructing a `UNNotificationResponse` object.
- Previous worker runs had stuck xctest processes (likely a hanging async test in InboxViewModelTests); killed them to unblock the build lock before running tests.
