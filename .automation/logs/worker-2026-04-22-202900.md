# Worker run: 2026-04-22-202900

## Task
REP-066 — DraftEngine: persist draft edits to disk between launches

## Outcome
shipped — committed to main

## File changes
- `Sources/ReplyAI/Services/DraftStore.swift` (new, +80 lines)
  - `DraftStore` class with injectable `draftsDirectory: URL?`
  - `write(threadID:text:)`, `read(threadID:) -> String?`, `delete(threadID:)`
  - `pruneStale(olderThan:)` called on init; defaults to 7 days
  - Thread ID sanitization (`/` → `_`, `:` → `-`) for safe filenames
- `Sources/ReplyAI/Services/DraftEngine.swift` (+4 lines)
  - `store: DraftStore?` injected via init
  - `apply(chunk:to:)` calls `store?.write(threadID:text:)` on `.done` chunk
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` (+7 lines)
  - `draftStore: DraftStore?` property
  - `selectThread(_:)` calls `draftStore?.read(threadID:)` and pre-populates `userEdits` before priming
- `Tests/ReplyAITests/DraftStoreTests.swift` (new, +70 lines)
  - 7 test cases using injected temp-directory

## Tests
- Before: 358
- After: 365
- All green

## Notes
- Only one M task taken; all other P2 non-ui-sensitive tasks are also M effort — no bundling needed per the substantiveness gate (gate applies to S tasks only)
- DraftStore wired at the DraftEngine level (on `.done` transition) rather than trying to hook into userEdits writes, since the persisted draft represents the last LLM output, which is the correct seed for the next launch
- InboxViewModel pre-populates `userEdits` from the store so the composer shows the stored text immediately before the LLM primes; when the LLM finishes, it will overwrite the cache entry (which is correct — the user gets a fresh draft on regenerate but sees their last stored draft while waiting)
