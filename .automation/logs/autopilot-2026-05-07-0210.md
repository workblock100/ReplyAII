# autopilot fire — 2026-05-07-0210

**Time used**: 50+ min — 15 actions

## Actions this fire

1. **P8 doc**: BACKLOG REP-ALERT-260506-2010 workaround note updated — third
   `--skip` flag (`InboxViewModelTests`) is now required → main `5158ebf`
2. **P9 fix**: SlackChannel display-name fallback bug — `is_channel: true,
   name: ""` rendered an empty sidebar row (the final `c.name ?? c.id`
   chain didn't screen empty strings); +5 SlackChannelTests covering all
   four branches of the display-name chain plus an unparseable-timestamp
   case → main `dc570d8`
3. **P9 fix**: LocalhostOAuthListener accepted `?code=` (present-but-empty
   value) and fired `.success("")` to the OAuth completion; +1
   regression-pin test → main `9007eab`
4. **P8 doc**: AGENTS.md gotcha #243 updated with the 2026-05-07
   third-skip-required note + the "expect a fourth suite to hang next
   fire" forecast → main `e093759`
5. **P9 fix**: SlackOAuthFlow accepted `{"ok":true,"access_token":""}` and
   silently persisted an empty token to Keychain (every later API call
   would 401 and look like a stale token); +1 regression-pin test →
   main `e2fe223`
6. **P5 tests**: AppleScriptMessageReader.prettyPhone — pinned 4 edge
   cases (non-US 11-digit, 12+ digit, empty input, alphanumeric) →
   main `4f54b43`
7. **P5 tests**: TimeOfDayPredicateTests — pinned 3 edge cases
   (single-hour window, midnight 0–0, overnight ending at midnight 22–0)
   → main `66a3f8a`
8. **P8 doc**: AGENTS.md test-count line updated to ~1430 (1391 under
   the documented workaround) → main `21a6900`
9. **P5 tests**: SearchIndex.ftsQuery — pinned per-character special-char
   quoting (`(`, `)`, `*`, `:`), mixed prefix/quoted token AND join order,
   surviving-token-after-drop edge → main `39e8f61`
10. **P5 tests**: PromptBuilder voiceExamples — pinned that empty entries
    render as bare `- ` bullets and that an all-empty list still emits
    the section header → main `0392661`
11. **P5 tests**: NotificationCoordinator.handleReply — pinned nil-text
    silent drop and empty-notificationID pass-through (the safety net
    lives downstream) → main `1c9896d`
12. **P5 tests**: Stats.acceptanceRate — pinned the sent-without-generated
    nil-and-preserve-counter case and the sent>generated raw-ratio case
    → main `affccdc`
13. **P9 fix**: NotificationCoordinator willPresent empty `sender` value
    bypassed the `?? content.title` fallback. Same shape as today's three
    other present-but-empty fixes — empty handle propagated into
    `applyIncomingNotification` where `chatGUID.hasSuffix("")` returns
    true for any string, silently incrementing the wrong thread's unread
    count → main `552dd6a`
14. **P5 tests**: IMessageChannel.formatRelative EEE + MMM d buckets —
    pinned locale-tolerant invariants for the 3-days-ago and 14-days-ago
    buckets so a refactor that widens or drops the EEE window can't land
    silently → main `ad561f5`
15. **P5 tests**: SmartRule.contactGroupMatchesName empty-pattern safety —
    audit of the empty-input bug class found this predicate is safe by
    accident (Swift `localizedCaseInsensitiveContains("")` returns false,
    unlike has{Prefix,Suffix}). Pinned the surprising-but-safe behaviour
    so a future "consistency fix" can't silently start matching every
    group on a user's device → main `e416eac`

## Files touched

Fixes (4):
- `Sources/ReplyAI/Channels/SlackChannel.swift`
- `Sources/ReplyAI/Channels/LocalhostOAuthListener.swift`
- `Sources/ReplyAI/Channels/SlackOAuthFlow.swift`
- `Sources/ReplyAI/Services/NotificationCoordinator.swift`

Test additions (9 files, +25 cases):
- `Tests/ReplyAITests/SlackChannelTests.swift` (+5 cases)
- `Tests/ReplyAITests/LocalhostOAuthListenerTests.swift` (+1)
- `Tests/ReplyAITests/SlackOAuthFlowTests.swift` (+1)
- `Tests/ReplyAITests/AppleScriptMessageReaderTests.swift` (+4)
- `Tests/ReplyAITests/RulesTests.swift` (+3)
- `Tests/ReplyAITests/SearchIndexTests.swift` (+3)
- `Tests/ReplyAITests/PromptBuilderTests.swift` (+2)
- `Tests/ReplyAITests/NotificationCoordinatorTests.swift` (+2)
- `Tests/ReplyAITests/StatsTests.swift` (+2)
- `Tests/ReplyAITests/IMessageChannelTests.swift` (+2)

Docs (3):
- `BACKLOG.md` (REP-ALERT-260506-2010 workaround)
- `AGENTS.md` (gotcha #243 + test-count line)

## Bug-fix theme

All four fixes are the same shape: a non-nil-but-empty value passed
through a `let x = … as? String` / `let x = …?.value` guard, then
treated as a valid input downstream. The fixes add `!x.isEmpty` checks
to each guard. The fourth fix (NotificationCoordinator willPresent)
landed mid-fire after the audit thread spotted it; the empty-string
`chatGUID.hasSuffix(senderHandle)` match on
`InboxViewModel.applyIncomingNotification` is the symmetric remaining
risk — flagging for next-fire as a P5 audit.

## REPs touched

- REP-ALERT-260506-2010 — workaround field updated; structural fix
  still open (the InboxViewModel test-class boundary continues to drift).

## Pipeline state at exit

- Open BACKLOG count: 18 (unchanged — no full task ships this fire,
  only fixes + tests)
- Wip queue depth: 0 (was empty at start; remains empty)
- Build cache: warm (`./scripts/build.sh debug` succeeded mid-fire)
- Last 3 main commits: `affccdc`, `1c9896d`, `0392661`
- `swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests`:
  1410 passing, 2 skipped, 0 failures, 13.2s (up from 1384 at fire start
  → +26 tests this fire across 10 test files + RulesTests +1)
- Last main commit before this log update: `e416eac`

## Next-fire hints

- The InboxViewModel test-class boundary is moving one suite per fire
  (IsSyncing → InboxViewModelTests so far). The next fire should expect
  a fourth `--skip` to be needed at some new InboxViewModel suite. The
  list is documented in AGENTS.md gotcha #243.
- The "present-but-empty value bypasses non-nil guard" bug pattern
  needs a sweep — three landed fixes in three different files this
  fire. Worth a P5 audit pass on remaining `as? String` sites.
- No outstanding wip branches; no in-progress task to resume.
