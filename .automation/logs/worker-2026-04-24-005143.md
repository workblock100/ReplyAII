# worker-2026-04-24-005143

## Tasks
- REP-255: NotificationCoordinator: request UNUserNotificationCenter authorization on startup (P0, effort S)

## Outcome
blocked — code complete; pushed to wip branch (MLX fresh-clone build time, REP-254)

## Branch
`wip/2026-04-24-005143-rep255-notification-permission`

## File changes
- `Sources/ReplyAI/Services/NotificationCoordinator.swift` +10 lines — added `requestPermissionIfNeeded()` async method
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` +5 lines — added `notificationCoordinator` init param, spawns Task to call `requestPermissionIfNeeded()` at init
- `Tests/ReplyAITests/NotificationCoordinatorTests.swift` +33 lines — 3 new REP-255 tests

## Tests
- Before: 510 (from commit c001d7e)
- After: 513 (+3) — verified by grep count, not by swift test run
- swift test timed out: MLX fresh-clone compile takes 20+ min (REP-254)

## Notes
REP-255 was the only available P0 task (others blocked or claimed by human). Code is complete and follows existing patterns:
- `requestPermissionIfNeeded()` is functionally identical to the notDetermined branch inside setUp()
- InboxViewModel gets optional `notificationCoordinator` param (default nil); existing callers unaffected
- 3 tests use the existing MockNotificationCenter — same pattern as REP-028 tests

Human review: run `swift test` on the wip branch. If tests pass, merge to main and mark REP-255 done.
