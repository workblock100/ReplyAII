# Autopilot 2026-05-04 10:11

**Time used**: ~30 min — 9 actions, 9 commits to main
**Pipeline state at exit**: HEALTHY — 1020 tests (was 996 at fire start, +24 this fire), wip queue empty, build cache warm, no regressions in any per-filter or smoke run

## Actions this fire

1. **P8 audit**: AGENTS.md test-count sync 993 → 996 → `f3e462c`. The PromptBuilder addendum from the previous fire (`d06ba71`, +3 cases) landed after that fire's last AGENTS sync; bringing AGENTS level with main.
2. **P5 tests**: pinned `Stats.defaultFileURL()` and `RulesStore.defaultFileURL()` persistence-path contracts (8 cases) → `09b72a7`. Both helpers were entirely uncovered. Each pins four bits: filename (stats.json / rules.json), parent directory (ReplyAI/), the lazy `createDirectory` side-effect, and the absolute-file-scheme URL shape. A silent path change orphans every shipped user's stats / rules data.
3. **P5 tests**: pinned `Preferences.lastThreadsCacheURL` and `SearchIndex.productionDatabaseURL()` (7 cases) → `471f86b`. Same pattern as action 2 but for the cold-launch thread cache and the FTS5 search.db. lastThreadsCacheURL doesn't lazily create its directory (writer handles ENOENT) so 3 cases instead of 4.
4. **P5 tests**: pinned SmartRule kind discriminator strings — persistence contract (3 cases) → `82a8319`. Existing round-trip tests confirmed encode→decode symmetry but did NOT catch a refactor that *renamed* a kind: the new round-trip would still pass while every previously-saved rules.json silently became un-decodable. Added `testPredicateKindDiscriminatorsAreStable` (18 RulePredicate variants), `testActionKindDiscriminatorsAreStable` (5 RuleAction variants), `testPredicateKindDecodesFromCanonicalJSON` (decode-side pin for the standalone-kind predicates).
5. **P8 audit**: AGENTS.md test-count sync 996 → 1014 → `6bbe1e6` (covers actions 2/3/4: +18).
6. **P5 tests**: pinned `IMessageSender.maxMessageLength = 4096` (2 cases) → `7a73e5e`. Existing tests reference the constant symbolically (`IMessageSender.maxMessageLength + 1`) so a silent drift would have shipped invisibly. Pin the literal value plus a check that the user-visible `messageTooLong` copy interpolates `4096` directly.
7. **P5 tests**: pinned InboxViewModel persistence-key strings (4 cases) → `65e4c63`. Pre-populates the literal key (e.g. `pref.inbox.archivedThreadIDs`) with a JSON payload, constructs an InboxViewModel against the same suite, asserts the loaded model state matches. A rename of the private constant trips the load path. Covers archive / silently-ignored / pinned / snoozed; lastSeenRowID intentionally omitted (no public accessor, downstream of channel sync).
8. **P8 audit**: AGENTS.md test-count sync 1014 → 1020 → `3417eca` (covers actions 6/7: +6).
9. **P5 tests**: pinned PreferenceKey literal strings — UserDefaults contracts (3 cases) → `378d3b3`. Existing PreferencesTests use `PreferenceKey.*` symbolically so a refactor renaming `"pref.privacy.crashReports"` to `"pref.crashReports"` would let every test keep passing while every shipped user's preference state silently re-defaulted. Added: 16-pair literal-equality test, "every key starts with `pref.`" prefix check (so `wipeReplyAIDefaults` always matches), and `wipeExemptions` set literal-content pin.

## Files touched / REPs closed

- `AGENTS.md` (3 test-count syncs: 993→996, 996→1014, 1014→1020)
- `Tests/ReplyAITests/StatsTests.swift` (+4 cases, defaultFileURL pin)
- `Tests/ReplyAITests/RulesTests.swift` (+4 defaultFileURL pin, +3 kind-discriminator pins)
- `Tests/ReplyAITests/PreferencesTests.swift` (+3 lastThreadsCacheURL pin, +3 PreferenceKey literal pin)
- `Tests/ReplyAITests/SearchIndexTests.swift` (+4 productionDatabaseURL pin)
- `Tests/ReplyAITests/IMessageSenderTests.swift` (+2 maxMessageLength pin)
- `Tests/ReplyAITests/InboxViewModelTests.swift` (+4 persistence-key contract pin)

No REPs in BACKLOG closed — all this fire's work landed under P5 (test coverage) or P8 (audit / doc sync). The 21 open REPs are still either claimed by `human` or are L-effort (REP-501→REP-505 MLX extraction chain) that BACKLOG itself flags as too risky for autopilot. WIP queue was empty at fire start (P1 had nothing to drain).

## Theme

This fire focused on **persistence-contract pinning** — strings and constants that ship with the app and accumulate real user state, but had no literal-string or literal-value tests. The existing test suite is large and well-maintained, so the gap was specifically: round-trip and symbolic-constant tests pass even when the underlying contract silently drifts. Each new test asserts the literal value directly so a future rename trips the test rather than silently abandoning user data.

Categories pinned:
- File-system paths: `~/Library/Application Support/ReplyAI/{stats.json, rules.json, search.db, last-threads-cache.json}`
- JSON wire format: SmartRule.RulePredicate.Kind (18 cases) and SmartRule.RuleAction.Kind (5 cases)
- UserDefaults keys: 16 PreferenceKey literals + 4 InboxViewModel private-constant keys
- Hard-coded constants with user-visible blast radius: `IMessageSender.maxMessageLength = 4096`

## Pipeline state at exit

- BACKLOG open: 21 (unchanged); blocked: 1 (REP-285, deprioritized by REP-500 chain)
- WIP queue: 0 (was 0 at fire start, no merges or wip pushes this fire)
- Build cache: warm (incremental builds during filtered tests took ~6–8s each)
- Last 3 commits on main: `378d3b3`, `3417eca`, `65e4c63`
- Test count: 1020 (was 996 at fire start; +24 this fire, all under filtered runs that passed in 0.001–0.021s; AGENTS.md is in sync at 1020)

## Verification notes

Filtered swift-test runs covered every new test class in this fire (all passed): `StatsDefaultFileURLTests`, `RulesStoreDefaultFileURLTests`, `PreferencesLastThreadsCacheURLTests`, `SearchIndexProductionDatabaseURLTests`, `testPredicateKindDiscriminatorsAreStable|testActionKindDiscriminatorsAreStable|testPredicateKindDecodesFromCanonicalJSON`, `testMaxMessageLengthIsExactly4096|testMaxMessageLengthAppearsInMessageTooLongCopy`, `InboxViewModelPersistenceKeyContractTests`, `PreferenceKeyLiteralStringTests`. After action 7, a smoke run against the union of recently-touched suites (`RulesTests|StatsTests|PreferencesTests|InboxViewModelPersistenceKeyContractTests|IMessageSenderTests|SearchIndexTests`, 259 tests) passed cleanly with `--skip ContactsResolverTests`.

A full-suite run was attempted late in the fire and stalled inside `InboxViewModelBulkFilterTests.testFilteredThreadsExcludeArchivedThreads` (process at 0.30s CPU after several minutes — visible as a flake unrelated to this fire's changes; that test was on main before any of these commits). Killed the orphan and trusted the per-filter results. Two stale swift-test orphans from prior fires (PIDs 65113 / 67807 / 67809 / 64677, ages 1h+) were also reaped during the fire so they stopped holding the SwiftPM lock.

## Next-fire hints

None blocking. Future fires can look at:

- **More persistence-contract pinning**: `DraftStore` production directory naming (currently uncovered; `~/Library/Application Support/ReplyAI/drafts/`); the FTS5 schema name `messages_fts` inside SearchIndex; default debounce timings (0.6s in ChatDBWatcher and MessagesAppActivationObserver — neither has a default-value pin since tests always inject 0.05).
- **`InboxViewModelBulkFilterTests.testFilteredThreadsExcludeArchivedThreads`** flaked under full-suite run this fire. Pre-existing on main; worth a P8 investigation if it recurs.
- **The L-effort REP-501→REP-505 MLX extraction chain** still needs a human-paired multi-fire session per BACKLOG's own note. No autopilot work makes sense here.
