# Worker Run: worker-2026-04-22-201500

**Outcome**: shipped  
**Tasks closed**: REP-108, REP-110, REP-115, REP-117  
**Tests before**: 349 | **Tests after**: 358 (+9)

## Tasks

### REP-108 — ContactsResolver: flush cache on CNContactStoreDidChange
- Added `notificationCenter: NotificationCenter = .default` param to `ContactsResolver.init`
- Registered observer for `CNContactStoreDidChange`; flushes cache (`state.cache.removeAll()`) on notification
- Stored `notificationCenter` ref for proper cleanup in `deinit`
- Tests: `testContactStoreChangeFlushesCache`, `testFlushDoesNotCrashOnEmptyCache` (isolated `NotificationCenter()`)

### REP-110 — RulesStore: export format version field
- Added `RulesExport` struct (`version: Int`, `rules: [SmartRule]`) and `RulesStoreError.unsupportedExportVersion`
- `export(to:)` now writes the versioned envelope
- `import(from:)` reads `version` key via `JSONSerialization`, throws `unsupportedExportVersion` for unknown versions, preserves element-by-element malformed-skip behavior
- Updated `testImportSkipsMalformed` to write versioned envelope format
- Tests: `testExportIncludesVersionField`, `testImportRoundTripWithVersionField`, `testImportUnknownVersionThrows`

### REP-115 — Preferences: pref.app.launchCount
- Added `PreferenceKey.launchCount = "pref.app.launchCount"` and `wipeExemptions: Set<String>`
- `wipeReplyAIDefaults` skips exempted keys, preserving `launchCount` across factory resets
- `ReplyAIApp.init()` increments the counter on each launch
- Tests: `testLaunchCountStartsAtZero`, `testLaunchCountIncrementsOnWrite`, `testLaunchCountSurvivesWipe`

### REP-117 — IMessageChannel: graceful handling of NULL text + NULL attributedBody
- Changed `messages(forThreadID:limit:)` and the incremental-messages path to produce `"[deleted]"` placeholder instead of skipping NULL rows
- Applied to both message-fetch loops in `IMessageChannel.swift`
- Test: `testNullTextAndAttributedBodyYieldsNonNilBody` — verifies NULL row yields non-nil `[deleted]` body

## Files changed
- `Sources/ReplyAI/Channels/ContactsResolver.swift` (+17)
- `Sources/ReplyAI/Rules/RulesStore.swift` (+35)
- `Sources/ReplyAI/Services/Preferences.swift` (+12)
- `Sources/ReplyAI/App/ReplyAIApp.swift` (+2)
- `Sources/ReplyAI/Channels/IMessageChannel.swift` (+10)
- `Tests/ReplyAITests/ContactsResolverTests.swift` (+30)
- `Tests/ReplyAITests/RulesTests.swift` (+65)
- `Tests/ReplyAITests/PreferencesTests.swift` (+17)
- `Tests/ReplyAITests/IMessageChannelTests.swift` (+15)
