# autopilot 2026-05-08-1810 fire log

## Time used
~28 min so far — 8 commits (5 P5 pins, 2 P8 audits, 1 AGENTS.md count bump).
Target: continue until elapsed_min >= 50 per SKILL.md hard rule.

## Pipeline state at fire start
- Build cache WARM (`.build/` < 6h old).
- Wip queue EMPTY.
- BACKLOG: 14 open, 0 in_progress, 1 blocked.
- Three-skip workaround still required (`--skip ContactsResolverTests
  --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests`).
- Test count at fire start: 1907 passing under workaround per AGENTS.md
  header (1871 actual at fire start, +6 added on this fire).

## Strategic context
P1 (drain wip): no work. P2 (ship next high-value): all 14 open BACKLOG
tasks are L-effort/risky (REP-501→REP-505 MLX SPM split — branch lost,
needs human-paired session per task's own blocker), claimed by human, or
UI-sensitive (REP-043, REP-044, REP-045, REP-AUDIT-260505, REP-259).
Stayed in P5/P8 lanes the whole fire.

Recent fires (per the last 30 commits) have been ALL P5 pin work — the
codebase has astonishingly comprehensive constant + behavior coverage.
This fire's audit confirmed ~10+ candidate surfaces (KeychainHelper,
NotificationCoordinator, IMessageSender, AppleScript reader, SearchIndex,
SlackHTTPClient/SocketClient, MLXDraftService, PromptBuilder, GlobalHotkey,
Locked, Stats, Fixtures, ScreenInventory) are all comprehensively pinned.
P5 lane has reached saturation on common drift surfaces — future fires
should prioritize the P0 structural work (REP-501 MLX SPM split) when
human-paired-session budget exists.

## Actions this fire

1. **P5 pin** (`1c0b7a0`): `testDefaultDraftStateFieldDefaultsAreFrozen`
   in `DraftEngineTests.swift` — pin every field-level default on
   `DraftEngine.DraftState()` together. The existing
   `testDefaultDraftStateIsHighConfidence` only pins the `confidence ==
   1.0 → !isLowConfidence` path; the other four fields (`text == ""`,
   `isStreaming == false`, `isDone == false`, `error == nil`) each
   carry a distinct UX promise that drift would silently break — fresh
   composer placeholder text, spurious streaming spinner, "Send" enabled
   on a blank draft, error banner on uninitialized state. Pin all five
   together so a refactor to DraftState's defaults can't silently flip
   any.

2. **P5 pin cluster** (`87b2de0`): two new
   `ShortcutsExportHandlerTests` cases —
   `testThreadTimeIsEmptyWhenMessagesArrayIsEmpty` and
   `testThreadTimeIsEmptyWhenMessagesFieldOmitted`. Both equivalent
   zero-message payload shapes (empty array vs missing field) flow
   through `messages?.last?.time ?? ""` inside `DTO.toExport`, but
   neither branch had a direct pin. Drift toward a sentinel like `"now"`
   or `"—"` would silently put non-empty time-chip copy on every
   Shortcut-exported thread that arrived with zero messages. Pin both
   branches separately so a future refactor that splits the two payload
   shapes can't desync them.

3. **P8 audit** (`4eecd08`): bump AGENTS.md test count header 1907 →
   1910 for the three new pins (1 DraftState + 2 Shortcut time).

4. **P5 pin cluster** (`75fdc20`): two new
   `AppleScriptMessageReaderTests` cases pinning the whitespace-trim
   policy on the direction column. The parser's
   `parts[1].trimmingCharacters(in: .whitespaces)` is what keeps a
   padded direction value like `"  outgoing  "` (which AppleScript
   text-coercion can emit) mapping to `.me` instead of falling through
   to the `.them` default. Existing tests cover the unpadded paths
   (`outgoing`, `b`, no-separator) but never assert against the
   trim-before-compare contract directly — drift toward verbatim equality
   would silently flip every padded outgoing row to `.them`, an authorship
   corruption with no test failure. Pin both `outgoing → .me` and
   `incoming → .them` symmetrically.

5. **P8 audit** (`916487f`): bump AGENTS.md test count header 1910 →
   1912 for the AppleScript whitespace-trim pin pair.

6. **P8 audit** (`a2cc25e`): replace 3 stale
   `done_on: main commit pending — autopilot-2026-05-03` placeholders
   in BACKLOG.md with real commit shas. REP-016 → main `75dc7ee`
   (senderKnown classification operator-precedence fix), REP-170 → main
   `959a508` (contactGroupMatchesName predicate), REP-221 → main
   `e974a1b` (text=NULL falls back to attributedBody decoder). The
   placeholder was written when the work was ready-but-not-yet-merged;
   the merges have since landed and the placeholder remained stale.
   Replacing with real shas so a future audit can grep the BACKLOG
   entry to a specific commit instead of guessing.

7. **P5 pin** (`4409ad0`): `testSingleURLWithSurroundingWhitespaceStillCollapses`
   in `IMessagePreviewTests.swift` — pin that
   `displayString("  https://example.com  ")` still produces
   `"🔗 example.com"`. The implementation passes the whitespace-trimmed
   body to `singleURLHost`, so the trim happens before URL detection.
   Three asymmetric padding shapes (both-sides, leading-only,
   trailing-only) all flow through the same `trimmed` variable; pin all
   three so a refactor that swaps `trimmed` for the raw body inside the
   URL branch surfaces here. Cross-channel paste-from-clipboard flows
   commonly add a trailing newline — drift would silently downgrade
   those rows from `🔗 host` to the verbatim raw URL with whitespace.

8. **P8 audit** (`9e7b0ed`): bump AGENTS.md test count header 1912 →
   1913 for the URL-whitespace pin.

## Files touched / REPs closed
- Code: 0 (all changes in tests/ + docs/ — no source files modified).
- Test files: `DraftEngineTests.swift`, `ShortcutsExportHandlerTests.swift`,
  `AppleScriptMessageReaderTests.swift`, `IMessagePreviewTests.swift` (+6 cases total).
- Docs: `AGENTS.md` (3 test-count bumps), `BACKLOG.md` (3 done_on shas
  rectified), `.automation/logs/autopilot-2026-05-08-1810.md` (this log).
- REPs closed: 0 (all open backlog items remain blocked/L-effort/UI-sensitive).
- REPs reconciled: 3 stale done_on placeholders fixed (REP-016, REP-170,
  REP-221 — all already `status: done`, just had placeholder shas).

## Pipeline state at exit (so far)
- Last 3 commits on main: `9e7b0ed` (AGENTS.md bump), `4409ad0` (URL-
  whitespace pin), `a2cc25e` (BACKLOG done_on rectification).
- Test count: 1913 (1877 passing under three-skip workaround, +6 from
  fire-start, ~17s warm).
- Wip queue depth: 0.
- Open BACKLOG: 14 (unchanged; same blocked/L-effort/UI-sensitive set).

## Next-fire hints

State is fully derivable from `git log`; no special continuation needed.

**Pin saturation observation for future P5-lane fires:** ~10+ surface
areas audited this fire (KeychainHelper, NotificationCoordinator,
IMessageSender, AppleScript, SearchIndex, Slack HTTP/Socket, MLXDraftService,
PromptBuilder, GlobalHotkey, Locked, Stats, Fixtures, ScreenInventory)
are comprehensively pinned both at constant level and at behavioral
level. Future P5 hunting should focus on:
(a) recently-added code (last 7 days) where the pin convention may not
    have caught up yet,
(b) cross-module integration boundaries (ShortcutsExport ↔ InboxViewModel,
    NotificationCoordinator ↔ InboxViewModel.applyIncomingNotification),
(c) error-path branches that production rarely hits (e.g. SQLite NOTADB
    on iCloud-sync-corrupted chat.db).

Don't burn a fire trying to find unpinned constants in long-stable files
— start by `git log --since="48 hours ago"` and audit whatever's new.

The L-effort REP-501→REP-505 MLX SPM split is still untouched and
remains human-paired-session territory per its own blocker note.
