# Autopilot fire — 2026-05-08-2210

## Time used

~38 min — 13 commits to main. No wip branches drained (queue was empty
on entry); fire spent its budget on documentation drift cleanup, doc-ref
repair, and one cross-module mirror pin.

## State on entry

- Wip queue depth: **0** (empty before this fire)
- BACKLOG: 14 open, 0 in_progress, 0 truly blocked (the lone `status: blocked` grep hit was a false positive on prose inside `files_to_touch`)
- Build cache: WARM (`find .build -maxdepth 0 -mmin -360` returned WARM)
- Last 3 commits on main were all autopilot work from the 2026-05-08-2010 fire (test-count bumps, BACKLOG cleanup)
- Test baseline: **1894 passing** under three-skip workaround in ~17s

## Actions this fire

1. **P5 cross-module pin:** `search-index-tests: pin outgoingSenderLabel == ShortcutsExportHandler.outgoingMessageMarker` → main `0e765f9`
   - Closes the third leg of the three-module `me` triangle (PromptBuilder.Template.speakerSelf ↔ SearchIndex.outgoingSenderLabel ↔ ShortcutsExportHandler.outgoingMessageMarker) directly inside `SearchIndexTests.swift`. Transitive equality already held via PromptBuilder, but a refactor that touched only SearchIndex + ShortcutsExportHandler would have relied on a sibling file's coverage to surface drift. +1 test (1894 → 1895).

2. **P8 doc bump:** `agents.md: bump test count header 1894 → 1895` → main `ba2136a`

3. **P3 doc-ref repair:** `docs: repair 9 stale 'Pinned by' test-name references in source comments` → main `bed449c`
   - Audited every `<TestSuite>.<testName>` reference inside `Sources/ReplyAI/` against actual test functions. 9 were stale — either suite renamed (`KeychainErrorTests` → `KeychainHelperTests`, `StatsDefaultFileURLTests` → `StatsTests`, `InboxViewModelArchivedTests` → `InboxViewModelStaticPinTests`, `ContactsResolverTests` → `ContactsResolverPhoneDigitsTests`) or test renamed (e.g. `LLMServiceTests` `*IsOneEightyMilliseconds` → `Constant*Is180Ms`).
   - Touched 7 files: `ChannelService.swift`, `ContactsResolver.swift`, `KeychainHelper.swift`, `SlackSocketClient.swift`, `InboxViewModel.swift` (5 references), `LLMService.swift` (2), `Stats.swift`. Drift was silent because no runner re-checks doc-comment ref validity. After this commit all 99 such refs in source resolve.

4. **P3 SHA repair:** `agents.md: replace 5 stale wip-branch SHAs with the actual main-landing SHAs` → main `fd07151`
   - The 5 most-recent entries in 'What's done' carried wip-branch SHAs that became unreachable when the branches were drained:
     - `e5074e2` → `5e761c9` AccessibilityAPIReader (REP-258+269)
     - `8cf5a15` → `7e932cf` per-channel Preferences keys (REP-231+208+220)
     - `ce76f2a` → `7c62474` SlackSocketClient (REP-267)
     - `6ae9022` → `cfe50f8` Thread-list cache (REP-278)
     - `ea6fc52` → `a819f59` ViewState enum (REP-247)
   - All 88 SHAs in AGENTS.md now resolve via `git cat-file -e`.

5. **P8 doc fix:** `agents.md: include SMS in stubbed-channel list` → main `923e71a`
   - The "What's still stubbed" bullet listed Slack/WhatsApp/Teams/Telegram but omitted SMS, even though `Sources/ReplyAI/Channels/SMSChannel.swift` ships and throws `authorizationDenied` identically.

6. **P8 architecture fix:** `agents.md: correct font-resolution architecture pattern` → main `1fb6119`
   - The Architecture-patterns bullet claimed Inter Tight uses per-weight static PostScript names (`InterTight-Medium / -SemiBold / -Bold`) and that `.weight()` on `.custom(...)` is unreliable. Both halves were wrong about Inter Tight: actual code does `.custom("Inter Tight", …).weight(weight)` against the variable-weight `wght` axis. The unreliable-`.weight()` workaround actually applies to JetBrains Mono, which IS shipped as per-weight static TTFs. Replaced the bullet with the actual two-path policy.

7. **P8 layout refresh:** `agents.md: refresh Repo layout — Channels list now reflects post-pivot 22 files` → main `7b283e7`
   - Channels/ tree-list mentioned only the 6 iMessage-era files (`ChannelService`, `IMessageChannel`, `ContactsResolver`, `AttributedBodyDecoder`, `ChatDBWatcher`, `IMessageSender`) when the actual directory ships 22 files including the entire Slack stack, the pivot alt-source readers, the four stub channels, and `IMessagePreview`. Also dropped the dead `_Placeholders.swift` line and added WelcomeGate to the Onboarding/ entry.

8. **P8 layout refresh:** `agents.md: refresh Services/ tree — list all 9 files` → main `82150d1`
   - Services/ list mentioned `StubLLMService.swift` as a separate file (it's a struct inside `LLMService.swift`) and omitted DraftStore, PromptBuilder, Stats, NotificationCoordinator, GlobalHotkey. Now lists all 9 actual files with one-line summaries.

9. **P8 layout fix:** `agents.md: add ModelLoadBanner to Inbox/ tree` → main `2ccb9cb`
   - `Sources/ReplyAI/Inbox/ModelLoadBanner.swift` ships in main but had no entry. Also annotated the SidebarView line with the TimelineView auto-tick mechanism.

10. **P8 strategy update:** `agents.md: strategic direction now refers to 'autopilot' instead of legacy planner/worker/reviewer agents` → main `44f4aac`
    - Single-line cleanup. The 2026-04-23 pivot section listed 'planner, worker, reviewer' as the agents owning new priorities, but those roles consolidated into the autopilot in 2026-05.

11. **P8 priority-queue reconciliation:** `agents.md: priority queue #4 (Better AttributedBodyDecoder) marked DEPRIORITIZED per the 2026-04-23 pivot` → main `6fce67b`
    - The Strategic-direction pivot section explicitly deprioritizes AttributedBodyDecoder rich-text parsing, but the priority-queue entry still pitched it as live work. Struck through with a paragraph explaining (a) the pivot deprioritization, (b) what already shipped (typedstream 0x2B tag scan + 28 tests, commit `e760a12`, REP-003), and (c) why future effort goes to alternative message sources instead.

12. **P3 doc-ref refinement:** `docs: refine 3 doc-refs to point at the actual test class (not just the .swift file)` → main `164ae70`
    - Late discovery: the bulk repair in commit `bed449c` treated test classes as one-per-file, but `KeychainHelperTests.swift` houses 3 classes (KeychainHelperTests, KeychainErrorTests, SlackTokenStoreTests) and `StatsTests.swift` houses 4. Three rewrites pointed at the file-namesake class when the actual test functions lived inside the sibling class. Restored `KeychainErrorTests.testUnhandledErrorFallbackToastFormatRoundTrips`, `StatsDefaultFileURLTests.testDefaultFileURLEndsWithStatsJSON`, `StatsDefaultFileURLTests.testProductionFileNameIsStatsDotJSON` — both class names ARE real, just shared a file with their namesake. Rebuilt the audit script to use loose-class-AND-loose-fn matching (instead of strict same-class-block) and confirmed all 99 source-side `<TestSuite>.<testName>` refs now resolve when treated as class-exists + fn-exists.

## Files touched this fire

- `Sources/ReplyAI/Channels/{ChannelService,ContactsResolver,KeychainHelper,SlackSocketClient}.swift` — doc-ref repairs only
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` — 5 doc-ref repairs (key constants)
- `Sources/ReplyAI/Services/{LLMService,Stats}.swift` — doc-ref repairs only
- `Tests/ReplyAITests/SearchIndexTests.swift` — +1 cross-module mirror pin
- `AGENTS.md` — 6 separate edits across Repo layout, Architecture patterns, What's done, What's still stubbed, Strategic direction, test count header

## Pipeline state at exit

- Wip queue depth: **0** (no new wip branches this fire)
- BACKLOG: 14 open, 0 in_progress, 0 truly blocked — unchanged
- Build cache: still WARM (single `swift build` + targeted `swift test --filter` runs only)
- Test count: **1895 passing** under three-skip workaround (was 1894 on entry); 0 failures, 2 skipped
- Last 3 commits on main:
  - `44f4aac` agents.md: strategic direction now refers to 'autopilot'
  - `2ccb9cb` agents.md: add ModelLoadBanner to Inbox/ tree
  - `82150d1` agents.md: refresh Services/ tree

## Next-fire hints

None requiring carry-over state. The pipeline is in a clean steady state:

- All AGENTS.md SHAs (88) and 'Pinned by' references (99) resolve.
- BACKLOG.md is reconciled — no open task whose work has shipped.
- The next fire's P1 wip-drain will have nothing to do (queue empty); next-highest priorities are the same UI-sensitive REP-043/044/045/259 polish items, the L-effort MLX extraction chain (REP-501→505), the L-effort XCUITest bootstrap (REP-XCUI-001), and the human-claimed REP-505/REP-062.
- If the autopilot finds itself again with no wip queue and no easy P5/P6/P7, the doc-drift audit pattern from this fire (verify every `TestSuite.testName` ref in source against actual tests; verify every backticked SHA in AGENTS.md/BACKLOG.md via `git cat-file -e`) is a reusable approach for spending session budget productively. Both checks are cheap and surface real, silent staleness.
