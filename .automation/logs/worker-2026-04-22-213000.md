# Worker Run: 2026-04-22-213000

## Summary
Bundled 12 S-effort P2 tasks. All 404 tests pass. Build clean.

## Tasks Completed
- REP-126 — SearchIndex disk-backed round-trip smoke tests
- REP-128 — IMessageSender chatGUID pre-flight validation
- REP-130 — Preferences firstLaunchDate set-once key
- REP-134 — InboxViewModel archive removes thread from SearchIndex (integration test)
- REP-137 — PromptBuilder oversized system instruction guard
- REP-138 — DraftEngine dismiss() deletes DraftStore entry
- REP-140 — SearchIndex concurrent upsert+delete race tests
- REP-141 — ContactsResolver batchResolve result contract tests
- REP-143 — RulesStore insertion-order invariant tests
- REP-144 — SmartRule unknown RuleAction kind graceful decode tests
- REP-145 — PromptBuilder empty message list tests
- REP-147 — DraftStore concurrent write+read race tests

## Files Changed
### Production
- `Sources/ReplyAI/Channels/IMessageSender.swift` — `SendError.invalidChatGUID`, `isValidChatGUID()`, validation guard in `sendRaw`
- `Sources/ReplyAI/Services/Preferences.swift` — `firstLaunchDate` key, added to `wipeExemptions`
- `Sources/ReplyAI/App/ReplyAIApp.swift` — set-once firstLaunchDate in init
- `Sources/ReplyAI/Services/PromptBuilder.swift` — `minHistoryReserve`, `systemPrompt(tone:)` with truncation guard
- `Sources/ReplyAI/Services/DraftEngine.swift` — `store?.delete(threadID:)` in `dismiss()`
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` — injectable `searchIndex` parameter

### Tests
- `Tests/ReplyAITests/SearchIndexTests.swift` — 4 new tests (disk round-trip, empty reopen, concurrent race x2)
- `Tests/ReplyAITests/IMessageSenderTests.swift` — 6 new tests (GUID validation)
- `Tests/ReplyAITests/PreferencesTests.swift` — 3 new tests (firstLaunchDate)
- `Tests/ReplyAITests/InboxViewModelTests.swift` — 1 new test (archive→search index)
- `Tests/ReplyAITests/PromptBuilderTests.swift` — 5 new tests (oversized instruction, empty messages)
- `Tests/ReplyAITests/DraftEngineTests.swift` — 3 new tests (dismiss clears store)
- `Tests/ReplyAITests/ContactsResolverTests.swift` — 3 new tests (batchResolve contract)
- `Tests/ReplyAITests/RulesTests.swift` — 5 new tests (insertion order, unknown action)
- `Tests/ReplyAITests/DraftStoreTests.swift` — 2 new tests (concurrent race)

## Test Results
- Before: 391 tests
- After: 404 tests, 0 failures

## Notes
- One test rename during implementation: `testEmptyGUIDThrowsInvalid` → `testInvalidGUIDThrowsInvalid` (empty chatGUID gets synthesized away by `chatGUID(for:)`; test was changed to use `"INVALID_NO_SEPARATORS"` which is non-empty but structurally invalid). Separate `testEmptyGUIDIsValidationFailed` directly tests `isValidChatGUID("")`.
- `isValidChatGUID` validates iMessage-only prefix; SMS GUIDs correctly fail validation.
