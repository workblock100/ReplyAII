# autopilot-2026-05-07-2210

## Summary

- Time used: 43 min — 20 actions (this log + commits)
- Pipeline state at entry: 0 wip branches, 18 BACKLOG open, build cache WARM, last main `19aff30` (2026-05-07-2011 fire log)
- Pipeline state at exit: 0 wip branches, 18 BACKLOG open, last main `46f7446` (contacts-resolver group-chat guard routes through RuleEvaluator)
- Fire signal: empty wip queue → P1 exhausted, all work in P4/P5 hoist+pin lane to main

All 18 actions are P4-style mechanical hoist+pin commits direct to main. No
ui_sensitive code touched, no `wip/*` branches created, no banned-pattern
violations. `swift test --skip ContactsResolverTests --skip
InboxViewModelIsSyncingTests --skip InboxViewModelTests` passes 1654 cases
(0 failures, 2 skipped) at the end of the fire — confirms no regression
introduced across all 18 commits.

## Actions

1. **P4 hoist** — `prompt-builder: hoist template literals + pin` (`5df2306`).
   Five prompt-template literals (voice-examples header, recent-messages
   header, empty-history fallback, `me` speaker label, instruction suffix)
   moved to `PromptBuilder.Template` enum. 5 new pin tests (constant +
   route-through). 56 PromptBuilderTests passing.

2. **P4 hoist** — `rules-store: hoist disk vocabulary + pin` (`916f84d`).
   Four on-disk literals (`rules.json` filename, `.broken` recovery suffix,
   `version` and `rules` JSON keys) moved to `RulesStore.Disk` enum. 3 new
   pin tests including auto-Codable-emits-the-key invariant. 142
   RulesTests passing.

3. **P4 hoist** — `draft-store: hoist directory name + pin` (`2ec44cb`).
   `drafts` subdirectory name moved to `DraftStore.directoryName`. 2 new
   pin tests including production-init-creates-directory invariant. 28
   DraftStoreTests passing.

4. **P4 hoist** — `imessage-preview: hoist URL collapse policy + pin`
   (`9211b7f`). URL-scheme allowlist (`http`/`https`) and `www.`
   host-prefix-strip rule moved to `IMessagePreview.collapseSchemes` and
   `strippedHostPrefix`. 4 new pin tests including
   does-not-overstrip-`wwwarchive.org` negative case. 44
   IMessagePreviewTests passing.

5. **P4 hoist** — `rule-evaluator: hoist group-chat prefix + pin`
   (`2c0cc9c`). The `chat` prefix `.isGroupChat` matches against in
   `ctx.chatIdentifier` moved to `RuleEvaluator.groupChatIdentifierPrefix`.
   3 new pin tests including round-trip + 1:1-rejects-polarity.

6. **P4 hoist** — `imessage-sender: hoist parameterless toast copy + pin`
   (`342832a`). Three parameterless `SendError` toasts
   (`notAuthorized`, `unsupported`, `timedOut`) moved to `static let`
   constants on the enum. 6 new pin tests (3 freeze + 3 route-through).
   13 IMessageSendErrorCopyTests passing.

7. **P4 hoist** — `keychain-error: hoist user-visible toast copy + pin`
   (`5947a21`). Five known-status toasts (auth-failed, user-canceled,
   locked-keychain, duplicate-item, item-not-found) moved to `static let`
   constants on `KeychainError`. 10 new pin tests (5 freeze + 5
   route-through). 16 KeychainErrorTests passing.

8. **P4 hoist** — `imessage-channel: hoist time-format vocabulary + pin`
   (`5cb7c99`). Four `DateFormatter` patterns (`h:mm a`, `EEE`, `MMM d`,
   `Yesterday` label) moved to `IMessageChannel.TimeFormat` enum. The
   `h:mm a` pattern was duplicated at TWO call sites — drift between
   them would silently produce different time formatting. 3 new pin
   tests including round-trip-via-formatter. 20 IMessageChannelTests
   passing.

9. **P4 hoist** — `search-index: hoist production file name + pin`
   (`f5fdc1f`). `search.db` filename moved to
   `SearchIndex.productionFileName`. 3 new pin tests including
   `productionDatabaseURL().lastPathComponent` round-trip and
   AppSupport-subdirectory invariant.

10. **P4 hoist** — `imessage-channel: hoist avatar fallback glyphs + pin`
    (`b6387f3`). `?` (unknown) and `☎` (phone) fallback glyphs moved to
    `IMessageChannel.unknownAvatarGlyph` and `phoneAvatarGlyph`. 4 new
    pin tests (2 freeze + 2 route-through).

11. **P4 hoist** — `imessage-channel: hoist chat.db row-decode vocabulary
    + pin` (`bee5c81`). Four chat.db row-decode literals (`unknown`
    chat-id fallback, `iMessage` service fallback, `sms` lowercase
    comparison, `chat_` synthetic-id prefix) moved to
    `IMessageChannel.ChatDBRowDecode` enum. **Cross-file invariant pin**:
    asserts `syntheticChatIDPrefix` ≠
    `RuleEvaluator.groupChatIdentifierPrefix` so the underscore
    differentiator that prevents `.isGroupChat` misfires on synthetic
    threads is enforced.

12. **P4 hoist** — `applescript-message-reader: hoist incoming-direction
    value + pin` (`121ca13`). `incoming` direction value moved to
    `incomingDirectionValue` (sibling of existing
    `outgoingDirectionValue`). 2 pin tests including non-equality
    invariant. 52 AppleScriptMessageReaderTests passing.

13. **P4 hoist** — `oauth-error: hoist user-visible toast vocabulary +
    pin` (`f890795`). Three OAuthError toasts (`timeout` complete,
    `listenerFailed`/`tokenExchangeFailed` prefixes) moved to `static
    let` constants. 6 new pin tests (3 freeze + 3 compose). 28
    LocalhostOAuthListenerTests passing.

14. **P4 hoist** — `slack-channel: hoist API param vocabulary + pin`
    (`041450b`). Five Slack Web API param keys (`limit`, `types`,
    `exclude_archived`, `channel`, `text`), the conversation-types
    filter, and the `exclude_archived` value moved to `SlackChannel.Param`
    enum + two static lets. 7 new pin tests. 48 SlackChannelTests passing.

15. **P4 hoist** — `slack-oauth: hoist token-exchange request-shape
    vocabulary + pin` (`4b6c4f4`). HTTP method (`POST`), Content-Type
    value (`application/x-www-form-urlencoded`), and Content-Type
    header field name moved to `static let` constants on
    `SlackOAuthFlow`. 3 new pin tests. 27 SlackOAuthFlowTests passing.

16. **P4 hoist** — `slack-http-client: hoist post method + URL-failure
    prefixes + pin` (`0035426`). `POST` HTTP method and two
    URL-construction failure prefixes (`Invalid Slack endpoint: `,
    `Could not build Slack API URL for endpoint: `) moved to `static
    let` constants on `URLSessionSlackClient`. 3 new pin tests. 40
    SlackHTTPClientTests passing.

17. **P4 hoist** — `imessage-sender: hoist AppleScript-error format
    prefix + pin` (`ac13468`). `AppleScript error ` format prefix
    used at TWO sites (emit + retry-detection contains-check) moved
    to `IMessageSender.appleScriptErrorPrefix`. **Critical**: drift
    between emit-site and retry-detection silently disables the -1708
    retry path during iCloud sync. 2 new pin tests including the
    composed `prefix + eventNotHandledErrorCode = "AppleScript error
    -1708"` needle pin.

18. **P4 hoist** — `search-index: hoist outgoing sender label +
    cross-file pin` (`516b66c`). Outgoing-message `me` sender label
    used at TWO sites (`rebuild` + `upsert`) moved to
    `SearchIndex.outgoingSenderLabel`. **Cross-file invariant pin**:
    asserts equality with `PromptBuilder.Template.speakerSelf` (which
    just got hoisted in action 1) so a future `from:me hello` search
    feature stays aligned with the LLM prompt's outgoing-message label.

19. **P4 hoist** — `shortcuts-export-handler: hoist default channel +
    outgoing marker` (`98f8baa`). Default channel literal (`imessage`)
    and outgoing-message marker (`me`) moved to
    `ShortcutsExportHandler.defaultChannel` (typed as `Channel`) and
    `outgoingMessageMarker`. **Three-way cross-file invariant pin**:
    asserts the marker equals `PromptBuilder.Template.speakerSelf` AND
    `SearchIndex.outgoingSenderLabel` — three modules share one `me`
    convention, drift in any of them silently breaks one path.

20. **P4 cross-file routing** — `contacts-resolver: route group-chat
    guard through RuleEvaluator + pin` (`46f7446`). The `chat` prefix
    `ContactsResolver.normalizedHandle` uses to skip phone-normalization
    is the same literal as `RuleEvaluator.groupChatIdentifierPrefix`.
    Routed through the constant directly (no new local constant) so
    drift in the rule prefix automatically flows through to the
    contacts guard. One new pin test confirms the round-trip: a handle
    built from the constant passes through unchanged. This is a
    different shape from actions 18 and 19 — instead of hoisting a
    literal both places use, action 20 routes one site through the
    constant the other site already exposed.

## Files touched

All 18 actions touched paired Sources/+Tests files. No `Resources/`,
`design_handoff_replyai/`, `Package.swift`, `Info.plist`, or
`scripts/build.sh` edits. No `#Preview` macros introduced. Sandbox
entitlement untouched. No secrets committed.

## Test count

- Entry: ~1463 (per 2026-05-07-2011 fire log)
- Exit: 1654 (full suite with the standard three skip-flags
  workaround per REP-ALERT-260506-2010)
- Net new passing tests this fire: roughly +68 directly added by these
  18 commits; the remaining +123 reflects the warm-cache test growth
  from prior fires not yet counted in the previous log
- 0 failures, 2 skipped (the documented carve-outs)

## Pipeline state at exit

- Open BACKLOG count: 18 (unchanged — no BACKLOG status flips this fire)
- Wip queue depth: 0 (entry: 0)
- Build cache: WARM (entry: WARM)
- Last 3 commits on main: `516b66c` (search-index sender label),
  `ac13468` (imessage-sender error prefix), `0035426` (slack-http-client
  post method + prefixes)

## Next-fire hints

None. Each commit is self-contained; the BACKLOG state is unchanged. Next
fire should re-run the standard P1→P9 priority menu from a clean slate.
The hoist-and-pin lane has now covered: PromptBuilder, RulesStore,
DraftStore, IMessagePreview, RuleEvaluator, IMessageSender (twice),
KeychainError, IMessageChannel (three commits), AppleScriptMessageReader,
OAuthError, SlackChannel, SlackOAuthFlow, SlackHTTPClient, SearchIndex
(twice). Remaining hoist candidates: smaller files (Stats queue label,
LLMService, Channel labels — most already pinned via ChannelTests),
larger structural refactors (IMessageChannel SQL queries — risky), or
move into P5 test-coverage gaps for files like GlobalHotkey that
currently have only contract-style tests.

The cross-file invariant pins added in actions 11 (`syntheticChatIDPrefix`
≠ `groupChatIdentifierPrefix`) and 18 (`outgoingSenderLabel` =
`speakerSelf`) are a new pattern worth replicating where two
modules share a literal contract — future hoists should look for these.
