# worker-2026-04-24-120000

## Tasks
- REP-247 (P0, M) — InboxViewModel: `ViewState` enum for loading/populated/demo/error states
- REP-274 (P1, S) — SlackTokenStore: structured Keychain wrapper for Slack access token + workspace name

## Outcome
**blocked** — code complete; pushed to `wip/2026-04-24-120000-viewstate-slacktokenstore`. MLX cold-build would exceed 13-min budget (REP-254). Human should run `swift test` and merge if green.

## File changes

### REP-247
- `Sources/ReplyAI/Services/Preferences.swift` (+8 lines): added `PreferenceKey.demoModeActive`, `PreferenceDefaults.demoModeActive = true`, registered in `registerReplyAIDefaults`, added to `wipeExemptions`
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` (+40 lines): added `EmptyReason` and `ViewState` enums (nested); added `viewState: ViewState = .loading` property; updated `syncFromIMessage()` to transition viewState on all paths (empty/populated/no-permissions/error)
- `Tests/ReplyAITests/InboxViewModelTests.swift` (+55 lines): 4 new tests in `InboxViewModelViewStateTests`

### REP-274
- `Sources/ReplyAI/Channels/KeychainHelper.swift` (+43 lines): added `SlackTokenStore` struct with `set(token:workspaceName:)`, `get()`, `delete()`
- `Tests/ReplyAITests/KeychainHelperTests.swift` (+37 lines): 4 new tests in `SlackTokenStoreTests`

## Tests before/after
- Before: 527 (from AGENTS.md header)
- After: 535 (+8 tests: 4 ViewState + 4 SlackTokenStore)
- Could not verify with `swift test` — MLX cold-build exceeds budget

## Notes
- REP-272 (SlackChannel.authorize) was considered for bundling but requires REP-266 which is still blocked on a wip branch
- `demoModeActive` pref key added here as a prerequisite for REP-247; REP-228 (full demo mode implementation with fixture threads) remains blocked — once merged, `syncFromIMessage()` should also clear `demoModeActive` after first successful real sync
- `ViewState.error` Equatable implementation compares `localizedDescription` strings (Error is not natively Equatable)
