# autopilot-2026-05-08-0411

## Summary

- Time used: ~36 min — 11 actions (10 hoist+pin commits + 1 doc-sync)
- Pipeline state at entry: 0 wip branches, 18 BACKLOG open, build cache WARM, last main `51a9617` (2026-05-08-0210 fire log)
- Pipeline state at exit: 0 wip branches, 18 BACKLOG open, last main `fb57ed4` (applescript-reader RuleEvaluator coupling)
- Fire signal: empty wip queue → P1 exhausted, P2 high-priority unclaimed work all UI-sensitive or claimed_by:human, all 11 actions in P4-style hoist+pin lane to main
- Model: claude-opus-4-7 medium-effort
- Test count progression: 1745 → 1764 (+19 new pin tests) under three-skip workaround
- Pre-existing P9 verification: ContactsResolverTests `XCTSkipIf` workaround still in place; full suite clean across all 11 commits

## Actions

1. **P4 hoist** — `imessage-sender: hoist applescript send template + named-constant pins` (`f099b03`).
   `IMessageSender.sendRaw` carried the AppleScript wire-format template inline (`tell application "Messages" / set targetChat to a reference to chat id / send / end tell`). Two coupled binding-name sites (`set <name>` and `send to <name>`) used the inline literal `targetChat` — drift on only one fails every send with an undefined-identifier AppleScript error. Hoist into `appleScriptApplicationTarget`, `appleScriptChatBindingName`, and `appleScriptSendSource(escapedText:escapedGUID:)` builder. 4 new pins: literal-equality on each constant, exact-equality of the rendered four-line template, and a round-trip witness that the builder threads both constants through the coupled sites. 1745 → 1749 tests.

2. **P4 hoist** — `imessage-channel: hoist formatRelative empty-time sentinel + round-trip pin` (`9a048b4`).
   `formatRelative(appleDate: 0)` returned an inline `""` literal as the sentinel for rows whose chat.db `date` column is genuinely zero. Every downstream time-chip renderer leans on the empty-string contract (`time.isEmpty` branch in ThreadRow). Drift to "—", "?", "0", or " " would silently render an extra glyph on every zero-date thread row. Hoist into `TimeFormat.noTimestampSentinel`. 3 pins: literal-equality, isEmpty invariant, round-trip witness via `formatRelative(0)`. 1749 → 1750 tests.

3. **P4 hoist + bug** — `applescript-reader: route heredocs through rowDelimiter + incomingDirection constants` (`2b518ea`).
   FOUND A REAL DRIFT BUG: `rowDelimiter` and `incomingDirectionValue` constants were hoisted previously, but the AppleScript-side emit sites in `recentChats()` and `messagesForChat()` STILL used inline `"||"` and `"incoming"` literals — the docstring claimed interpolation that didn't exist. Drift in either constant left the AppleScript producing the old value while the Swift parser split on the new — silent empty parses with no error path. Wire both heredocs through `\(Self.rowDelimiter)` and `\(Self.incomingDirectionValue)`. Add `testMessagesForChatScriptUsesIncomingDirectionConstant`. Update stale "uses the *literal* `||`" doc-comment. 1750 → 1751 tests.

4. **P4 hoist** — `search-index: hoist FTS5 createSchema DDL + cross-check pins` (`7ff874b`).
   `createSchema()` built the FTS5 virtual-table DDL as an inline `let sql = """..."""` literal — the only critical SQL string in SearchIndex that wasn't routed through the `SQL` enum. Drift in column order silently breaks every positional INSERT AND every snippet query (which assumes `text` is column index 3). Drift in the tokenize spec changes recall surface for every shipped user. Hoist into `SearchIndex.SQL.createMessagesFTSTable` and route `createSchema()` through it. 3 pins: byte-for-byte literal equality, cross-check that DDL and every DML statement reference the same `messages_fts` table name, and cross-check that DDL column position of `text` matches `snippetTextColumnIndex`. 1751 → 1754 tests.

5. **P4 hoist** — `slack-channel: hoist channel display-name `#` prefix + freeze pin` (`0a9c821`).
   `recentThreads`'s display-name builder used `"#\(name)"` inline for public/private Slack channels (Slack's UI convention; the only visual signal channel rows aren't DM rows in the sidebar). Inline literal isn't reachable from a copy review surface like the rest of `Endpoint.*` / `Param.*`. Hoist into `SlackChannel.channelDisplayPrefix`. 1 pin: literal-equality. 1754 → 1755 tests.

6. **P4 hoist** — `inbox-viewmodel: hoist UserDefaults persistence keys + prefix-coverage pin` (`ea3b5cb`).
   Five InboxViewModel UserDefaults keys (archivedKey, silentlyIgnoredKey, pinnedKey, snoozedUntilKey, lastSeenRowIDKey) were `private static let` literals. Existing `*LoadFromLiteralKey` round-trip tests catch literal-symmetry drift, but pass when the constant value drifts in lockstep with the test literal AND don't enforce the wipe-namespace prefix invariant — a refactor renaming all 5 to drop `pref.inbox.` (and updating test literals) would silently leak archived sets, snoozed sets, etc. past `wipeReplyAIDefaults`. Hoist `private` → package access. 2 pins: byte-for-byte freeze on all 5 constants, cross-check that every key starts with `PreferenceKey.wipeNamespacePrefix` so factory-reset coverage is enforced at compile-pin level. 1755 → 1757 tests.

7. **P4 hoist** — `llm-service: hoist StubLLMService timing defaults + named-constant pins` (`2336f81`).
   `StubLLMService(tokenDelay:initialDelay:)` defaults `22-58ms` per-token and `180ms` cold-start were inline default-arg expressions. Calibrated for demo composer perceived cadence — drift up makes stub feel slow, drift down ruins streaming illusion. Existing tests pinned via instance instantiation but a refactor introducing named statics without rewiring would leave them as dead code. Hoist into `defaultInitialDelay`, `tokenDelayLowerBoundNanoseconds`, `tokenDelayUpperBoundNanoseconds`, `defaultTokenDelay` (composed range). 3 pins: literal pins on each named constant, range-composition cross-check, round-trip witness that no-arg init routes through the constants. 1757 → 1760 tests.

8. **P8 doc-sync** — `agents: refresh test count from 1598 → 1796 (1760 pass under three-skip workaround)` (`ecf89a4`).
   AGENTS.md layout-header test count was 1598 / 1562 pass as of 2026-05-07-1855. Stale numbers mislead future fires — the count survey at fire start would see 1598 and a post-fire `Executed 1760` confirmation looks like a regression rather than a running total. Updated to 1796 / 1760 pass for 2026-05-08-0411.

9. **P4 hoist** — `imessage-channel: hoist openReadOnly busy-retry delay + FDA recovery hint + open-error format` (`2f4cbe6`).
   `openReadOnly()` carried three inline values: `Thread.sleep(forTimeInterval: 0.1)` for SQLITE_BUSY retry cadence (drift adds visible inbox-open lag during iCloud sync); the multi-line FDA recovery hint embedded in `ChannelError.permissionDenied(hint:)` — the only path users have out of FDA-denied state, drift sends users to wrong macOS settings page (e.g. "Files and Folders" exists but doesn't cover chat.db); the `"Can't open chat.db: <msg>"` error format — support-log grep discriminator that distinguishes open from query failures. Hoist into `sqliteBusyRetryDelay`, `fdaPermissionDeniedHint`, `openErrorMessage(_:)`. 3 pins: literal-equality on the 0.1s delay, byte-for-byte freeze of the FDA hint with sub-substring sanity (`Full Disk Access`, `System Settings`), format-equality + prefix invariant on openErrorMessage. 1760 → 1763 tests.

10. **P4 hoist + bug** — `applescript-reader: route group-chat prefix through RuleEvaluator constant + cross-file pin` (`fb57ed4`).
    FOUND A REAL DRIFT BUG: AppleScriptMessageReader's three group-chat-prefix checks (`formatHandleFromChatID`, `prettyPhone`, `resolveContact`) used inline `"chat"` literals while ContactsResolver already routed its equivalent check through `RuleEvaluator.groupChatIdentifierPrefix`. Drift in the constant flows to ContactsResolver but leaves AppleScriptMessageReader unchanged → silent split where ContactsResolver no longer recognizes group IDs as groups while AppleScriptMessageReader still does. Wire all three sites through the constant. Add `testGroupChatPrefixRoutesThroughRuleEvaluatorConstant` cross-file pin: synthesize a row with chat ID whose suffix begins with the live constant value, assert AppleScriptMessageReader labels it as a group AND that prettyPhone passes synthetic chat keys through unchanged. 1763 → 1764 tests.

## Pipeline state at exit

- main: `fb57ed4`
- last 3 commits: `fb57ed4` (applescript-reader RuleEvaluator coupling), `2f4cbe6` (imessage-channel openReadOnly hoist trio), `ecf89a4` (AGENTS.md test count refresh)
- BACKLOG: 18 open (unchanged), 1 blocked (REP-285 superseded by REP-500 chain), 0 in_progress
- wip queue: 0 branches (P1 still exhausted)
- build cache: WARM (continuous swift test throughout fire)
- ContactsResolverTests carve-out: still in place; never had to invoke `--skip ContactsResolverTests` workaround this fire because the suite no-ops cleanly in headless

## Notable findings (real bugs, not just pin scaffolding)

- **Action 3** uncovered a docstring-vs-source drift in AppleScriptMessageReader: the `rowDelimiter` constant claimed "the AppleScript heredocs interpolate `\(rowDelimiter)`" but the heredocs used the inline `"||"` literal. Drift in the constant would have silently broken every parse. Now fixed at both emit sites.
- **Action 10** uncovered cross-file constant drift between ContactsResolver and AppleScriptMessageReader: ContactsResolver routed through `RuleEvaluator.groupChatIdentifierPrefix` but AppleScriptMessageReader had three inline `"chat"` literals. A future rename of the constant would have produced a silent classification split. Now coupled.

## Next-fire hints

- Hoist+pin lane is starting to show diminishing returns on this codebase — most non-UI-sensitive constants are pinned. Two structural targets remain: (a) the IMessageChannel SQL queries themselves (4 query strings spanning thread-list, single-thread, history) carry many positional column references; pinning column-presence invariants would catch drift the existing integration tests miss because they create SQLite tables matching the production query shape. (b) `Theme.swift` tokens — UI-sensitive, must go through wip.
- The two real bugs found this fire (action 3 and 10 above) suggest there's a subset of cross-file invariants where one site routes through a constant but a sibling site has inline literals. Worth a future fire surveying for `hasPrefix("...")` or `contains("...")` patterns where the literal also appears as a named constant in an adjacent module.
- REP-501→REP-505 (MLX SPM extraction) remains untouched. SKILL.md flags it as L-effort and human-paired; not a fit for autopilot fires.
- AGENTS.md test count was bumped from 1598 → 1796 (1760 pass) this fire. Keep this current — the fire-survey step reads it.
