# Autopilot fire — 2026-05-08 08:10 EDT

**Time used**: 43 min — 25 actions (1 P5 cross-pin to main + 1 wip skip + 23 test-side
constant reroutes to main + 1 fire-log commit).

**Model**: Opus 4.7 medium-effort.

## Wip queue at fire start

- `wip/autopilot-2026-05-08-0628-willpresent-cksenderid` (2 h old, +1 commit)

  **SKIP** — banned-pattern hit: test-count regression of −9. Branch removed nine
  recent pin tests on main while harmonizing willPresent's CKSenderID lookup with
  the parser. Worse: it removed `Stats.writeQueueLabel` constant + its pin tests
  while leaving the inline `"com.replyai.stats.write"` literal in place — i.e. it
  removed the divergence pin without harmonizing the divergence. Commit message
  said "align willPresent" but the diff was actually a stealth pin-removal bundled
  with a legit fix. Per banned-pattern: "test-count regression vs main → SKIP".

## Actions this fire

All 22 reroute commits follow one pattern: a production constant exists
(hoisted, with a pin test), but functional tests duplicate the literal value
inline. A future rename would update the production constant and the pin test
would fail loudly — but the duplicated test sites would silently keep using the
old literal, masking the drift in green tests until it ships in production.
Routing the functional sites through the constant ties them to the same source
of truth; pin tests stay literal as the deliberate single-place value freeze.

1. **P5 cross-pin to main**: `16756d3` — `<Service>Channel.displayName` ↔
   `Channel.<case>.displayName` cross-pin (6 new tests). Each ChannelService
   stub hard-codes `displayName: String = "Slack"` etc.; the Channel enum has
   its own `var displayName` from `label`. No prior cross-pin; drift between
   them produced inconsistent labels (Settings vs toasts/filters).
   Companion `testServiceChannelPropertyMatchesChannelEnumCase` guards a
   subtler drift: a future `let channel: Channel = .imessage` on SlackChannel
   would pass the displayName pin (still "Slack") while routing Slack messages
   to the iMessage sender. Test count: 1810 → 1816.

2. `665dd4c` — MessagesAppActivationObserverTests: 10 inline
   `"com.apple.MobileSMS"` → `MessagesAppActivationObserver.messagesAppBundleID`.

3. `764dc0b` — KeychainHelperTests: 3 `deleteAll(prefix: "ReplyAI-")` →
   `KeychainHelper.accountPrefix`.

4. `163662a` — SlackHTTPClientTests: 5 functional sites for header names,
   POST verb, Bearer token format → `URLSessionSlackClient.Header.*` /
   `.postHTTPMethod` / `.bearer(_:)`.

5. `163ee57` — Channel stub tests (SMS, Teams, WhatsApp, Telegram): 12
   inline `"<channel>-token"` sites → `<Channel>Channel.keychainTokenKey`.

6. `ee84c13` — UNNotificationContentParserTests: 19 inline userInfo dict
   keys (`"CKSenderID"`, `"sender"`, `"CKChatIdentifier"`, `"CKChatGUID"`)
   → `UNNotificationContentParser.UserInfoKey.*`.

7. `d244c2b` — SlackOAuthFlowTests: 7 functional sites for OAuth form fields
   ("client_id", "redirect_uri", "scope") and the loopback redirect URI →
   `SlackOAuthFlow.FormField.*` / `.redirectURI`.

8. `178a1f5` — RulesTests: 2 tempfile helper sites for `"rules.json"` →
   `RulesStore.Disk.fileName`.

9. `5b6e05a` — StatsTests: 5 `tempURL(_ name: String = "stats.json")` helper
   defaults → `Stats.productionFileName`.

10. `46717f9` — StatsTests: 25 inline rule-action literals (`"archive"`,
    `"silentlyIgnore"`, `"markDone"`, `"setDefaultTone"`, `"pin"`) →
    `Stats.RuleAction.*`.

11. `1a2f355` — InboxViewModelTests: 6 `forKey:` sites for inbox preference
    keys → `InboxViewModel.archivedKey` / `.silentlyIgnoredKey` / `.pinnedKey` /
    `.snoozedUntilKey`.

12. `b39f27a` — StatsTests: 5 inline channel.rawValue dict-key lookups →
    `Channel.<case>.rawValue`.

13. `ba6fafa` — SearchIndexTests: 1 senderName assertion ("me") →
    `SearchIndex.outgoingSenderLabel`.

14. `27a8e48` — AccessibilityAPIReaderTests: 20 inline `role: "AXRow"`
    MockAXElement constructions → `AccessibilityAPIReader.conversationRowRole`.

15. `58c1457` — RulesTests: 16 inline `forKey: "pref.inbox.*"` UserDefaults
    cleanups → `InboxViewModel.<X>Key`.

16. `9169492` — ChannelStubAuthGateEdgeTests: 4 tearDown
    `keychain.delete(key: "<channel>-token")` → `<Channel>Channel.keychainTokenKey`.

17. `920ff2c` — RulesTests: 9 export-envelope JSON-key sites (`"version"`,
    `"rules"`, `"broken"`) → `RulesStore.Disk.versionKey` / `.rulesKey` /
    `.brokenSuffix`.

18. `946e872` — NotificationCoordinatorTests: 3 inline-reply UI strings
    ("Reply", "Send", "Your reply…") → `NotificationCoordinator.InlineReplyAction.*`.

19. `b35d57e` — InboxViewModelTests: 1 sendToast assertion → routes through
    `InboxViewModel.sentToToast(recipient:)` formatter.

20. `6053e51` — PromptBuilderTests: 1 channel-label assertion → `Channel.imessage.label`.

21. `99a7487` — SlackOAuthFlowTests: 2 token-exchange Content-Type sites →
    `SlackOAuthFlow.contentTypeHeaderField` / `.formURLEncodedContentType`.

22. `1f96cff` — Cross-file: 4 app-support `parent == "ReplyAI"` integration
    checks (PreferencesTests, StatsTests, SearchIndexTests, RulesTests) →
    `Preferences.appSupportDirectoryName`.

23. `0a4e723` + `bb3d2e7` — 3 productionURL filename checks (SearchIndex,
    Rules, Stats) → routed through `productionFileName` / `Disk.fileName`.

24. `758b3b5` — AppleScriptMessageReaderTests: 1 group-chat label assertion
    (`"Group chat"`) → `AppleScriptMessageReader.groupChatDisplayLabel`.

## Files touched

Tests only (defensive — no Sources changes), plus AGENTS.md test-count refresh in #1:

- AGENTS.md (test count refresh 1810 → 1816)
- Tests/ReplyAITests/ChannelTests.swift (+50 lines, 6 new tests)
- Tests/ReplyAITests/MessagesAppActivationObserverTests.swift
- Tests/ReplyAITests/KeychainHelperTests.swift
- Tests/ReplyAITests/SlackHTTPClientTests.swift
- Tests/ReplyAITests/SMSChannelTests.swift
- Tests/ReplyAITests/TeamsChannelTests.swift
- Tests/ReplyAITests/WhatsAppChannelTests.swift
- Tests/ReplyAITests/TelegramChannelTests.swift
- Tests/ReplyAITests/UNNotificationContentParserTests.swift
- Tests/ReplyAITests/SlackOAuthFlowTests.swift
- Tests/ReplyAITests/RulesTests.swift
- Tests/ReplyAITests/StatsTests.swift
- Tests/ReplyAITests/InboxViewModelTests.swift
- Tests/ReplyAITests/SearchIndexTests.swift
- Tests/ReplyAITests/AccessibilityAPIReaderTests.swift
- Tests/ReplyAITests/ChannelStubAuthGateEdgeTests.swift
- Tests/ReplyAITests/NotificationCoordinatorTests.swift
- Tests/ReplyAITests/PromptBuilderTests.swift
- Tests/ReplyAITests/PreferencesTests.swift

No REPs closed (the 22 reroutes are drift-prevention work; the 1 cross-pin is
quality work that doesn't track to a REP).

## Pipeline state at exit

- **Test count**: 1816 (from 1810 — 6 new ChannelTests cross-pin tests)
- **Full-suite run**: 1780 tests pass / 0 fail / 2 skipped under three-skip
  workaround in 16.4s. No regressions across any reroute.
- **BACKLOG**: 18 open, 0 in_progress, 1 blocked (REP-280 superseded). No
  status changes this fire.
- **Wip queue depth**: 1 (the willpresent-cksenderid branch I skipped with
  reason). Will re-evaluate next fire — branch is 2h old, not yet at the 5-day
  archive threshold.
- **Build cache**: WARM throughout the fire (16.4s test runs).
- **Last 3 commits on main**: bb3d2e7, 0a4e723, 1f96cff (all today's autopilot
  reroute work).

## Next-fire hints

The wip branch `wip/autopilot-2026-05-08-0628-willpresent-cksenderid` is still
on origin and still has the test-count regression. If the next fire's Step 1
re-evaluates it: same SKIP applies. It needs human attention to either rebase
onto current main (preserving the 9 pin tests it's removing) or be closed as
redundant if main now contains an equivalent willPresent harmonization.

There's a deep vein of remaining reroute opportunities — I touched 13 test files
this fire. Test files I did NOT touch but that have similar inline-literal
patterns (per quick grep): IMessageChannelTests (SQL fixtures with "iMessage"
service literals — coupling tests to ChatDBRowDecode is debatable),
ShortcutsExportHandlerTests (multiline JSON literals — embedded "me" markers
should stay literal for readability), IMessageSenderTests (AppleScript error
test data with embedded -1708 codes — composing through constants is verbose).
Future fires may find higher-value reroutes elsewhere (e.g. inline channel
raw-values like `"slack"`, `"telegram"` outside the dict-key context I touched).
