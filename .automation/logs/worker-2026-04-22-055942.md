# Worker run: 2026-04-22-055942

## Tasks

- REP-029: IMessageChannel: SQLITE_BUSY graceful retry — **shipped**
- REP-033: SearchIndex: add BM25 ranking tests — **shipped**
- REP-036: IMessageChannel: Message.isRead from chat.db is_read column — **shipped**
- REP-055: IMessageChannel: map message.date_delivered to Message.deliveredAt — **shipped**

## Outcome: shipped to main

All four tasks bundled as a single commit (substantiveness gate: 4×S at same priority).

## File changes

| File | Change |
|------|--------|
| `Sources/ReplyAI/Models/Message.swift` | +12 lines — added `isRead: Bool` and `deliveredAt: Date?` fields with defaulted init params |
| `Sources/ReplyAI/Channels/IMessageChannel.swift` | +30 lines — added `dbOpener` injectable closure, SQLITE_BUSY retry in `openReadOnly()`, projected `is_read`/`date_delivered` in `messages(forThreadID:)` |
| `Tests/ReplyAITests/IMessageChannelTests.swift` | +120 lines — updated both `buildSchema()` helpers to add `date_delivered` column; added `IMessageChannelBusyRetryTests` (2 tests), `IMessageChannelIsReadTests` (1 test), `IMessageChannelDeliveredAtTests` (2 tests) |
| `Tests/ReplyAITests/SearchIndexTests.swift` | +45 lines — added `testExactMatchRanksAbovePartialMatch` and `testResultsOrderedByRelevance` |

## Tests

- Before: 211 tests, 0 failures
- After: 218 tests, 0 failures (+7 new tests)

## Build

`./scripts/build.sh debug` — passed, valid bundle, ad-hoc codesign verified.

## Notes

- `Thread.sleep(forTimeInterval: 0.1)` used for retry delay — acceptable since `openReadOnly()` is called from cooperative-thread async contexts and 100 ms is the specified wait. Not on MainActor.
- `dbOpener` default uses `sqlite3_open_v2` directly; all existing callers receive the default via `init` default parameter, so no call-site changes required.
- Both `buildSchema()` helpers updated to add `date_delivered INTEGER DEFAULT 0`; existing insert helpers use explicit column lists so existing tests are unaffected.
- BM25 ranking tests rely on FTS5's `ORDER BY rank` returning best-first (lower rank value = better). Verified empirically with term-frequency variation across 2–3 documents.
