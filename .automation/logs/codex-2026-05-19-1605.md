# Codex run 2026-05-19 16:05

## Outcome
Main advanced with production-readiness and verification work. All changes were pushed to `origin/main`.

## Commits
- `ac94af2` - REP-XCUI cover first-run onboarding in smoke.
- `3914b34` - Add one-command ReplyAI verification gate.
- `69dc003` - Cap SearchIndex caller-supplied limits.
- `63747e7` - Make SearchIndex initialization Swift 6 clean.
- `55c7305` - Clean InboxViewModel test Swift 6 warnings.

## What changed
- Extended `scripts/smoke-ui.swift` from an open-inbox click check into a first-run flow: force onboarding incomplete, verify all four permission controls, press WelcomeGate Get started, open the inbox, select a thread row, then verify the composer and warm tone pill.
- Added `scripts/verify.sh` as the repeatable local gate: three-skip XCTest invocation, bundled debug build, and AX UI smoke.
- Hardened `SearchIndex.search(query:limit:)` so `limit: -1` can no longer pass through as SQLite's unbounded `LIMIT -1`; negative and oversized limits now resolve to the shipped 50-row cap while `0` still returns no rows.
- Removed a Swift 6 warning in `SearchIndex` initialization by moving SQLite open/schema setup into static helpers.
- Removed Swift 6 warnings from InboxViewModel test mocks by using `Locked<T>` instead of raw `NSLock` in async methods and marking `InboxViewModel` static-key pin tests `@MainActor`.

## Validation
- `swift test --filter SearchIndexTests` -> 68 selected tests / 0 failures.
- `swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` -> 1976 executed / 2 skipped / 0 failures.
- `./scripts/verify.sh` -> passed three-skip XCTest gate, `./scripts/build.sh debug`, and `scripts/smoke-ui.swift`.
- UI smoke result: `smoke-ui: PASS - onboarding, open-inbox click, thread selection, composer, and warm tone verified [Inbox, ReplyAI]`.

## Next
- REP-XCUI-001 still wants a proper XCUITest target; the AX smoke harness now covers the highest-risk flow until that target exists.
- Continue reducing Swift 6 warning debt as it appears in the gate logs.
