# Worker Run: 2026-04-23-130000

## Task
- **REP-129** — SmartRule: `threadNameMatchesRegex(pattern:)` predicate

## Outcome
**blocked** — implementation complete; `swift test` not run due to MLX build time exceeding worker budget on fresh clone

## Branch
`wip/2026-04-23-130000-thread-name-regex`

## File Changes
- `Sources/ReplyAI/Rules/SmartRule.swift` — +8 lines: new `threadNameMatchesRegex(pattern:)` enum case, Kind discriminator `"thread_name_matches_regex"`, decode/encode cases, validatePredicateRegexes pattern match
- `Sources/ReplyAI/Rules/RuleEvaluator.swift` — +7 lines: new `threadDisplayName: String = ""` field on RuleContext, populated in `from(thread:)`, new evaluation case using NSRegularExpression
- `Sources/ReplyAI/Screens/Surfaces/SfcRulesView.swift` — +2 lines: new case in `humanize(predicate:)` returning `"thread name matches /pattern/"`
- `Tests/ReplyAITests/RulesTests.swift` — +56 lines: 4 new tests in `ThreadNameMatchesRegexTests` class + 1 rule added to `testExportImportRoundTripAllPredicateKinds`

## Tests Before/After
- Before: 493 (per AGENTS.md)
- After: 497 (4 new tests added)

## Notes
- Code follows exact same pattern as `textMatchesRegex(pattern:)` predicate
- New `threadDisplayName` field has `= ""` default — zero breaking changes to existing RuleContext call sites
- `validatePredicateRegexes` uses a multi-pattern match to validate both `textMatchesRegex` and `threadNameMatchesRegex` in one case
- Blocker is purely build time on fresh clone (no cached MLX artifacts); human should `swift test` on a warm machine and merge if green
