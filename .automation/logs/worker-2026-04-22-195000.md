# Worker log — 2026-04-22-195000

## Tasks shipped

- REP-116: SmartRule `hasUnread` predicate
- REP-118: DraftEngine evict on archive (dismissHandler)
- REP-119: SearchIndex result cap at 50 (limit default 20→50)
- REP-125: SearchIndex upsert ghost-term test coverage

## Outcome: shipped

## File changes

| File | Change |
|------|--------|
| `Sources/ReplyAI/Rules/SmartRule.swift` | +`case hasUnread`, Codable Kind + encode/decode |
| `Sources/ReplyAI/Rules/RuleEvaluator.swift` | +`unreadCount: Int = 0` on RuleContext, `from(thread:)` populates it, `case .hasUnread` in evaluator |
| `Sources/ReplyAI/Screens/Surfaces/SfcRulesView.swift` | +`case .hasUnread` in `humanize(predicate:)` switch |
| `Sources/ReplyAI/Inbox/InboxViewModel.swift` | +`dismissHandler: ((String) -> Void)?`, wired in `archive(_:)` |
| `Sources/ReplyAI/Search/SearchIndex.swift` | limit default: 20 → 50 in both `search` overloads |
| `Tests/ReplyAITests/RulesTests.swift` | +4 tests (`HasUnreadPredicateTests` class) |
| `Tests/ReplyAITests/InboxViewModelTests.swift` | +1 test (`ArchiveDraftEvictionTests` class) |
| `Tests/ReplyAITests/SearchIndexTests.swift` | +4 tests (cap, below-cap, upsert-old-gone, upsert-new-searchable) |

## Test count

- Before: 340
- After: 349 (+9)

## Notes

- SfcRulesView.swift required `case .hasUnread` in exhaustive switch — not UI-sensitive (string logic only)
- `unreadCount: Int = 0` default avoids breaking existing RuleContext call sites in tests
- SearchIndex limit was already implemented (default 20); changed default to 50 as required
