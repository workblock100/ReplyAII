# Worker run 2026-04-21-181128

## Tasks
- REP-004 — silentlyIgnore action parity (P1/S)
- REP-006 — IMessageSender AppleScript escaping tests (P1/S)
- REP-012 — RulesStore remove/update/resetToSeeds test coverage (P1/S)

Bundled per SUBSTANTIVENESS GATE (top task was S effort; two other S tasks available at same priority).

## Outcome: shipped

## File changes

| File | +/- |
|------|-----|
| Sources/ReplyAI/Inbox/InboxViewModel.swift | +35 / -2 |
| Sources/ReplyAI/MenuBar/MenuBarContent.swift | +1 / -1 |
| Sources/ReplyAI/Channels/IMessageSender.swift | +7 / -3 |
| Tests/ReplyAITests/IMessageSenderTests.swift | +86 / -0 |
| Tests/ReplyAITests/RulesTests.swift | +93 / -0 |
| BACKLOG.md | +6 / -6 |

## Tests

- Before: 77 passing
- After: 98 passing, 0 failures

New tests:
- `testSilentlyIgnoreAndArchiveAreDistinct` — REP-004
- `testMenuBarHidesSilentlyIgnored` — REP-004
- `testEscapePlainTextUnchanged`, `testEscapeEmptyString`, `testEscapeDoubleQuote`, `testEscapeBackslash`, `testEscapeBackslashBeforeQuote`, `testEscapeBackticksUnchanged`, `testEscapeShellInterpolationAttempt`, `testEscapeNewlineInMessage`, `testEscapeNullByte`, `testEscapeZeroWidthChars`, `testEscapeEmojiZWJSequence`, `testEscapeMixedScripts`, `testEscapeVeryLongString`, `testEscapeMultipleConsecutiveQuotes`, `testEscapeControlCharacters` — REP-006
- `testRemoveRulePersistsToDisk`, `testUpdateRulePersists`, `testResetToSeedsRestoresDefaults`, `testRemoveNonExistentUUIDIsNoOp` — REP-012

## Notes

REP-004: `silentlyIgnoredThreadIDs` is a distinct `Set<String>` from `archivedThreadIDs`. The `.silentlyIgnore` rule action now writes ONLY to `silentlyIgnoredThreadIDs` (not `archivedThreadIDs`). `InboxViewModel.menuBarWaitingThreads` filters both archived (threads can still appear if unread) and silently-ignored (completely hidden). `MenuBarContent.waitingThreads` delegates to this computed property.

REP-006: `escape(_:)` (private) is now a one-liner wrapper around the new `escapeForAppleScriptLiteral(_:)` (internal), so all existing behavior is preserved while tests can reach the escape logic directly.

REP-012: Tests mirror the `testStoreRoundTripsAddedRule` pattern — each test creates a unique temp URL so there's no shared state between cases.
