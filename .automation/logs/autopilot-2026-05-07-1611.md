# Autopilot fire — 2026-05-07-1611

**Time used:** ~43 min — fire complete (action loop, then log)

## Pre-fire state

- wip queue: 0 branches
- BACKLOG: 18 open, 1 blocked, 0 in_progress, no STOP marker
- build cache: WARM (.build/ < 6h)
- last main sha: `af26b63` (prior fire's log v2)
- workaround: `--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` (1523 tests pass in ~15.6s warm)

## Actions this fire

1. **P5 hardening** — `MessagesAppActivationObserver.defaultDebounce = 0.6` hoisted; the no-arg-debounce production init at `InboxViewModel.activationObserver` site now routes through `Self.defaultDebounce`. New pin asserts both the literal and that the no-arg init plumbs through it (catches dead-constant + lingering-literal failure mode) — main `3e6bf64`

2. **P5 hardening** — `ChatDBWatcher.defaultDebounce = 0.6` and `defaultRestartDelay = 5.0` hoisted; init defaults route through them. Two pin sets cover the constants and the no-arg-init plumbing — main `01997ad`

3. **P5 hardening** — `MLXDraftService.defaultModelID = "mlx-community/Llama-3.2-3B-Instruct-4bit"` hoisted; new `MLXDraftServiceTests.swift` (2 cases) pins the slug + that the no-arg init routes through the constant. Drift here re-downloads ~2 GB of weights for every shipped user — main `72fd354`

4. **P5 hardening** — `DraftStore.defaultPruneDays = 7` hoisted; pin asserts the constant + that the no-`days`-arg `pruneStale()` call routes through it via a backdated-mtime sweep — main `d2447c1`

5. **P8 audit** — Mid-fire AGENTS.md test count refresh (1557 → 1565, 1521 → 1529 passing) — main `f6019ff`

6. **P5 hardening** — `SlackOAuthFlow.redirectURI = "http://localhost:4242/callback"` hoisted; the auth-URL `URLQueryItem` leg and the token-exchange POST-body leg now share a single constant. Pin asserts the literal AND that the URI's port suffix matches `LocalhostOAuthListener.defaultPort` (cross-module port-coupling guard) — main `484ad93`

7. **P5 hardening** — `IMessageSender.tccDeniedErrorCode = -1743` and `eventNotHandledErrorCode = -1708` hoisted from the inline error classifier. The retry-classifier substring now builds from the constant so drift can't desync the classifier from the formatter. Two pins cover the codes + the formatted substring shape — main `a606cf0`

8. **P5 hardening** — `RulesStore.exportVersion` constant pinned directly. The export integration test asserted the wire-format literal `1` only; the static-constant pin catches the combined-edit failure mode (bumping the constant + updating the fixture in one diff) — main `d5ba6a9`

9. **P5 hardening** — `LocalhostOAuthListener.maxRequestBytes = 8192` hoisted from the inline `maximumLength: 8192` in `connection.receive`. New pin catches drift in either direction (truncated codes / memory-pinning attack) — main `eddfd08`

10. **P5 hardening** — `ContactsResolver.defaultTTL = 1800` hoisted; pin lives in a new `ContactsResolverDefaultPinTests` (deliberately non-substring of the skipped `ContactsResolverTests`) so it runs under the autopilot's three-skip workaround — main `d4253ba`

11. **P5 hardening** — `PreferenceRange.maxVoiceExamples = 20` and `maxVoiceExampleLength = 500` hoisted; setter now references the constants. Tests previously pinned the literals via behavior; new pin ties the named constants to those literals so a refactor can't desync them — main `9ca382d`

12. **P5 hardening** — Added `AppleScriptMessageReader` script-structure pins for both `recentChats()` and `messagesForChat(chatGUID:limit:)`. Captures the script source via the executor closure and asserts the production invariants (`tell application "Messages"` opener, `every chat`/`first chat whose id` selectors, `||` delimiter, GUID embedding). Companion to the existing `IMessageSenderAppleScriptTemplateTests` for the send-side template — main `b1f4c64`

13. **P5 hardening** — `InboxViewModel.perThreadMessageLoadLimit = 40` hoisted; both `syncFromIMessage` focus-preload and `loadMessages(for:)` route through it. Pin lives in a new `InboxViewModelLoadLimitPinTests` (non-substring of skipped suites) — main `69f30d4`

14. **P5 hardening** — `SlackChannel.maxAPILimit = 200` and `minAPILimit = 1` hoisted from the duplicated `min(max(limit, 1), 200)` clamp expression; both `recentThreads` and `messages` route through the constants. Existing literal pins covered behavior; named-constant pin catches the combined-edit failure mode — main `e5db0f0`

15. **P5 hardening** — `KeychainHelper.accountPrefix = "ReplyAI-"` hoisted; the three duplicate sites in `set/get/delete` now route through the single constant. Pin catches the partial-rename failure mode that would orphan every shipped user's stored channel tokens — main `4c5567c`

16. **P8 audit** — End-of-fire AGENTS.md test count refresh (1565 → 1577, 1529 → 1541 passing) — main `828d564`

17. **P5 hardening** — `MessagesAppActivationObserver.messagesAppBundleID = "com.apple.MobileSMS"` hoisted; both this observer and `AccessibilityAPIReader.pidProvider` now reference the single constant. Cross-file invariant pin catches drift on Apple's published bundle ID — main `fb2a570`

## Files touched

Source:
- `Sources/ReplyAI/Channels/MessagesAppActivationObserver.swift` (defaultDebounce + messagesAppBundleID constants)
- `Sources/ReplyAI/Channels/ChatDBWatcher.swift` (defaultDebounce + defaultRestartDelay constants)
- `Sources/ReplyAI/Channels/IMessageSender.swift` (TCC + errAEEventNotHandled error codes)
- `Sources/ReplyAI/Channels/SlackOAuthFlow.swift` (redirectURI single source of truth)
- `Sources/ReplyAI/Channels/LocalhostOAuthListener.swift` (maxRequestBytes)
- `Sources/ReplyAI/Channels/ContactsResolver.swift` (defaultTTL)
- `Sources/ReplyAI/Channels/SlackChannel.swift` (maxAPILimit + minAPILimit)
- `Sources/ReplyAI/Channels/KeychainHelper.swift` (accountPrefix)
- `Sources/ReplyAI/Channels/AccessibilityAPIReader.swift` (route through messagesAppBundleID)
- `Sources/ReplyAI/Services/MLXDraftService.swift` (defaultModelID)
- `Sources/ReplyAI/Services/DraftStore.swift` (defaultPruneDays)
- `Sources/ReplyAI/Services/Preferences.swift` (maxVoiceExamples + maxVoiceExampleLength on PreferenceRange)
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` (perThreadMessageLoadLimit)

Tests (new pin coverage):
- `Tests/ReplyAITests/MessagesAppActivationObserverTests.swift` (+2)
- `Tests/ReplyAITests/ChatDBWatcherTests.swift` (+2)
- `Tests/ReplyAITests/MLXDraftServiceTests.swift` (NEW, +2)
- `Tests/ReplyAITests/DraftStoreTests.swift` (+1)
- `Tests/ReplyAITests/SlackOAuthFlowTests.swift` (+1)
- `Tests/ReplyAITests/IMessageSenderErrorDescriptionTests.swift` (+2)
- `Tests/ReplyAITests/RulesTests.swift` (+1)
- `Tests/ReplyAITests/LocalhostOAuthListenerTests.swift` (+1)
- `Tests/ReplyAITests/ContactsResolverDefaultPinTests.swift` (NEW, +1)
- `Tests/ReplyAITests/PreferencesTests.swift` (+1)
- `Tests/ReplyAITests/AppleScriptMessageReaderTests.swift` (+2)
- `Tests/ReplyAITests/InboxViewModelLoadLimitPinTests.swift` (NEW, +1)
- `Tests/ReplyAITests/SlackChannelTests.swift` (+1)
- `Tests/ReplyAITests/KeychainHelperTests.swift` (+1)

Docs:
- `AGENTS.md` (test count refresh, twice)

## Suite state

- Before: 1523 tests pass under workaround (~15.6s warm)
- After:  1542 tests pass under workaround (~15.4s warm)
- Total tests in tree: 1577 (was 1559)
- Net +19 passing tests across 17 commits

Two-skip form (`ContactsResolverTests` + `InboxViewModelIsSyncingTests`) still
hangs in this fire — three-skip remains the validated form (consistent with
AGENTS.md gotcha #243).

## Pipeline state at exit

- main HEAD: `fb2a570`
- wip queue: 0 (drained)
- BACKLOG: 18 open, 1 blocked, 0 in_progress (unchanged this fire — autopilot focused on P5 hardening, no P2 ships)
- build cache: WARM
- last 3 commits on main:
  - `fb2a570` messages-bundle-id: hoist messagesAppBundleID + cross-file pin
  - `828d564` docs: refresh AGENTS.md test count to 1577 / 1541
  - `4c5567c` keychain: hoist accountPrefix + pin

## Next-fire hints

No structural state to carry over — every commit landed cleanly on main, no
in-flight wip branches, no half-applied refactors. The constant-hoisting
pattern still has runway: candidate sites observed-but-not-yet-touched include
`Theme.Color.*` token raw values (UI-sensitive — wip-only), `SearchIndex`
FTS5 schema string, `IMessageChannel` chat.db SQL queries, `Stats` Snapshot
CodingKeys.
