# autopilot fire — 2026-05-19 17:43 EDT

Manual fire (Elijah's web Claude budget was exhausted; cron'd scheduled tasks remain disabled). Goal: keep ReplyAI moving toward complete-shippable. Operating loop: audit current state, ship next highest-leverage production-readiness work in small verified increments. Three commits to main, all pushed to `origin/main`.

## actions

1. **Audit** — confirmed `Tests/ReplyAITests/ContactsResolverTests.swift:15-20` already has `XCTSkipIf(CNContactStore.authorizationStatus(for: .contacts) != .authorized)` in setUpWithError (per the prior fire's groundwork on REP-009/REP-ALERT-260506-2010). BACKLOG state: 3 open (REP-XCUI-001 M-L XCUITest target, REP-043 sync error state, REP-062 human-only product copy), 1 in_progress (REP-UI-STR-HOIST-001). Distribution scripts referenced from RELEASE.md (`scripts/notarize.sh`, `scripts/create-dmg.sh`) did not exist. No CONTRIBUTING.md.

2. **scripts/create-dmg.sh** — authored. Uses `hdiutil create -format UDZO` (no Homebrew dep). Validates bundle exists + is codesigned, stages via `ditto` to preserve xattrs, drops the conventional `/Applications` symlink for drag-install. Tested end-to-end: produced a 21 MB `build/ReplyAI.dmg` from the current ad-hoc bundle.

3. **scripts/notarize.sh** — authored. Validates the bundle is signed by `Developer ID Application:` (rejects ad-hoc), accepts credentials via env (`APPLE_ID` / `APPLE_TEAM_ID` / `APPLE_APP_SPECIFIC_PASSWORD`) or `--keychain-profile NAME`. Zips with `ditto -c -k --keepParent`, submits via `xcrun notarytool submit --wait`, parses status, staples on success, runs final `spctl --assess`. Tested against the current ad-hoc bundle: exits 1 with `unexpected signing authority: ReplyAI Dev → notarization expects 'Developer ID Application:'` — the desired clean-error behavior.

4. **RELEASE.md** — updated Step 3/4 of the "next tier" section to reference the shipped scripts directly, with the one-time `xcrun notarytool store-credentials` setup block. The mechanical post-enrollment work is now: edit one signing identity in `build.sh` → run `./scripts/build.sh release` → run `./scripts/notarize.sh` → run `./scripts/create-dmg.sh`. No further scripts need to be written.

5. **CONTRIBUTING.md** — authored (121 lines). Surfaces the 3-skip Swift test gate (`ContactsResolverTests`, `InboxViewModelIsSyncingTests`, `InboxViewModelTests`) with explicit rationale per suite, `verify.sh` as the one-command canonical gate, branch strategy (`wip/*` for UI-sensitive + ff-only main), commit format, and a table of file/directory ownership. New contributors no longer need to read 376 lines of AGENTS.md before they can ship a change.

6. **README.md refresh** — dropped the stale "MLX SPM split pending" line (the split landed in REP-501→REP-505, shipped 2026-05-19), dropped the hardcoded `cd ~/Code/ReplyAI` (clones don't all live there), added the `verify.sh` one-stop gate in the Build section, cross-linked CONTRIBUTING.md.

7. **scripts/smoke-ui.swift** — extended. Previously only verified the warm tone pill rendered after thread selection; now loops over all 3 (`warm`, `direct`, `playful`) so a regression that hides two pills doesn't slip through. The toneIDs ordering matches `Tone.allCases` (pinned at `FolderToneTests.swift:81`), so a case rename surfaces in the pin test first and the smoke list gets updated alongside.

8. **verify.sh re-validation** — end-to-end clean. Test gate: 1976 tests / 2 skipped / 0 failures / 16.07s. Debug build: clean, 5.71s, codesign valid + satisfies Designated Requirement. UI smoke: PASS (onboarding → open-inbox → thread selection → composer → all 3 tone pills).

## commits

- `c3e4db2` distribution: scripts/create-dmg.sh + scripts/notarize.sh
- `b2bb074` docs: CONTRIBUTING.md + README refresh
- `b1fd7ee` smoke-ui: verify all 3 tone pills, not just warm

All three pushed to `origin/main`. Zero rebases, zero force pushes.

## pipeline state at exit

- main HEAD: `b1fd7ee`
- open BACKLOG: 3 (REP-XCUI-001 / REP-043 / REP-062)
- in_progress BACKLOG: 1 (REP-UI-STR-HOIST-001, autopilot owner — `wip/autopilot-2026-05-09-1011-menubar-strings-hoist` merged; next view candidate not picked yet)
- wip queue: none (last wip `wip/autopilot-2026-05-09-1011-menubar-strings-hoist` merged on 2026-05-18-1628 fire)
- build cache: WARM (1.9s incremental swift build, .build/ touched within the hour)
- verify.sh status: green
- distribution mechanics: scripts present + tested; remaining work is Elijah-side (Developer Program enrollment $99, credential generation, then mechanical sign+notarize+dmg run)

## next-fire hints

- The XCUITest target proper (REP-XCUI-001) is the next engineering item worth attacking, but SwiftPM has limited first-class XCUITest support — likely needs an `.xcodeproj` with a UI Testing target, or a custom approach. The AX-harness `smoke-ui.swift` covers ~80% of what an XCUITest target would. Could be the right move to mark REP-XCUI-001 as `done_by_alternative` and explicitly accept the AX harness as the canonical solution.
- REP-UI-STR-HOIST-001 has 4 more views to attack toward its 5-view goal. Next candidate noted in the prior fire log: `SidebarView` (heavy brand-glyph + sync-chip reuse) or `OnboardingStage` (welcome copy is high-leverage for the channel-agnostic pivot).
- App icon at production resolutions is the cheapest remaining blocked-on-Elijah item if he wants to unblock launch assets — see RELEASE.md "What's blocked on Elijah specifically."
