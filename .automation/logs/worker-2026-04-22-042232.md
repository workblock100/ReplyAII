# Worker run: worker-2026-04-22-042232

**Date**: 2026-04-22  
**Model**: claude-sonnet-4-6  
**Outcome**: shipped

## Tasks closed

- **REP-034** — DraftEngine: draft cache eviction for idle entries (effort: S)
- **REP-056** — Stats: weekly aggregate file writer (effort: S)
- **REP-057** — SearchIndex: concurrent search + upsert stress test (effort: S)

Bundled per SUBSTANTIVENESS GATE (three S-effort tasks, other S/M work available at same priority).

## File changes

| File | Change |
|------|--------|
| `Sources/ReplyAI/Services/DraftEngine.swift` | +16 lines — `evict(threadID:)` + `cacheSize` property |
| `Sources/ReplyAI/Inbox/InboxScreen.swift` | +1 line — `.onChange` to call `engine.evict` on thread deselection |
| `Sources/ReplyAI/Services/Stats.swift` | +30 lines — `writeWeeklyLog(to:)` method |
| `Tests/ReplyAITests/DraftEngineTests.swift` | +38 lines — 2 new eviction tests |
| `Tests/ReplyAITests/StatsTests.swift` | +38 lines — 3 new weekly log tests |
| `Tests/ReplyAITests/SearchIndexTests.swift` | +38 lines — 1 concurrent stress test |
| `BACKLOG.md` | status updates for REP-034, REP-056, REP-057 |

## Tests

- Before: 199 `func test` declarations
- After: 206 `func test` declarations (+7)
- `swift test`: all green

## Notes

- REP-034: `evict` wired in `InboxScreen.swift` via `.onChange(of: model.selectedThreadID)` — fires with the old ID before the new selection takes effect, so the evicted thread is always the one being navigated away from.
- REP-056: `writeWeeklyLog` emits all counter keys even when zero — planner scripts can rely on a stable schema.
- REP-057: `SearchIndex` is a Swift actor so concurrency safety is guaranteed by the actor executor. The stress test verifies 100 concurrent `withTaskGroup` tasks drain cleanly with deterministic results.
