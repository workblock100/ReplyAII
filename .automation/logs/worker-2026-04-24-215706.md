# Worker log — 2026-04-24-215706

## Tasks claimed
- REP-190 (P2, S) — InboxViewModel: thread sort stability, same-timestamp threads
- REP-210 (P2, S) — IMessageSender: combined newline + backslash escaping + tab
- REP-212 (P2, S) — InboxViewModel: selectThread seeds userEdits from DraftStore
- REP-215 (P2, S) — SmartRule: validateRegex empty pattern + Python named-group syntax

## Outcome
**pushed to wip** — MLX cold build on fresh clone would exceed 13-min budget (`.build/` absent, `SWIFTPM_BUILD_PATH` unset). All P0/P1 open tasks either effort=L (REP-500, REP-285), superseded (REP-280), or have unmerged wip prereqs (REP-272 needs REP-266 merged; REP-257 needs REP-237+REP-242). Substantiveness gate: 4 bundled S-effort P2 tasks = 8 new tests.

## Branch
`wip/2026-04-24-215706-test-bundle-sort-escape-draftstore-regex`

## File changes
| File | Lines added |
|------|-------------|
| `Tests/ReplyAITests/IMessageSenderTests.swift` | +25 |
| `Tests/ReplyAITests/RulesTests.swift` | +22 |
| `Tests/ReplyAITests/InboxViewModelTests.swift` | +78 |
| `BACKLOG.md` | +8 (status updates) |

## Tests before → estimated after
527+ → ~535 (8 new tests)

## New tests
- `IMessageSenderCombinedEscapeTests.testNewlineAndBackslashBothEscapedInAppleScript` — verifies `\n → \\n` and `\\ → \\\\` in the same escape pass
- `IMessageSenderCombinedEscapeTests.testTabCharacterPassesThroughUnescaped` — tab is legal in AppleScript strings and must not be escaped
- `ValidateRegexBoundaryCasesTests.testEmptyPatternAccepted` — `""` is a valid ICU regex (match-all)
- `ValidateRegexBoundaryCasesTests.testPythonNamedGroupSyntaxThrows` — `(?P<name>x)` is PCRE-only; ICU (NSRegularExpression) must reject it
- `InboxViewModelOrderingTests.testEqualTimestampThreadsSortStably` — sync twice, threads at equal effective position maintain order
- `InboxViewModelOrderingTests.testEqualTimestampSortPreservesChannelOrder` — three syncs, channel-input order [A,B] preserved
- `InboxViewModelDraftStoreSeedTests.testSelectThreadSeedsUserEditsFromDraftStore` — stored draft appears in userEdits after selectThread
- `InboxViewModelDraftStoreSeedTests.testSelectThreadWithNoStoredDraftLeavesUserEditsEmpty` — no stored draft → userEdits key absent

## Notes
- No production code changes — all new tests exercise existing behavior
- REP-215 boundary: `(?P<name>x)` is expected to throw because ICU uses `(?<name>...)` syntax, not `(?P<name>...)`; if this assumption is wrong the test will fail and the human reviewer should decide whether to relax it or leave it as documentation
- Human: run `swift test` and merge if green; mark REP-190, REP-210, REP-212, REP-215 done in BACKLOG
