# Worker run: 2026-04-22-130300

## Tasks
- REP-053: InboxViewModel archive/unarchive round-trip tests
- REP-061: AttributedBodyDecoder fuzz test (10k random blobs)
- REP-084: IMessageChannel NULL message.text + both-NULL test coverage
- REP-093: IMessageSender — remove isDryRun, add dryRunHook()
- REP-094: Stats rulesMatchedCount counter

## Outcome: shipped

## File changes
- `Sources/ReplyAI/Channels/IMessageSender.swift`: removed `isDryRun` static var, added `dryRunHook()` factory, removed `if isDryRun { return }` guard (+2/-5)
- `Sources/ReplyAI/Services/Stats.swift`: added `rulesMatchedCount: Int` to Snapshot, added `incrementRulesMatched()` method (+10/-0)
- `Sources/ReplyAI/Inbox/InboxViewModel.swift`: added `incrementRulesMatched()` calls in 3 rule-matching sites; made archive/silentlyIgnored/lastSeenRowID load+save use injected `defaults` instead of `UserDefaults.standard` (+10/-10)
- `Tests/ReplyAITests/IMessageSenderTests.swift`: replaced `isDryRun`-based tests with `dryRunHook()`, added `testDryRunHookReturnsSuccessWithoutScript`, `testCustomHookIsInvokedOnSend` (+28/-22)
- `Tests/ReplyAITests/IMessageChannelTests.swift`: added `testBothNullProducesEmptyMessage`, `insertMessageBothNull` helper (+30/-0)
- `Tests/ReplyAITests/AttributedBodyDecoderTests.swift`: added `testFuzzRandomBlobsNeverCrash` (10k random blobs) (+25/-0)
- `Tests/ReplyAITests/InboxViewModelTests.swift`: added 3 archive/unarchive tests with isolated UserDefaults (+45/-0)
- `Tests/ReplyAITests/StatsTests.swift`: added 3 rulesMatchedCount tests (+25/-0)

## Tests
- Before: 254 passing
- After: 278 passing, 0 failures

## Notes
- REP-084 already had `testNullTextFallsBackToAttributedBody` (added previously); only `testBothNullProducesEmptyMessage` was missing. The both-NULL message is filtered at SQL level (`m.text IS NOT NULL OR m.attributedBody IS NOT NULL`), so the test verifies the real message wins preview when a both-NULL row exists as the "newer" one.
- The InboxViewModel persistence fix (defaults injection) was needed for REP-053's `testArchivedIDsPersist` — the static save/load methods previously hardcoded `UserDefaults.standard`, making isolation impossible.
