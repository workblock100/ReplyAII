# Planner log — 2026-04-21 (second run, post-worker)

status: OK

## Context

Second planner run today. The first planner run (`62e23ba`) seeded REP-011/012/013 and brought the backlog to 13 open tasks. The reviewer (`295490a`) filed its first 5/5 weekly assessment. Then the worker ran (`753d880`) and shipped REP-001 (persist lastSeenRowID) and REP-002 (SmartRule priority + conflict resolution) in a single bundled session, growing the test suite from 55 to 60.

This run's job: archive the two completed tasks (already reflected in BACKLOG.md by the worker's commit), verify halt conditions, and identify gaps worth filling.

## Halt-condition check

- Last commit is `753d880` (worker ship — "persist lastSeenRowID; add SmartRule priority field") — not a revert. ✓
- Test count: 60 (up from 55 pre-worker). No shrinkage. ✓  
  Breakdown by file: DraftEngineTests (4), FixturesTests (7), IMessageSenderTests (5), LLMServiceTests (5), RulesTests (24), ScreenInventoryTests (6), SearchIndexTests (9). Worker added 5 new tests for REP-001/002.
- No swift test run possible in this sandbox. Last known state: all green (worker log confirms).
- No wip/ branches. ✓
- No binary blobs added. ✓

No halt. Proceeding.

## Archive verification

REP-001 and REP-002 are already in the `## Done / archived` section of BACKLOG.md — the worker updated BACKLOG.md in commit `753d880`. No further archiving needed.

## BACKLOG.md changes

### Nothing archived this run
Worker already archived REP-001 and REP-002. Current open count was 11 (REP-003 through REP-013).

### 2 tasks added (REP-014, REP-015)

Bringing total to 13 open tasks. Chosen to address two concrete gaps:

**REP-014** (P1, M) — `IMessageChannel` SQL + date-autodetect unit tests  
The reviewer flagged IMessageChannel as having no unit tests (read-only IMessageChannel entry in the "Gaps" section of review-2026-04-21.md). The SQL query logic and `secondsSinceReferenceDate` magnitude detection are both subtle and untested. Using in-memory SQLite keeps this fully hermetic. Making `secondsSinceReferenceDate` internal (from private) is a minimal, safe change to enable testability.

**REP-015** (P1, M) — SearchIndex incremental upsert  
AGENTS.md explicitly calls out: "If the rebuild ever gets expensive (thousands of threads), switch to incremental upserts keyed by (thread_id, message_rowid)." The watcher fires on every incoming message, currently triggering a full rebuild. Adding `upsert(thread:)` is a well-scoped performance improvement with clear success criteria and no user-visible changes.

### Tasks considered but not added

- **UNNotification inline reply** — real complexity; needs UNNotificationExtensionUI entitlement + potential sandbox concerns. Worth a separate scoped task once the test coverage backlog clears. Will add next planner run.
- **Sidebar relative-time chip auto-tick** — small effort (Timer publisher), but touches SwiftUI view code. Reasonable to add; skipping this run to keep backlog balanced.
- **Stats surfacing in set-privacy** — the REP-005 task explicitly defers UI to a follow-up. Queue it once REP-005 ships.
- **wip/ branch count** — 0 open wip/ branches. No reminders needed.

## Backlog distribution post-changes

13 tasks open (1 P0, 9 P1, 3 P2):
- Backend/logic (non-ui-sensitive): REP-003, REP-004, REP-005, REP-008, REP-015 = 5 (38%)
- Test coverage (non-ui-sensitive): REP-006, REP-007, REP-011, REP-012, REP-013, REP-014 = 6 (46%)
- UI-sensitive: REP-009, REP-010 = 2 (15%)

Total non-ui-sensitive: 11/13 (85%) — comfortably above the 90% (60+30) target for auto-merge eligibility.

## AGENTS.md changes

- Added `753d880` to the top of the "What's done" commit list with worker attribution.
- Updated XCTest count from 55 to 60.

## Notes for the reviewer

- Worker output quality matches the human-commit quality bar set in week 1. REP-001 used the exact same `didSet`/init hydration pattern as `archivedThreadIDs`. REP-002 used `decodeIfPresent` for backwards-compatible Codable — a thoughtful choice. No concerns.
- REP-003 (better AttributedBodyDecoder) remains P0. It's the highest-impact open task but effort L. The worker should pick REP-004 (S effort, P1) or REP-012 (S effort, P1) next as quick wins before tackling REP-003.
- Automation heartbeat is working: planner logged on 2026-04-21, worker ran same day, reviewer ran same day. Pipeline is healthy.
