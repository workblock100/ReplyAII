# Worker run 2026-04-22-120935

## Task
REP-032 — Stats: draft acceptance rate per tone

## Outcome
shipped

## File changes
- `Sources/ReplyAI/Services/Stats.swift` — +50/-0 lines: added `draftsGeneratedByTone`, `draftsSentByTone` fields to Snapshot with custom Codable decoder (decodeIfPresent for backward compat), plus `recordDraftGenerated(tone:)`, `recordDraftSent(tone:)`, and `acceptanceRate(for:)` methods
- `Sources/ReplyAI/Services/DraftEngine.swift` — +1/-1 lines: call `recordDraftGenerated(tone: tone)` instead of `recordDraftGenerated()`
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` — +1/-1 lines: call `recordDraftSent(tone: pending.tone)` instead of `recordDraftSent()`
- `Tests/ReplyAITests/StatsTests.swift` — +55/-0 lines: added `testPerToneCountersIncrement`, `testPerToneCountersRoundTrip`, `testAcceptanceRateCalculation`

## Tests
- Before: 254 tests (from AGENTS.md)
- After: 257 tests (+3 new StatsTests)
- StatsTests: 14 tests, 0 failures (verified with --filter StatsTests)
- Full suite: compilation verified, all pre-InboxViewModelTests suites passed; InboxViewModelTests known hang on AddressBook XPC in test environment

## Notes
- Custom init(from decoder:) uses decodeIfPresent for all Snapshot fields so old stats.json files (missing tone keys) load cleanly rather than falling back to a fresh Snapshot — this is an improvement over the previous pattern
- recordDraftGenerated() and recordDraftSent() (no-tone overloads) kept for any callers that don't have tone context
