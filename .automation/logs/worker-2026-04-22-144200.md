# worker-2026-04-22-144200

## Task IDs
- REP-041 (effort M): SearchIndex: persist FTS5 index to disk between launches
- REP-073 (effort S): PromptBuilder: verify most-recent-message invariant + short-thread passthrough test

## Outcome
**shipped** — committed to main.

## File changes
| File | Change |
|------|--------|
| `Sources/ReplyAI/Search/SearchIndex.swift` | +25/-6: added `init(databaseURL:)`, `productionDatabaseURL()`, refactored `open(url:)` |
| `Sources/ReplyAI/Inbox/InboxViewModel.swift` | +1/-1: use `productionDatabaseURL()` in production |
| `Sources/ReplyAI/Services/PromptBuilder.swift` | +2/-2: `truncate` visibility private→internal; added `budget` param |
| `Tests/ReplyAITests/SearchIndexTests.swift` | +32/-0: `testPersistenceAcrossReopens`, `testInMemoryIndexUsedWhenURLIsNil` |
| `Tests/ReplyAITests/PromptBuilderTests.swift` | +20/-0: `testShortHistoryPassesThroughUnchanged`, `testMostRecentMessageAlwaysRetained` |

## Tests
- Before: 290
- After: 294 (all passing)
- `swift test`: ✅ 294 tests, 0 failures, 9.7s

## Notes
- Bundled REP-073 (S) alongside REP-041 (M) per substantiveness gate intent; REP-073 was trivial (access-level bump + 2 test cases).
- Background `swift test` runs hung on AddressBook CoreData XPC errors — pre-existing environmental issue (macOS AddressBook migration error, not triggered by code changes). Final test run used perl timeout wrapper; passed on a clean attempt after clearing stuck processes.
- `testPersistenceAcrossReopens`: creates file-backed index in tmp dir, upserts a message, deinits, reopens same file, verifies row still searchable.
- `testInMemoryIndexUsedWhenURLIsNil`: confirms backward-compatible behavior with `nil` URL.
