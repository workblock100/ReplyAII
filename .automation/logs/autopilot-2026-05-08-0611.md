# Autopilot fire 2026-05-08-0611

**Time used**: 32 min — 8 actions (7 to main, 1 to wip)

## Actions this fire

1. **P8 doc-sync** → main `ae486fb`
   - AGENTS.md test-count refresh: 1796 → 1800 (1764 pass under three-skip
     workaround). Drift caught during fire-start survey vs the 0411 fire's
     header.

2. **P5 cross-vocabulary pin** → main `85f209e`
   - `stats-rule-action: pin cross-vocabulary divergence vs rules.json snake_case`
   - `Stats.RuleAction` (camelCase: `silentlyIgnore`/`markDone`/`setDefaultTone`)
     diverges intentionally from `RuleAction` Codable's snake_case form
     (`silently_ignore`/`mark_done`/`set_default_tone`). Both vocabularies
     individually pinned but no test asserted the divergence — a future
     "consistency fix" would have passed both pins while orphaning every
     shipped user's `stats.json`. New test
     `testStatsRuleActionConstantsDivergeFromRulesJSONSnakeCase` pins both
     the inequality (3 actions) and the accidental equality (2 single-word
     actions). Doc comment on `Stats.RuleAction` now flags the divergence.

3. **P7 doc-comment fix** → main `bf71943`
   - `notifications: document two-contract divergence between willPresent and parser`
   - The willPresent inline comment claimed "Prefer the explicit sender key
     iMessage/CKSenderID sets" but the code only read `userInfo[sender]`,
     skipping `userInfo[ckSenderID]` entirely. The parser's mirror
     divergence note also only documented one of two contracts (empty
     CKChatIdentifier handling). This commit:
     * Rewrites the willPresent comment to describe what the code
       actually does.
     * Expands the parser's divergence note to call out both contracts
       (sender-key three-step path + empty-CKChatIdentifier handling).

4. **P0/P4 wip — behavioral fix** → `wip/autopilot-2026-05-08-0628-willpresent-cksenderid` `f7ea88e`
   - `notifications: align willPresent sender resolution with parser (CKSenderID first)`
   - Real fix for the divergence documented in action 3. willPresent now
     reads `ckSenderID` → `sender` → `title` (three steps, mirroring the
     parser). Modern iMessage/Continuity payloads populate CKSenderID but
     not always sender, so willPresent on those previously fell through to
     the title (contact display name) instead of returning the raw handle.
     `applyIncomingNotification`'s chatGUID-suffix match is the fallback
     that worked when chatGUID was present; absent chatGUID + non-matching
     title left those notifications unmatched. Parser divergence note
     updated on the same branch from "two contracts" back to "one
     contract" (empty CKChatIdentifier still differs). Pushed to wip
     because it's a behavioral change affecting real iMessage notifications.

5. **P5 cross-file divergence pin** → main `1daca30`
   - `stats: hoist writeQueue label + pin com.replyai vs co.replyai prefix divergence`
   - `Stats.writeQueue`'s queue label `"com.replyai.stats.write"` was the
     only inline queue/notification/keychain literal using the Java-style
     `com.replyai.` prefix; every sibling uses `co.replyai.`
     (ChatDBWatcher, MessagesAppActivationObserver, KeychainHelper,
     GlobalHotkey). Hoisted the label to `Stats.writeQueueLabel` static
     constant + added two pins: `testWriteQueueLabelIsFrozen` (literal
     value) and `testWriteQueueLabelDivergesFromCoReplyAIPrefix`
     (cross-file inequality with sibling labels). Harmonization wasn't
     attempted because external observability tooling may key off the
     literal `com.replyai.stats.write`.

6. **P5 cross-file equality pin** → main `2f0ae70`
   - `imessage: cross-pin serviceFallback equals IMessageSender service IDs`
   - `IMessageChannel.ChatDBRowDecode.serviceFallback` ("iMessage") and
     `IMessageSender.iMessageServiceID` ("iMessage") both individually
     pinned but no cross-pin asserted their equality. They name the same
     Messages.app service convention used in adjacent code paths
     (chat.db row decoder + AppleScript GUID synthesizer). Drift would
     mean we read rows tagged "iMessage" but emit chat GUIDs tagged with
     a different name; every send would fail at the production AppleScript
     site rather than surface immediately. Added two cross-file pins:
     `testServiceFallbackEqualsIMessageSenderServiceID` (exact equality)
     and `testSMSServiceLowercaseEqualsIMessageSenderSMSServiceIDLowercased`
     (case-insensitive equality — casing differs intentionally between
     the chat.db lowercased classification key "sms" and the AppleScript
     canonical-cased identifier "SMS").

7. **P8 doc-sync (mid-fire)** → main `66b672b` (with this log)
   - AGENTS.md test-count refresh: 1800 → 1805 (1769 pass under three-skip
     workaround). Reflects actions 2/5/6 each adding test cases.

8. **P5 three-way cross-file pin (Slack OAuth + HTTP)** → main `231fd9b`
   - `slack: cross-pin three duplicate OAuth + HTTP literals across modules`
   - Three duplicate-value pairs across Slack-related files were
     individually pinned but no test asserted their cross-file equality:
     * `LocalhostOAuthListener.codeQueryParameterName` ("code") ↔
       `SlackOAuthFlow.FormField.code` ("code") — OAuth authorization-code
       identifier.
     * `URLSessionSlackClient.postHTTPMethod` ("POST") ↔
       `SlackOAuthFlow.tokenExchangeHTTPMethod` ("POST") — HTTP verb for
       chat.postMessage and oauth.v2.access.
     * `URLSessionSlackClient.Header.contentTypeField` ("Content-Type") ↔
       `SlackOAuthFlow.contentTypeHeaderField` ("Content-Type") — standard
       HTTP header name (values intentionally differ; header name does
       not).
   - Three new tests in `SlackOAuthFlowTests`. Test count: 1769 → 1772.

## Files touched / pin sites added

* `AGENTS.md` — test-count header refreshed twice (open + close).
* `Tests/ReplyAITests/StatsTests.swift` — 3 new tests in
  `StatsResetAndGuardTests` (cross-vocab divergence, queue-label literal,
  queue-label prefix divergence).
* `Tests/ReplyAITests/IMessageChannelTests.swift` — 2 new tests in
  `IMessageChannelAvatarInitialTests` (cross-file service-name equality
  + SMS-service case-insensitive equality).
* `Sources/ReplyAI/Services/Stats.swift` — doc-comment on `RuleAction`
  + new `writeQueueLabel` static constant.
* `Sources/ReplyAI/Services/NotificationCoordinator.swift` — willPresent
  comment rewritten to be accurate (main); behavioral fix on wip branch.
* `Sources/ReplyAI/Channels/UNNotificationContentParser.swift` —
  divergence note expanded to two contracts (main); reduced back to one
  contract on the wip branch.

## Pipeline state at exit

* Open BACKLOG count: 18 (unchanged — none of this fire's actions
  closed an open task; all were P5/P7/P8 hoist+pin/doc-sync work
  not tracked as REPs).
* wip queue depth: 1 (`wip/autopilot-2026-05-08-0628-willpresent-cksenderid`,
  pushed this fire — first wip branch since the queue was last drained).
* Build cache: warm (~2-8s rebuilds throughout the fire).
* Last 3 commits on main: `2f0ae70` (cross-file service-name pin),
  `1daca30` (queue-label divergence pin), `bf71943` (parser divergence
  doc).
* `swift test --skip ContactsResolverTests --skip InboxViewModelIsSyncingTests
  --skip InboxViewModelTests`: 1772 tests pass, 2 skipped, 0 failures, ~16s.

## Next-fire hints

* `wip/autopilot-2026-05-08-0628-willpresent-cksenderid` is ready for
  human review. Behavioral change: willPresent now reads `ckSenderID`
  before `sender` so modern iMessage notifications return the raw handle
  instead of falling through to `content.title`. Test suite passes
  unchanged — willPresent isn't headlessly testable so the diff is the
  artifact to review, not a green-bar signal. If the alignment is
  correct, this branch reduces the documented divergence between the
  inline path and the parser from two contracts to one.

* The `Stats.writeQueueLabel` `com.replyai.` vs sibling `co.replyai.`
  prefix divergence is now pinned but unfixed. Harmonization would
  require coordinating with whoever owns the production observability
  tooling that keys off the literal — leave for a human decision.

* Codebase is now extremely heavily pinned. Future hoist+pin work is
  hitting diminishing returns: this fire spent significant survey time
  rejecting candidates that were already pinned (SearchIndex, PromptBuilder,
  IMessageSender, NotificationCoordinator, AccessibilityAPIReader, all
  4 channel stubs, ContactsResolver, DraftEngine, all OAuth flow Slack
  literals, all Folder/Tone rawValues, all Channel rawValues/icons/labels,
  all UserDefaults keys). The wip queue is the highest-leverage next-fire
  starting point if any branches age in.

* Three-skip workaround unchanged in this fire (1769 tests pass). The
  documented per-fire-shifting hang boundary did not move — same
  `--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests
  --skip InboxViewModelTests` form passes cleanly.
