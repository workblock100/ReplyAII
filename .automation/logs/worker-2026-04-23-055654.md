# Worker run: worker-2026-04-23-055654

**Date:** 2026-04-23  
**Outcome:** shipped  
**Model:** claude-sonnet-4-6

## Tasks closed

- REP-146 — IMessageChannel: per-thread message cap applied independently across threads
- REP-156 — ContactsResolver: `name(for:)` fallback to raw handle when store returns nil

Bundled per the substantiveness gate (both effort S at P2).

## File changes

### Tests/ReplyAITests/IMessageChannelTests.swift
- +66 lines (new `insertThread` helper + 2 new tests in `IMessageChannelMessageLimitTests`)
- `testPerThreadMessageCapAppliedIndependently` — 3-thread fixture (100/3/50 msgs), cap=20; verifies each is capped independently (20/3/20)
- `testTotalMessageCountRespectsCappedSum` — same fixture; asserts total = 43, not 60

### Sources/ReplyAI/Channels/ContactsResolver.swift
- +2 lines changed: both return sites in `name(for:)` changed from `nil` to `handle` when resolved name is empty (cache-hit path and store-miss path)

### Tests/ReplyAITests/ContactsResolverTests.swift
- +26 lines: 2 new tests (`testNameForHandleFallsBackToHandleWhenNotInStore`, `testNameForHandleReturnsContactNameWhenFound`)
- 2 lines changed: `testCacheRemembersMissesAsEmpty` updated `XCTAssertNil` → `XCTAssertEqual` to match new fallback behavior

## Test counts

- Before: 465 tests
- After: 469 tests (4 new tests added)
- Result: 469/469 passed, 0 failures

## Notes

REP-156 required a small source change to `ContactsResolver.name(for:)` in addition to tests — both return sites (cache-hit and store-miss paths) needed to return the raw handle instead of nil. The `resolveAll` method has independent logic and was not affected; its contract (absent keys for unresolvable handles) is unchanged.

Build time was long (~52 min) due to MLX dependency compile on fresh clone. Compilation succeeded; tests passed.
