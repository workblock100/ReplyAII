# Worker run: 2026-04-22-064413

## Task
- REP-072 — InboxViewModel: consume pending UNNotification inline reply (P1, effort M)

## Outcome
Shipped to main.

## File changes
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` +40 lines
  - Added `startObservingNotificationReply()` called from init
  - Added `consumeNotificationReply()` that dispatches IMessageSender.send on match, discards on unknown threadID, always clears `pendingNotificationReply`
- `Tests/ReplyAITests/InboxViewModelTests.swift` +55 lines
  - `InboxViewModelNotificationReplyTests` class with 2 cases
  - `testNotificationReplyConsumedAndSent` — verifies chatGUID used verbatim and reply text embedded
  - `testNotificationReplyUnknownThreadDiscarded` — verifies no send fires, pending cleared

## Tests
Before: 230 | After: 232 | Failures: 0

## Notes
- Used `IMessageSender.executeHook` (already present from REP-025/REP-040) for interception — no new mock protocol needed.
- `consumeNotificationReply()` is exposed as `internal` (not private) so tests can call it directly if needed.
- `NotificationCoordinator.swift` required no changes — it already sets `pendingNotificationReply` correctly (REP-028).
