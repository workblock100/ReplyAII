# worker-2026-04-21-183617

## Task

- REP-008 — contextual preview: link + attachment detection

## Outcome

shipped to main. Solo-shipped under the "only S at this priority" branch of the substantiveness gate.

## Files changed

- `Sources/ReplyAI/Channels/IMessagePreview.swift` (new, +68) — pure display-logic extractor: URL collapse, attachment sentinel, fallback
- `Sources/ReplyAI/Channels/IMessageChannel.swift` (+2 / -1) — call the extractor in `recentThreads`
- `Tests/ReplyAITests/IMessageChannelPreviewTests.swift` (new, +105) — 14 cases

## Tests

- before: 126 passing
- after: 140 passing (14 new)
- `./scripts/build.sh debug` — success

## Notes

- The extractor lives next to the channel as `IMessagePreview`, not inside a View. The strings it produces (`🔗 <host>`, `📎 Attachment`) are derived display content, same layer as the pre-existing `[non-text message]` fallback.
- Design-side: the emoji choices (`🔗` / `📎`) are worth a human's glance before we call this final. They mirror common messaging-app conventions but haven't been design-reviewed. Flagging in case we want different glyphs.
- Stripped `www.` from collapsed hosts — `🔗 example.com` reads better than `🔗 www.example.com`.
- Non-http/https schemes (ftp, mailto, tel) fall through to plain text — the sidebar is about at-a-glance recognition and those aren't clickable from it anyway.
