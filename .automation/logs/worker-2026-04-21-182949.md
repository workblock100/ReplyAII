# worker-2026-04-21-182949

## Task

- REP-014 — IMessageChannel SQL query + date-autodetect unit tests

## Outcome

shipped to main.

## Files changed

- `Sources/ReplyAI/Channels/IMessageChannel.swift` (+10 / -3) — optional `dbPathOverride` init param so the query path can point at a test DB file
- `Tests/ReplyAITests/IMessageChannelTests.swift` (new, +217) — 6 cases covering: sort order, ns/s date autodetect (pure + in-query), null-text fallback to typedstream, group chat GUID projection

## Tests

- before: 112 passing
- after: 118 passing (6 new)
- `./scripts/build.sh debug` — success

## Notes

- `secondsSinceReferenceDate` was already `internal` (default access for `static` methods) — no access change needed, so the task's "make internal" step was already satisfied.
- The task asked for 5 tests; I shipped 6 because the in-query autodetect is a different code path from the pure-function autodetect and catches a class of bug that the pure test wouldn't (e.g. the SQL binding losing precision on a 64-bit column).
- Reused the typedstream fixture shape from `AttributedBodyDecoderTests` — cost of duplication is lower than introducing shared test-fixture surface at this size.
