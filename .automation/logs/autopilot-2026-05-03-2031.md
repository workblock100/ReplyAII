# Autopilot fire 2026-05-03 20:31 EDT

## Runtime
- Start: 20:31 EDT
- End: ~21:22 EDT
- Elapsed: ~51 min (over the 50-min minimum budget)
- Model: claude-sonnet-4-6

## Summary
Healthy fire. Drained P1 wip backlog reconciliation, shipped 6 P2–P3 tasks in a single bundled commit, then shipped REP-222 as a standalone. Two commits pushed to main. Test count advanced from 663 → 722.

---

## Actions taken

### 1. Workspace sync
- Pulled main; verified `.build/.lock` clear; confirmed warm cache from prior build.
- Build: `swift build` clean in ~6s.

### 2. Backlog reconciliation (P1 drain)
- REP-242 (SlackChannel.recentThreads) and REP-245 (InboxViewModel.filterByChannel): both already implemented in earlier commits but still marked `open`. Reconciled to `done` in BACKLOG.md (commit `2b69116`).

### 3. REP-226 — SmartRule.messageCount predicate
- Added `case messageCount(atLeast: Int)` to `RulePredicate` enum with Codable round-trip.
- Added evaluator case `ctx.messageCount >= n` in `RuleEvaluator`.
- Added `humanize` case in `SfcRulesView`.
- Added `var messageCount: Int = 0` to `RuleContext`.
- 5 new tests in `RulesTests.swift` (evaluator + Codable round-trip).

### 4. REP-250 — DraftEngine mid-prime invalidation
- Added 2 tests: `testInvalidateMidPrimeTransitionsToIdle` and `testInvalidateMidPrimeCancelsPrimingTask`.
- Uses `StubLLMService(initialDelay: 5_000_000_000)` to ensure prime is in-flight when invalidate fires.

### 5. REP-251 — RulesStore compound predicate Codable round-trip
- Added `RulesStoreCompoundPredicateTests` (3 tests): `.and`, `.or`, `.not` round-trips through export+import.

### 6. REP-252 — SearchIndex BM25 ranking tests
- Added `SearchIndexBM25Tests` (2 tests): higher-frequency token ranks first; monotonic ordering.

### 7. REP-268 — Preferences.inbox.lastSyncDate + InboxViewModel writes it
- Added `PreferenceKey.inboxLastSyncDate` key.
- InboxViewModel.syncFromIMessage writes `Date()` to the key after a successful sync (≥1 thread).
- 3 tests in `PreferencesTests` + 2 in `InboxViewModelTests`.

### 8. REP-207 — autoPrime and autoApplyOnSync not pre-set in persistent domain
- Added `PreferencesAutoActionSafetyDefaultTests` (2 tests): use `persistentDomain(forName:)` to assert the keys are absent before any write. Fixed initial false-negative by avoiding the registration domain bleed.

**Commit: `efdb0db` — REP-226/250/251/252/268/207 bundled**
**Commit: `e67140d` — BACKLOG.md + AGENTS.md updates (test count 663 → 716)**

### 9. Build lock incident (mid-fire)
- Background `swift-test` (PID 81280, ContactsResolverTests XPC, started ~20:25) held `.build/.lock` for 35+ min without progress.
- Killed PIDs 81280, 91542, 96267, 1660, 82758, 9552, 5795; removed stale lock file.
- Fresh `swift build` completed in 6.13s.

### 10. REP-222 — UserVoiceProfile data model + Preferences key + PromptBuilder injection
- Added `PreferenceKey.voiceExampleMessages = "pref.voice.exampleMessages"` to `Preferences.swift`.
- Added `UserDefaults.voiceExampleMessages()` getter and `setVoiceExampleMessages(_:)` setter with 20-entry cap and 500-char per-entry truncation enforced at write.
- Updated `PromptBuilder.build(thread:tone:history:voiceExamples:)` — optional `voiceExamples` inserts "Style examples from the user's prior messages:" block above history when non-empty.
- 6 new tests: 4 in `PromptBuilderTests` (injection, empty-no-header, ordering, multi-example) + 2 in `PreferencesVoiceExamplesTests` (cap at 20, truncate at 500 chars).

**Commit: `b0d4d0c` — REP-222 standalone**

---

## Files touched
- `Sources/ReplyAI/Rules/SmartRule.swift`
- `Sources/ReplyAI/Rules/RuleEvaluator.swift`
- `Sources/ReplyAI/Screens/Surfaces/SfcRulesView.swift`
- `Sources/ReplyAI/Services/Preferences.swift`
- `Sources/ReplyAI/Services/PromptBuilder.swift`
- `Sources/ReplyAI/Inbox/InboxViewModel.swift`
- `Tests/ReplyAITests/RulesTests.swift`
- `Tests/ReplyAITests/DraftEngineTests.swift`
- `Tests/ReplyAITests/SearchIndexTests.swift`
- `Tests/ReplyAITests/PreferencesTests.swift`
- `Tests/ReplyAITests/InboxViewModelTests.swift`
- `Tests/ReplyAITests/PromptBuilderTests.swift`
- `BACKLOG.md`
- `AGENTS.md`

## REPs closed this fire
REP-207, REP-222, REP-226, REP-242 (reconcile), REP-245 (reconcile), REP-250, REP-251, REP-252, REP-268

## Pipeline state at exit
- main: `b0d4d0c` (HEAD)
- All tests passing: 722 total, 0 failures
- `.build` cache: warm
- No wip branches left open by this fire

## Next-fire hints
- REP-223 already done (check BACKLOG line); skip if status is `done`.
- REP-233 (PromptBuilder voice-style section already done by REP-222; verify REP-233 is a distinct task or reconcile as done).
- Next open P2 tasks: REP-219, REP-221, REP-227, REP-229 — check for stale `claimed_by` before picking.
- Consider a BACKLOG audit pass: several `done_on: wip/...` entries may need reconciling against current main commits.
