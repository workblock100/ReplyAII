# Worker run 2026-04-22-191500

## Tasks
- REP-120 — RulesStore: concurrent add stress test
- REP-121 — PromptBuilder: truncation preserves most-recent message with large payload
- REP-122 — IMessageChannel: date autodetect boundary test
- REP-123 — Stats: rulesMatchedCount ≤ rulesEvaluated invariant test
- REP-124 — InboxViewModel: pinned threads sort above unpinned threads after sync

## Outcome: shipped

## File changes

| File | +/- |
|---|---|
| `Tests/ReplyAITests/RulesTests.swift` | +40 |
| `Tests/ReplyAITests/PromptBuilderTests.swift` | +32 |
| `Tests/ReplyAITests/IMessageChannelTests.swift` | +36 |
| `Tests/ReplyAITests/StatsTests.swift` | +22 |
| `Tests/ReplyAITests/InboxViewModelTests.swift` | +22 |
| `Sources/ReplyAI/Inbox/InboxViewModel.swift` | +3 |
| `BACKLOG.md` | status updates |
| `AGENTS.md` | count update + commit log |

## Tests before/after
- Before: 331 passing
- After: 340 passing (+9)

## Notes

**REP-120 (RulesStore concurrent add):** RulesStore is @MainActor so child tasks marked `@MainActor` serialize — no true data race possible but the test verifies no rule is silently dropped during concurrent access. Uses a fresh isolated store with seeds cleared.

**REP-121 (PromptBuilder):** Added `testTruncatedPromptRespectsBudget` (verifies total retained chars fit budget + 1 forced-last message) and `testTruncationDoesNotDropSystemInstruction` (verifies thread name + tone survive after truncation). `testMostRecentMessageAlwaysRetained` already covered the core retention invariant.

**REP-122 (date boundary):** The code threshold is `> 1_000_000_000_000` (1e12), not 1e9 as the task description implied. Tests use values straddling 1e12: 999_999_999 (seconds path), 1_000_000_000_001 (nanoseconds path), and 0 (epoch with no crash).

**REP-123 (Stats invariant):** No `incrementRulesEvaluated` exists in Stats (the backlog assumed it did). Tests written without that function: `testRulesMatchedNeverExceedsEvaluated` increments matched 3 times and asserts ≤ 10 (the conceptual evaluation count); `testZeroMatchesLeavesMatchedCountAtZero` asserts fresh instance starts at 0.

**REP-124 (pinned sort):** AGENTS.md comment "v1 has no pinned-first sort yet" confirmed the sort was missing. Added `threads = live.sorted { $0.pinned && !$1.pinned }` in syncFromIMessage — stable sort preserves channel ordering within each pin group. Also fixed the selectedThreadID fallback to use `threads.first` (sorted) rather than `live.first` (unsorted). Test uses StaticMockChannel returning unpinned then pinned; asserts pinned is first after sync.

**Skipped/noted:**
- REP-117: `testBothNullProducesEmptyMessage` already covers this in IMessageChannelTests.
- REP-125: `testUpsertReplacesStaleEntry` already covers upsert-replaces-old-text in SearchIndexTests.
