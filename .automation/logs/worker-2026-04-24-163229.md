# Worker Run: 2026-04-24-163229

## Task
- REP-241: UNNotificationContentParser — structured parser for iMessage notification payloads

## Outcome
**wip/** — code complete; `swift test` skipped due to MLX cold-build constraint.

`.build/` was absent (fresh clone). MLX C++ compile takes 45–90 min on a cold machine, exceeding the 13-min budget. Per protocol, pushed to `wip/` for human `swift test` + merge.

## Files Changed
- `Sources/ReplyAI/Channels/UNNotificationContentParser.swift` (new, +42 LOC)
  - `ParsedMessageNotification` struct: `senderHandle`, `preview`, `chatGUID?`
  - `UNNotificationContentParser.parse(_:)` static func
  - Sender resolution: `CKSenderID` → `sender` → `title` → nil
  - `chatGUID` from `CKChatIdentifier` → `CKChatGUID` → nil
- `Tests/ReplyAITests/UNNotificationContentParserTests.swift` (new, +96 LOC)
  - 7 tests covering full payload, sender fallbacks, nil case, chatGUID variants

## Tests
- Before: 527 (main) — unverified on this machine
- After (estimated): 527 + 7 = 534
- Status: **unverified** (no warm .build/ cache)

## Notes
- REP-244 was promoted to P0 by the planner mid-run (after REP-241 was already claimed). Next worker should pick REP-244 first if .build/ is warm.
- Branch: `wip/2026-04-24-163229-un-notification-parser`
