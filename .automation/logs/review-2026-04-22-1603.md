---
window: 2026-04-22 10:03 UTC – 2026-04-22 16:03 UTC
stars: 5
commits_in_window: 12 (worker: 9 [4 substantive + 5 claim], planner: 3, reviewer: 0)
substantive_rep_tickets_closed: 12 (REP-030, -031, -040, -072, -059, -064, -069, -076, -077, -078, -039, -071, -081 — some bundled)
test_count_delta: 211 → 254 (+43)
banned_actions: 0
---

# Reviewer Audit Notes — 2026-04-22 16:03 UTC

## Window boundary

- Previous review landed at commit `5338e10` (2026-04-22 10:03 UTC).
- This review covers `5338e10..HEAD` → 12 new commits.
- Wall-clock window: 2026-04-22 10:03 UTC – 2026-04-22 16:03 UTC.

## Commits audited (newest first)

| SHA       | Type        | Author          | Notes |
|-----------|-------------|-----------------|-------|
| `95ea987` | claim       | Worker          | REP-070, REP-058, REP-038 — in progress at review time, not audited |
| `874f483` | substantive | Worker          | autoPrime + autoApplyOnSync flags, thread selection tests |
| `2186127` | planner     | Planner (run6)  | Stall-reset REP-039/071/081; +3 new tickets |
| `98f1b83` | planner     | Planner (run5)  | Archived 10 done tasks, added REP-082–091 |
| `248fe9a` | claim       | Worker          | REP-039, REP-071, REP-081 |
| `7667f22` | substantive | Worker          | Msg length guard, -1708 retry, 100-rule cap, mark-read, SQLITE_NOTADB, notif tests |
| `9742979` | claim       | Worker          | REP-059/064/069/076/077/078 |
| `bbedd1a` | substantive | Worker          | Notification reply consumption in InboxViewModel |
| `80d32ae` | claim       | Worker          | REP-072 |
| `3169995` | substantive | Worker          | threadLimit/autoPrime pref keys, regex validation, dry-run sender |
| `8593207` | planner     | Planner (run4)  | Archived 12, added REP-072–081; bumped test count |
| `1c46ba8` | claim       | Worker          | REP-030, REP-031, REP-040 |

## Substantive commit audit

### `3169995` — REP-030, REP-031, REP-040

- Diff: `Preferences` adds `inbox.threadLimit`/`drafts.autoPrime` keys; `SmartRule` gains `RuleValidationError` + regex pre-validation; `IMessageSender` grows an `isDryRun` flag with executor injection path.
- Tests: `+PreferencesTests` (thread limit + autoPrime round-trip), `+RulesTests` (invalid regex rejected, valid regex stored), `+IMessageSenderTests` (dry-run path asserts executor is hit, AppleScript isn't).
- Commit message accurately names all three REPs, explains why each change exists (silent regex failures, power-user thread tuning, test harness isolation). Claim for "ComposerView wiring deferred to human review" is honest — matches current `ui_sensitive` discipline.
- No UI files touched. No banned actions. Proportional test coverage.

### `bbedd1a` — REP-072

- Diff: `InboxViewModel` gains `withObservationTracking`-driven consumption of `pendingNotificationReply`, dispatches to `IMessageSender.send`. Uses `chatGUID` from the loaded thread (right call — avoids synthesizing a 1:1-shaped GUID for groups).
- Tests: 2 new cases in `InboxViewModelTests` covering (a) known-thread send-dispatch, (b) unknown-thread-ID logged-and-discarded.
- Commit message pins down the problem (REP-028 wired coordinator but consumption was a no-op), the pattern (same observation approach as rules), and the safety (`executeHook` injection in tests).
- Solid. No concerns.

### `7667f22` — REP-059, REP-064, REP-069, REP-076, REP-077, REP-078

- Largest substantive commit of the window. Touches 7 source files + 5 test files. Net: +~570 lines with test delta dominating.
- Each concern justified in the commit body:
  - REP-064: 4096-char message length guard before AppleScript touch (sensible cap — covers iMessage service limits with headroom).
  - REP-059: single retry on `-1708 errAEEventNotHandled` (transient during Messages.app cold start / iCloud sync).
  - REP-069: 100-rule ceiling on `RulesStore.add()` with `tooManyRules` error — prevents unbounded O(n) eval growth.
  - REP-076: optimistic local unread-count clear on `selectThread`; badge drops without DB round-trip. Also fixes `markUnreadZero` to preserve `chatGUID` + `hasAttachment` (previously stomped them).
  - REP-077: `ChannelService.databaseCorrupted` case + `IMessageChannel.openReadOnly` SQLITE_NOTADB (26) detection, separate from generic `databaseError`.
  - REP-078: three extra `handleReply` test cases in `NotificationCoordinatorTests`.
- Commit body claims "232 → 245 tests, all green" — matches grep delta for this commit.
- No banned actions. Proportional test coverage across every ticket.

### `874f483` — REP-039, REP-071, REP-081

- Adds `pref.rules.autoApplyOnSync` default (+ registration), injectable `UserDefaults` / `primeHandler` closure on `InboxViewModel`. Guards `applyRules` + `processIncomingForRules` in `syncFromIMessage` behind the new flag. Guards draft-prime in `selectThread` behind `autoPrime`.
- Tests: 9 new cases across `PreferencesTests` (+22 lines) and `InboxViewModelTests` (+188 lines, including new `StaticMockChannel` test double). Worker log notes 24/24 in a targeted run.
- Pattern choice of `primeHandler` closure over a new `DraftEngine` protocol is defensible — no new abstraction, ComposerView retains ownership of the real engine, tests inject a recording closure. Matches the project's "no premature abstraction" stance.
- `testConcurrentSyncCallsDoNotOverlap` hang in sandbox-less CI is noted in worker log as pre-existing (CoreData XPC AddressBook retries) and unrelated to this bundle. Not a new regression; no action.
- Commit message explicitly bundles three S-effort P2s per the substantiveness gate and names every file touched. Honest.

## Banned-action scan

- `git diff 5338e10..HEAD -- 'Sources/**' 'Tests/**' Info.plist Package.swift project.yml scripts/**` shows **no** changes to `Info.plist`, `Package.swift`, `project.yml`, or `scripts/*`.
- No `#Preview` macros reintroduced.
- No `com.apple.security.app-sandbox` flip.
- No test file shrunk; every touched `Tests/*.swift` has a net-positive line delta.
- No force-push / amend / history rewrite — linear, attributed commits only.

## Test coverage delta

- `grep -rE "^\s+func test" Tests/` → **254** (was 211 at last review). **+43 tests**.
- Source delta: +224 lines across 8 source files. Test delta: +740 lines across 6 test files. Test-to-source ratio ≈ **3.3:1** (well above the proportional bar).
- No new test *files* this window; all growth is expansion of existing suites (InboxViewModelTests +302, IMessageSenderTests +137, RulesTests +137, NotificationCoordinatorTests +53, PreferencesTests +68, IMessageChannelTests +43).
- `swift test` not runnable in reviewer sandbox — count is grep-based, same method as prior reviews.

## Substantiveness-gate check

- Every S-effort bundle either closed ≥3 related REPs in one commit or paired a single M/L ticket with test-first rigor (REP-072 alone). No lone S-commits when higher-value work was available.
- 5 claim commits vs. 4 substantive commits — ratio slightly worse than ideal (roughly 1:0.8). Non-rating-affecting but worth a planner nudge (see suggestions).

## Planner behavior

- 3 planner runs in the window (run4, run5, run6). All three refreshed BACKLOG.
- `run4` archived 12 completed items and added REP-072–081 — healthy turnover.
- `run5` archived another 10 completed items, added REP-082–091, updated AGENTS.md test count 218→245.
- `run6` reset REP-039/071/081 as a stall after ~4h of no output from `worker-2026-04-22-111201`. **33 min after the reset, the worker shipped all three in `874f483`.** Not a false alarm in the strict sense — the worker was genuinely slow and the stall reset happened to race the delivery — but a note for the planner: a 4h no-output window on an S×3 bundle is borderline, and the planner's stall threshold may want to consider active-claim file timestamps on the worker log (e.g. if the worker is still writing to `.automation/logs/worker-*.md`, don't reset). Minor tuning suggestion, not a defect.

## Concerns

- **Slight claim/substantive ratio erosion.** 5 claim commits vs. 4 substantive this window. History reads as more noise than necessary. Worker could batch claims with the following substantive commit in one push (stage claim, do work, commit+push atomically), cutting the visible main-branch history roughly in half.
- **Stall-reset / long-running-claim race.** Noted above. REP-039/071/081 were reset to `open` and then immediately closed by the same worker. If another worker instance had picked them up between the reset and the push, both would have raced. Low probability given current fire cadence, but the planner's "active-claim" detection should consider the worker log mtime, not just BACKLOG claim state.
- **AGENTS.md test count drift.** `AGENTS.md:97` still says "245 tests" at review time. After `874f483` the count is 254. Fix below.
- **AGENTS.md 'testing expectations' section** still says "60 tests today" on line 216. That's stale by ~200. Low priority (it's in a narrative paragraph about philosophy, not a machine-readable field), but worth a one-line edit next time the planner touches that file.

## Suggestions for next planner cycle

1. **Archive pass on REP-030/031/039/040/059/064/069/071/072/076/077/078/081** — all confirmed shipped this window. Ensure they're moved from `open`/`in_progress` → `status: done` in BACKLOG.md before run7.
2. **Active-claim stall detection.** Current planner stall rule is "no substantive commit N cycles after claim". Augment with "worker log for this claim ID was updated in the last 30 min → not a stall". Prevents races like REP-039/071/081 in run6.
3. **Batch claim + work commits.** If feasible without breaking the claim-visibility invariant, encourage the worker to ship a single commit containing both the claim and the work (body notes the claim-id) rather than two commits. Halves visible history noise.
4. **Address AGENTS.md narrative drift.** "60 tests today" in the testing-expectations section is stale copy — either remove the number or make it a machine-updated line like the one at `AGENTS.md:97`.
5. **Keep draining.** P1 queue was ~12 items at start of window; workable slice landed. Next planner can keep the additions/closures roughly in balance — queue is healthy at 45 open.

## Rolling-window pattern

Last five windows (oldest → newest):

- `review-2026-04-21.md` — ⭐⭐⭐⭐⭐
- `review-2026-04-21-addendum.md` — ⭐⭐⭐⭐⭐
- `review-2026-04-22-0403.md` — ⭐⭐⭐⭐⭐
- `review-2026-04-22-1003.md` — ⭐⭐⭐⭐⭐
- `review-2026-04-22-1603.md` (this) — ⭐⭐⭐⭐⭐

Zero consecutive sub-par windows. STOP AUTO-MERGE trigger remains disarmed.

## Rating

**⭐⭐⭐⭐⭐ (5/5).** 4 substantive commits, 12 REP tickets closed, +43 tests at a 3.3:1 test-to-source ratio, zero banned actions, honest and explanatory commit messages, three clean planner refreshes. The only things keeping this from "perfect" are the claim-ratio noise and the stall-race around REP-039 — both process-tuning notes, not quality regressions.
