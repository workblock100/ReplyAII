# Worker run 2026-04-22-011918

## Tasks
REP-049 — DraftEngine: concurrent prime guard
REP-051 — IMessageChannel: preserve sqlite3 result code in ChannelError

## Outcome
shipped

## File changes
- `Sources/ReplyAI/Services/DraftEngine.swift` — +11 lines: `primingTasks` dict added; `prime()` cancels prior task via `primingTasks` before calling `generate()`; `dismiss()` clears `primingTasks` entry; `generate()` catch block guards on `Task.isCancelled` to prevent cancelled tasks from overwriting fresh state
- `Sources/ReplyAI/Channels/ChannelService.swift` — +9 lines: `ChannelError.databaseError(code: Int32, message: String)` case added with `errorDescription` wired to message
- `Sources/ReplyAI/Channels/IMessageChannel.swift` — +1 line: `openReadOnly()` throws `databaseError(code: rc, message:)` instead of `unavailable` for non-auth sqlite open failures
- `Tests/ReplyAITests/DraftEngineTests.swift` — +30 lines: `testDoublePrimeCancelsFirst`, `testDoublePrimeResultReflectsSecond`
- `Tests/ReplyAITests/IMessageChannelTests.swift` — +44 lines: `ChannelErrorResultCodeTests` with `testDatabaseErrorPreservesCode` (structural) and `testOpenFailureSurfacesDatabaseOrPermissionError` (integration)

## Tests
- Before: 167
- After: 171
- Result: 0 failures

## Notes
Bundled REP-049 + REP-051 (both S-effort P1); SUBSTANTIVENESS GATE satisfied.

REP-049: The key fix is two-fold: (1) `prime()` now cancels `primingTasks[key]` before calling `generate()`, removing the old `isStreaming` guard that allowed dangling tasks to accumulate; (2) the `catch` block in `generate()` returns early when `Task.isCancelled` is true, preventing a cancelled Task A1 from calling `fail(key:)` and corrupting Task A2's fresh state.

REP-051: `fileExists` guard in `openReadOnly()` fires before `sqlite3_open_v2` for non-existent paths; `databaseError` is therefore raised only for other open failures (e.g., SQLITE_BUSY, SQLITE_LOCKED). The structural test verifies the code field is preserved; the integration test uses a directory path to reach `sqlite3_open_v2` and accepts either `databaseError` or `permissionDenied` depending on macOS's error message for that scenario.
