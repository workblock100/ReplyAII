# Worker Run: worker-2026-04-22-041448

## Task
REP-052 — ChatDBWatcher: FSEvents error recovery with restart backoff

## Outcome
shipped

## File Changes
- `Sources/ReplyAI/Channels/ChatDBWatcher.swift` (+47 / -14)
  - Added `restartDelay: TimeInterval` injectable init param (default 5s)
  - Added `stopped: Locked<Bool>` flag for intentional-shutdown guard
  - Added `restartCount: Locked<Int>` for backoff tracking (package-internal for tests)
  - Added `stopWatching()` — sets stopped flag then cancels sources; `stop()` delegates to it
  - Updated `setCancelHandler` to call `scheduleRestart()` unless stopped
  - Added `scheduleRestart()` — exponential backoff (restartDelay * 2^n, capped 60s) via DispatchQueue.main.asyncAfter
  - Added `simulateSystemCancel()` — package-internal test hook; queue.sync drives same code path as cancel handler
- `Tests/ReplyAITests/ChatDBWatcherTests.swift` (+32 / 0)
  - `testCancellationSchedulesRestart`: verifies restartCount increments after simulateSystemCancel
  - `testStopWatchingDoesNotRestart`: verifies stopWatching blocks subsequent simulateSystemCancel from scheduling

## Test Counts
- Before: 197
- After: 199
- All green

## Notes
- Used existing `Locked<T>` from REP-050 for thread-safe stopped flag and restart counter
- `stop()` retained as alias for backwards compat (InboxViewModel calls it implicitly via deinit)
- The asyncAfter restart also re-checks `stopped` before calling `start()` to handle the race where stopWatching fires between scheduling and execution
