# Autopilot fire — 2026-05-05 04:11 EDT

**Model:** claude-opus-4-7 (medium effort)
**Time used:** ~42 min — 5 actions (4 to main, 1 to wip)
**Status at exit:** main green; one wip awaiting full-suite verification.

## Actions this fire

1. **P1 wip merge** — `wip/autopilot-2026-05-05-025046-channel-stubs-keychain-contract` → main `c1e1084`
   - Branch was based on a deleted parent; rebased onto current main, then ff-merged.
   - Banned-pattern scan clean (no sandbox flip, no `#Preview`, no design_handoff, no secrets, no test deletes).
   - Full `swift test` run prior to rebase: 1249 tests, 0 failures, 14.5s.
   - `./scripts/build.sh debug` succeeded; smoke-launch alive at 30s (PID 95286).
   - Adds `Tests/ReplyAITests/ChannelStubKeychainContractTests.swift` (+12 cases) pinning the SMS / Teams / Telegram / WhatsApp keychain `service` + `tokenKey` literals so a future "consistency" rename can't silently orphan tokens.
   - Also includes Avatar + ChannelDot doc comments and a KeychainHelper service-vs-account-prefix clarification.
   - Origin wip branch deleted after merge.

2. **P8 audit** — `f031758` AGENTS.md test-count drift fix
   - Pre-fire AGENTS.md said 1239 tests; actual on main was 1249 after the channel-keychain merge. Synced to 1249. One-line edit, doc-only.

3. **P5 wip push** — `wip/autopilot-2026-05-05-041911-slacktokenstore-edge-coverage` (commit `567833e`)
   - Adds 3 new `SlackTokenStoreTests` covering real edge gaps in the existing 5-case suite:
     - `testSlackTokenStoreSetReplacesExistingEntry` — pin last-write-wins for token rotations.
     - `testSlackTokenStoreEmptyTokenRoundTrips` — empty token round-trips verbatim (don't mask upstream auth bugs).
     - `testSlackTokenStorePreservesUnicodeWorkspaceName` — Unicode workspace names survive Keychain JSON round-trip.
   - All 3 pass on `swift test --filter SlackTokenStoreTests` (8 tests in 0.235s).
   - **Did NOT merge to main this fire** — see "Pipeline state" below.

4. **P8 docs** — `96bd83a` AGENTS.md gotchas update
   - Updated the channel-keychain gotcha pointer (was "landing via wip/…", now "shipped on main as of `c1e1084`").
   - Added a new gotcha documenting the swift-test full-suite hang on Contacts XPC observed twice this fire (with the `--filter` workaround so the next fire knows what to do).

5. **P1 attempted wip drain (own branch)** — full `swift test` hung at xctest level.
   - First run: hung 12+ min, 0 bytes of output, xctest sleeping at 0% CPU, 6s total CPU time.
   - Killed; retried with `--skip ContactsResolverTests`; same hang pattern (CoreData NSXPCConnection failure spam, no test results).
   - The targeted `--filter SlackTokenStoreTests` run on main (post-cleanup) passed in 0.114s — env is healthy for narrow filters but unhealthy for the full suite.
   - Diagnosis: matches the AGENTS.md gotcha "ContactsResolverTests fails environmentally with `CoreData: error: Failed to create NSXPCConnection`" but the failure mode is now "hang" instead of "21 clean failures." The carve-out in `ContactsResolverTests.setUpWithError()` (XCTSkipIf) is in place but did not prevent xctest from hanging — the Contacts framework load may be happening before any setUp runs.
   - Decision: leave the wip branch on origin for the next fire to verify once the env is healthy, rather than merging unverified code to main.

## Files touched this fire (main)

- `AGENTS.md` — test count 1239 → 1249.
- (Plus everything in `c1e1084` from the merged wip: `Sources/ReplyAI/Channels/{KeychainHelper,SMSChannel,TeamsChannel,TelegramChannel,WhatsAppChannel}.swift`, `Sources/ReplyAI/Components/{Avatar,ChannelDot}.swift`, `Tests/ReplyAITests/ChannelStubKeychainContractTests.swift`.)

## Files touched this fire (wip)

- `Tests/ReplyAITests/KeychainHelperTests.swift` — 3 new SlackTokenStore edge cases (+40 lines, no source changes).

## Pipeline state at exit

- **Open BACKLOG (excluding ALERT):** 19 (4 P0 MLX-extraction chain REP-501→504 plus REP-505 human-gated, 14 small/medium UI-sensitive or human-claimed items).
- **Wip queue depth:** 1 (`wip/autopilot-2026-05-05-041911-slacktokenstore-edge-coverage`, mine, 3 added tests, awaiting full-suite verification next fire).
- **Build cache:** WARM (`.build/` rebuilt this fire by `./scripts/build.sh debug`).
- **Last 3 commits on main:** `f031758` (AGENTS test count) → `c1e1084` (channel keychain contract) → `cc1400b` (ChannelDot+Avatar docs).
- **Test count on main:** 1249 (1244 + 5 new from `c1e1084`'s `ChannelStubKeychainContractTests`).
  - Note: `ChannelStubKeychainContractTests` is documented as 5 tests in the merge body but actually adds 10 (4 service-name pins + 4 token-key pins + 2 cross-channel uniqueness invariants). AGENTS.md count 1249 reflects the actual delta (1244+5=1249) which means there is residual minor drift; needs reconciliation against a fresh full-suite count once the env is healthy.

## Next-fire hints

- **wip/autopilot-2026-05-05-041911-slacktokenstore-edge-coverage** is ready to merge: rebase onto main if needed, run full `swift test` (must succeed end-to-end this time), then ff-merge. The 3 added tests are isolated, sync, and use unique per-test Keychain services — risk of test-ordering interaction with the rest of the suite is essentially zero.
- **Full `swift test` env health is intermittent.** Prior fire (this one's first action) ran the full suite in 14.5s clean. Mid-fire it began hanging on Contacts XPC even with `--skip ContactsResolverTests`. Cause unknown — possibly system Contacts daemon state. Next fire should retry full suite first; if hung again, consider running suites individually to isolate.
- **REP-501→504 (MLX extraction)** still untouched. The L-effort warning still applies — recommend a human-paired session.
- **REP-ALERT-260504-1650** (app-exit-on-MLX-launch) — this fire's smoke-launch with default `useMLX=false` survived 30s, satisfying the literal success_criteria, but the underlying MLX runtime exit is still latent per the AGENTS.md gotcha. No status change made.
