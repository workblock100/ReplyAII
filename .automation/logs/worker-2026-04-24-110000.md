# worker-2026-04-24-110000

## Task
- REP-271 — AGENTS.md + worker.prompt: document MLX build-time budget workaround and wip-branch protocol

## Outcome
**shipped** — docs-only change, committed and pushed to main.

## Build / test status
- `.build/` absent on this run (fresh clone). No `swift test` attempted — docs-only task, N/A per test_plan.
- No code changes; build state unchanged.
- Tests before: 527 | Tests after: 527 (no change)

## File changes
- `AGENTS.md` +4 lines — added MLX cold-build warning to Gotchas section
- `.automation/worker.prompt` +4 lines — added MLX build-time check at step 8 with explicit wip-branch protocol
- `BACKLOG.md` — marked REP-271 done, moved to Done/archived section

## Notes
- Only open P0 task available this run (all others blocked or human-claimed).
- REP-271 is effort: S and the only open P0 task — shipped alone per substantiveness gate exception.
- Both files updated per success_criteria; grep checks pass.
- This resolves the protocol ambiguity that produced 10+ wip branches: workers now have explicit guidance to push wip when `.build/` is absent or >6h old.
