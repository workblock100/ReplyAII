# Autopilot fire — 2026-05-09 10:11 EDT

Model: `claude-opus-4-7` (Opus 4.7).

## Time used

~50 min — 5 substantive actions across two priority lanes (P1 wip drain, P3 backlog reconciliation, P8 docs audit). Wip queue went from 2 branches → 0.

## Actions this fire

1. **P1 wip merge — REP-AUDIT-260505 issue #1 fix → main `80b6aad`.** Cherry-picked
   `wip/autopilot-2026-05-09-0211-open-inbox-button-hit-test` (commit `1e7e364` —
   `.contentShape(Capsule(style: .continuous))` on the gallery toolbar's
   "Open inbox →" button). Test gate: `swift test --skip ContactsResolverTests
   --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests` ⇒ 1901 → 1909
   passes after merge to main, 2 skipped, 0 failures (the 1901 run was on the
   older base of the wip branch; current main with the merge is 1909). Build:
   `./scripts/build.sh debug` clean. Smoke launch: 3-layer gate green —
   PID alive, `windows=2 [Inbox 1180x752] [ReplyAI 1360x852]`, 60-second log
   scan found no `SIGABRT|SIGSEGV|fatal error|Crashed|EXC_BAD_` signatures.
   Picked the hit-test candidate over the `frame-height` alt because
   `.contentShape(Capsule())` is the canonical SwiftUI fix for the symptom
   (background-fill region not hit-testable on `.buttonStyle(.plain)`). Per
   the 2026-05-09-0811 cross-ref audit, `frame-height` would also bring the
   site into structural parity with sibling buttons; that polish is still
   available later if a smoke-click shows the click is still broken after
   the contentShape fix.

2. **P1 cleanup — deleted superseded alt branch `wip/autopilot-2026-05-09-0211-open-inbox-button-frame-height` from origin.** Same bug, alt
   hypothesis; not needed once the hit-test branch shipped. Wip queue is now
   empty for the first time in 8+ hours.

3. **P3 — REP-AUDIT-260505 → done in BACKLOG main `53866c0`.** Updated `status` to
   `done`, recorded `done_on: 80b6aad`, flipped `claimed_by` from `human` to
   `autopilot-2026-05-09-1011`, and wrote a `merge_note_2026-05-09-1011` line
   capturing the test-gate counts, the smoke-launch results, the rationale
   for picking hit-test over frame-height, and the still-open caveat that
   the actual smoke-click hasn't been verified by a human (the autopilot's
   gate doesn't drive the mouse).

4. **P3 — REP-505 → blocked in BACKLOG main `8f431f7`.** Same drift surface as
   REP-501's existing blocker note: the `wip/REP-500-mlx-extraction` branch
   doesn't exist on origin (only `main` and `archive/2026-05-08-willpresent-superseded-by-8da2e2e` remain — verified by `git for-each-ref refs/remotes/origin/`). REP-505 is a human-review gate that has nothing to verify until REP-501→504 land a successor wip branch, so flipping
   to `blocked` makes future open-queue surveys correctly skip it. Reset to
   `open` once REP-504 produces a fresh branch.

5. **P8 — AGENTS.md hash audit fix → main `3140fa6`.** Audited every backticked 7-char
   commit hash in AGENTS.md (90 total) against `git cat-file -e`. All 90 resolve
   except line 184, which had a 40-character mangled string
   `80035e18d6f16e8197320222262df7771182e31b` — five concatenated paste artifacts
   where only the leading 7 chars (`80035e1`, the real REP-097/100/106/107/112/113
   commit) actually exist. Trimmed to the canonical 7-char prefix matching every
   sibling provenance row in this section, so future grep-by-hash on AGENTS.md
   actually finds the right commit. Same audit on BACKLOG.md (23 hashes) found
   3 "invalid" hashes — all of them are correctly documented as historical
   bad-hashes that REP-201/REP-211 already corrected, so no change needed.

## Files touched / REPs closed

- Source: `Sources/ReplyAI/Screens/AppPrototypeView.swift` (+12 lines, contentShape modifier)
- Docs: `BACKLOG.md` (REP-AUDIT-260505 → done, REP-505 → blocked)
- Docs: `AGENTS.md` (line 184 hash trim)
- REPs closed: REP-AUDIT-260505 issue #1 (the gallery toolbar click bug)
- REPs reclassified: REP-505 (open → blocked)

## Pipeline state at exit

- main: HEAD `3140fa6` (5 commits ahead of fire start `bbe6e4d`):
  - `80b6aad` AppPrototypeView contentShape fix (P1 wip merge)
  - `53866c0` BACKLOG REP-AUDIT-260505 → done
  - `8f431f7` BACKLOG REP-505 → blocked
  - `3140fa6` AGENTS.md mangled-hash trim
  - (this log will be a 5th commit once written and pushed)
- Wip queue: **0 branches** (was 2). Origin now has `main` + `archive/2026-05-08-willpresent-superseded-by-8da2e2e` and nothing else.
- BACKLOG: 13 open (was 14 — REP-AUDIT-260505 closed), 1 → 1 blocked (REP-505 newly so), 0 in_progress.
- Build cache: WARM (`swift build` ≤ 7s for both wip-branch verify and post-merge main).
- Test count: 1909 passing under three-skip gate (`--skip ContactsResolverTests --skip InboxViewModelIsSyncingTests --skip InboxViewModelTests`), 0 failures, 2 skipped — unchanged from fire start (no test additions or deletions this fire). Total still 1946 per AGENTS.md.
- No regressions; full three-skip gate passes in ~18s warm.

## Hash audit findings (P8 inventory)

Audit covered every backticked hex string of length 7+ in AGENTS.md and BACKLOG.md against `git cat-file -e`:

- AGENTS.md, 90 7-char hashes: 89 valid before fix, 1 (mangled concatenation on line 184) repaired to 7-char canonical → 90/90 valid after.
- AGENTS.md, 8+ char strings: only one 8-char valid (`5c00f0bf` etc., not exhaustive — sampled). False-positive class: chat GUID example string `chat1234567890` produces a non-hash `12345678` substring.
- BACKLOG.md, 23 7-char hashes: 20 valid + 3 documented as historical bad-hashes corrected by REP-201/REP-211 (`05e7035`, `904b0e7`) and the lost-branch reference (`0b0d66f` on the deleted `wip/2026-04-24-205912-mlx-spm-target`). All three are correctly contextualized as past-state references; none warrant a current-state edit.

## What's NOT yet test-pinned and would benefit from it (forward-looking)

This fire spent ~5 min hunting for unpinned literal-pin opportunities and found
that the codebase is in extraordinarily thorough shape — every constant I
audited (MessagesAppActivationObserver: messagesAppBundleID/defaultDebounce/dispatchQueueLabel; ChatDBWatcher: defaultDebounce/defaultRestartDelay/restartBackoffCap/defaultChatDBPathRaw/defaultChatDBWalPathRaw/dispatchQueueLabel; LocalhostOAuthListener: okResponseTemplate/defaultPort/defaultTimeout/maxRequestBytes/codeQueryParameterName/syntheticParseURLHost; OAuthError: timeoutToast/listenerFailedPrefix/tokenExchangeFailedPrefix; ReplyAIWindowSummoner: inboxWindowID/inboxWindowTitle/logPrefix/registeredLog/replyAIRequestSummonInbox; Channel: iconName/dotColor/label literals; Folder.Kind: rawValues + allCases; Tone: rawValues + allCases) is already pinned with literal-equality assertions. Empty-string class (gotcha §270) is pinned at all 9 known sites. Every public type I sampled has a load-bearing doc-comment.

What remains as candidates for a future fire:

- **In-source hoist of inline UI string literals** (`"R"` brand letter — used in
  8 view files; `"⌘K"` palette hint; `"Loading on-device model"`; SendConfirmSheet
  copy `"Send to <recipient>"`/`"via <channel> · tone <tone>"`/`"ReplyAI will
  type this into Messages as you..."`). All UI-touching → wip branch only.
  Hoisting unblocks literal-pin tests against `BrandStrings.brandLetter` etc.
  Estimated effort: M (one wip branch, ~10–15 small edits, 5 new tests).
- **REP-XCUI-001 scaffolding** (last fire's #1 forward-looking suggestion). Still
  not attempted — XCUITest in SwiftPM has known gotchas (typically requires
  xcodebuild + a test plan), and this autopilot fire didn't have budget to
  derisk that. Still a high-value future investment for catching regressions
  the AppleScript window-introspection layer can't see (button labels, empty
  screens, broken click handlers).
- **REP-501→REP-504 MLX target split**. Still L-effort and risky for a 50-min
  fire. The blocker note recommends a human-paired session.

## Next-fire hints

- Wip queue is empty — next fire's P1 lane will be no-op. Expect P3 (BACKLOG
  reconciliation) and P5/P7/P8 (audit/test/doc) lanes to dominate, or a P2
  ship of one of the smaller open BACKLOG items.
- The "Open inbox →" click bug fix went out but hasn't been smoke-clicked by
  a human. If a future fire runs a manual computer-use audit and the click
  is still broken, the alt fix `.frame(height: 26)` is documented in
  REP-AUDIT-260505's diagnostic block and the cross-ref audit (now line 311
  of BACKLOG) explicitly recommends it as the structurally cleaner option.
  A combined fix (both `.contentShape(Capsule())` and `.frame(height: 26)`)
  is also viable.
- `swift test` three-skip gate is still the validated form. Boundary hasn't
  moved this fire (still ContactsResolverTests + InboxViewModelIsSyncingTests
  + InboxViewModelTests). Don't run plain `swift test` — that hung the
  2026-05-09-0411 fire (per its log).
