# autopilot-2026-05-08-0210

## Summary

- Time used: ~30 min — 8 actions (this log + commits)
- Pipeline state at entry: 0 wip branches, 18 BACKLOG open, build cache WARM, last main `f68ad42` (2026-05-08-0010 fire log)
- Pipeline state at exit: 0 wip branches, 18 BACKLOG open, last main `a749237` (imessage-channel openReadOnly error-message formats + pins)
- Fire signal: empty wip queue → P1 exhausted, P2 high-priority unclaimed work all UI-sensitive or claimed_by:human, all 8 actions in P4-style hoist+pin lane to main
- Model: claude-opus-4-7 medium-effort
- Pre-existing P9 verification: ContactsResolverTests already has `XCTSkipIf` workaround in `setUpWithError()` (commit prior to this fire); the two derivative test files (`ContactsResolverPhoneDigitsTests`, `ContactsResolverDefaultPinTests`) are pure-static and don't touch CNContactStore. P9 is effectively done.

## Actions

1. **P4 hoist** — `slack-channel: hoist networkError fallback copy + format pin` (`be0e127`).
   The three Slack call sites (recentThreads, messages, send) each held an inline `"Slack <endpoint> failed"` literal as the fallback when Slack returned `ok: false` without an `error` field. Hoist into `SlackChannel.networkErrorFallback(endpoint:)` with endpoint name sourced from the existing `Endpoint.*` constants. 2 new pins: per-endpoint output pin against the existing fallback-copy literals + literal-shape pin using a non-production endpoint. Existing 3 fallback-copy pins still pass.

2. **P4 hoist** — `mlx-draft-service: hoist loadProgress copy + byte formatter + pins` (`13a016e`).
   Three user-visible loadProgress strings ("Preparing on-device model…", "Downloading model · X of Y" / "Downloading model · N%", "Warming weights…") and the GB/MB byte-rendering policy lived inline inside `ensureContainer` continuation closure with no test pin. Hoist into static constants and helper functions (`preparingMessage`, `warmingMessage`, `downloadingMessage(completedBytes:totalBytes:)`, `downloadingMessage(fraction:)`, `formatBytes(_:)`). 8 new pins covering: exact preparing/warming literals, distinctness between the two phases, byte-aware download format on both render branches (MB/MB and MB/GB), fraction-form download with percentage rounding, formatBytes 1024 MiB threshold + per-side precision, with 1.8 GB / 47%/100% boundary cases referencing REP-ALERT-260504-1650. 11 MLXDraftServiceTests passing (was 3).

3. **P4 hoist** — `global-hotkey: hoist diagnostic NSLog bodies + triage-grep pins` (`2e4fb1d`).
   Three diagnostic NSLog sites (RegisterEventHotKey-failed, InstallEventHandler-failed, ⌘⇧R-registered) each held an inline interpolation as the line body. The prefix was hoisted last fire but the bodies were not — drift to a different verb or keyword silently breaks the triage runbook grep on `RegisterEventHotKey failed` / `⌘⇧R registered` in `log show` output. Hoist into `registerFailedLog(status:)`, `installFailedLog(status:)`, and `registeredLog`. 5 new pins covering exact format on each line, failure-leg distinctness, prefix composition for failure + success lines, and a positive-OSStatus edge case. 9 GlobalHotkeyContractTests passing (+5 new), 2 still skipped per AppKit-touching gate.

4. **P4 hoist** — `inbox-vm: hoist empty-result sync-failure copy + sidebar-truncation pin` (`4a3e418`).
   InboxViewModel.syncFromIMessage's empty-result branch held the user-visible "No conversations returned. chat.db may be empty on this account." sidebar-pill copy as an inline `.failed(...)` payload with no test pin. The string is rendered through SidebarView's `error · <msg.prefix(24)>` truncation. Hoist into `InboxViewModel.emptyChatDBSyncFailureMessage`. 1 new pin covering full string + first-24-char truncation prefix + non-empty invariant. The pin makes the eventual pivot-aligned "stop saying chat.db" rewrite a deliberate single-line diff against an explicit prior wording. (Test landed in `InboxViewModelMessagesForThreadTests` class — same class as existing pin tests.)

5. **P4 hoist** — `search-index: hoist FTS5 syntax constants + cross-site pins` (`91568a1`).
   ftsQuery's three FTS5-specific literals — the special-character set `()*:` that triggers phrase-quoting, the AND token joiner, and the `*` prefix-match suffix — lived inline inside the function body. Drift on any one silently changes search semantics: extending the special set over-quotes ordinary tokens, shrinking it under-quotes real special chars, `OR` instead of ` AND ` widens every multi-word query to a union, dropping `*` disables prefix matching. Hoist into `ftsSpecialCharacters`, `ftsTokenJoiner`, `ftsPrefixSuffix`. 4 new pins with both literal-equality checks and round-trip witnesses through ftsQuery. 65 SearchIndexTests passing.

6. **P4 hoist** — `prompt-builder: hoist user-prompt format strings + drift pins` (`15fdd99`).
   Four user-prompt format strings — the thread-header line "Conversation with X via Y.", the voice-example bullet "- X", the per-message line "<speaker>: <text>", and the final "Write my next reply..." instruction — lived inline inside build(). Existing tests rely on substring-match assertions which would not catch any of these drifts. Hoist into `Template.threadHeader(name:channelLabel:)`, `voiceExampleBullet(_:)`, `messageLine(speaker:text:)`, and `instructionLine(toneLowercased:)`. 7 new pins: format-shape pins + round-trip witnesses (`prompt.contains(Template.X(...))`) for each formatter. 67 PromptBuilderTests passing (+7 new).

7. **P4 hoist** — `stats: hoist weekly-log markdown vocabulary + format pins` (`c47502c`).
   writeWeeklyLog emits a markdown file the .automation/ planner+reviewer scripts parse to roll up cumulative stats. The field names (draftsGenerated, draftsSent, messagesIndexed, ruleLoadSkips, sessionDuration, rulesFiredByAction) and the line shape (`- <field>: <value>`) lived as inline literals. Existing tests use substring matching — a misspelled or renamed field would slip through silently. Hoist into `Stats.WeeklyLogFormat` enum: 8 named field-name constants + parameterized `line(_:value:)` formatter. 10 new pins: each field name independently with distinct rationale, line-format shape, and round-trip witness through writeWeeklyLog. 40 StatsTests passing (+5 new from the 35 prior).

8. **P4 hoist** — `imessage-channel: hoist openReadOnly error-message formats + pins` (`a749237`).
   Two error-message formats in `openReadOnly` were inline literals: the `ChannelError.unavailable("No Messages database found at <path>.")` payload and the SQLite-error fallback `"unknown SQLite error <rc>"` used when `sqlite3_errmsg` can't be queried. Both flow into user-visible inbox banners or support reports. Hoist into static helpers `missingDatabaseErrorMessage(path:)` and `unknownSQLiteErrorFallbackMessage(rc:)`. 4 new pins: exact format, prefix-stability for the path-embedding form, and a negative-rc edge case. (Tests landed in `IMessageChannelFormatTimeTests` — same class as existing TimeFormat pins.)

## Pipeline state at exit

- main: `a749237`
- last 3 commits: `a749237` (imessage-channel openReadOnly error formats), `c47502c` (stats weekly-log vocabulary), `15fdd99` (prompt-builder user-prompt formats)
- BACKLOG: 18 open (unchanged), 1 blocked (REP-285 superseded by REP-500 chain), 0 in_progress
- wip queue: 0 branches (P1 still exhausted)
- build cache: WARM (continuous build throughout fire)
- ContactsResolverTests carve-out: still in place (XCTSkipIf in setUpWithError applied previously); never had to invoke `--skip ContactsResolverTests` workaround this fire because the suite no-ops cleanly in headless

## Next-fire hints

- Hoist+pin lane is getting deeper but still has surface area: the AppleScript template in IMessageSender's `sendRaw`, the IMessageChannel SQL column-name vocabulary (`thread_id`, `is_from_me`, etc.), and the AttributedBodyDecoder magic-byte constants (0x2B, 0x81, 0x82, 65_535 length cap) are all unpinned and would each be solid 1-action targets.
- The `InboxViewModel.emptyChatDBSyncFailureMessage` pin landed THIS fire is a documented hook for the pivot-aligned "stop saying chat.db" rewrite — when human re-scopes that into a UI-sensitive backlog ticket, the autopilot can route it through wip/ for review with the pin as the explicit prior to diff against.
- REP-501→REP-505 (MLX SPM extraction) remains untouched. SKILL.md flags it as L-effort and human-paired; not a fit for autopilot fires.
