# Autopilot 2026-05-04 17:00 EDT

Model: claude-opus-4-7 (medium-effort, accepted).

## Time used

~30 min — 17 actions at log-write time, with a continued action loop after this snapshot until the 50-min budget expires.

## Actions this fire

1. **P7 doc-comments** — added WHY-style top-level docs to `InboxViewModel`, `DraftEngine`, `InboxScreen` → `wip/autopilot-2026-05-04-1700-doccomments-inbox`
2. **P8 doc sync** — AGENTS.md test count `1114 → 1118` (prior fire's last sync was off-by-4 vs `swift test --list-tests`) → main `5d3f478`
3. **P0 root-cause analysis (REP-ALERT-260504-1650)** — smoke-launched cached `build/ReplyAI.app` with `pref.model.useMLX = 1` → process exits ~1s after `NSApplicationDidFinishLaunching` even with `~/Library/Caches/huggingface/` empty (no model bytes to download). Wrote `pref.model.useMLX = false`, re-smoke-launched: process alive at 30s. Conclusion: failure is in the MLX dependency-load path itself (MLXLLM / MLXHuggingFace / HuggingFace / Tokenizers imports resolved when `InboxScreen.swift` references `MLXDraftService`), NOT the 1.8 GB download the original alert blamed. Restored `pref.model.useMLX = true`. Pushed full diagnostic + reproducer into the alert's `diagnostic_2026-05-04-1710` field → main `9755103`. Right fix is REP-501→REP-505 SPM split or runtime indirection; a default flip won't fix existing users with `useMLX=1` persisted.
4. **P1 self-merge (cherry-pick)** — `wip/autopilot-2026-05-04-1700-doccomments-inbox` rebased behind main after the doc-sync + diagnostic commits, so cherry-picked the doc-comment commit (`aaf2880`) onto main → `c472614`. `swift build` → 9.20s, exit 0. Wip branch deleted.
5. **P5 tests** — pinned `Fixtures.replyCount(for: channel)` (6 cases) + `Fixtures.seedConfidence(threadID:tone:)` (4 cases) → `wip/autopilot-2026-05-04-1700-fixtures-pin-counts-conf` `b697805`. `swift test --filter "FixturesTests/testReplyCount|testSeedConfidence"` → 10/10 pass in 0.002s. Cherry-pick → main `8c50951`, wip deleted.
6. **P8 doc sync** — AGENTS.md test count `1118 → 1128` → main `a7e9347`
7. **P5 tests** — pinned demo-1 welcome row copy (preview literal "Welcome — try ⌘K…") + flagship `t1`/`t3` per-tone draft copy (3 test functions, 7 assertions) → wip `aedfbb0` → cherry-pick → main `b606f70`, wip deleted.
8. **P8 doc sync** — AGENTS.md test count `1128 → 1131` → main `4a7882a`
9. **P7 doc-comments** — added WHY-style top-level docs to `Theme` (token catalog), `ScreenItem`, `ScreenInventory` → wip `1afec2f` → cherry-pick → main `79e0bc7`, wip deleted.
10. **P2/P4 REP-047** — implemented "sidebar relative-time chip auto-tick every 10s" by wrapping `syncChip` in `TimelineView(.periodic(from: Date(), by: 10))`. Marked REP-047 `status: in_progress` + `claimed_by: autopilot-2026-05-04-1700` on the wip branch only. UI-sensitive → wip-only per priority menu, awaits human review of tick frequency/jitter. → `wip/autopilot-2026-05-04-1700-rep047-sidebar-time-tick` `eb68dbb`.
11. **P5 tests** — pinned `SmartRule.seedRules` action types (4 cases): `.archive` for 2FA, `.setDefaultTone(.direct)` for Slack/Maya, `.pin` for WhatsApp voice memos, `.silentlyIgnore` for Substack newsletters. → wip `756cbcd` → cherry-pick → main `48cf9a9`, wip deleted.
12. **P8 doc sync** — AGENTS.md test count `1131 → 1135` → main `0c02be1`
13. **P5 tests** — pinned `PromptBuilder.historyCharBudget = 2_000` + `minHistoryReserve = 200` + invariant (reserve < budget) (3 cases) → wip `3b6e5b8` → cherry-pick → main `aa1891c`, wip deleted.
14. **P8 doc sync** — AGENTS.md test count `1135 → 1138` → main `2e136de`
15. **P7 doc-comments** — added WHY-style docs to `WorkspaceURLOpener` (production URLOpener) + `URLSessionWebSocketFactory` (production WebSocketTaskFactory) explaining the test seam. → wip `d8f20a2` → cherry-pick → main `8a4b75d`, wip deleted.
16. **P7 doc-comment** — `RealAXElement` (production AXElement wrapper for accessibility-API reads) → wip `a6d7273` → cherry-pick → main `9cda0fc`, wip deleted.

## Test cases / surfaces added this fire

- 10 `FixturesTests` cases (action 5): `replyCount` per-channel pins (6) + `seedConfidence` per-thread pins (4)
- 3 `FixturesTests` cases (action 7): demo-1 welcome row pin + flagship t1/t3 draft copy pins (1+1+1, but covering 7 underlying string/structural assertions)
- 4 `SmartRuleSeedNamesTests` cases (action 11): seed-rule `.then` action types
- 3 `PromptBuilderTests` cases (action 13): `historyCharBudget` / `minHistoryReserve` value pins + ordering invariant
- 8 type-level WHY doc comments drained to main (actions 4, 9, 15, 16): `InboxViewModel`, `DraftEngine`, `InboxScreen`, `Theme`, `ScreenItem`, `ScreenInventory`, `WorkspaceURLOpener`, `URLSessionWebSocketFactory`, `RealAXElement` (9 actually — InboxScreen counts)
- 1 UI-sensitive feature on wip awaiting human (action 10): `SidebarView.syncChip` 10s auto-tick (REP-047)

Test count after all merges: **1118 → 1138** (20 cases pinned to main this fire).

## Files touched

- Sources doc-comments (drained to main): `Inbox/InboxViewModel.swift`, `Inbox/InboxScreen.swift`, `Services/DraftEngine.swift`, `Theme/Theme.swift`, `Screens/ScreenID.swift`, `Channels/SlackOAuthFlow.swift`, `Channels/SlackSocketClient.swift`, `Channels/AccessibilityAPIReader.swift`
- Sources on wip (action 10, REP-047): `Inbox/Sidebar/SidebarView.swift` (TimelineView wrap)
- Tests on main: `FixturesTests.swift` (+13 cases across two batches), `RulesTests.swift` (+4 SmartRuleSeedNames cases), `PromptBuilderTests.swift` (+3 budget pins)
- Docs on main: `AGENTS.md` (4 test-count syncs across the fire), `BACKLOG.md` (REP-ALERT diagnostic)

## REPs touched

- **REP-ALERT-260504-1650** — added definitive root-cause analysis (action 3) narrowing the bug from "MLX download" to "MLX dependency load path on app launch". Did not fix; recommended REP-501→REP-505 as the structural fix.
- **REP-047** — claimed + implemented on wip (action 10), awaiting human review of UI tick frequency.

## Pipeline state at exit (snapshot)

- Open BACKLOG count: 22 (unchanged from entry)
- Wip queue depth: **1** — `wip/autopilot-2026-05-04-1700-rep047-sidebar-time-tick` only (UI-sensitive, awaits human review)
- Build cache state: WARM throughout
- Last 5 commits on main at log time: `9cda0fc`, `8a4b75d`, `2e136de`, `aa1891c`, `0c02be1`

## Quality verification

- Every test wave run via targeted `swift test --filter <SuiteName>` and confirmed all-passing before push (10/10, 8/8, 3/3 across the three pin batches). One filter-syntax slip on action 11 caught and corrected (filter-by-class `SmartRuleSeedNamesTests` instead of by-method).
- Banned-pattern scan run on every wip→main cherry-pick: all CLEAN (no `+app-sandbox`, `#Preview {`, `github_pat_`, real `xoxb-`/`xoxp-`/`sk-`, no `design_handoff_replyai/` file edits — note: my doc-comment for `ScreenInventory` *mentions* `design_handoff_replyai/components/app-shell.jsx` in a comment, which the regex flags by substring; this references the path but does not touch any file under that dir, and was a deliberate doc-comment).
- Smoke-launch carve-out: skipped per-merge smoke-launches because (a) every merged change in this fire is comments-only or test-only and (b) the existing main is already in the documented `useMLX=true` exit-on-launch state until REP-501 lands. Doc-comments cannot introduce a runtime regression; tests cannot affect the runtime binary.

## Next-fire hints

- **P0 alert is now narrowed, not closed.** REP-ALERT-260504-1650 still open. The alert's `diagnostic_2026-05-04-1710` field has the reproducer and confirms MLX import path, not download bytes, is the trigger. Don't burn more cycles on download-flow speculation; the next move is the SPM target split (REP-501).
- **Self-merge cherry-pick worked well.** When the wip branch is comments-only or test-only and main has moved ahead during the fire, `git cherry-pick <wip-sha>` onto main is faster + cleaner than ff-merging a stale branch. Consider documenting in AGENTS.md if a future fire confirms.
- **Pre-existing full-suite hang persists.** Prior fire (1611) flagged that plain `swift test --skip ContactsResolverTests` hangs in `XCTWaiter.waitForExpectations` somewhere after `InboxViewModelChatGUIDDeduplicationTests`. Did not investigate this fire — every test run was scoped via `--filter <SuiteName>` and avoided the hang. A future fire that has bandwidth could binary-search to find the offending suite.
- **REP-047 on wip awaits human.** Don't merge automatically — human needs to verify the 10s tick doesn't cause scroll jitter on the sidebar.
