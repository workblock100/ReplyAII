# worker-2026-04-21-181957

## Task

- REP-005 — observability: counters in stats.json

## Outcome

shipped to main.

## Files changed

- `Sources/ReplyAI/Services/Stats.swift` (new, +117)
- `Sources/ReplyAI/Services/DraftEngine.swift` (+6 / -3) — optional Stats injection, bump on draft generate
- `Sources/ReplyAI/Inbox/InboxViewModel.swift` (+14 / -3) — Stats held on VM; wired at focus-time rules, incoming-rule actions, send, and per-sync indexed-message count
- `Tests/ReplyAITests/StatsTests.swift` (new, +120)
- `BACKLOG.md` — REP-005 marked done + moved to archive

## Tests

- before: 98 passing
- after: 104 passing (6 new: counter math, empty file, malformed file, round-trip, messagesIndexed guard, concurrent writes)
- `./scripts/build.sh debug` — success

## Notes

- `Stats` is `@unchecked Sendable` + `NSLock`-guarded, matching `ContactsResolver`. `@Observable` was tempting but surfacing is deferred and the thread-safety requirement dominates — DraftEngine can fire from its cooperative-queue stream task without bridging to the main actor.
- Persists to `~/Library/Application Support/ReplyAI/stats.json` (atomic writes). The task title references `.automation/stats.json` but the scope says `~/Library/Application Support/ReplyAI/stats.json` — scope wins; this is the same directory used for other user-data preferences.
- Injectable `fileURL` for tests so assertions never touch the running app's state.
