# worker-2026-04-21-183251

## Task

- REP-011 — ContactsResolver cache + access-state unit tests

## Outcome

shipped to main.

## Files changed

- `Sources/ReplyAI/Channels/ContactsResolver.swift` (+74 / -47) — extracted `ContactsStoring` protocol; real CNContactStore lookups live in `CNContactStoreBackedStoring`; DEBUG-only `overrideAccessForTesting`
- `Tests/ReplyAITests/ContactsResolverTests.swift` (new, +165) — 8 cases

## Tests

- before: 118 passing
- after: 126 passing (8 new: cache miss queries store, cache hit skips store, negative results cached, denied short-circuits, unknown→granted, unknown→denied, already-granted skips prompt, concurrent reads coalesce)
- `./scripts/build.sh debug` — success

## Notes

- Protocol (`ContactsStoring`) has three methods: `currentAccess`, `requestAccess`, `lookup`. Everything CNContactStore/CNContactFormatter-specific now lives in the backed impl, so the resolver itself no longer imports Contacts semantics — tests never have to fabricate a `CNContact`.
- `overrideAccessForTesting` is `#if DEBUG`-gated so release builds can't accidentally use it to bypass the permission flow.
- Went to 8 tests instead of the scoped 4 because the extra four (negative-cache memoization, denied short-circuit, explicit unknown→granted vs. unknown→denied branches) are cheap to write and catch specific regressions the scope-listed four would miss.
