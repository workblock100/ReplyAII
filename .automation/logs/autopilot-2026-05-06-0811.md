# autopilot 2026-05-06-0811

**Time used**: ~30 min — 9 actions

## Actions this fire

The wip queue had one branch (`wip/autopilot-polish-2026-05-06-063434-channel-count-fix`)
which is UI-sensitive (touches a SwiftUI view body) and based on stale main
(would revert 499 lines if merged). Per the P4 rule it's awaiting human review;
not auto-merged.

No P2 ship-to-main candidate: the only `claimed_by: null, ui_sensitive: false`
open tasks are REP-ALERT-260504-1650 (the MLX launch crash, complex SPM split),
REP-501→504 (the same MLX split sub-tasks), and REP-XCUI-001 (L-effort full
XCUITest target setup). Same conclusion as the previous fire: drop into the
P5/P7/P8 lanes for pin tests + doc fixes.

P5 pin-test additions (commits to main):

1. P5: PromptBuilder.build header literal pins (5 tests, +76 LOC). Main `9ceab0d`.
   First-line literal `Conversation with <name> via <channel.label>.`,
   empty-history fallback `(no messages yet)`, recent-messages header
   `Recent messages (oldest first):`, voice-examples header
   `Style examples from the user's prior messages:`, and bullet format
   `- <example>`. Existing tests use substring `contains()` which would still
   pass after rewordings that change LLM behavior; pinned exactly.

2. P5: SlackChannel.parseThreads / parseMessages derived-field pins (4 tests,
   +113 LOC). Main `6fdb44a`. chatGUID equals conversation id verbatim across
   channel/DM/group, avatar derives from `String(display.prefix(1)).uppercased()`
   including the "#" channel prefix, hasAttachment=true for non-empty files
   array, hasAttachment=false for empty files array (Slack's empty-array
   convention for removed attachments).

3. P5: AppleScriptMessageReader.parseMessages direction edge cases (3 tests,
   +65 LOC). Main `7a39314`. Direction parsing is case-insensitive
   (OUTGOING/Outgoing/OuTgOiNg → .me), unknown direction values default to
   .them (safe default — misattributing to .me would feed user voice as
   contact voice into the LLM), parse-side limit cap enforced even when the
   executor returns more rows than requested.

4. P5: AttributedBodyDecoder length-cap + magic-search-window pins (2 tests,
   +59 LOC). Main `5630772`. `readPrimitiveString` rejects strings whose
   declared length exceeds 65 535 bytes; streamtyped magic must appear within
   the first ~31 bytes (20 + magic length). Both are security-shaped
   invariants — the existing fuzz test catches crashes; these pin the
   rejection itself.

5. P5: GlobalHotkey ReplyAIWindowSummoner notification + fast-path pins
   (2 tests, +67 LOC). Main `d290dc5`. summon() posts the
   `.replyAIRequestSummonInbox` notification when no inbox window exists
   (the path SwiftUI uses to call `openWindow(id: "inbox")`); summon() skips
   the fallback when an NSWindow titled "Inbox" exists in NSApp.windows
   (pins the title literal that must match `WindowGroup("Inbox", id: "inbox")`).

6. P5: InboxViewModel.confirmSend success-toast prefix pin (1 test,
   +29 LOC). Main `676480c`. The existing
   `testSendSuccessClearsConfirmationAndShowsToast` only checks that the
   recipient name appears, so a copy edit that swapped the verb (`Delivered
   to`, `Reply sent to`) would still pass. Pin the exact `Sent to <name>`
   prefix.

7. P5: Stats.Snapshot forward-compat decode pins (2 tests, +60 LOC).
   Main `6762e6f`. Valid-JSON-but-missing-keys decodes with defaults (the
   contract that makes the upgrade path safe — drift to a bare `decode`
   instead of `decodeIfPresent ?? default` would lose every user's lifetime
   counters on upgrade). Valid-JSON-with-extra-keys decodes ignoring the
   unknowns (downgrade-safe).

P8 audit (commits to main):

8. P8 audit: AGENTS.md repo-layout test count 1301 → 1313 (after actions
   1–3). Main `ea32990`.
9. P8 audit: AGENTS.md repo-layout test count 1313 → 1320 (after actions
   4–7). Main `12fa4d7`.

## Notable observations

- **Test count on main moved 1301 → 1320** (+19 across 7 pin-test commits).
  All new tests pass in their filtered runs; pre-merge full sweep showed a
  flaky SIGSEGV in `IMessageChannelBusyRetryTests.testPersistentBusyThrows...`
  on first run that disappeared on the second run. The test passes in
  isolation; likely environmental timing-pressure from the busy-retry's
  `Thread.sleep(0.1)` interacting with concurrent xctest workers.
- **wip queue is unchanged** (still 1 branch — the channel-count-fix
  awaiting human review). No new wip pushes this fire because every action
  was either a pin test or a doc fix that lands cleanly on main.
- **Two distinct pin-test patterns surfaced as still-leaky:** (a)
  literal-prefix copy that the existing tests check via `contains()`
  rather than equality (the InboxViewModel "Sent to" toast was a clear
  example — the recipient name was checked but not the verb), and (b)
  forward-compat decode invariants where empty-file and malformed-file
  paths were tested but the in-between "valid JSON missing some keys" path
  wasn't. Both shapes of gap are common across the codebase; future fires
  should look for them specifically.

## Pipeline state at exit

- **Test count on main**: 1320 (was 1301 at fire start, +19)
- **Open BACKLOG**: 23 (unchanged — no autopilot-actionable shippable tasks)
- **wip queue**: 1 (`wip/autopilot-polish-2026-05-06-063434-channel-count-fix`
  — UI-sensitive polish awaiting human review)
- **Build cache**: WARM
- **Last 3 commits on main**: `12fa4d7` (AGENTS.md test-count update),
  `6762e6f` (Stats forward-compat pins), `676480c` (send-toast prefix pin)

## Next-fire hints

- The `wip/autopilot-polish-...-channel-count-fix` branch remains the only
  outstanding wip; still UI-sensitive and still skipped per P4. Older than
  5 days threshold is not yet hit (created today's earlier fire).
- AGENTS.md test count drift caught up twice this fire (mid-fire and
  pre-log). Future fires should still expect to update it as part of the
  log step; with ~3 pin-tests per action, each fire of 6+ actions tends
  to accumulate ~15 tests of drift.
- The `testPersistentBusyThrowsDatabaseError` flake is worth keeping an
  eye on — if it recurs deterministically, it will block P1 merges. The
  test passes in isolation, so the trigger is suite-ordering or shared
  resource pressure.
